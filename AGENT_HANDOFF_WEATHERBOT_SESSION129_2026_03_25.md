# AGENT HANDOFF — WeatherBot Session 129 (2026-03-25)

## STATUS: LIVE PAPER TRADING | DEPLOYED `20260324_235616` | SPREAD INFLATION ACTIVE (AUTO-DECAY DAY 1.8) | +$4,780 P&L (2,101 resolutions, 61.9% WR)

---

## READ THESE BEFORE DOING ANYTHING

1. `CLAUDE.md` — Prime directive, rules of engagement, architecture facts, critical traps
2. `C:\Users\samwa\.claude\projects\C--lockes-picks-polymarket-ai-v2\memory\MEMORY.md` — Session history, P&L data, outstanding items
3. This handoff doc — everything you need

**This is a WeatherBot-only session. No bleed to other bots unless explicitly demanded.**

---

## SYSTEM OVERVIEW

- **What**: 14-bot automated Polymarket trading system. WeatherBot trades temperature-bucket markets using NOAA ensemble forecasts (GFS/ECMWF/HRRR/AIFS, 133 members).
- **Phase**: Paper trading (SIMULATION_MODE=true). Real capital at risk when boolean flips. Paper trading IS production.
- **VPS**: Ubuntu-3 at 34.251.224.21 (16GB/4vCPU)
- **SSH**: `ssh -i ~/.ssh/LightsailDefaultKey-eu-west-1.pem ubuntu@34.251.224.21`
- **Deploy**: `KEY="C:/Users/samwa/.ssh/LightsailDefaultKey-eu-west-1.pem" VPS="ubuntu@34.251.224.21" bash deploy/deploy.sh`
- **Logs**: `sudo journalctl -u polymarket-ai -f`
- **Current deploy**: `20260324_235616` (S129)
- **DB**: PostgreSQL on VPS. Access: `sudo -u postgres psql polymarket`
- **P&L script**: `cd /opt/polymarket-ai-v2 && source /opt/pa2-shared/venv/bin/activate && PYTHONPATH=/opt/polymarket-ai-v2 python scripts/bot_pnl.py WeatherBot <hours>`
- **Restart**: `sudo systemctl restart polymarket-ai`

---

## WHAT WAS DONE THIS SESSION (S129)

### 1. Full Code Verification of S128 Bug Fixes (8 fixes)

All 8 fixes from `AUDIT_WEATHERBOT_S127.md` were verified line-by-line against the actual deployed code using parallel agents:

| # | Fix | File(s) | Verified At | Status |
|---|-----|---------|-------------|--------|
| 1 | `asyncio.sleep(0)` Py3.13 crash | `forecast_client.py:1098,1107` | Both lines correct | **CONFIRMED** |
| 2 | Cooldown on EXIT not ENTRY | `weather_bot.py:1016,2847-2850` | Entry-side removed, exit-side only | **CONFIRMED** |
| 3 | `date.today()` → UTC | `metar_monitor.py:105,186,218,232,257` | All 5 locations use `datetime.now(timezone.utc).date()` | **CONFIRMED** |
| 4 | Dual-format JOIN | `weather_bot.py:1046,3356` | `CAST(m.id AS TEXT) OR m.condition_id` both locations | **CONFIRMED** |
| 5 | Precip `if/elif/elif` | `precipitation_engine.py:171-180` | Correct `if/elif/elif` chain | **CONFIRMED** |
| 6 | Tail threshold 50 + docstring | `probability_engine.py:361,351-355` | `< 50`, docstring says 0.90 | **CONFIRMED** |
| 7 | Wind variance `(N-1)` | `weather_bot.py:1738-1739` | Guard + Bessel correction | **CONFIRMED** |
| 8 | Discovery cache deepcopy | `weather_bot.py:24,1115` | `import copy` + `copy.deepcopy()` | **CONFIRMED** |

### 2. Code Flow Verification

- **Spread inflation**: Applied exactly once per group in `fit_distribution()` (L126-131). `bucket_probabilities()` does NOT re-apply. No double-application.
- **Negative-EV gate**: Fires at L2696-2730 BEFORE any sizing. Blocks both Kelly and S-T paths.
- **Exposure locking**: Increment under `_exposure_lock` at L2735-2778. Decrement under lock at L1022 (exit) and L1056 (fallback). Rollback under lock at L2859-2860.
- **S-T multi-bucket path**: Delegates to `_execute_weather_trade()` for each opp. Same event_data, same neg-EV gate. No separate path.
- **Opportunity fields**: `city` and `lead_time_hours` set in `_analyze_group()` L2143-2145. Immutable through pipeline.

### 3. Config Fix: Duplicate WEATHER_COMBINED_BOOST_CAP (P1-14)

`config/settings.py:746` had a duplicate definition overriding S122's 1.5→2.0. **Fixed**: Line 746 is now a comment-only note. Single active definition at L720 = 1.5. Positions were **33% larger than intended** before this fix.

Also deployed: `TOTAL_CAPITAL` default $100K→$20K (shared infra fix).

### 4. Deploy `20260324_235616`

- 1595 tests passed (full suite)
- Deploy successful, health check passed at 60s
- All S128 WeatherBot fixes + shared infrastructure audit fixes + config fix live

### 5. Post-Deploy Monitoring (10.5 hours observed)

**Zero runtime errors.** No TypeError, UnboundLocalError, or any exception in WeatherBot logs.

### 6. Empty event_data Investigation — RESOLVED

The 124 "empty" ENTRY events (NULL confidence, `{}` event_data) all have `idempotency_key LIKE 'repair-entry-%'`. Generated by `database.py:5197-5221` `repair_orphaned_positions()` — a backfill process that creates skeleton ENTRY records for positions lacking trade_events ENTRY records. It intentionally has no confidence/event_data because the data is unavailable for historical orphans. **Not a bug.** Analysis queries correctly exclude via `event_data->>'lead_time_hours' IS NOT NULL`.

### 7. 35-City Freeze — Not a Bug

`unmatched_cities=[]`, `total_markets=1500`. Polymarket genuinely has 35 cities. The mapper and registry work correctly. No new cities to detect.

---

## POST-DEPLOY DIAGNOSTIC (04:00–14:35 UTC Mar 25)

### Trading Activity

| Metric | Value |
|--------|-------|
| Real ENTRY trades | 3 (Tokyo YES, LA YES ×2) |
| Repair ENTRY (orphan backfill) | 87 |
| SHADOW_ENTRY | 4,309 (~410/hr, normal range) |
| RESOLUTION | 60 (43W/17L = 71.7% WR, +$796.52) |

### Resolution Breakdown

**By side:**
| Side | N | Wins | WR | P&L |
|------|---|------|----|-----|
| NO | 42 | 38 | **90.5%** | +$658.71 |
| YES | 18 | 5 | **27.8%** | +$137.81 |

**Top cities:** Dallas (+$65.91, 6/6), Austin (+$47.69), Chicago (+$46.70, 3/3)
**Worst cities:** Singapore (-$8.08, 0/1), Hong Kong (-$7.46), LA (-$6.83)

**Biggest single win:** +$849.91 (NO)
**Biggest single loss:** -$476.76 (NO, NYC)

### Why Only 3 Real Entries?

**Daily exposure cap hit.** At 14:30 UTC: `daily_spent=$19,987.43` of `$20,000` limit. All trade signals blocked with `Daily exposure limit would be exceeded`. 155 open positions with $33,399 cost basis. New entries resume at daily boundary reset.

### Shadow Analysis (post-deploy)

| Reason | Side | N | Avg Would-Bet | Avg Cal Conf | Avg Price |
|--------|------|---|---------------|-------------|-----------|
| negative_ev | NO | 4,309 | $410 | 0.7318 | 0.7933 |

100% NO-side shadows. Calibrator compresses raw confidence below market price → correctly rejected.

---

## ALL-TIME P&L

| Metric | Value |
|--------|-------|
| Total resolutions | 2,101 |
| Wins | 1,301 |
| Win rate | **61.9%** |
| Total realized | **+$4,779.74** |
| Open positions | 155 |
| Cost basis | $33,398.52 |
| Market value | $33,407.19 |
| Unrealized | +$8.67 |

### Lead-Time WR (all-time)

| Bucket | N | WR | P&L |
|--------|---|-----|-----|
| <24h | 139 | 48.2% | -$204 |
| 24-48h | 247 | 53.4% | -$601 |
| 48-72h | 282 | 60.3% | +$631 |
| 72-120h | 496 | 65.5% | +$2,222 |

72-120h improved from +$1,453 (S126) → +$2,222. Spread inflation is working as intended.

---

## SPREAD INFLATION STATUS

- **Active since:** 2026-03-24T20:00:00Z
- **Current day:** ~1.8
- **Decay factor:** 0.90^1.8 ≈ 0.83
- **Current effective:** BASE≈0.124, FACTOR≈0.041
- **24h multiplier:** ×1.124 (was ×1.15 at start)
- **Hard zero:** April 16, 2026 (day 23)

**Decay countdown:**
```
Day 0 (Mar 24):  BASE=0.150, FACTOR=0.050 → 24h=×1.15
Day 1.8 (NOW):   BASE=0.124, FACTOR=0.041 → 24h=×1.12
Day 7 (Mar 31):  BASE=0.072, FACTOR=0.024 → 24h=×1.07
Day 14 (Apr 7):  BASE=0.031, FACTOR=0.010 → 24h=×1.03
Day 23 (Apr 16): Hard zero — inflation off permanently
```

---

## PROBABILITY ENGINE: ROOT CAUSES OF OVERCONFIDENCE

### Identified in S124 exploration (6 sources of systematic narrowness):

| Issue | Location | Impact | Status |
|-------|----------|--------|--------|
| **Population std divisor** | `forecast_client.py:1250` | ~5% underestimate | **FIXED S124** |
| **No lead-time spread inflation** | `probability_engine.py` | Short-lead worst | **FIXED S126** (base+factor+decay) |
| **GEFS subsampling at 48h+** | `forecast_client.py:1232-1245` | Removes diversity, narrows 10-20% | UNFIXED |
| **No EMOS before 20 pairs** | `probability_engine.py:96` | Cold-start overconfidence | Not a trading gate |
| **Hard floor 0.5F** | `probability_engine.py:84,96` | Asymmetric inflation | Low priority |
| **Tail discount post-normalization** | `probability_engine.py:150-162` | Compresses tail mass | Low priority |

---

## FULL CALIBRATION ARCHITECTURE (3 independent systems)

### 1. EMOS — Station-level bias correction (AUTO-REFIT every 6h)
- Per-station, per-lead-time OLS regression: corrected_mean = a + b * forecast_mean
- Data source: `weather_calibration` table. Window: 90 days rolling.
- Minimum: 20 resolved pairs per (station, lead_bucket). Below that, uses simple bias offset.
- Regime-aware: ENSO phase splits.

### 2. Platt+Isotonic — Confidence calibration (STATIC, set in S123)
- T=2.271 compresses all probabilities toward 0.5. Does NOT auto-refit.
- Spread inflation decay compensates for this being static.

### 3. Spread Inflation — Distribution widening (S126, AUTO-DECAY)
- Widens effective_std before CDF integration. Both `effective_std` and `emos_sigma` inflated.
- Auto-decays 10%/day. Hard zero at day 23.

---

## SIZING SEQUENCE

```
1. RAW MODEL PROBABILITY
   -- probability_engine.bucket_probabilities() -> model_prob per bucket
      |-- Skew-normal CDF integration
      |-- EMOS mean correction: corrected = a + b * forecast_mean
      |-- EMOS spread correction: corrected_sigma = sigma (if EMOS fitted)
      |-- S126: Lead-time spread inflation (BASE+FACTOR with auto-decay)
      +-- Tail isotonic calibration: multiply by tail discount [0.5, 1.0]

2. EDGE FILTER (L1373)
   -- |model_prob - yes_price| >= min_edge (8% US, 12% intl)

3. CONFIDENCE ASSIGNMENT (L2100-2108)
   -- raw_conf = model_prob (YES) or 1 - model_prob (NO)
   -- base_confidence = min(0.95, raw_conf)
   -- IF calibrator fitted: effective_confidence = calibrate(base_confidence) [S123]

4. S124 NEGATIVE-EV GATE (L2699)
   -- IF confidence < price OR _raw_size <= 0: shadow entry + return False

5. KELLY SIZING (bankroll_manager.py L156-162)
   -- kelly_full = (confidence * b - q) / b
   -- size_usd = kelly_full * fraction * capital

6. SIZING MULTIPLIERS (combined_boost, L2350-2600)
   |-- Expiry boost (1.0-2.0x), Regime boost (1.2x), Severe weather, Jump boost
   |-- NBM boost (1.3x), Model freshness (0.8-1.2x), Station reliability (0.5-1.2x)
   |-- Buhlmann credibility (0.0-1.0x), Baker-McHale uncertainty (0.5-1.0x)
   +-- Combined cap: min(combined_boost, 1.5) [S122, verified S129]

7. EXPOSURE CAPS (L2727-2733)
   |-- Group cap: $10,000 | City cap: $5,000 | Per-bet cap: $600
   +-- Daily cap: $20,000

8. PAPER FILL SIMULATION (paper_trading.py)
   |-- BUY: ask-side VWAP walk | SELL: bid-side VWAP walk (S121)
   +-- Per-scan book depletion (S121)
```

---

## ARCHITECTURE REFERENCE (WeatherBot scan path)

```
scan_and_trade() [L983]
  |-- _handle_daily_boundary() [L988] -- resets exposure, restores P&L
  |-- asyncio.gather(_maybe_reload_calibration, _load_category_params) [L999]
  |-- _check_monitoring_thresholds() [L1007] -- Brier/drawdown halt + Kelly graduation
  |-- PM exit detection [L1010-1070] -- tracks PM exits, decrements exposure, sets cooldown
  |-- _prefetch_severe_weather_alerts() [L1200]
  |-- asyncio.gather(*[_analyze_group(g) for g in groups]) [L1210]
  |   |-- forecast_client.get_combined_forecast() -- GFS+ECMWF+AIFS (133 members)
  |   |-- prob_engine.fit_distribution() -- skew-normal + EMOS + S126 spread inflation
  |   |-- prob_engine.bucket_probabilities() -- CDF integration per bucket
  |   +-- tradeable filtering: edge, cooldown, penny-bet, position check, DB guard
  |-- _compute_regime_boost()
  |-- _exec_group() for each group with edge:
  |   |-- >=2 buckets: _execute_group_trades() -> S-T multi-bucket Kelly
  |   +-- 1 bucket: _execute_weather_trade() -> independent Kelly
  |       |-- S124 negative-EV gate (covers BOTH paths)
  |       |-- exposure lock reservation (atomic, verified S129)
  |       +-- place_order() -> paper_trading VWAP fill
  |-- _reevaluate_open_positions()
  +-- every 10 scans: backfill_outcomes + check_emos_drift + close_stale_positions
```

---

## PENDING PRIORITIES

| Pri | Item | Status | Notes |
|-----|------|--------|-------|
| **P1** | Monitor spread inflation decay | Active — day 1.8, working. Check at day 7 (Mar 31). | Hard zero Apr 16 |
| **P2** | GEFS subsampling at 48h+ narrows spread | Identified S124, not addressed | 10-20% diversity loss |
| **P3** | BUG-19: GEFS member count assumption | Hardcoded 31/82 thresholds. Log warning when < 25. | Deferred from S128 |
| **P3** | BUG-GRP: `_market_group_cache` not persisted | Startup rebuild covers it. Redis if leaks observed. | Deferred from S128 |
| **P3** | City auto-onboarding | Currently manual. New cities CAN trade day 1. | |
| **P3** | NO vs YES asymmetry | NO=90.5% WR, YES=27.8% WR post-deploy. Monitor. | |
| **P4** | BUG-LOSS: `_consecutive_losses` resets on restart | Restore from trade_events. | Deferred from S128 |
| **P4** | Platt T=2.271 is static | Compensated by spread inflation. Future: auto-refit? | |
| **P4** | INEFF-4/5, LOG-3, LOG-REDIS, BUG-10 | Low priority P4/P5 items from S127 audit | |
| **P5** | Kalshi cross-platform arbitrage | Deferred, 8-16h effort | |

---

## CURRENT CONFIG (live VPS values post-S129)

```
WeatherBot BotBankrollManager:
  capital=$20,000, kelly_fraction=0.25, max_bet_usd=$600, max_daily_usd=$20,000

WEATHER_MIN_EDGE=0.08, WEATHER_INTL_MIN_EDGE=0.12
WEATHER_MIN_CONFIDENCE=0.10
WEATHER_MAX_POSITIONS=1000
WEATHER_MAX_PER_GROUP_USD=10000
WEATHER_MAX_CORRELATED_EXPOSURE=5000
WEATHER_COMBINED_BOOST_CAP=1.5  [S129: duplicate at L746 removed]
WEATHER_MAX_BUCKETS_PER_GROUP=5
WEATHER_NO_MAX_ENTRY_PRICE=1.0 (effectively removed)
WEATHER_KELLY_FRACTION=0.25
WEATHER_DEFAULT_SIZE=25
WEATHER_EXIT_COOLDOWN_SECS=14400 (4h)
WEATHER_MIN_TRADE_USD=5.0
WEATHER_EMOS_WINDOW_DAYS=90
WEATHER_BM_FLOOR=0.50
WEATHER_BUHLMANN_KAPPA=30.0
WEATHER_DAILY_LOSS_LIMIT=10000
WEATHER_MAX_TOTAL_EXPOSURE_USD=50000
WEATHER_TOTAL_CAPITAL=20000
WEATHER_MAX_LEAD_TIME_HOURS=168
TOTAL_CAPITAL=20000  [S129: was 100000, shared infra fix]

# S123:
WEATHER_CONFIDENCE_CAL_ENABLED=true
WEATHER_CONFIDENCE_CAL_WINDOW_DAYS=30
WEATHER_CONFIDENCE_CAL_MIN_SAMPLES=200

# S126:
WEATHER_SPREAD_INFLATION_BASE=0.15
WEATHER_SPREAD_INFLATION_FACTOR=0.05
WEATHER_SPREAD_INFLATION_START=2026-03-24T20:00:00+00:00
# Auto-decays 10%/day. Hard zero at day 23 (Apr 16, 2026).

PAPER_TAKER_FEE_BPS=150
PAPER_REALISTIC_FILLS=true
PAPER_DEFAULT_SPREAD=0.04
PAPER_TRADING_CAPITAL=10000000
```

---

## KEY FILES (WeatherBot-specific)

| File | Lines | Purpose |
|------|-------|---------|
| `bots/weather_bot.py` | ~4,690 | Main bot: scan, analyze, trade, exit, calibration |
| `base_engine/weather/probability_engine.py` | ~560 | Skew-normal distribution + EMOS + spread inflation |
| `base_engine/weather/precipitation_engine.py` | 235 | Rain/snow probability |
| `base_engine/weather/market_mapper.py` | 1,136 | Polymarket question -> city/bucket mapping |
| `base_engine/weather/forecast_client.py` | 1,587 | GFS/ECMWF/HRRR/AIFS ensemble fetching |
| `base_engine/weather/metar_client.py` | 237 | Airport observations (resolution day) |
| `base_engine/weather/metar_monitor.py` | 283 | METAR running max tracking |
| `base_engine/weather/model_run_monitor.py` | 339 | Model freshness tracking |
| `base_engine/weather/station_registry.py` | ~1,550 | Station definitions (106), lookup, health monitor |
| `base_engine/execution/paper_trading.py` | ~900 | Paper fills: VWAP walk, book depletion |
| `base_engine/execution/order_gateway.py` | ~1,200 | Order routing, VWAP gate (BYPASSED for WeatherBot) |
| `base_engine/risk/bankroll_manager.py` | 263 | Kelly sizing, per-bot caps |
| `base_engine/data/database.py` | ~5,200 | DB operations, trade_events, repair_orphaned_positions |
| `base_engine/data/resolution_backfill.py` | ~400 | Resolution queue |
| `config/settings.py` | WEATHER_* block | All config |
| `tests/unit/test_weather_bot.py` | ~1,940 | Main test suite |
| `tests/unit/test_weather_cold_start.py` | ~600 | Cold start tests |

---

## DATA SOURCES

| What | Where | Notes |
|------|-------|-------|
| P&L (realized) | `trade_events` table | SOLE AUTHORITY. `realized_pnl` column. |
| P&L (unrealized) | `positions.unrealized_pnl` | Mark-to-market, updated every 10s |
| Shadow entries | `trade_events WHERE event_type='SHADOW_ENTRY'` | reason=negative_ev/zero_kelly/sub_min_trade/exposure_cap |
| Repair entries | `trade_events WHERE idempotency_key LIKE 'repair-%'` | Orphan backfill — no confidence/event_data. Exclude from analysis. |
| EMOS calibration | `weather_calibration` | forecast_temp, actual_temp, bias, lead_time |
| Tail calibration | `weather_tail_calibration` | model_prob, actual_outcome by bucket_type |
| Canonical P&L | See VPS command in System Overview | |

### Key Queries

**Shadow entry monitoring:**
```sql
SELECT event_data->>'reason' AS reason, side, COUNT(*) AS n,
    ROUND(AVG(CAST(event_data->>'raw_size_usd' AS FLOAT))::numeric, 2) AS avg_would_bet,
    ROUND(AVG(confidence)::numeric, 4) AS avg_cal_conf,
    ROUND(AVG(price)::numeric, 4) AS avg_price
FROM trade_events
WHERE event_type = 'SHADOW_ENTRY' AND bot_name = 'WeatherBot'
  AND event_time >= NOW() - INTERVAL '24 hours'
GROUP BY reason, side ORDER BY reason, side;
```

**Lead-time WR:**
```sql
WITH entries AS (
    SELECT DISTINCT ON (market_id) market_id, confidence, price,
        CAST(COALESCE(event_data->>'lead_time_hours','0') AS FLOAT) AS lead_hours
    FROM trade_events WHERE bot_name='WeatherBot' AND event_type='ENTRY'
      AND event_data->>'lead_time_hours' IS NOT NULL
    ORDER BY market_id, event_time
),
resolutions AS (
    SELECT market_id, realized_pnl FROM trade_events
    WHERE bot_name='WeatherBot' AND event_type='RESOLUTION'
)
SELECT
    CASE WHEN e.lead_hours < 24 THEN 'a_lt24h'
         WHEN e.lead_hours < 48 THEN 'b_24_48h'
         WHEN e.lead_hours < 72 THEN 'c_48_72h'
         WHEN e.lead_hours < 120 THEN 'd_72_120h'
         ELSE 'e_120h+' END AS lead_bucket,
    COUNT(*) AS n,
    ROUND(100.0 * COUNT(*) FILTER (WHERE r.realized_pnl > 0) / NULLIF(COUNT(*), 0), 1) AS wr,
    ROUND(SUM(r.realized_pnl)::numeric, 2) AS pnl
FROM entries e JOIN resolutions r ON r.market_id = e.market_id
GROUP BY lead_bucket ORDER BY lead_bucket;
```

---

## CRITICAL TRAPS (cumulative — DO NOT VIOLATE)

1. **trade_events is P&L AUTHORITY** — never read paper_trades for P&L
2. **YES/NO mandate**: `place_order()` requires `side="YES"/"NO"`. NEVER BUY/SELL
3. **VWAP edge gate BYPASSED for WeatherBot** (order_gateway.py). Weather CLOBs have 99c structural asks. Do NOT re-enable.
4. **Paper capital is $10M intentionally.** BotBankrollManager is the real limit.
5. **EMOS has 90-day rolling window**. Cold stations fall back to global EMOS.
6. **`boundary_risk` persisted in event_data** for audit but NO LONGER discounts confidence (S122).
7. **NO confidence discount REMOVED (S122).** Do not re-add. Kelly self-regulates.
8. **NO entry price cap REMOVED (S122).** `WEATHER_NO_MAX_ENTRY_PRICE=1.0`.
9. **`confidence` is a TOP-LEVEL column on trade_events**, NOT in event_data JSONB.
10. **Python 3.13 scoping**: `from X import Y` inside function makes Y local for ENTIRE function.
11. **asyncpg JSONB**: `CAST(:x AS jsonb)` NOT `:x::jsonb`.
12. **`paper_trades` has NO `metadata` JSONB column.**
13. **Resolution backfill excludes SELL trades** — SELL P&L computed by paper engine at exit time.
14. **BOT_REGISTRY=14 bots** — shared module change requires all 14 verified.
15. **positions table**: NO `closed_at`, NO `updated_at`. Only `opened_at` + `status`.
16. **SHADOW_ENTRY events** are best-effort (try/except pass). Do not block trades on logging failure.
17. **S123 Platt semantics**: T > 1 COMPRESSES toward 0.5. T < 1 AMPLIFIES. OPPOSITE of intuition.
18. **`raw_confidence` field** in opp dict (L2139) flows to event_data for audit.
19. **Location onboarding is MANUAL** — station_registry.py must be edited by developer.
20. **S124: Spread inflation inflates BOTH `effective_std` and `emos_sigma`** — skewnorm reads `emos_sigma` independently.
21. **S125: Resolution backfill priority_bot removed** — expired-first ordering. Do NOT re-add.
22. **S125: `RESOLUTION_QUEUE_BATCH_SIZE=500`** — do not reduce below 500.
23. **S126: Spread inflation has TWO components** — BASE (uniform) + FACTOR (lead-time scaled). Both auto-decay.
24. **S126: Hard zero at day 23** — `_days_active >= 23.0` sets both to 0.0.
25. **S129: `repair-entry-*` in trade_events** — `repair_orphaned_positions()` creates skeleton ENTRY records. No confidence/event_data. Filter with `idempotency_key NOT LIKE 'repair-%'` or `event_data->>'city' IS NOT NULL`.
26. **S129: WEATHER_COMBINED_BOOST_CAP** — single definition at settings.py:720 = 1.5. L746 is comment-only.
27. **S129: TOTAL_CAPITAL** — default changed from $100K to $20K. % risk checks now correct.
28. **S129: Daily exposure cap** — bot hits $20K/day cap with 155 open positions. New entries blocked until daily reset.

---

## VERIFIED FALSE ALARMS (don't re-investigate)

| Claim | Status | Evidence |
|-------|--------|----------|
| Empty event_data on ENTRY | **EXPLAINED S129** | `repair-entry-*` orphan backfill — intentional skeleton records |
| 35-city freeze | **Not a bug S129** | `unmatched_cities=[]`, Polymarket hasn't added cities |
| Drawdown compression disabled | FALSE | `health_scheduler.py:214` updates every cycle |
| DATA-4 exposure lock race | FALSE (S128) | Lock correctly applied in all paths |
| All PSW scan methods dead | FALSE | Called at L1085-1087 |
| EMOS 20-sample is trading gate | FALSE (S126) | New cities trade day 1 with raw forecast + global T |
| Shadow entries = trades | FALSE | Shadows are LOG entries for rejected signals |

---

## SESSION HISTORY (recent)

| Session | Date | Key Changes |
|---------|------|-------------|
| **S129** | **03-25** | **Verified all 8 S128 fixes in code. Deployed `20260324_235616`. Fixed duplicate WEATHER_COMBINED_BOOST_CAP (33% oversize). Fixed TOTAL_CAPITAL $100K→$20K. Resolved empty event_data mystery (repair-entry orphans). Confirmed 35-city freeze is Polymarket-side. Post-deploy: 60 resolutions, 71.7% WR, +$797. All-time +$4,780.** |
| S128 | 03-25 | 8 audit bugs fixed: asyncio.sleep Py3.13 crash, cooldown on EXIT, date.today UTC, dual-format JOIN, precip normalization, tail threshold, wind variance, discovery cache deepcopy |
| S126 | 03-25 | Spread inflation activated: BASE=0.15 + FACTOR=0.05 + 10%/day auto-decay. Lead-time WR analysis. Shadow deep analysis. |
| S125 | 03-24 | SHADOW_ENTRY DB constraint fix, resolution queue fix, 771 manual backfill |
| S124 | 03-24 | Negative-EV gate, shadow logging, population→sample std, spread inflation foundation |
| S123 | 03-23 | Platt+Isotonic confidence calibration. T=2.271, Brier +11%. |
| S122 | 03-23 | Cap uncapping, shadow entries, confidence penalty removal, data analysis |
| S121 | 03-23 | SELL VWAP walk, book depletion, live retry, model freshness, PAPER_TAKER_FEE 150bps |
| S119 | 03-22 | NO price trap, correlated blowup, position stacking fixes |
| S108 | 03-19 | Fill pipeline: taker 0.85, bestAsk pre-filter, same-side dedup |
| S104 | 03-18 | Fill quality logging, exposure leak fix, alpha decay BUY-only |
| S100 | 03-17 | Alpha decay, canary persistence, SSH timeouts, backoff Redis |

---

## AUDIT DOCUMENTS

- **WeatherBot audit**: `AUDIT_WEATHERBOT_S127.md` — 12 bugs, 2 inefficiencies, 2 other. 8 fixed in S128, rest deferred.
- **Shared infrastructure audit**: `AUDIT_SHARED_INFRASTRUCTURE_S128.md` — 143 findings (3 P0, 18 P1, 52 P2, 58 P3, 12 P4). Partially addressed in S129 deploy.

---

## CURRENT STATE POST-S129

| Metric | Value |
|--------|-------|
| Deploy | `20260324_235616` |
| Open positions | 155 |
| Cost basis | $33,399 |
| Unrealized P&L | +$8.67 |
| All-time realized | **+$4,779.74** |
| Total resolutions | 2,101 (61.9% WR) |
| Active cities | 35 (106 in registry) |
| Spread inflation | Day 1.8, BASE≈0.124, FACTOR≈0.041 |
| Daily cap | Hit ($19,987 of $20,000) |
| Tests | 1,595 passed (full suite) |
| Errors | 0 (zero runtime errors post-deploy) |
