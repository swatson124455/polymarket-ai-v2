# WORK PROGRAM — MirrorBot / Polymarket AI V2

**Canonical reference for the multi-item work program. Read this in §0 orientation. Update it like code (commit changes).**

Created 2026-05-29 (S234) to stop the program living in conversational memory only — the same "memory ≠ reality" drift that produced operator-memory-vs-code gaps (S231 Bug 17) and the WALLET_LEDGER paper-vs-live drift caught in S234.

## Completeness status
**Complete as of 2026-05-29 (S234).** Items 2, 4, 6, 10, 12, 14 (+2b, +15) were specified during the S234 deploy work; items **1, 3, 5, 7, 8, 9, 11, 13 were supplied by the operator in the S234 close review** (originally left as TBD rather than invented — anti-fabrication). **WI-1 and WI-3 are already COMPLETE** (done this session). All others OPEN.

## Standing rules
- MB session is the VPS deploy channel; MB has priority on shared resources (CLAUDE.md SESSION PRIORITY).
- **No further deploys until WI-2 + WI-2b are resolved** — every deploy currently reinforces the consolidated-symlink drift.
- **Do not re-flip MB to live until the WI-14 gate is defined and met.**
- **Do not start WI-4 before WI-2 + WI-2b.**
- Don't add another tactical Bug 21 patch for the CB-OPEN-masking gap — that's WI-6's job.

## Items

| # | Item | Status | Depends on | Completion gate |
|---|---|---|---|---|
| 1 | **Deploy Bug 21** (terminal-exit close for resolved/delisted positions). | ✅ **COMPLETE (S234)** | — | Deployed in release `20260529_154845`; kill-switch storm collapsed. |
| 2 | **Splinter symlink topology verification.** Confirm whether WB/EB are meant to run their own splinter releases (`/opt/pa2-weather-current`, `/opt/pa2-esports-current`) or the consolidated master symlink. S233/S234 found all 4 services on the single `/opt/polymarket-ai-v2 → master` symlink; splinter symlinks absent. | OPEN | — | topology documented + matches intent; splinter symlinks restored **OR** splinter intent formally retired |
| 2b | **deploy.sh per-bot test gating** (or formal retirement of splinter intent). `deploy.sh` runs all of `tests/unit/`, so any bot's red tests block every bot's deploy (S234: EB test rot blocked the MB deploy). Either gate per-bot, or accept the shared-test-debt model explicitly. | OPEN (new S234) | couples WI-2 | deploy gates MB on MB+shared tests only, **OR** a committed decision doc accepting shared-test gating |
| 3 | **EB pre-existing test-failure coordination.** (Originally: a coordination doc for the red EB tests blocking deploy.) **Superseded** — the failures were root-caused as time-dependent test rot and fixed inline as part of the deploy unblock (`195a6fc`), not as a separate coordination doc. | ✅ **COMPLETE / SUPERSEDED (S234)** | — | EB `tests/unit/` green; absorbed into deploy unblock. |
| 4 | **bot_pnl.py per-bot segmentation.** | OPEN | WI-2, WI-2b, this file | per-bot P&L segmentation correct; no live/paper conflation |
| 5 | **Wallet-ledger reconciliation — forensic + on-chain.** Two-phase: (a) forensic reconstruction from `journalctl` `deposit_wallet_balance_pusd` probes across the 05-27/05-28 window; (b) on-chain transaction trace of deposit wallet `0xBB3988…` for the same window. Output: `WALLET_LEDGER.md` updated with complete history. | OPEN | — | ledger covers every wallet balance transition with timestamp + source; gaps closed or explicitly flagged unresolvable |
| 6 | **Position lifecycle module.** Explicit state machine that checks position state (RESOLVED / DELISTED / wallet-imbalanced) **before** any exit attempt. **Design note (S234): the state-check MUST precede BOTH `place_order` AND the Bug 11C balance guard (`mirror_bot.py:1601`). RESOLVED/DELISTED handlers MUST NOT depend on the SELL path completing or the terminal classifier (`mirror_bot.py:1688`) running.** Retires the Bug 21 tactical family + the CB-OPEN-masking gap. | OPEN | — | resolved/delisted/imbalanced positions route to write-off/redemption handlers without doomed SELLs; Bug 21 tactical path removable |
| 7 | **Safety-mechanism terminal states.** All safety mechanisms (Bug 17, `CircuitBreaker`, `kill_switch`, mid-runtime mode-flip guard) gain explicit terminal states: after N consecutive engagements without an intervening success → transition to **PERMANENT_HALT** requiring operator action to clear (auto-clear removed in that state). Audit every existing safety mechanism for "what's the terminal state when this keeps firing?" — the answer must NOT be "keep firing periodically." | OPEN | — | every safety mechanism has a documented + tested terminal state with a last-verified date in `THINGS_WE_TOLERATE` |
| 8 | **Schema invariants for the `positions` table.** DB-level CHECK constraints (`entry_price > 0 AND <= 1`, `size > 0`, `status IN ('open','closed','resolved')`, `is_paper IS NOT NULL`) plus a trigger that blocks `INSERT … (is_paper=false)` when the deposit wallet's most recent balance probe was below `entry_cost`. Catches the phantom-live-position class at insertion time. | OPEN | — | constraints active in prod DB; tests confirm rejection of invalid inserts; deploy.sh preflight verifies constraint state |
| 9 | **Reporting-tool test suite vs synthetic mixed-mode data.** Insert known synthetic positions (live/paper mix, various `entry_cost`/states), run `bot_pnl.py` with each `--mode` flag, assert output matches expected per-mode aggregations. Locks in the WI-4 fix. | OPEN | WI-4 | suite passes; integrated into CI; breaks the build if segmentation regresses |
| 10 | **Wallet-ledger auto-update from journalctl probes.** `WALLET_LEDGER.md` operational-state + balances auto-synced from bot probes. Rationale: manual maintenance fails silently — S234 caught the ledger still claiming paper mode ~3 days after the S232 live flip. | OPEN | — | ledger operational-state + balance table auto-refreshed from probes; no silent manual drift |
| 11 | **Audit-on-write hooks for the `positions` table.** Hook fires on `INSERT … WHERE is_paper=false`: verify deposit wallet had pUSD ≥ `entry_cost` at insertion; verify a corresponding `trade_events` ENTRY row was written within N seconds; log `DISCREPANCY` at WARNING if either fails; after M discrepancies in window, escalate to CRITICAL and engage the (now terminal-stated, WI-7) safety net. | OPEN | WI-7 | hook active; tested with valid + phantom insertion patterns; escalation verified end-to-end |
| 12 | **Review-checkpoint scheduling (calendar entries).** Operator-actionable — quarterly safety-net audit + other recurring review reminders. | OPEN | — | review checkpoints scheduled |
| 13 | **Root-cause current live failure mode + Bug 17 escalation verification.** After prior items land (clean tooling + structural exit semantics): root-cause why the 05-28 deploy produced **0 successful orders** (was Bug 16 NO→SELL translation actually fixed? did 05-28 introduce a regression? did Polymarket change server-side?). Verify Bug 17 escalation end-to-end: ~49 reopens observed → confirm the threshold value, confirm escalation fired at the correct count, confirm the escalation consequence matches operator memory. | OPEN | WI-4, WI-6, WI-9 (do last) | current live failure mode root-caused with documented fix path; Bug 17 verified end-to-end |
| 14 | **Live re-flip gate.** The criteria that must be satisfied before re-flipping MB to live (capital, safety nets, verification). **Do not re-flip until defined + met.** | OPEN | — | gate criteria documented + satisfied; operator sign-off |
| 15 | **Resolution-backfill audit** (new S234). `markets.resolved` reads `false` for known-resolved markets (all 3 S234 positions + the 4 S230 positions). Quantify drift scope: count `markets.resolved=false` across live-mode positions, cross-reference authoritative CLOB/on-chain resolution, root-cause (Phase 4b backfill gap in `database.py`). **If broad, any code path gating on `markets.resolved` — including paper-mode resolution accounting in `bot_pnl.py` — is operating on stale data.** | OPEN (new S234) | **run in parallel with / just after WI-4** (segmentation needs to know whether `markets.resolved` is trustworthy or needs CLOB cross-ref) | drift scope quantified; if structural, backfill fixed; code paths gating on `markets.resolved` audited |

## Change log
- **2026-05-29 (S234), part 2:** operator supplied items 1, 3, 5, 7, 8, 9, 11, 13 in the close review → populated. Marked WI-1 + WI-3 COMPLETE. Added dependencies (WI-9→WI-4, WI-11→WI-7, WI-13→WI-4/6/9). Sequenced WI-15 in parallel with/after WI-4. Noted WI-15's reach into `bot_pnl.py` resolution accounting.
- **2026-05-29 (S234), part 1:** file created. Added WI-2b and WI-15. Added WI-6 design note (lifecycle check must precede the SELL path + Bug 11C guard). 8 slots left TBD pending operator input.
