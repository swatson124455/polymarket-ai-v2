# KALSHI MAKER — ADVERSARIAL REVIEW DECISION BATCH — 2026-08-06 (operator-ruled 1a+, findings-first)

> **⭐ ADDITIVE NOTICE (2026-08-06, operator-ordered "triple blind findings to verify"): every MAJOR finding in this
> document was RE-VERIFIED by three independent blind verifiers. Results: `KALSHI_TRIPLE_BLIND_VERIFY_2026-08-06.md`.
> 93 blind votes, ZERO REFUTED. Per-finding majority: 23 CONFIRMED / 8 DOWNGRADE / 0 REFUTED; 22 of 31 panels
> unanimous. Six findings changed verdict vs the single-verifier pass — M-9 was RAISED (DOWNGRADE -> CONFIRMED),
> and M-1/M-10/M-14/M-15/M-20 came back DOWNGRADE on majority.
> **RULE NINE: nothing in this document has been demoted, reordered, or dropped.** Every M-number keeps its position
> and its place in decision items R-1..R-12; the verdict changes are REPORTED for operator ruling, not applied.
> The verdicts written inline below are the ORIGINAL single-verifier verdicts, preserved as-written for comparison.**

SCOPE ECHO-BACK (RULE TWELVE): this session's commits ae89543..7e43554 first (9 commits, checked against their
failing-before tests), THEN full line-depth on the 10 runtime money-path files (~10,900 lines, quoter first) AND the
canon-feeding measurement tools (netev rebuild/calibrate, settlement_pnl, presence_calibrate, ledgers, w16, w17, w11,
fill_costs, drift suite), THEN the end-to-end live logic flow. EXCLUDED BY OPERATOR RULING (named, not silently
dropped): the ~160 frozen one-shot study/diagnostic artifacts (VER*, AUD_*, _refute*, census/study scripts) —
reviewable later on operator order. Findings-first: NOTHING has been fixed; every item below awaits operator ruling.
Opportunities are CATALOGED with effort + expected value; nothing built (ruling 4a).

EXECUTION RECORD: 12 reviewers + 8 adversarial verify batches (20 agents, 0 errors), full multi-agent fan-out per
operator authorization. 147 deduplicated findings: 0 BLOCKER / 31 MAJOR / 61 MINOR / 55 NOTE. Verify pass covered
all 31 MAJORs: 27 CONFIRMED, 4 DOWNGRADE, 0 REFUTED. 116 catalog opportunities + 3 operator-ordered additions.
79 known-open-note assessments. Reviewed at HEAD e981bf1 (= deployed quoter md5 5c7aed6f, verified at step zero).
Suite baseline 1233 passed / 2 xfailed, PYTEST_EXIT=0 (this session, PIPESTATUS-captured).

POST-REVIEW LIVE-ENV MEASUREMENT (19:48:01Z read of /opt/pa2-maker-kalshi-live/live.env — changes two verdict
contexts; ESTABLISHED): KALSHI_DROP_GRACE=3, KALSHI_WS_HOT=1, KALSHI_WS_BOOK_COLD=1, KALSHI_SELECT_BUDGET=1,
KALSHI_NETEV_MIN_MARGIN_PCT=-7.0. The 08-06 EOD handoff flag list omitted WS_HOT/WS_BOOK_COLD — both are LIVE, so
the two WS-layer findings below are live-now, not latent; DROP_GRACE=3 weakens the strand transient-cancel finding
(single transients retained ~3 cycles) — reported as measurement, severity left to operator per RULE NINE.

====================================================================================================
## S1. DECISION ITEMS (each: finding refs -> options -> stated default; operator rules)

**R-1. Reference-price model root defect (CLASS).** The share/capture model anchors the reference at the BEST BID
and truncates the rival denominator at the cumulative-Target cut; the venue formula (08-05, ESTABLISHED) sets the
reference at the Target/5 cumulative-depth walk and scores every qualifying-side order. CONFIRMED independently by
4 reviewers + verify (see M-2, M-5, M-7, M-13, M-14; quoter:2558/:2566/:2731, scorecard:32). Every consumer is
contaminated: presence floor, capture gate, NETEV model fallback, F5-corrected shares, D-A max(model,venue) blend.
Thin-top books are over-ranked ~2-20x (worked examples in the findings); the model is decoy-manipulable. MECHANISM
FIX: one shared venue-faithful scorer (Target/5 walk + full-side denominator). STANDING DETECTOR: daily model-vs-
estimates-feed calibration residual (the D-A recorder is the instrument). DEFAULT: build dark, full NORM.

**R-2. NETEV bench arm is a live no-op — unit mismatch (M-27).** The table writes net_pct_notional as a FRACTION
(shipped table: temp -0.060929); the gate compares against KALSHI_NETEV_MIN_MARGIN_PCT=-7.0 (percent-scale,
live.env 19:48:01Z). -0.06 < -7.0 is never true -> a family must lose 700% of notional to bench. CONFIRMED. The
operator's margin-ladder mental model ('-7% tolerance') is not what runs. OPTIONS: (a) knob to fraction (-0.07) +
startup unit-coherence warning; (b) scale table to percent. DEFAULT: (a), dark, folded into the B8 margin re-rule.

**R-3. D-C macro-probe dark-path defects — ARM GATE (M-4, M-8, M-31).** Three CONFIRMED defects sit in the dark
D-C path queued for arming: (i) all-or-nothing breaks at placement — a post-only reject of a crossing rung rests a
partial, unqualifiable ladder carrying fill risk with zero reward (quoter:705 area); (ii) an L3 sibling taint marks
a DESIGNATED ticker explore and the 5ct explore clamp collapses the ladder (:5311 — no macro exemption, unlike
_d3_apply_ramp:646); (iii) the budget walk charges the macro ticker ~$5 while the ladder intends up to
MACRO_PROBE_USD/2 per side, and the per-order create capital gate can rest a partial ladder (:5070, :5851).
DEFAULT: fix all three dark BEFORE the D-C arm step in the queue; arming without ruling these is not offered.

**R-4. 0b payout-timing canon contamination (M-3).** w0b tags BEFORE_CLOSE against the event's LATEST market close;
a normal close+1 credit for an early-closing strike in a multi-market event prints as BEFORE_CLOSE. CONFIRMED.
The '9/57 BEFORE_CLOSE' evidence line in memory canon is therefore UNSAFE until re-derived per-market (the
estimates recorder's program_id->market map is the join). SENATEADJOURN-27 (credited while its market was open)
may well survive — but that is what the re-derivation must show, not assume. TIME-CRITICAL: the 2026-08-07T15:00Z
APRPOTUS est->credit checkpoint (queue item 1, gates D-A arming) interprets timing through this canon. DEFAULT:
re-derive the 57-credit classification per-market BEFORE the checkpoint read; canon text updated only on ruling.

**R-5. B8-poisoning measurement defects — TIME-CRITICAL, B8 due ~Aug 9-10 (M-21, M-22, M-16).** (i) netev_rebuild
scores still-OPEN markets as pure loss (open collateral = negative cash, no mark-to-market; :203) — D-C/FIX-H
far-close holdings are DESIGNED to be open at read time; (ii) whole-market attribution pulls PRE-window defect-era
cash into a clean window on straddling markets (:152) — the launch-defect-era exclusion canon cannot hold; (iii)
kalshi_fill_costs settled replay-fault fallback silently prices every settled market at cost 0 (:124). All
CONFIRMED. DEFAULT: fix (i)+(ii)+(iii) dark before B8; a B8 run without them is not a valid basis for the margin
re-rule or the P2 verdict cross-read.

**R-6. Estimates recorder map atomicity — TIME-CRITICAL, program endings 08-09 (M-6/M-17 duplicate pair).**
Non-atomic json.dump + bare-except reset-to-{} can permanently lose ended-program rows exactly when the FIX-H and
est->credit joins need them. CONFIRMED. DEFAULT: tmp+os.replace (the sibling pattern at attribution_ledger:269-273
/ fill_costs:172-175), S effort, deploy with next naming.

**R-7. Exit-retention / halt-path class (M-19, M-20, M-11; flow M-30 DOWNGRADE).** CONFIRMED: (i) quote-loop
read-budget RuntimeError mid-loop cancels the in-flight market's standing book incl. exits — PIVOT_SELECT puts it
in fp_tickers, grace-INELIGIBLE (:5230); (ii) STOP-flatten pass 1 rests fresh exit offsets over swallowed failed
cancels — stacked reducing size can cross THROUGH flat while halted (:6572; the main cycle already guards this
class via C7, the flatten path does not). MEASURED CONTEXT: DROP_GRACE=3 live (19:48:01Z) retains single strand
transients (M-19's scenario needs >3 consecutive failures or budget exhaustion); flow-M-30's version of the same
class was DOWNGRADEd by verify for the hysteresis-protected subset. DEFAULT: retention on the RuntimeError
branches + failed-cancel deferral in _flatten_all (mirror C7), dark.

**R-8. WS layer — LIVE NOW (M-18; daemon M-12 DOWNGRADE; env 19:48:01Z: WS_HOT=1, WS_BOOK_COLD=1).** CONFIRMED:
a venue error frame leaves WS mirrors CLEAN+FROZEN through the whole backoff sleep (up to 60s) — cold cycles read
dead books as fresh (kalshi_ws_feed:271; the seq-gap and disconnect arms dirty mirrors, the error arm does not).
DOWNGRADE-but-real: the hot path omits own_orders — F5 gates re-read double-counted shares, so hot repricing is
silently DISABLED on exactly the solo/thesis books (no wrong orders; verify-confirmed). DEFAULT: dirty-all-mirrors
on error frames + thread ctx.standing into the hot desired_quotes call, dark.

**R-9. PRESENCE_GATE flat-activate structural refusal — seed lane dead (M-10).** The activate $1-floor check runs
the share model on the RAW book; a void book while flat is BELOW Target by definition -> expected credit $0 ->
refused every cycle. The bot can never bridge a 900/1000ct book it could profitably complete. CONFIRMED. Directly
blocks the D-D symbiote thesis (queue item 3) and is the code path the 08-06 memory flagged as the D-C lead.
DEFAULT: rule jointly with the D-D design — the fix (score the PROSPECTIVE book = raw + our candidate depth) IS
the symbiote entry condition.

**R-10. F3 floor x restore-order latent rung-0 deadlock (M-1).** _expected_credit_usd scales by _d3_est_ct which
reads rung 0 for any ticker absent from _D3_FIRST_SEEN; both restore sites are conditional (SELECT_BUDGET guard /
post-quote belt). Latent today (SELECT_BUDGET=1 live) but one env flip from book-wide 10x under-admission after a
restart. CONFIRMED. DEFAULT: unconditional _d3_first_seen_ensure at cycle start when D3_RAMP=1, dark.

**R-11. Canon-feeding tool corrections batch (M-23, M-24, M-25, M-15, M-26 DOWNGRADE-but-real, M-28, M-29).**
CONFIRMED: ledger_reconstruct re-implements the REFUTED side-derived sign model (wrong on every sell-no fill —
~half the tape); kalshi_ledger headlines a RULE-ZERO-banned residual as 'REWARDS (derived)' and its docstring
denies credit_history exists; the scorecard still divides the R1 pool by window days (the refuted form); w11
replay's 'recorded' baseline is the shadow ranking, cyc['actual'] never read; w17's B-4 staleness sentinel
lexically never alarms; drift-suite mids have no book-sanity filter (the largest measured runover step is a
shell-book artifact) and windowed detectors treat candle INDEX as minutes (750/4893 = 15.3% of in-window steps
are gaps, measured on the frozen data). DEFAULT: fix batch + a QUARANTINE-UNTIL-RERUN label on the affected
published outputs (labels are a flag, not a deletion; every removal needs per-item authorization).

**R-12. Everything else.** 61 MINOR + 55 NOTE findings (S3/S4), 79 known-open-note assessments (S5), the
116-item opportunity catalog + 3 operator-ordered additions (S6/S7). Operator names what elevates; nothing has
been reordered or dropped.

DOWNGRADE verdicts for the record (verify pass; operator may overrule): M-9 activate emitter INV_HARD_CT clamp
(masked by M-10's refusal under live flags), M-12 daemon hot own_orders (churn mechanism refuted — gates only),
M-26 w17 B-4 sentinel (real bug, alarm-only blast radius), M-30 flow read-budget teardown (hysteresis-protected
subset).

====================================================================================================
## S2. MAJOR FINDINGS — full text with adversarial verdicts (M-1..M-31)

### M-1 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1320
**F3 ties the floor gate to _D3_FIRST_SEEN restore order: rung-0 deadlock after restart with SELECT_BUDGET=0**  (reviewer: commits:0b+recorder+DB-DG)
- MECHANISM: _expected_credit_usd (F3) scales pc by _d3_est_ct/join_ct, and _d3_est_ct reads `(_D3_FIRST_SEEN or {})` — unknown/unrestored map = rung 0 (5ct). _D3_FIRST_SEEN is restored in only two places: _d3_first_seen_ensure at line 5019 (guarded by `if SELECT_BUDGET and footprint` — SELECT_BUDGET defaults 0 at line 1011) and the quote-loop belt at line 5324 (guarded by `if q and D3_RAMP`, which runs AFTER desired_quotes returns q for that market). The gate call inside desired_quotes therefore precedes every belt restore.
- FAILURE: Restart with D3_RAMP=1, SELECT_BUDGET=0, no held inventory: cycle 1, market 1 — gate evaluates an established rung-3 market (real join 50ct) at 5ct, expectation cut 10x, presence gate refuses, q=[] → belt never runs → _D3_FIRST_SEEN stays None → every market every cycle keeps evaluating at rung 0. Self-sustaining book-wide under-admission until some market clears the floor even at 5ct or an unwind quote triggers the belt. Live is believed to run SELECT_BUDGET on (budget figures in plan rows), so this is latent, not firing today — but it is one env-flag flip away.
- VERIFY: Every element of the claimed mechanism verified in code. (a) _d3_est_ct reads `(_D3_FIRST_SEEN or {})` at :625 — unrestored None → rung 0 → D3_RUNGS[0]=5ct. (b) _expected_credit_usd calls it under `if D3_RAMP:` at :1321-1331 and scales pc by eff_ct/join_ct — a real rung-3 market is evaluated at 10x-cut expectation. (c) Only two restore sites exist (grep confirms): _d3_first_seen_ensure at :5019, reachable only inside `if SELECT_BUDGET and footprint:` (:5015; SELECT_BUDGET default 0 at :1011), and the belt at :5322-5334, guarded by `if q and D3_RAMP:` — which runs only AFTER desired_quotes returned a non-empty q for some market. (d) The presence gate at :2938-2951 returns [] for a flat market under MIN_CREDIT_USD, and the prune site at :6093 is guarded `_D3_FIRST_SEEN is not None`, so if every footprint market is refused at rung-0 valuation and nothing holds inventory (unwind quotes bypass the gate at :2952 and would break the deadlock), _D3_FIRST_SEEN stays None indefinitely — the deadlock is self-sustaining exactly as claimed. The finding honestly scopes its own limits: any single market clearing the floor at 5ct (large pool, or an explore probe that quotes) restores the map globally and ends it, and live is believed to run SELECT_BUDGET on (plan rows carry select_budget_used, set only under the flag at :5088). Latent-but-one-env-flip-away with a lost-reward (not lost-capital) blast radius; the weaker form — first-cycle rung-0 evaluation of every market preceding the first non-empty q after ANY restart with SELECT_BUDGET=0 — fires unconditionally, which is precisely the C-1 restart-noise class the :560-565 docstring says was fixed (it was fixed only for the budget-walk consumer, not the floor-gate consumer). MAJOR per the 'wrong under a reachable condition' bar is fair.

### M-2 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2558
**Share model reference = best bid, not the venue's Target/5 cumulative-walk reference (LIP formula, 08-05)**  (reviewer: commits:0b+recorder+DB-DG)
- MECHANISM: _qualifying_score sets `ref = bids[0][0]` (highest bid) and applies df^ticks from THAT price; the venue-published formula defines per-side Reference Price as the first level where CUMULATIVE resting size >= Target/5 walking from best bid, with 1.0x for orders at/better than reference. When the top of book is thin (e.g. a 1-ct decoy at a high price), the venue reference sits deeper: rival orders between best bid and the true reference earn 1.0x from the venue but get df^n<1 from the model — the rival denominator is systematically understated. Also the model total stops accumulating at the Target cut, while the venue scores all resting orders on a qualifying side.
- FAILURE: A rival rests 1ct at 0.45 over 1000ct at 0.40 (Target 1000, Target/5=200 → venue ref 0.40). Model ref=0.45: the 1000ct rival scores 0.5^5*1000≈31 df instead of 1000, so our modeled share at 0.45 is ~60% vs a true ~5% — the presence floor, capture gate, net-EV model fallback, and the F5-corrected telemetry/SCORES ranking all over-admit and over-rank exactly thin-top books, and the model is decoy-manipulable. Pre-existing (also in the kalshi_market_scorecard replica), but the 08-06 audit/D-B commits claim the fresh venue formula and did not correct it; F5's corrected shares inherit the wrong reference.
- VERIFY: Verified: _qualifying_score sets `ref = bids[0][0]` at :2558 and scores every deeper level df^n from THAT price (:2561-2563), and it stops accumulating the rival total at the Target cut (`if cum >= target: break`, :2566-2567). Grep across kalshi_live/*.py finds no Target/5 cumulative-walk reference anywhere — kalshi_market_scorecard.qualifying_share (:26-28) documents the same 'reference = highest bid' model, so the replica and the original share the defect. Against the ESTABLISHED venue formula (reference = first level where cumulative size >= Target/5 from best bid; at/better than reference = 1.0x; qualification is a side-level condition, not a per-order cumulative cutoff), the failure arithmetic checks out exactly: rival 1ct@0.45 over 1000ct@0.40, Target/5=200 → venue ref 0.40, both rival levels 1.0x, venue denominator ≈1001; model ref 0.45 → 1000ct scores 0.5^5·1000≈31.25, denominator ≈32.25, so our 50ct join at 0.45 models raw≈1.55 → share≈61% vs true ≈4.8%. Consumers verified: _prospective_capture (:2597-2603) feeds the presence/$1-floor gate (:2917-2951, live PRESENCE_GATE=1, no haircut applied there), the capture gate (:3105), the NETEV unproven-family model fallback, and the SCORES/telemetry rank — all systematically over-admit and over-rank thin-top books, and a 1ct decoy above real depth manipulates the model exactly as claimed. F5's own_orders subtraction (:2570-2577) inherits the wrong ref. MAJOR is the right severity: it mis-gates and corrupts model-side measurements on a reachable book shape but is a relative-signal model error, not a direct wrong-order path.

### M-3 [CONFIRMED] [ESTABLISHED] kalshi_live/w0b_payout_timing.py:92
**0b tags BEFORE_CLOSE vs the event's LATEST close — multi-market events can mint a false timing canon**  (reviewer: commits:0b+recorder+DB-DG)
- MECHANISM: Credits are per EVENT (reason string carries only the event ticker), but delta is computed as credit_ts − max(close_time over ALL the event's markets). A credit that is in fact close+1 for an individual market that closed earlier than the event's last market lands at delta<0 and is printed as 'BEFORE_CLOSE ... direct evidence payment is NOT gated on close'. The (mkts N) count is printed but the summary tallies and the explicit BEFORE_CLOSE discriminator section do not distinguish single- from multi-market events.
- FAILURE: Event with strikes closing 08-02 and 08-20; program for the early strike pays 08-03 → tool prints BEFORE_CLOSE −408h and the operator (or a later session reading the transcript) ratifies 'venue pays before close' as canon, mis-arming the FIX-H cash-lock assumption and the D-A est→credit checkpoint interpretation. Only single-market events (or per-market attribution via the estimates recorder's program_id→market_ticker map) are valid discriminators.
- VERIFY: Verified in w0b_payout_timing.py: credits are keyed by EVENT ticker parsed from the reason string (:74-78), ev_close stores (closes[0], closes[-1], len(mkts)) at :84-85, and the per-credit delta at :99-100 uses `close_max` — the maximum close_time across ALL the event's markets. The tag at :101-103 prints BEFORE_CLOSE for any d_h<0, the summary at :109-112 tallies without distinguishing single- from multi-market events, and the explicit discriminator section at :113-116 ('payment not gated on close — the 0b discriminator') likewise never filters on nmk. So a program paying close+1 for an early-closing market inside an event whose sibling closes weeks later produces a large negative delta and is presented as direct evidence against close-gating — a false positive in exactly the direction the tool exists to discriminate. The (mkts N) count is printed per row (:105) but nothing downstream conditions on it, so a transcript reader or the summary line can ratify the false canon. The confounding population is not hypothetical: the FIX-H far-close class this tool was ordered for is precisely where program end and close diverge, and events mixing near- and far-close markets are the ones being admitted. MAJOR under 'corrupts canon measurements' is correct — this is the evidence tool for the cash-lock/FIX-H timing question and the 08-07 APRPOTUS checkpoint interpretation.

### M-4 [CONFIRMED] [INFERRED] kalshi_live/maker_kalshi_quoter.py:705
**Macro ladder all-or-nothing breaks at placement: post-only reject of one rung rests a partial, unqualifiable ladder**  (reviewer: commits:DA+DC+followups)
- MECHANISM: _macro_probe_quotes decides affordability/qualification all-or-nothing at quote-BUILD time, but each rung is placed as an independent post_only order (create path :5859, client :212-248). A yes bid at MACRO_PROBE_TOP=0.03 crosses whenever a rival NO bid rests at >=0.97 (decided/longshot book, or a mid-flight price collapse — and macro designation EXEMPTS late-life/far-close drops per review #6, so a designated market can ride into decided territory). The venue rejects the crossing rung(s) post-only while the lower rungs rest, leaving side depth ~60% of Target — depth that can never qualify under LIP (side must reach Target Size), i.e. pure fill risk with zero reward, exactly the class the 'rest NOTHING' rule (:711-715) was built to prevent. Additionally, on such a book the surviving 1-2c rungs sit at/near fair, so the 'any fill is far below fair' premise of the reserved-$=max-loss argument does not hold there. Armed-only; flags are OFF today.
- FAILURE: Operator designates a macro ticker; the market later decides toward NO (yes fair ~2c). Cycle builds the 3-rung yes ladder; the 0.03 rung is post-only rejected, 0.02/0.01 rungs (≈596ct, ≈$9) rest; side never reaches 1000ct Target; a seller hits the 2c bids on genuine information; bot pays fill-risk dollars for a side that mathematically cannot earn a cent of LIP reward.
- VERIFY: Verified end-to-end. Build: _macro_probe_quotes (:681-724) decides affordability/qualification per side at quote-build time and checks only target, own-depth subtraction, prices>=0.01, and cost<=side_cap — it never consults the OPPOSITE side's book, so a yes rung at MACRO_PROBE_TOP=0.03 is built even when a rival NO bid rests at >=0.97 (yes ask <=0.03). The crossed-book safety gate at :2859 does NOT catch this: it tests best_y+best_n>=1.0 on the EXISTING book (e.g. best_y 0.01 + best_n 0.97 = 0.98 passes), while it is OUR ladder that crosses. Placement: each rung is an independent create_quote(post_only=True) at :5857-5859; a GTC post-only order that fails to rest raises in create_order_v2 (:232-238 'order not resting'), caught per-order at :5871 as create_fail with no compensating cancel of sibling rungs — no ladder atomicity exists. Surviving rungs carry weights 0.3+0.3 of need (:711-713) ≈60% of Target, and under LIP a side short of Target earns zero for everyone — exactly the 'partial depth cannot qualify = pure fill risk' class the function's own :691-692 rest-NOTHING rule articulates. Persistence: the standing diff re-attempts the crossing rung each cycle until the J6 per-ticker create ratchet (:5833) suppresses creates, while the resting partial rungs are never pulled (they match desired), so the unqualifiable partial ladder persists. Reachability: designation exempts the market from the quality gates (:2863-2870) and, per review #6, from late-life/far-close drops, so a designated market can ride into decided territory where the surviving 1-2c rungs sit at/near fair and fills are informed, not bargains. Armed-only today (MACRO_PROBE_TICKERS empty) with loss bounded by side_cap and the $10 daily halt, but D-C arming is queued — MAJOR ('wrong under a reachable condition') is the right severity.

### M-5 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2559
**Reward model anchors the Reference Price at the best bid, not the venue's Target/5 cumulative-depth level**  (reviewer: quoter:1-2600 select+gates)
- MECHANISM: The venue-published LIP formula (08-05, ESTABLISHED canon in this task) defines the per-side Reference Price as the first level where CUMULATIVE resting size >= Target/5, with 1.0x distance-mult for anything at/better than that level. `_qualifying_score` (:2555-2581) and `_qualifying_breakdown` (:2718-2741) instead set `ref = bids[0][0]` (best bid) and penalize every deeper level by DF^ticks-from-best-bid. Grep confirms NO module in kalshi_live implements a Target/5 walk; kalshi_market_scorecard's own docstring says 'reference = highest bid'. Rivals resting between the true reference and the best bid are venue-scored 1.0x but model-scored 0.5^n, so book_total is understated and our share overstated. This basis feeds the LIVE $1.20 floor gate (PRESENCE_GATE=1 via _expected_credit_usd), the SCORE_RANK cache, the D2 feedback basis, the net-EV model fallback, the sweeper, and every capture_usd_day telemetry row. It also inverts the throttle-step economics: a quote 1 tick behind a thin touch can still be at/better than the true reference (full 1.0x venue credit) while the model believes it halved.
- FAILURE: Book: 10ct dust at 0.55, 900ct at 0.50, Target 1000 (Target/5=200). Venue reference = 0.50; the 900ct rival scores 1.0x, total ~910. Model: ref=0.55, rival scores 0.5^5=0.031x, total ~38. Our 100ct join at 0.55 models a ~20x larger share than the venue pays -> _expected_credit_usd clears the $1.20 floor on a market whose real period credit is sub-$1 -> we rest, carry fill risk, and the credit is forfeited at the venue's $1 cliff (the exact W10 drive-by class the gate exists to stop). The same bias corrupts the score cache ranking toward books with depth spread below the touch.
- VERIFY: ESTABLISHED. _qualifying_score sets ref = bids[0][0] at maker_kalshi_quoter.py:2558 and discounts by df**round((ref-price)/TICK) (:2562-2563); _qualifying_breakdown repeats it (:2731-2735). Grep over all of kalshi_live/ finds NO Target/5 cumulative-reference walk in any module; kalshi_market_scorecard.py:27-28,:32 documents 'reference = highest bid'. Per the ESTABLISHED venue LIP formula (reference = first level where cumulative size >= Target/5; at/better = 1.0x), any book whose touch holds < Target/5 has rivals below the touch venue-scored 1.0x but model-scored 0.5^n — denominator understated, our share overstated. Consumer chain verified in code: _prospective_capture (:2597-2603) -> _expected_credit_usd (:1316) -> live PRESENCE_GATE join path (:2917-2919) AND activate path (:3105-3107), NETEV unproven-model fallback (:3021-3024), capture gate (:3046), sweeper _sweep_measure (:2651+), and telemetry capture_usd_day (:2758-2797). The failure arithmetic in the scenario checks out (0.55 dust/0.50 wall, Target 1000: venue total ~910 @1.0x vs model ~38). The bias is book-shape-dependent, so the scalar NETEV_MODEL_HAIRCUT=3.0 cannot absorb it and it corrupts relative ranking, not just absolute level. MAJOR is correct (wrong gate admits/refusals on a reachable book shape + corrupts every capture/calibration measurement). NOTE: this is the same defect as finding 3 — the two should be merged into one finding.

### M-6 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3105
**PRESENCE_GATE activate credit check structurally refuses EVERY flat activate — seed lane dead, not gated**  (reviewer: quoter:2500-5000 sizing+budget)
- MECHANISM: The activate-path $1 floor (:3098-3110) calls _expected_credit_usd -> _prospective_capture -> _qualifying_score, which returns (0.0, False) whenever a side's RAW cumulative depth < Target (:2568-2569). A void book when we are FLAT has raw depth = external depth < Target by the definition of void (:2896, own=0), so pc is identically 0, expected credit is $0.00 < MIN_CREDIT_USD (1.20), and the branch returns [] on every flat void book. The gate can only pass when our OWN resting depth already brings the raw book to Target — i.e. only steady-state maintenance of an activate we could never have started. With PRESENCE_GATE=1 live, gate_activate_credit is a constant-refuse for fresh entries; the 08-02 comment frames it as a check ('same floor, same model'), not a lane kill.
- FAILURE: A $500/day program with rivals resting 900 of 1000ct Target — 100ct short, affordable within MAX_ACTIVATE_CAPITAL, near-certain qualification once bridged — is refused every cycle with gate_activate_credit. The bot never supplies the depth, earns $0 there all window, and the only remaining route into empty/thin books is the operator-designated D-C macro probe (dark). The activate lane the code still documents and counts (activate_markets, gate_activate_cost) has been silently unreachable since PRESENCE_GATE armed.
- VERIFY: ESTABLISHED. Chain verified end-to-end: void = external (own-subtracted) depth < target on either side (:2893-2896); the activate open path requires flat (:3085); the PRESENCE_GATE activate check (:3098-3110) calls _expected_credit_usd -> _prospective_capture -> _qualifying_score, which judges qualification on the RAW book and returns (0.0, False) when cum < target (:2568-2569); _prospective_capture returns 0.0 unless BOTH sides qualify (:2601-2602). Flat with no resting orders means raw = external < target by definition of void, so pc is identically 0, expected = 0*days = $0.00 < MIN_CREDIT_USD, return [] at :3110 — every cycle, every fresh activate, including the 100ct-short scenario (one side failing suffices per :2601). The EST_FEED floor (:1334-1340) is dark (KALSHI_EST_FEED=0) and would carry no estimate for a never-quoted market anyway. The only pass-through is raw>=target via our OWN already-resting orders — the bootstrap the gate itself prevents, exactly as claimed. The design/implementation mismatch is provable in-file: the earlier _qualifiable gate (:2905-2906) deliberately models addable depth (ext + MAX_ACTIVATE_CAPITAL/price >= target), and the capture gate comment (:3043) documents void books were scoped OUT because 'we supply Target depth there' — the activate presence check (:3099-3104, framed 'Same floor, same model') omitted both. MAJOR is correct: not a money-losing path (over-conservative), but a documented earning lane silently unreachable with PRESENCE_GATE=1 live, and its counters (gate_activate_credit) misreport a structural refusal as a market-quality verdict.

### M-7 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2558
**Share model anchors reference at BEST BID; venue LIP reference is the Target/5 cumulative walk**  (reviewer: quoter:2500-5000 sizing+budget)
- MECHANISM: _qualifying_score (:2555-2581), _qualifying_breakdown (:2718-2741) and _prospective_capture's join-at-reference (:2584-2603) all set ref = highest bid and discount every deeper order by DF^ticks-from-best. The venue formula (ESTABLISHED 08-05) sets the per-side Reference Price at the first level where CUMULATIVE resting size >= Target/5, and pays 1.0x to everything at-or-better than THAT. On any book whose top level holds < Target/5, rival size (and our own resting orders' rest_df subtraction, :2575, :2772) sitting between best and the true reference is under-counted by the model but fully scored by the venue — denominator too small, our share and capture_usd_day too big.
- FAILURE: Book: best bid 10ct thin, 400ct one tick down, Target 1000 (Target/5 = 200 -> venue ref is the second level, both levels 1.0x). Model scores the 400ct at 0.5x, halving the rival denominator -> prospective capture and expected credit roughly double -> the live $1.20 presence floor and the NETEV model fallback admit markets whose true share cannot clear the floor (the W10 sub-$1 drive-by class), and every quotes-*.jsonl capture row + SCORES rank carries the same skewed basis into B8 calibration.
- VERIFY: ESTABLISHED — but this is a DUPLICATE of finding 1 (same root defect, same lines, same consumers); merge them. Verified independently: ref = bids[0][0] at :2558 (_qualifying_score), :2731 (_qualifying_breakdown), and the join-at-reference model in _prospective_capture scores our order at DF^0 against best bid (:2589, :2597-2600); the F5 rest_df subtraction (:2575-2576, :2770-2776) uses the same best-bid tick exponents, so our own resting orders' df-weight is also mis-anchored as claimed. No Target/5 walk exists anywhere in kalshi_live (grep), and the scorecard the function is pinned byte-equivalent to (docstring :2552-2554) has the same best-bid anchor (kalshi_market_scorecard.py:27-32). Failure arithmetic checks out: touch < Target/5 puts the venue reference deeper, rival size between touch and true reference is under-counted (0.5^n vs 1.0x), inflating prospective capture into the live $1.20 floor (:2917-2919) and NETEV model fallback (:3021-3024), and skewing capture_usd_day rows (:2792-2797) that feed B8 calibration. MAJOR severity appropriate for the merged finding, per finding 1's reasoning.

### M-8 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5311
**L3 taint can mark a DESIGNATED macro ticker explore; explore clamp collapses the D-C ladder to 5ct dust**  (reviewer: quoter:2500-5000 sizing+budget)
- MECHANISM: The L3 sibling re-clamps (:4094-4104 and :4578-4583) set explore=True on ANY footprint row whose series has a permanently-OUT member — designation is not checked (D-C review #6/#11 exempted late-life/far-close/incumbent-strip only). The explore probe clamp (:5311-5317) then caps every non-'unwind' quote at EXPLORE_PROBE_CT=5, and unlike _d3_apply_ramp (:646, which exempts 'macro_probe') it has no macro exemption. Separately, an explore-tainted non-allowlist macro row enters _cap_probe_slots competition (:3265-3314) and can be dropped from rows entirely (slot cap or probe_close_unknown) despite designation.
- FAILURE: Operator arms KALSHI_MACRO_PROBE_TICKERS with a ticker whose series later takes a $5-rung ban on any sibling. Next cycle the L3 re-clamp tags the designated row explore; the ladder desired_quotes built to meet Target 1000 is clamped to 5ct per level (15ct/side) — the side never qualifies, the deep-discount dust rests as pure fill risk, macro_probe_markets still counts a success, and the receipts D-C exists to produce never arrive. No test covers this interaction (the select_footprint macro hunks are untested per D-C #8).
- VERIFY: ESTABLISHED for the primary mechanism; one sub-claim REFUTED. Confirmed: both L3 re-clamp sites set explore=True on any footprint row whose series has a still-open permanently-OUT member, checking ONLY `not r.get('explore')` + series membership — no MACRO_PROBE_TICKERS check (:4094-4104 selection-time; :4578-4585 governor-time re-clamp) — despite 'designation IS the override' doctrine at :2863-2867 and the review #6/#11 designation exemptions elsewhere. The explore clamp (:5311-5317) caps every quote with reason != 'unwind' at EXPLORE_PROBE_CT (live 5ct); macro ladder quotes carry reason='macro_probe' (:720-721) with need-covering counts (~400/300/300 per side for Target 1000, :713), so the tainted ladder collapses to 15ct/side — the exact formula-invisible failure D-C documents at :668-670. Asymmetry proves oversight: _d3_apply_ramp exempts 'macro_probe' explicitly (:641-646, blind-review BLOCKER #1) while the explore clamp exempts only 'unwind'. Also confirmed: macro_probe_markets increments at quote-build (:722-723) before the downstream clamp, so telemetry counts a success. REFUTED sub-claim: the _cap_probe_slots drop path cannot fire — select_footprint (sole call site :4082, containing _cap_probe_slots at :2187) rebuilds rows fresh each cycle; designated tickers get probe_only=False (:2004-2009) so no explore key at row build (:2064), and the L3 taint runs only AFTER selection returns, so a designated row is never in probe_idx at slot-competition time and cannot be dropped by slot cap or probe_close_unknown. Severity: MAJOR stands with a caveat — currently unreachable live (KALSHI_MACRO_PROBE_TICKERS empty, D-C dark), but D-C arming is the operator-ruled next step and the triggering condition (a $5-rung ban on any still-open same-series sibling, with EXPLORE_PROBE_CT=5 armed) is realistic on daily-minted strike series; must be fixed before arming.

### M-9 [DOWNGRADE] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3126
**Activate emitter clamps at INV_HARD_CT vs dollar-based gate — rests depth that cannot reach Target**  (reviewer: quoter:2500-5000 sizing+budget)
- MECHANISM: The unqualifiable gate (:2905-2906) judges bridgeability with _addable = MAX_ACTIVATE_CAPITAL / max(best) — a DOLLAR budget (e.g. 150/0.05 = 3000ct). The activate emitter then clamps each side's count at _acl = INV_HARD_CT = 80 (:3125-3131, audit F1). Whenever the shortfall target−ext exceeds 80ct but its cost fits MAX_ACTIVATE_CAPITAL, the gate passes, the branch rests 80ct, the side still ends below Target, and per R3 the snapshot pays $0 to everyone while our 80ct carries full adverse-fill risk. The two limbs disagree on what 'we can fund it' means.
- FAILURE: Void book: Target 1000, ext_y=100 at 0.05, ext_n healthy. add_y=900, cap = $45 <= MAX_ACTIVATE_CAPITAL -> passes the cost gate; emitter rests min(900,80)=80ct. Book tops out at 180ct — never qualifies, we earn $0 and hold an 80ct fill magnet. Currently masked for FLAT entries by the presence-gate constant-refuse (finding 1); becomes live the moment that gate is fixed or PRESENCE_GATE is toggled off, and is already reachable while maintaining a legacy resting activate.
- VERIFY: Code mismatch is ESTABLISHED: the unqualifiable gate judges bridgeability with _addable = MAX_ACTIVATE_CAPITAL/max(best) at :2905 (dollar budget, e.g. 150/0.05=3000ct) while the emitter clamps at _acl=int(INV_HARD_CT)=80 at :3125-3131 (INV_HARD_CT default 80 at :1555). BUT under the LIVE flag set the failure scenario is unreachable: PRESENCE_GATE=1 routes every void-book activate through the credit gate at :3098-3110 BEFORE the emitter, and _expected_credit_usd -> _prospective_capture (:2601-2603) -> _qualifying_score returns (0.0, False) whenever a side's RAW book cannot cumulate to Target (:2568-2570, 'book can't reach Target on this side -> $0 for everyone'), so _expA=0.0 < MIN_CREDIT_USD=1.20 (:1213) and the function returns [] at :3107-3110. Since the emitter can never rest >80ct, our own resting depth can never bridge a >80ct external shortfall, so the exact window where the clamp binds (shortfall>80) is precisely the window the presence gate refuses — the two limbs' disagreement is fully masked while PRESENCE_GATE=1. The sub-claim 'already reachable while maintaining a legacy resting activate' is REFUTED under live flags: qualification is judged on the raw book INCLUDING our resting order (:2552-2554 docstring), so a legacy activate on a still-raw-void book yields _expA=0 -> [] -> diff_orders CANCELS the legacy order rather than maintaining it. Residual exposure: only with PRESENCE_GATE=0 (the code default at :1211, but live=1), or a one-cycle transient where a previously-healthy book collapses while our large join still rests raw-qualifying. Real latent inconsistency worth a catalog entry, not a MAJOR under current live flags.

### M-10 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5463
**Strand-unwind loop cancels a live resting EXIT on any transient book-read failure (no retention, unlike quote loop)**  (reviewer: quoter:4900-7317 exec+exits)
- MECHANISM: Steady state for a held ticker outside the footprint: the strand-unwind pass (:5455-5492) re-computes desired[t] every cycle so the diff is a no-op. But on `except RuntimeError: break` (:5461, read budget exhausted) or `except Exception: continue` (:5463, one transient 5xx/timeout), desired never gets an entry for t. The ticker is not in fp_tickers, so with DROP_GRACE=0 (code default; DROP_GRACE not in the live flag set) apply_drop_grace never runs for it, and diff_orders (:3475-3492) emits cancels for ALL its standing orders — including the live reducing unwind. The strand loop itself has already moved on, so nothing re-rests an exit this cycle. The quote loop has explicit fetch-fail retention for exactly this class (:5231-5258, review 07-22 skeptic); the strand loop never received the same fix.
- FAILURE: Held naked +10 on a rotated-out ticker with its NO unwind resting at the touch; one orderbook GET times out → this cycle cancels the exit (queue position destroyed), position has NO resting exit until next cycle re-rests at the back of the queue; a flapping book feed alternates cancel/create every other cycle, burning write tokens and keeping the exit perpetually at the back of the queue while the strand clock runs toward a paid taker cross.
- VERIFY: Mechanism verified end-to-end (ESTABLISHED). Strand loop at :5455-5492: for a held ticker outside fp_tickers, `except RuntimeError: break` at :5460-5461 and `except Exception: continue` at :5462-5463 both leave desired without an entry for t and the loop moves on (or aborts). No retention exists on either branch — contrast the quote loop's explicit fetch-fail retention+unwind-tagging at :5231-5258, which the strand loop never received. DROP_GRACE defaults 0 (:366), is NOT in the live flag set, apply_drop_grace is only called when DROP_GRACE>0 (:5712-5714) and early-returns for grace_cycles<=0 anyway (:3403-3404); even if enabled it only covers tickers absent from footprint, which this ticker is — but it is not enabled. diff_orders (:3475-3492) iterates set(standing)|set(desired) and emits cancels for every standing order whose ticker has no desired entry, unwind-tagged or not (diff is reason-blind, keys on side/price/count). The reducing create next cycle re-rests at the back of the queue; nothing guards this class (failed_cancel machinery at :5799-5843 is about failed cancels, not missing desired). The RuntimeError `break` variant is strictly worse: ALL subsequent held out-of-footprint tickers lose their exits that cycle. Severity MAJOR is correct: reachable on a single transient 5xx/timeout, damages live exits (queue position, write tokens, strand-clock drift toward taker cross), but does not by itself produce wrong position sizes.

### M-11 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5230
**Read-budget exhaustion mid-quote-loop cancels the in-flight market's whole standing book with no retention**  (reviewer: quoter:4900-7317 exec+exits)
- MECHANISM: In the quote loop, `except RuntimeError: break` (:5229-5230) fires when public_get's budget (READ_BUDGET_PER_CYCLE=200) exhausts. Under PIVOT_SELECT=1 (live) the market was already appended to `consumed` (:5219) BEFORE the fetch, so after `footprint = consumed` (:5438) it IS in fp_tickers — grace-ineligible ('we looked at it') — yet has no desired entry and no fetch-fail retention (the generic-Exception branch :5231-5258 retains standing + tags unwind; the RuntimeError branch does not). diff_orders cancels everything resting on it, exits included. If it is a held ticker, the strand loop also skips it (t in fp_tickers, :5456). With PIVOT_SELECT=0 the damage widens to every unvisited pool-tail market with standing orders.
- FAILURE: WS mirror degrades (src_err) so books fall back to REST; budget exhausts at market #k of the loop; market #k's two resting quotes plus its live unwind are cancelled this cycle for no reason, and the exit is not re-rested until next cycle. Budget exhaustion is not hypothetical — the 08-02 STOP-path incident (:3975-3987 comment) was this same budget going monotone.
- VERIFY: Mechanism verified (ESTABLISHED). Under PIVOT_SELECT=1 (live), `consumed.append(m)` at :5219 runs BEFORE the book fetch try-block; `except RuntimeError: break` at :5229-5230 exits the loop with no retention — the retention+unwind-tagging code lives only in the generic `except Exception` branch at :5231-5258 and is never reached for RuntimeError (RuntimeError is caught first). After the loop, `footprint = consumed` at :5437-5438 puts market #k in fp_tickers (:5444), making it grace-ineligible by apply_drop_grace's own contract ('we looked at it') and — decisively — skipped by the strand-unwind loop at :5456 (`if t in fp_tickers ... continue`). With no desired entry, diff_orders (:3475-3492) cancels every standing order on it including a live unwind. The exception is real, not hypothetical: public_get raises RuntimeError('read budget exhausted') at :1855-1856 when _reads hits READ_BUDGET_PER_CYCLE=200 (:1844), and the :3976-3987 comment documents the 08-02 STOP-path incident of exactly this budget going monotone (the fix there covers only the STOP branch reset, not mid-quote-loop exhaustion). WS degradation to REST fallback is the stated reachable trigger. With PIVOT_SELECT=0 the blast radius claim also holds (footprint stays the full pool; every unvisited market with standing orders is in fp_tickers with no desired entry). MAJOR is the right severity: wrong (unnecessary) cancels of live exits under a reachable degraded-feed condition.

### M-12 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:6572
**STOP flatten pass 1 places fresh exit offsets over failed cancels — stacked reducing size can cross THROUGH flat**  (reviewer: quoter:4900-7317 exec+exits)
- MECHANISM: _flatten_all cancels via `_cancel_each` which swallows failures (:6414-6427, returns only a count), then the creates loop (:6577-6586) rests the new offsets unconditionally. There is no failed-cancel deferral: the main cycle guards exactly this with failed_cancel_tickers/failed_cancel_sides (C7, :5797-5843) because a stale same-side reducing order plus a fresh unwind sums to up to 2x|pos|, and a full fill of both crosses through flat into the opposite position (the class _unwind_size's own contract forbids, :2353-2376). Pass 2's taker escalation is properly cancel-confirmed, but pass 1 is not.
- FAILURE: Auto-halt during venue stress; the diff cancels the stale 20ct@0.55 NO offset but the DELETE 429s; the new 20ct@0.60 NO offset rests anyway → 40ct reducing against a 20ct position. Market trends through both prices; the bot exits its long and opens a naked 20ct short while halted, with the next corrective pass up to STOPFLAT_REPEAT_S=1800s away.
- VERIFY: Mechanism verified (ESTABLISHED). _cancel_each (:6414-6427) swallows every cancel exception and returns only a success count — callers get no identity of WHICH cancels failed. In _flatten_all pass 1, `n += _cancel_each(client, cancels)` (:6570) is followed by the creates loop (:6576-6586) which rests each fresh offset unconditionally via client.create_quote at :6581 — there is no analog of the main cycle's C7 failed-cancel deferral (failed_cancel_tickers/failed_cancel_sides at :5799-5843, whose own comment names the exact hazard: 'a second unwind stacks to ~2x|inv| and a full fill crosses THROUGH flat into the opposite position'). The function's duplicate-survivor block (:6541-6567) closes the identical-twin variant of the same through-flat hazard but only for orders diff_orders failed to enumerate — a cancel that was EMITTED but REJECTED by the venue (429 under halt-time stress) is invisible to it. Pass 2 (_taker_cross_capped, :6640+) is confirmed cancel-first with abort-on-unconfirmed-cancel, matching the finding's contrast claim. Failure scenario checks out with one refinement that does not change the verdict: pass 2's 90s escalation can partially self-correct IF the stacked orders are both still resting and residual >= STOP_TAKER_MIN_CT (it cancels-confirmed then crosses), but (a) if the through-flat double-fill happens during the 90s wait the naked opposite position is then taker-crossed at the moved price (loss realized), (b) if the FRESH offset alone fills during the wait, residual reads empty, flatten prints 'done' and returns (:6624-6626 region) leaving the un-cancelled STALE exit resting against a FLAT position — the documented 2026-07-27 19:40:03Z naked-entry class from _taker_cross_capped's own docstring — with the next corrective run STOPFLAT_REPEAT_S=1800s away (:1199), and (c) a repeated cancel failure in pass 2 aborts the cross, leaving the stack resting for the full 1800s window. MAJOR (reachable condition: venue 429/5xx on a DELETE during halt-time stress) is correct; not BLOCKER since it requires a failed cancel plus adverse price traversal.

### M-13 [DOWNGRADE] [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:448
**Hot path omits F5 own_orders — Stage B recomputes quotes with own-size double-count**  (reviewer: daemon+client+wsfeed)
- MECHANISM: F5 threaded own_orders through the scoring model: every run_once call to desired_quotes passes own_orders=_own_order_levels(_f5_standing.get(t)) (maker_kalshi_quoter.py:5291, :5390, :5419), which changes _prospective_capture/_qualifying_score and therefore the desired prices. The daemon's hot_event call at line 448 passes no own_orders (defaults None = documented legacy byte-identical pre-F5 model), even though HotContext.build has the standing dict in hand and could carry the levels.
- FAILURE: Operator arms KALSHI_WS_HOT=1 (the documented next step for Stage B). On a book move, hot computes a desired price under the pre-F5 model (our own resting size inflating the level), emits a reprice pair, then the next cold cycle (<=60s) computes the F5-correct price and reprices back. Result: perpetual cancel/create churn on every active ticker — queue position forfeited twice per minute, doubled write-token burn, and windows where our size is absent from the venue's 1s reward snapshots. Must be fixed before WS_HOT is ever armed.
- VERIFY: The omission is ESTABLISHED (daemon:448 passes no own_orders; quoter:5291/5390/5419 all do; ctx.standing is in hand at daemon:458), but the claimed churn mechanism is refuted. own_orders feeds ONLY gate decisions inside desired_quotes — presence gate :2918, NETEV model fallback :3021, capture gate :3046, activate credit :3105 — and never the price or count of an emitted quote, so hot and cold can never disagree on a quote's price. Missing own_orders always LOWERS modeled capture (_qualifying_score:2570-2577 subtracts own size from the rival denominator), so the hot model is strictly stricter; a gate flip returns [] or reducing-only, and hot_reprice_ops maps des==[] to NO ACTION (daemon:259-261, 'incl. des == [] -> no pull') with reduce_side untouchable (:257-258). The presence gate additionally cannot flip for tickers with standing orders (_resting_here bypass, quoter:2933-2938). So no reprice pair at a wrong price and no cancel/create churn is reachable; the real defect is the hot path silently NO-OPING (missed repricing) on tickers where the pre-F5 model drops the NETEV/capture signal below threshold — a liveness/consistency gap, MINOR, and only when WS_HOT (currently off) is armed. Worth fixing before arming Stage B, but not the stated MAJOR churn/queue-forfeit failure.

### M-14 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_ws_feed.py:271
**Venue error frame leaves mirrors CLEAN+FROZEN for the whole backoff sleep — Stage C vouches for a dead book**  (reviewer: daemon+client+wsfeed)
- MECHANISM: _dispatch's error-frame arm (feed :270-273) returns False WITHOUT dirtying any mirror — unlike the seq-gap arm (:280-284, dirties all) and the disconnect arm (:349-351, dirties all). run() then sleeps the escalating backoff (2s..60s, :334-339) BEFORE break; confirmed_channels is only cleared in the finally (:342-343) AFTER the sleep. So during the entire backoff window the Feed object still shows confirmed_channels populated and mirrors clean, and Daemon.mirror_book (daemon :332-353) passes every one of its decline checks and vouches for books frozen at their pre-error state. The daemon docstring claims 'disconnect/error -> EVERY mirror dirty in the except arm' (daemon :323) — false for the controlled error-frame break path, which never reaches the except arm.
- FAILURE: With KALSHI_WS_BOOK_COLD=1: books seed, prices move, venue emits a mid-stream error frame (e.g. WS-side rate limit or command rejection) while fails is already elevated -> 60s backoff. A book-move-triggered cold cycle lands inside that window, reads all ~40 books from frozen mirrors believed fresh, and rests real post-only orders priced off a book that is up to 60s stale — resting far from the true reference (0.5^ticks reward penalty) or getting adversely filled at a stale price. Same window lets Stage B hot writes through (mirror.dirty False). Severity is conditional on the Stage C/B flags; with both off the impact is nil.
- VERIFY: Mechanism verified end to end. Feed error-frame arm :270-273 returns False WITHOUT dirtying any mirror, unlike the seq-gap arm :280-284 and the except arm :348-351 which dirty all. In run(), ok==False takes the backoff sleep :334-338 (min(2*2^n,60) — up to 60s) BEFORE break :339; confirmed_channels is cleared only in the finally :341-344 AFTER the sleep. During the sleep the dispatch loop is parked, so mirrors are frozen at pre-error state yet clean, and confirmed_channels is still populated. Daemon.mirror_book :332-353 then passes every decline check (feed non-None :335, 'orderbook_delta' in confirmed_channels :337, mirror not dirty :340) with a DELIBERATE no-age-bound policy :327-331, so a cold cycle under WS_BOOK_COLD=1 reads up to-60s-stale books as fresh; _hot_precheck's mirror.dirty check :410 passes for the same reason. No neutralizing guard exists: the watchdog :731-743 fires only on feed_task.done(), and the task is alive inside asyncio.sleep. The daemon docstring :321-323 ('disconnect/error -> EVERY mirror dirty in the except arm') is provably false for this controlled-break path. Severity correctly scoped by the finding itself: MAJOR conditional on Stage C/B flags; nil with both off.

### M-15 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_fill_costs.py:124
**Settled-market replay-fault fallback prices all settled markets at revenue-only (cost 0), not ABSENT; fault is silent**  (reviewer: recorder+ledger+estrec)
- MECHANISM: In build(), when replay_fills(fills) raises (lines 121-123), the except sets cash_by/fee_by to {} and increments SETTLED_REPLAY_FAILS — but execution FALLS THROUGH into the `for s in settlements:` loop at line 124, which is inside the same `if replay_fills is not None:` block. Every settled ticker then gets realized = revenue + 0, i.e. revenue >= 0, so cost_usd_day = max(0, -realized)/days = 0 for ALL settled markets, stamped settled=1 and indistinguishable from a genuinely costless market. This directly contradicts the module's own contract at lines 31-33 ("those markets fell back to ABSENT (the pre-fix behaviour)") — absent rows would at least be recognizably missing. Compounding it: SETTLED_REPLAY_FAILS is incremented but never printed, never written into kalshi_fill_costs.json, and never read by fill_costs.main() (only MISSING_FIELD_ROWS is, line 176) nor by the quoter's in-process refresher maker_kalshi_quoter.py:_refresh_fill_costs (2667-2705), which happily writes the degraded table over the good file via atomic replace. The trigger class (fill_outcome raising on an unrecognized venue fill shape, kalshi_attribution_ledger.py:140-152) is exactly the venue-rename hazard this module tripwires everywhere else. Note only the import-failure path (lines 104-107) actually produces the documented ABSENT behaviour; the replay-exception path does not.
- FAILURE: Venue adds/renames a fill field so one fill in the tape lacks book_side/outcome_side -> fill_outcome raises inside replay_fills -> next hourly _refresh_fill_costs cycle writes kalshi_fill_costs.json where every settled market (129+ tickers, including e.g. KXTEMPAUSH's -$9.49 all-in loss) shows realized_pnl_usd = revenue >= 0 and cost_usd_day = 0.0000. kalshi_capital_rank and the net-EV calibration read this feed: every historically losing settled market now looks free, the capital ranking stops penalizing the exact markets that burned money, and the quoter keeps committing size to them. No warning appears anywhere — the degradation is fully silent in both the standalone script and the live quoter path.
- VERIFY: Every element verified in code. build(): the except at :121-123 sets cash_by/fee_by={} and increments SETTLED_REPLAY_FAILS, then execution FALLS THROUGH into `for s in settlements:` at :124 (same `if replay_fills is not None:` block), so every settled ticker gets realized = revenue + 0 >= 0 -> cost_usd_day = max(0,-realized)/days = 0, stamped settled:1 (:132-141) — indistinguishable from a genuinely costless market. This contradicts the module's own contract at :31-33 ('fell back to ABSENT'); only the import-failure path :104-106 (replay_fills=None skips the whole block) produces ABSENT. Silence verified: grep shows SETTLED_REPLAY_FAILS referenced ONLY at :31 and :122 in the entire tree — never printed (main() surfaces only MISSING_FIELD_ROWS :176-178), never written to the JSON, never read by the quoter's _refresh_fill_costs (:2691-2703), which atomically replaces the good file with the degraded table (its blanket except :2704 does not fire because build() returns normally). Trigger class is real: replay_fills (kalshi_attribution_ledger.py:226-233) has no per-fill guard; fill_cashflow -> fill_outcome (:140-152) raises ValueError by design on any fill lacking book_side/outcome_side, exactly the venue-rename hazard. MAJOR is right: silently corrupts the capital-rank / net-EV measurement feed for all 129+ settled tickers.

### M-16 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_estimates_recorder.py:73
**Program map written non-atomically and reset to {} on load failure; torn write permanently loses ended-program entries**  (reviewer: recorder+ledger+estrec)
- MECHANISM: `json.dump(pmap, open(MAP_PATH, "w"))` at line 73 truncates kalshi_program_map.json in place with no tmp+os.replace (unlike every sibling state file: kalshi_attribution_ledger.save_state uses tmp+replace at :269-273, kalshi_fill_costs.main at :172-175, the quoter's _refresh_fill_costs likewise). A crash/OOM-kill/reboot mid-write leaves truncated JSON. On the next 5-minute run, `json.load(open(MAP_PATH))` fails and the bare `except Exception: pmap = {}` at lines 54-55 silently RESETS the map, after which only currently-ACTIVE programs are re-merged. Entries for ENDED programs — the exact rows the map exists to remember ("ended programs leave the active feed but their estimate rows persist until payout — the map must remember them", lines 51-52) — are unrecoverable, because they are no longer in /incentive_programs?status=active. Downstream: (a) the est->credit join for the 08-07T15:00Z APRPOTUS checkpoint and the P2 measurement loses market_ticker/end_date/period_reward resolution for those program_ids; (b) the quoter's D-A est-feed (maker_kalshi_quoter.py:756-771) drops those estimate rows from the table (pmap.get miss -> mt None -> skipped) — fail direction is safe for the entry floor but lossy for the measurement.
- FAILURE: The 2026-08-09 program endings (KXTOPMODEL-26AUG31 FIX-H timing confirmation, KXCHIPBURRITO-26SEP02) leave the active feed on 08-09; their estimate rows keep appearing until payout. On 08-10 the VPS reboots (or the recorder is killed) exactly during the map dump; next run resets pmap to {} and re-merges only active programs. When the payout credits land, the estimate rows for those two program_ids can no longer be joined to a ticker or end_date — the FIX-H payment-timing confirmation and the estimate->credit calibration row are lost, silently (only symptom: the `unmapped` count at line 79 ticks up in a journal nobody alerts on).
- VERIFY: Mechanism verified. :73 `json.dump(pmap, open(MAP_PATH, "w"))` truncates in place with no tmp+os.replace — unlike siblings kalshi_fill_costs.main :172-175 and quoter _refresh_fill_costs :2700-2703 which both use tmp+replace. Load at :53-56 is a bare `except Exception: pmap = {}` that silently resets; the re-merge :57-72 pulls ONLY status=active programs, so ended-program entries (the map's stated purpose, :51-52) are unrecoverable via this path. Reachability is actually BROADER than claimed: the dump sits inside the try whose except at :74 only prints, so any IO fault mid-dump (disk full, ENOSPC) tears the file with NO crash/reboot needed, in addition to the kill-mid-write window on a 5-min timer. Downstream verified: quoter D-A consumer :756-771 skips rows on pmap miss (mt None) — safe for the entry floor, lossy for the est->credit join; the only symptom is the `unmapped` count in an unalerted journal line :79-81. MAJOR is fair under the stated rubric ('corrupts canon measurements'): the map is the sole persistence of ended-program market_ticker/end_date/period_reward, feeding the ruled 08-07 APRPOTUS est->credit checkpoint and the FIX-H 08-09 timing confirmation. The trigger window per write is narrow, but the loss is permanent and silent when it lands.

### M-17 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2558
**Model reference price = best bid; venue LIP reference = Target/5 cumulative walk — capture/share computed vs wrong ref**  (reviewer: scores+rank+feedback)
- MECHANISM: The venue-published LIP formula (08-05, ESTABLISHED per task context) defines the per-side Reference Price as the FIRST LEVEL where CUMULATIVE resting size >= Target/5, walking down from best bid. `_qualifying_score` (maker_kalshi_quoter.py:2558 `ref = bids[0][0]`), `_qualifying_breakdown` (:2731), and kalshi_market_scorecard.qualifying_share (:32) all set ref = highest bid unconditionally. No Target/5 walk exists anywhere in kalshi_live (grepped). When top-of-book cumulative depth < Target/5 (200ct on a 1000ct-target program — common in exactly the thin books the thesis targets), the venue reference sits DEEPER than best bid, so rival orders between best bid and the true reference earn 1.0x from the venue but are discounted df^ticks by the model. The rival denominator is under-counted, our share over-estimated — anti-conservative.
- FAILURE: Qualifying book: best bid 0.30 carries 50ct, 0.29 carries 300ct, 0.28 carries 700ct (venue ref = 0.29). Venue scores the 0.29 level at 1.0x and 0.28 at 0.5x; the model scores them at 0.5x and 0.25x. Modeled rival total ~325 vs venue ~650: our modeled share is ~2x the venue share. This inflated capture feeds _expected_credit_usd -> a market whose true period credit is $0.70 models >$1.20 and passes the PRESENCE_GATE floor (live), rests real size, takes fill risk, and the program pays $0 (the W10 sub-$1 drive-by class). Same inflated number is folded into the score cache (SCORE_RANK ordering) and the NETEV unproven-model fallback (NETEV_GATE=1 live).
- VERIFY: Verified in code: _qualifying_score sets ref = bids[0][0] unconditionally (maker_kalshi_quoter.py:2558), _qualifying_breakdown does the same (:2731), and kalshi_market_scorecard.qualifying_share (:32) is the pinned reference implementation. A grep across kalshi_live for any Target/5 / cumulative-fifth walk returns nothing — no code computes the venue's published reference (first level where cumulative size >= Target/5). Direction is anti-conservative as claimed: when top-of-book depth < Target/5 the venue reference sits deeper, so rivals between best bid and the true reference earn 1.0x from the venue but df^ticks from the model, under-counting the denominator and over-stating our share. The failure path is live: _expected_credit_usd (:1292-1341) -> _prospective_capture (:2584-2620) -> _qualifying_score feeds the PRESENCE_GATE $1-floor admit decision (:2917-2918, PRESENCE_GATE=1 live), the score cache (:2661), and the NETEV unproven-model fallback (:3021, :3046, NETEV_GATE=1 live). The NETEV_MODEL_HAIRCUT=3.0 scalar cannot neutralize a book-shape-dependent bias. MAJOR is correctly calibrated (wrong admit decisions on the live money path, not directly wrong order prices/sizes).

### M-18 [CONFIRMED] [INFERRED] kalshi_live/maker_kalshi_quoter.py:2566
**Scored set truncated at cumulative Target — rivals below the walk cutoff contribute zero to the share denominator**  (reviewer: scores+rank+feedback)
- MECHANISM: _qualifying_score and _qualifying_breakdown stop accumulating `total` when cum >= target (:2566-2567 break, :2739-2740) and score our order 0 if below lowest_q (:2579). The LIP spec as given qualifies orders at SIDE level (side reaches Target => orders on that side qualify; short side => none) and normalizes score = size x distance-mult per side — it contains no per-order cutoff at the cumulative-Target boundary. Deep rivals that the venue counts at 0.5^ticks are excluded entirely from the model denominator, so our modeled share exceeds the venue share whenever meaningful size rests just past the Target boundary near the reference.
- FAILURE: Book with 1000ct at reference (Target exactly met at level 1) plus 5000ct one tick behind: the model's denominator is 1000 (walk breaks at level 1) vs the venue's 1000 + 5000x0.5 = 3500. A 50ct join at reference models share 50/1050 = 4.8% vs venue ~1.4% — a 3.4x over-estimate feeding the same floor gate / score cache / net-EV fallback as the reference-walk finding. Both biases were absorbed into the fitted NETEV_MODEL_HAIRCUT=3.0 as an aggregate scalar, but they are book-shape-dependent, so cross-sectional ranking between thick-boundary and thin-boundary books is systematically wrong in a way no scalar haircut corrects.
- VERIFY: Verified in code: the walk breaks the moment cum >= target (maker_kalshi_quoter.py:2566-2567; identical in _qualifying_breakdown :2739-2740 and scorecard :40-41), and our own order scores 0 below lowest_q (:2579). The venue formula as given (ESTABLISHED, 08-05) qualifies orders at SIDE level and normalizes score = size x 0.5^ticks per side with no per-order cumulative cutoff, so rivals resting just past the Target boundary — counted by the venue at 0.5^ticks — contribute zero to the model denominator. The arithmetic in the failure scenario checks out (model denom 1000 vs venue 3500 in the stated book; ~3.4x share over-estimate). Same live consumers as finding 1 (PRESENCE_GATE floor, score cache, NETEV fallback), same direction (anti-conservative), and the book-shape dependence means the aggregate haircut fitted on M7's 2-6x over-prediction cannot correct cross-sectional ranking between thick-boundary and thin-boundary books. MAJOR stands.

### M-19 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_market_scorecard.py:77
**Scorecard still divides period_reward/10000 by window days — the R1 form the canon explicitly refuted**  (reviewer: scores+rank+feedback)
- MECHANISM: kalshi_market_scorecard.py:77 computes `"usd_day": (_f(p.get("period_reward")) / 10000) / days`. CANON (07-25, hook-injected) and the quoter's own comment at maker_kalshi_quoter.py:2055 state period_reward/10000 IS ALREADY the daily per-market pool and the /days form was refuted by blind audit (56% of series pushed below the documented $10/day floor). The quoter was fixed; this canon-feeding diagnostic was not, and _qualifying_score is pinned byte-equivalent to this module's qualifying_share, implying the scorecard is still treated as a reference tool.
- FAILURE: Any operator or session running the scorecard to sanity-check a market's reward density gets pool numbers deflated by the program window length (a 7-day program reads 7x too small relative to the quoter's truth). A cross-check of quoter usd_day against scorecard usd_day 'disagrees', and whichever way the discrepancy is resolved at 2am corrupts a canon measurement — the exact class Forbidden Pattern 8/9 exists for.
- VERIFY: Verified in code: kalshi_market_scorecard.py:77 computes usd_day = (period_reward/10000)/days with days = program window length (:72-76), while the quoter at maker_kalshi_quoter.py:2053-2065 carries the explicit canon comment 'R1 POOL — DO NOT DIVIDE BY WINDOW LENGTH... The old /days form was refuted by a blind audit' and uses period_reward/10000 raw (:2065). The scorecard is not a throwaway script: the quoter pins _qualifying_score byte-equivalent to this module's qualifying_share (:2536-2554 docstring, :2724-2727), i.e. it is treated as the reference/diagnostic tool, and it is read-only-canon-feeding by design (its own header: 'READ-ONLY... GETs only'). Any cross-check of scorecard estRwd$/d against the quoter's usd_day disagrees by the window length (7x on a 7-day program). Not on the live order path — so not BLOCKER — but it corrupts canon measurements whenever run, which is exactly this review's MAJOR definition. MAJOR stands.

### M-20 [CONFIRMED] [INFERRED] kalshi_live/kalshi_capital_rank.py:142
**shadow_rank commit denominator trusts cached ref with NO age cutoff — stale-extreme-ref defect lives in the alloc key**  (reviewer: scores+rank+feedback)
- MECHANISM: shadow_rank reads `ref = (scores.get(t) or {}).get("ref")` (kalshi_capital_rank.py:142) and feeds it to est_commit_usd (:189) with no freshness check. The select-budget walk was explicitly fixed for this class (maker_kalshi_quoter.py:5028-5033, 'audit lens 2 #3: refs age out... a stale extreme ref halves est and over-admits; stale -> unknown -> max est'), but _alloc_priority (:3640-3678, live when ALLOC_KEY=1) calls shadow_rank whose internal est_commit still trusts a ref of any age — up to EVICT_AGE_S = 7 days old. A days-old skewed ref (e.g. 0.95) halves the modeled commitment, inflating cap_score, and cap_score orders the budget walk (:5034), cap_desired (:5745) and bound_creates (:5773).
- FAILURE: Market measured once at ref 0.95 five days ago, book has since reverted to 0.50: est_commit models ~half the true paired commitment, cap_score reads ~2x too high, and the ticker wins allocation priority over a genuinely better market. The budget walk then charges its (correctly age-gated, hence 0.50-max) est — so the ordering key and the charging key disagree on the same cycle, precisely the split-key inconsistency the 07-31 blind review condemned. Liveness depends on ALLOC_KEY=1 (staged 08-02; D2_FEEDBACK=1 in the live flag set only has rank effect through this same path).
- VERIFY: Verified in code: shadow_rank reads ref = (scores.get(t) or {}).get('ref') with no freshness check (kalshi_capital_rank.py:142) and feeds it to est_commit_usd (:189); SCORES rows persist up to EVICT_AGE_S = 7 days (kalshi_market_scores.py:84). The contrast is real and acknowledged by the code itself: the select-budget walk age-gates the very same refs at ALLOC_PCAP_MAX_AGE_S = 6h with the comment 'a stale extreme ref halves est and over-admits; stale -> unknown -> max est (conservative)' (maker_kalshi_quoter.py:5028-5033), while the ordering key that sorts that same walk (:5034-5036) comes from _alloc_priority -> shadow_rank (:3667) using the un-aged ref — the split-key inconsistency as claimed. Direction verified: an extreme stale ref shrinks est_commit_usd (skewed books commit less under _capped_join mirroring, :80-103), inflating cap_score. One caveat, which does not change the verdict: the finding's liveness argument that 'D2_FEEDBACK=1 only has rank effect through this path' is imprecise — _load_credit_feedback also feeds the observation-only _caprank_telemetry (:842), so D2_FEEDBACK=1 does not prove ALLOC_KEY=1. ALLOC_KEY defaults 0 (:3631) and is absent from the stated live flag set, but it was staged on the VPS env 08-02 and is a single-flag arm — a reachable condition, which is exactly the MAJOR bar (BLOCKER would require live-now). MAJOR stands, with the explicit note that the defect is inert while ALLOC_KEY=0.

### M-21 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_netev_rebuild.py:203
**Markets still OPEN at read time are scored as pure loss — open collateral poisons family net_pct**  (reviewer: tools:netev+settlement)
- MECHANISM: build_table attributes ALL cash of any market traded in-window (fam_pnl, :195) and offsets it with settlement revenue ONLY for markets present in the settlements list (fam_settle loop, :203-214). A market traded in-window but UNSETTLED at API-read time contributes its full open-position collateral as negative cash with no offsetting value and no mark-to-market. The module root-caused and fixed this exact shape at the WINDOW edge (docstring :155-161: fill-windowed cash read gas -$23.29 vs canon +$0.25) but left the READ-time edge unguarded. The credits caveat (:314-317) explicitly acknowledges read-time truncation for credits; there is no equivalent caveat, exclusion, or marker for trading cash.
- FAILURE: B8 rebuild ~Aug 9-10: D-C macro-probe ladders and est-feed-designated holdings (HELD_MAX_USD) are DESIGNED to hold far-close positions through program end. Run kalshi_netev_rebuild while e.g. KXAPRPOTUS or a weekly gas position is open: that family's net_pct_notional is dragged down by unresolved collateral (100 ct held at $0.40 reads -$40 with zero offsetting value). If the family is receipt-grade (>=40 fills, credits>0), the armed NETEV_GATE benches it on money that has not been lost. Same table row flips once the market settles — a non-reproducible verdict.
- VERIFY: Mechanism verified in code. _traded_in_window (kalshi_netev_rebuild.py:164-168) admits any ticker with one in-window fill with no settlement-status check; fam_pnl then takes ALL of that market's fill cash (:195), and the ONLY offsetting value is the settlements loop (:203-214), which cannot contain a still-open market. No mark-to-market exists anywhere in build_table, and the read-time-truncation caveat (:312-317) explicitly covers only CREDITS — trading cash of open positions has no caveat, exclusion, or marker. A 100ct position bought at $0.40 and still open at read time contributes -$40 with zero offset, exactly as claimed. Reachable: main() pulls live fills/settlements (:387-389) and nothing stops running it with open positions; D-C macro-probe/HELD designs hold far-close positions by design, and the receipt bar (>=40 fills, credits>0, :274-281) does NOT neutralize it — an established family (gas/temp) with an open weekly position keeps receipt grade while its net_pct is dragged down, and the armed NETEV_GATE consumes the written table. The verdict flips when the market settles, i.e. non-reproducible, as claimed. MAJOR is correct (corrupts the gate's canonical measurement under a reachable condition; not a direct live order path).

### M-22 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_netev_rebuild.py:152
**Whole-market attribution pulls PRE-window defect-era cash into a 'clean' window on straddling markets**  (reviewer: tools:netev+settlement)
- MECHANISM: A market enters scope if ANY fill lands in-window (:164-168); then ALL of its lifetime cash, fills, notional, and settlement revenue count, whenever they happened (:169-201, documented at :152-154). A window deliberately chosen to EXCLUDE the launch-defect era (e.g. the deployed table's 2026-07-24 start, or a post-restart B8 window) still inherits every defect-era fill of any market first traded before the window boundary and touched once inside it — weekly gas (KXAAAGASW spans 7 days) and far-close macro markets are exactly the straddling shapes. The module docstring warns at length about windows OVER the defect era but never that a window AFTER it is not clean for straddlers, and no per-family pre-window-cash share is emitted to detect contamination.
- FAILURE: B8 rebuild on a post-restart window: a weekly gas market opened during the defect era carrying a naked-settlement-tail loss gets one 5ct probe fill inside the clean window -> its entire defect-era loss lands in the clean table's gas row -> gate benches gas -> 'this family loses money' is exactly the agent-defect laundering the module's own docstring (:18-23) forbids, with no signal that it happened.
- VERIFY: Mechanism verified in code. The fill loop iterates the FULL replayed tape (:163, :169) and includes a fill solely on ticker membership in _traded_in_window (:171-172) — no per-fill window check — so every pre-window fill of a market touched once inside the window lands in the window's row, cash, notional, fills and settlement alike (:195-201, :209-214). This is deliberate and documented (:152-154), but only as the fix for the window-edge open-position shape (:155-161); the module docstring's contamination warning covers exclusively windows OVER the defect era (:18-23), never that a window AFTER it inherits straddlers' defect-era cash, and the emitted document (:282-303) contains no pre-window-cash share or any field from which a reader could detect the contamination. The straddling shapes named (weekly gas spanning 7 days, far-close macro markets held into a post-restart window) are real: one 5ct probe fill inside a clean window imports the market's entire lifetime loss into the clean table, which the armed gate then acts on — the exact agent-defect laundering the docstring forbids, with no signal. Documented-but-undetectable does not refute the failure scenario. MAJOR stands (corrupts canon measurement feeding a live gate).

### M-23 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_ledger_reconstruct.py:71
**Reconstruct re-implements the refuted side-derived sign model — wrong on all 'sell no' fills (~49% of tape)**  (reviewer: tools:ledgers+w16+w17+w11)
- MECHANISM: Lines 59-72 derive yes-equivalent position and cash from (side, action) read literally: for action=sell it books cash IN (+price*count) and net_yes -= signed. On this account's tape only two shapes exist ((buy,yes) and (sell,no) — 277/265 of 542 in AUD_fills.json; kalshi_attribution_ledger.py:105-123 documents the same 317/317). A 'sell no' fill is US ACQUIRING NO via our resting ask: cash OUT, -1 yes-equiv. Reconstruct books it as cash IN and +count yes-equiv — the exact legacy mapping whose refutation (07-23 root fix, 'booked it as cash IN, on 156 of 317 fills') is the reason rewards_residual once printed +$57.82/day. Additionally mkt_gross_buy only accumulates on action=buy, so 'gross buy cost (both sides)' never sees a single NO acquisition — the headline is one-sided by construction. The cash model is also position-blind, which the attribution canon separately proves insufficient (a sign flip alone scored -$741.15 vs balance-implied -$60.91).
- FAILURE: Anyone re-running this tool (or citing its committed conclusions: 'gross cost is round-tripped away', per-market net cash figures) gets netCash$/netYesEq inverted on every market with sell-no fills — i.e., every market where we rested NO bids. Its 'empirical' NET-within-market demonstration was computed with corrupted inputs and could feed canon.
- VERIFY: Mechanism verified in code against canon. For a (side=no, action=sell) fill: signed = -c (:65), the sell branch does mkt_net_yes[t] -= signed → +c yes-equivalent and mkt_cash[t] += price*c → cash IN (:70-72). Canon in the same directory (kalshi_attribution_ledger.py:107-115) establishes the opposite: 'sell no' is our resting NO bid filling — us BUYING NO, cash OUT, short-yes — and that reading (action, side) literally 'booked it as cash IN, on 156 of 317 fills', the exact defect behind the +$57.82/day rewards_residual artifact. Both cash and position are inverted on this shape, which is ~half the tape (the account's fills come in exactly two shapes, 317/317). Also verified: mkt_gross_buy accrues only inside the action=='buy' branch (:69), so the 'gross buy cost (both sides)' headline structurally excludes every NO acquisition; and the model is position-blind, which kalshi_attribution_ledger.py:117-123 separately proves insufficient (sign-flip-only scored -$741.15 vs balance-implied -$60.91). (sell,yes) — a genuine taker exit — would be handled correctly, but that shape is not what dominates the tape. The tool's committed 'empirical' NET-within-market conclusion was computed on these corrupted inputs. MAJOR is appropriate: it is a measurement tool, not a live order path, but any run or citation corrupts canon.

### M-24 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_ledger.py:255
**Headline REWARDS is a residual (banned class); the 'no API exposes this' premise is canon-refuted**  (reviewer: tools:ledgers+w16+w17+w11)
- MECHANISM: rewards = equity - deposits - realized - unreal (line 255) is definitionally the same residual class as the RULE-ZERO-banned rewards_residual: every model error (mark_open fallbacks, FIFO edge cases, deposit mis-entry, unpriceable lots) lands in the number labeled 'REWARDS (derived) <-- LIP income, no API exposes this' (line 257). Canon 08-01 established GET /v1/users/{uid}/credit_history as the reward truth (maker_kalshi_client.py:419 already implements it); the docstring's 'Kalshi exposes NO credits/ledger/transactions endpoint' (lines 4-5) is refuted. The tool still appends rewards_derived snapshots (lines 286-290) usable by --rate.
- FAILURE: A future session runs kalshi_ledger.py for a reward figure (its docstring invites exactly that), reads rewards_derived, and re-introduces a residual-basis reward number into canon — the failure mode RULE ZERO exists to prevent. Any unmarked open lot or stale deposits value silently shifts dollars between 'trading' and 'REWARDS'.
- VERIFY: Mechanism verified in code. rewards = equity - deposits - realized - unreal at kalshi_ledger.py:255 is definitionally a residual: every model error — mark_open's silent fallback that books uPnL=0 for any unpriceable lot (:192-194), FIFO edge cases in split_lots, an operator-typed deposits value (:203, :211-212, no API check) — lands in the number printed 'REWARDS (derived) <-- LIP income, no API exposes this' (:257). That label and the docstring premise (:4-5, 'Kalshi exposes NO credits/ledger/transactions endpoint', probed 07-25) are refuted by the codebase itself: maker_kalshi_client.py:419-435 implements get_credit_history (v1/users/{uid}/credit_history on the elections host, canon 08-01 reward truth). The residual is persisted as rewards_derived in snapshots (:286-290) and consumed by --rate (:314) to print a $/day reward run-rate — a standing invitation to re-introduce a residual reward basis, the exact RULE-ZERO failure mode. Mitigations do not neutralize: the --rewards-actual cross-check (:259-263) is optional/manual/stale-able, and the sign-reconcile gate (:98-116) validates fill direction only, not the residual class. MAJOR is right — read-only diagnostic, no live order path, but its whole purpose is producing a reward number, and that number is banned-class.

### M-25 [CONFIRMED] [ESTABLISHED] kalshi_live/w11_replay.py:96
**Replay baseline 'recorded' is the SHADOW ranking, not live selection — cyc['actual'] never read**  (reviewer: tools:ledgers+w16+w17+w11)
- MECHANISM: The caprank row carries the live selection under 'actual' (maker_kalshi_quoter.py:845 row = {"ts":..., "actual": actual, "variants":[...]}) and each variant's components come from shadow_rank top-FOOTPRINT_TOP, sorted by (-cap_score, ticker) (kalshi_capital_rank.py:200). policy_recorded re-sorts components by cap_score — i.e., it reproduces the SHADOW env-variant order exactly, not the live footprint. replay() never references 'actual' anywhere. The module docstring promises comparison against '(a) what the live bot actually selected'; the quoter logs an 'overlap' field precisely because actual and shadow diverge (and with PIVOT_SELECT=1 live they can diverge structurally).
- FAILURE: A D2 knob decision is made from w11 output showing 'd2_feedback beats recorded by X payer_slot_frac' — but 'recorded' is shadow-vs-shadow, not live behavior. If live selection already differs from the env shadow (nonzero would_enter/would_exit), the measured uplift misstates what changing the live policy would do.
- VERIFY: ESTABLISHED. Read the full module: replay() accesses only cyc['ts'] (w11_replay.py:142) and cyc['variants'] (:146); 'actual' appears nowhere in the file outside the docstring's promise (:11-12). policy_recorded (:94-97) sorts components by (-cap_score, ticker) — the identical sort shadow_rank already applied (kalshi_capital_rank.py:200) — so 'recorded' exactly reproduces the env shadow variant order. The quoter records actual = picked[:FOOTPRINT_TOP] (maker_kalshi_quoter.py:843-844) separately from the shadow, and logs overlap/would_enter/would_exit (:865-867) precisely because they diverge; with PIVOT_SELECT the live picked set is built by a quote-until-FOOTPRINT_TOP-succeed loop (maker_kalshi_quoter.py:5208-5210), so divergence is structural, not incidental. Any 'd2_feedback vs recorded' uplift from this harness measures shadow-vs-shadow, not change-from-live-behavior. MAJOR is right: this is the designated D2 knob-decision instrument (corrupts a canon-feeding measurement). Additional unclaimed corroboration: components carry only the top-FOOTPRINT_TOP candidates (:858, :868), so at top_n >= FOOTPRINT_TOP the slot-fraction metrics cannot even distinguish policies (same set, different order) — further evidence the baseline comparison is not measuring what the docstring says.

### M-26 [DOWNGRADE] [ESTABLISHED] kalshi_live/w17_coverage_ledger.py:233
**B-4 staleness detector: 'never-in-window' sentinel compares lexically NEWER than any date — never alarms**  (reviewer: tools:ledgers+w16+w17+w11)
- MECHANISM: last = last_by_series.get(s, 'never-in-window'); stale = last < stale_cut where stale_cut is an ISO string ('2026-07-23T...+00:00'). 'n' (0x6E) > '2' (0x32), so 'never-in-window' < any ISO date is always False → stale=False → the `if stale and noprog` print is suppressed. A proven series (credits_n>0 in kalshi_credit_feedback.json) whose credits no longer parse-match the current window — e.g., the venue changes the reason prefix, or credit rows age out of the 1000-row window — is exactly a 'paid-but-STALE+PROGRAMLESS' case and is exactly the case that stays silent.
- FAILURE: Venue renames the credit reason format: A-1 parse-rate alarm fires only if <90% match, but a single proven series dropping out entirely (its rows gone from the window) produces last='never-in-window' → no B-4 line → the rename/decay alarm this wave-2 hardening (commit 9390877) shipped to catch never fires for the strongest signal it has.
- VERIFY: Mechanism CONFIRMED in code (w17_coverage_ledger.py:233-237): last = last_by_series.get(s, 'never-in-window'); stale = last < stale_cut with stale_cut an ISO string starting '2'; 'n' (0x6E) > '2' (0x32) makes the comparison always False, suppressing the B-4 print for exactly the never-in-window branch. But severity MAJOR is overstated: (1) the sentinel branch is only reachable when a PROVEN series has zero parse-matched rows in the 1000-row credit_history window — a venue-wide reason rename drops parse-match to ~0% and fires the A-1 alarm instead (:218-221), and per-series aging-out requires >1000 lifetime credit rows (lifetime is ~58-200 credits; the len==1000 B-7 truncation warning at :222 fires before aging is possible), so the silent case is latent, not currently reachable; (2) proven series with an old-but-present last credit still alarm correctly (real ISO date < stale_cut works); (3) this is a read-only operator diagnostic — no live orders, no canon measurement is corrupted, the failure mode is one missing alarm line. Real bug, wrong tier: MINOR (detector edge case in a hardening script).

### M-27 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_drift_detect.py:89
**build_timeline mid has no book-sanity filter; shell-book mids corrupt value/jump_share (audit fix not propagated)**  (reviewer: tools:drift-suite)
- MECHANISM: build_timeline computes value = (yes_bid+yes_ask)/2 with no b>0 / a<1 / spread-cap sanity (kalshi_drift_detect.py:89), unlike the corrected fair() in signal_book_drift.py:44-50, which was fixed after signal_fair_value_audit.py proved unsanitized mids manufacture fake moves. All of D1/D6/D9, the eval AUC scores, and kalshi_drift_jump.py's jump_share consume this contaminated value series. Additionally lines 84-88 fall back to last-trade price when the book is absent — the exact fallback the sibling audit removed (verified it fired 0/4893 window minutes on the frozen cache, so that half is latent).
- FAILURE: Measured on the frozen data: the single largest runover, KXTEMPLAXH-26JUL2212-T70.99, has its worst 1-minute step (0.59) computed as the mid of a 0.20/0.99 shell book (spread 0.79 — the very book signal_fair_value_audit condemned) collapsing to 0.00/0.01; true exitable moved 0.20→0.00, so the step is inflated ~2x. 4/22 positives' worst step involves a spread>0.20 book; 40 window minutes (0.8%) are spread>0.20. jump_share medians and the 'loss arrives in one minute → drift detection misconceived' headline are overstated for those positions; the qualitative verdict survives (5/6 RUNOVER worst steps sit on 1c-wide books) but the published magnitudes are wrong.
- VERIFY: ESTABLISHED, code and data both verified. build_timeline (kalshi_drift_detect.py:84-89) computes mid=(yb+ya)/2 with no b>0 / a<1 / spread-cap sanity and falls back to last-trade price when the book is absent — while the sibling signal_book_drift.py fair() (:43-50) applies exactly those filters, its docstring (:34-41) explicitly stating the audit proved the unsanitized version manufactured fake moves on the very same ticker. D1 (:155-160), D6 (:222-235), D9 (:255-268) and the eval harness consume this contaminated 'value'. Re-measured on the frozen cache (read-only run): KXTEMPLAXH-26JUL2212-T70.99 worst 1-min step = 0.59 computed as the mid of a 0.20/0.99 book (spread 0.79) collapsing to 0.00/0.01 — exactly the claimed ~2x inflation vs the 0.20->0.00 exitable move; 40/4893 window minutes (0.82%) have spread>0.20; the last-trade fallback fired 0 times (unquoted rows = 0), confirming that half is latent as the finding itself states. MAJOR stands: the published drift-study magnitudes (jump_share, worst-step tables) that fed the 'jump not drift' verdict are contaminated, even though the qualitative verdict likely survives.

### M-28 [CONFIRMED] [ESTABLISHED] kalshi_live/kalshi_drift_detect.py:150
**Windowed detectors treat candle index as minutes; 15.3% of window steps are gaps so W-minute windows span hours**  (reviewer: tools:drift-suite)
- MECHANISM: _val_at (detect.py:150-152), D2 (:163-169, j=i-W), D6 (:222-235, last m entries), D8 (:245-252), the D3/D4 tape windows (:172-195, trailing W candle indices), and every eval.py score analogue (eval.py:56-104) assume one timeline entry per minute. The timeline drops candles lacking both book and price (detect.py:85-87), leaving holes. Measured on the frozen data: 750/4893 (15.3%) of consecutive in-window steps are non-60s, max gap 3540s, 36/67 positions affected. A 'theta over 6 minutes' threshold is therefore evaluated over up to ~1 hour of wall time at those points. signal_book_drift.py handles this correctly (checks ts spacing ==120 at :98-99, :153-154) — the fix never reached the detect/eval harness.
- FAILURE: Every TPR/FPR/lift/AUC in kalshi_drift_detect.txt and kalshi_drift_eval.txt is computed over horizon-inconsistent windows. Gaps concentrate where books thin (death regions of positives) while TEMP-control windows are short and near-entry, so window inflation differs BY GROUP — longer effective windows accumulate more drift, flattering detector TPR on positives. The published verdict ('coin flip / weak, jump not drift') is directionally conservative under this bias, but no individual number in the tables is a clean W-minute measurement, and any future re-run that tunes a live D2_FEEDBACK-style knob from these tables inherits the distortion.
- VERIFY: ESTABLISHED, code and data both verified. _val_at uses j=i-back_min pure index arithmetic (kalshi_drift_detect.py:150-152); D2 (:166-169), D3/D4 tape windows iterate trailing W candle INDICES (:176, :191), D6 counts the last m entries (:232-234), D8 medians the last W entries (:251) — all assume one entry per minute, while build_timeline drops candles lacking both book and price (:86-87), leaving holes. Re-measured on the frozen cache: 741/4827 in-window consecutive steps are non-60s (15.4%), max gap 3540s, 36/67 positions affected — matching the claimed 750/4893 15.3% within counting-convention rounding (steps vs minutes). signal_book_drift.py does handle spacing correctly (exact-120s gate at :98-99 and :153-154), confirming the fix never propagated to detect/eval. A W=6 'minute' window therefore spans up to ~1h of wall time at gap points, and gaps concentrate where books thin (the death regions of positives), so window inflation differs by group. MAJOR stands: every TPR/FPR/lead-time/duty-cycle number in the published detector tables is horizon-inconsistent, and these tables are the input to any future drift-knob decision.

### M-29 [CONFIRMED] [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3018
**NETEV margin unit mismatch: fraction table values vs percent-scale knob (live -7.0) - receipt bench arm cannot fire**  (reviewer: flow:end-to-end trace)
- MECHANISM: kalshi_netev_calibrate.py:174 (and kalshi_netev_rebuild.py:292) write net_pct_notional = net/notional, a FRACTION (verified from the shipped kalshi_netev_table.json: gas -0.046816, temp -0.060929, KXB200WS -1.0 = -100%). The gate at :3018 does `poor = net_pct < NETEV_MIN_MARGIN_PCT`. The 08-05 handoff records the live knob as 'margin -7.0'. -0.0609 < -7.0 is never true - a family must lose 700% of notional to bench. The in-code margin ladder comment (:3010-3013, '-4% keeps 1 of 6 ... -7% keeps all 6') is only reproducible with knob values -0.04..-0.07, so the documented ladder and the live setting disagree by 100x. Today's outcome (keep all 6) coincides with operator intent, but only by accident of scale.
- FAILURE: A receipt family deteriorates to -20% of notional over the P2 window; the operator believes a -7% tolerance stands; the gate never benches it and the family keeps opening full two-sided. At B8's 'margin re-rule' the operator picks another percent-looking number (e.g. -5) and gets a silent no-op. Verify live: grep NETEV_MIN_MARGIN_PCT /opt/pa2-maker-kalshi-live/live.env.
- VERIFY: Mechanism verified end-to-end in code. maker_kalshi_quoter.py:1362 loads KALSHI_NETEV_MIN_MARGIN_PCT via _envf with no scaling; :3018 does `poor = net_pct < NETEV_MIN_MARGIN_PCT` where net_pct is net_pct_notional, written as a FRACTION by kalshi_netev_calibrate.py:174 (`net/notional`) and confirmed by shipped kalshi_netev_table.json (temp -0.060929, gas -0.046816). The deployed knob value being literally -7.0 is evidenced in-repo: w1_restart_checkpoint.py:69 defaults --expect-margin to -7.0, its docstring (:10-11) records 'operator ruling 2026-08-04: -7.0 for the P2 window', and its margin checkpoint (:114) asserts the live plans jsonl echoes netev_min_margin_pct == -7.0 (plan echoes the raw global at :6130) — the P1 restart checkpoint passed on that expectation. The percent-vs-fraction foot-gun is even pre-documented (KALSHI_NETEV_FLAGGING_BUILD_2026-07-24.md:138 'is a FRACTION despite the _PCT name') and the ladder shown to the operator (quoter :3011-3012 and KALSHI_HANDOFF_2026-08-04_EOD.md:63-69) is in percent, only reproducible with -0.04..-0.07. So -0.0609 < -7.0 is never true and the receipt bench arm cannot fire on any realistic family. One refinement: I verified the divergence is currently LATENT, not active — every table family below -0.07 (KXB200WS -1.0 etc.) is confidence 'unproven', so at knob -0.07 vs -7.0 today's kept/benched set is identical (all 6 receipt families kept, matching operator intent). The failure scenario (receipt family deteriorates past -7% and never benches; future percent-looking re-rule silently no-ops) is real and reachable. MAJOR is the right severity: no wrong orders now, but the protective gate the operator believes stands at -7% is inert. Residual caveat: live.env itself is outside the worktree — the -7.0 value is established from the checkpoint script contract, not a direct env read, exactly as the finding's own verify step notes.

### M-30 [DOWNGRADE] [INFERRED] kalshi_live/maker_kalshi_quoter.py:5229
**Read-budget exhaustion mid quote loop cancels the unread tail's standing book, including held-ticker exits**  (reviewer: flow:end-to-end trace)
- MECHANISM: public_get raises RuntimeError at READ_BUDGET (200). The quote loop's `except RuntimeError: break` (:5229) bypasses the fetch-fail retention (:5232-5258) that protects standing orders on transient errors, and the strand-unwind loop breaks on the same RuntimeError (:5461). Every footprint market not reached before exhaustion is then absent from `desired`, and diff_orders(:3475) cancels ALL its standing orders - including reason-tagged exits on held tickers (which are in fp_tickers, so the strand loop skips them even when it does run). Next cycle rebuilds at the back of the queue.
- FAILURE: Candidate pool + cache misses approach 200 reads (allowlist widening after P2 PASS restores a ~3,300-row universe: programs fetch + up-to-100-read prefilter + up-to-120 pool books + held-ticker marks + settle checks): the first post-exhaustion cycle tears down the tail of the resting book, a held ticker spends one cycle with no resting exit, and every torn ticker loses queue position (the thing LIP pays for). Not binding on today's 33-row pilot; close_unchecked_tail was already measured nonzero on 08-06 warmup.
- VERIFY: The mechanism fragments are real but the headline failure is neutralized by a live-armed guard the finding missed: DROP HYSTERESIS. Verified: public_get raises RuntimeError at the 200-read budget (:1854-1856); the quote loop's `except RuntimeError: break` (:5229-5230) does bypass the transient-fetch retention (:5231-5258); the strand-unwind loop breaks on the same error (:5460-5461); diff_orders (:3481-3488) cancels any standing ticker absent from desired. HOWEVER: KALSHI_DROP_GRACE=3 is live (KALSHI_LOGIC_AUDIT_FINDINGS_2026-08-05.md:147 'LIVE-ARMED — KALSHI_DROP_GRACE=3 in live.env'). Under PIVOT_SELECT=1, footprint collapses to `consumed` (:5437-5438), so every unreached-tail ticker is absent from footprint_tickers and not in _budget_said_no (that set holds only SELECT-BUDGET walk refusals, :5014/:5087). apply_drop_grace (:3370-3425, invoked :5712-5725) therefore retains the tail's standing orders VERBATIM — the diff emits neither cancel nor create, queue position is KEPT — for 3 cycles, with held tickers' reducing side re-tagged 'unwind' (A9-F4, :3411-3418) so cap_desired keeps them unconditionally (:3731-3736). So 'the first post-exhaustion cycle tears down the tail' and 'a held ticker spends one cycle with no resting exit' are both false in the common case, and 'rebuilds at the back of the queue' is false while grace holds. Real residuals, all narrow: (1) the single boundary ticker that triggered the RuntimeError was already appended to `consumed` (:5219, before the fetch at :5224), so it is grace-DENIED ('actively rejected') and its standing book — including a held exit — is cancelled immediately, one ticker per exhausted cycle; (2) sustained exhaustion >3 consecutive cycles exhausts grace and the teardown then occurs (grace resets only when the ticker re-enters the footprint, :6084-6088); (3) a held unreached ticker with NO pre-existing standing exit cannot get a strand exit that cycle. These are edge cases under a not-yet-live universe widening, not the claimed wholesale teardown: MAJOR overstates it. MINOR (edge case) with the boundary-ticker immediate-cancel as the sharpest kernel.

### M-31 [CONFIRMED] [INFERRED] kalshi_live/maker_kalshi_quoter.py:5070
**D-C (dark): budget walk charges macro ticker ~$5 while its ladder commits ~$30/side; partial ladder can rest**  (reviewer: flow:end-to-end trace)
- MECHANISM: The walk's est for a macro ticker takes `min(est_commit, _d3_est_ct)` (:5067-5070) - macro tickers are exempt from the ramp at SIZING (_d3_apply_ramp :646) but NOT at the walk's charge, so a designated market is admitted against ~$5 of budget while _macro_probe_quotes (:681-724) intends up to MACRO_PROBE_USD/2 per side. Downstream, the create loop's capital gate is PER ORDER (:5851-5856): with committed near _total_cap it can skip individual ladder levels, resting a partial ladder. A partial ladder does not meet Target - the side never qualifies, so the probe pays nobody while carrying fill risk: exactly the state _macro_probe_quotes refuses to create atomically (its unaffordable arm rests NOTHING). Sharpens known-open #6-part2 with a concrete partial-rest path.
- FAILURE: D-C armed next session with 2 designated tickers and a ruled MACRO_PROBE_USD; a busy cycle has committed $260/290; the walk admits the macro market at $5 est; the create loop places levels 1-2 of the 6-order two-sided ladder and skips the rest on the capital gate; the book shows partial depth below Target on one side for many cycles; the receipts test D-C exists to run never gets a qualifying snapshot and the operator reads it as 'macro probes don't pay'.
- VERIFY: Both halves verified in code. (a) Walk under-charge: the SELECT-BUDGET walk applies `_est9 = min(_est9, float(_d3_est_ct(...)))` at :5067-5070 with NO macro exemption — macro designation is checked at selection (:2004-2009), prefilter exemptions (:2041, :2146, :5361) and sizing (_d3_apply_ramp :646 exempts reason='macro_probe' from the rung clamp) but nowhere in the walk. _d3_est_ct (:617-636) returns rung 0 for a ticker not in _D3_FIRST_SEEN and clamps unproven series to rung ≤1 (D3_NEWSERIES_MAX_RUNG=1, :527), i.e. D3_RUNGS 5-10ct ≈ $5-10 charged, while _macro_probe_quotes (:681-724) intends up to min(MACRO_PROBE_USD/2, 0.9×HELD_MAX_USD) per side (:697) — the asymmetry (exempt at sizing, charged at rung scale in the walk) is exactly as claimed. (b) Partial-rest path: cap_desired (:3709, whole-market) and bound_creates (:3762-3784, whole-ticker groups) are atomic, but the create loop's capital gate is PER ORDER (:5844-5856): each accumulating create is individually skipped when funding_committed/committed + cost exceeds the cap. Its accounting diverges from cap_desired's — committed = surviving standing + held_cost (:5813-5825) while cap_desired counts desired notional and seeds held dollars only into per-family totals, not `total` (:3728-3730 vs :3721) — so with held inventory (or a binding free_cash funding gate) the loop can skip later ladder levels after placing earlier ones, resting a below-Target partial ladder: no side qualifies under LIP, pure fill risk, precisely the state _macro_probe_quotes's atomic unaffordable arm (:715-718) refuses. The partial state also persists: next cycle recomputes the full ladder and the missing levels keep failing the same gate. Severity: MAJOR is fair under this review's scale ('wrong under a reachable condition') — the code is dark today (MACRO_PROBE_TICKERS empty, :675-676) so no live money path yet, but D-C arming on operator naming is the queued next-session action, and the defect fires on the arming config itself, not on an exotic state. If one insists on flags-off-now framing this would be MAJOR-dark rather than MINOR: the failure mode silently invalidates the very receipts test D-C exists to run.

====================================================================================================
## S3. MINOR FINDINGS (61 — unverified by the adversarial pass; severity per reviewer)

**m-1 [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:448 — WS hot path calls desired_quotes without own_orders — F5 double-count survives there (WS_HOT=1 only)** (rev: commits:0b+recorder+DB-DG)
  hot_event calls M.desired_quotes(c["m"], ys, ns, ..., cost=c["cost"]) with no own_orders kwarg, while the WS mirror book (ys/ns) is the public book containing our resting orders. The presence/net-EV/capture gates inside desired_quotes therefore evaluate with the pre-F5 double-counted denominator on the hot path, diverging from the F5-corrected cold path. ctx.standing is available in the same function and could be threaded. WS_HOT defaults 0 (line 100) and is not in the listed live flags.
  FAILURE: With KALSHI_WS_HOT=1 and us resting solo on a thin book: cold cycle admits (share ~1.0, floor passes); a book event fires hot_event, the gate re-reads ~0.5 share, expectation halves below the floor, desired comes back empty → hot_reprice_ops takes NO ACTION (no standalone pull), so no wrong orders — but hot repricing is silently disabled on exactly the thesis (solo) books, leaving stale prices up to a full cold cycle and forfeiting the latency benefit the hot path exists for.

**m-2 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2560 — F5 subtracts cycle-start standing — a fill before the book fetch over-subtracts rivals for one cycle** (rev: commits:0b+recorder+DB-DG)
  run_once reads standing once at cycle start (_live_standing) and fetches orderbooks later in the same cycle. If our resting order fills (or partially fills) in between, the book no longer contains that depth, but _qualifying_score still subtracts the stale remaining_count — total is floored at 0 but rivals are under-counted. The blind-review #4 guard only excludes prices ABOVE the fetched ref, not filled-at-ref orders.
  FAILURE: Our 50ct at ref fills right after the standing read; the fetched book shows only rivals (200 df); subtraction removes a phantom 50 → total 150 → our modeled share inflated ~33%, potentially flipping the presence/capture gate open for one cycle on a market that just showed adverse-fill pressure. Self-corrects next cycle; the direction is anti-conservative.

**m-3 [ESTABLISHED] kalshi_live/kalshi_estimates_recorder.py:78 — Program map written non-atomically — one crash mid-write breaks the merge-only promise** (rev: commits:0b+recorder+DB-DG)
  kalshi_program_map.json is rewritten in place with json.dump(pmap, open(MAP_PATH, "w")); a crash/disk-full mid-write leaves a truncated file, and the next run's `except Exception: pmap = {}` silently restarts the map empty. Ended programs have already left the active feed, so their program_id→market_ticker/end_date rows are unrecoverable — exactly the rows the merge-only design exists to remember (and the rows the D-A ended-program exclusion and est→credit join need).
  FAILURE: Recorder killed mid-dump on 08-08; 08-09 run loads {}, refreshes only currently-active programs; the KXAPRPOTUS program (ended 08-07T15:00Z) is no longer resolvable, and the 08-07 checkpoint's est→credit join loses its ticker mapping. The quoter already has an atomic-replace pattern (_refresh_fill_costs) this file should copy.

**m-4 [INFERRED] kalshi_live/maker_kalshi_quoter.py:3105 — EST_FEED floors the activate-path gate with partly-banked dollars; commits capital for near-zero marginal reward** (rev: commits:DA+DC+followups)
  The venue estimate aggregates accrued-to-date plus projection (semantics unresolved until the 2026-08-07T15:00Z APRPOTUS est→credit checkpoint). _expected_credit_usd is called from both the presence gate (:2918) and the void-book ACTIVATE gate (:3105). With EST_FEED armed, a program with >=$1.20 banked but ~zero MARGINAL remaining credit (bot present early, now crowded out) passes gate_activate_credit and the activate path may commit up to MAX_ACTIVATE_CAPITAL=$150 of Target-seeding depth into a void book. EST_FEED_MIN_FRAC=0.25 (:729) only cuts the tail — the banked-vs-marginal conflation persists anywhere above 25% window left. Partially defensible (floor already cleared → marginal credit pays without re-clearing $1), but no gate on this path asks the marginal fill-risk-vs-marginal-reward question. Armed-only.
  FAILURE: Armed post-checkpoint: mid-window program, $2 banked from day-1 presence, rival now saturates the book (model expected ≈$0.10). Est floor admits; activate seeds an empty sibling book with $150 of resting depth; marginal reward is pennies while adverse-fill exposure is full.

**m-5 [ESTABLISHED] kalshi_live/test_da_est_feed.py:1 — None of the thirteen 7e43554 follow-up fixes are test-pinned; the 4+4 tests pin only base D-A/D-C semantics** (rev: commits:DA+DC+followups)
  Reviewed both test files and reran them (8 passed, exit 0). They pin: flag-off no-op, venue-est floors, never-reduces, stale fail-open; ladder shape/cost cap, reduce-only with inventory, unaffordable-rests-nothing. They do NOT pin any follow-up behavior: ramp exemption (#1), self-depth subtraction (#2 — no test passes own with a standing ladder), min(...,0.9*HELD_MAX) floor (#5 — the fixture raises HELD_MAX_USD to 40, which also masks that live defaults rest nothing), late-life/far-close/incumbent overrides (#6/#11), ended-program row exclusion (#4), EST_FEED_MIN_FRAC (#3), torn-line retention (#9), 1c-wall refusal (#13). The commit self-reports only #8 (selection hunks) as untested; the true uncovered set is all thirteen follow-ups.
  FAILURE: A future refactor of _d3_apply_ramp or _est_feed_cached silently regresses a follow-up (e.g. ramp re-clamps the macro ladder to 5ct, re-creating the receipts deadlock) and the suite stays green; the regression is discovered live after arming.

**m-6 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3884 — KALSHI_EST_FEED_MAX_AGE_S missing from both knob maps — the silent-no-op class review #10 fixed for its siblings** (rev: commits:DA+DC+followups)
  _refresh_safety_knobs restart_only now covers EST_FEED, EST_FEED_MIN_FRAC, MACRO_PROBE_USD/TOP/TICKERS, but EST_FEED_MAX_AGE_S (:728) is in neither `watch` nor `restart_only`. An operator changing the staleness bound in the env file mid-incident gets silence and no needs-restart warning.
  FAILURE: Recorder degraded to 40-min cadence; operator sets KALSHI_EST_FEED_MAX_AGE_S=3600 in live.env expecting the feed to stay alive; nothing changes and nothing warns; est-floor-admitted markets flap out on staleness.

**m-7 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:743 — _est_feed_cached rescans the entire monthly estimates jsonl every 120s to read one line (armed-only, efficiency)** (rev: commits:DA+DC+followups)
  The 5-min recorder appends ~8,900 lines/month, each carrying the full ~63-row estimate array (several KB). The cache re-read iterates every line of the newest file to find the last one, every 120s of quoting — tens of MB of IO per read by month-end, synchronous inside _expected_credit_usd on the hot cycle path.
  FAILURE: Late-month armed operation: each cache refresh adds read latency inside the quote cycle; on a slow disk this stretches cycle time exactly when the presence game is a queue-position race.

**m-8 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1324 — Floor-gate probe clamp condition diverges from the actual probe sizing clamp for allowlisted explore rows** (rev: quoter:1-2600 select+gates)
  The sizing clamp (:5311) probe-sizes ANY row with explore=True when EXPLORE_PROBE_CT>0 — including allowlisted rows tainted by the L3 series-probe (:4089-4104 sets explore=True on siblings of a permanently-OUT series regardless of allowlist) and, with SERIES_ALLOW empty, rank()'s explore picks (kalshi_market_scores.py:298-299 tags unconditionally when explore_exempt is None). But _expected_credit_usd's mirror clamp (:1324-1329) additionally requires `SERIES_ALLOW and series not in SERIES_ALLOW`, so for those rows the floor gate models eff_ct at the D3 ramp rung (up to 50ct for a credits-proven series) while 5ct actually rests — up to a 10x over-estimate of expected credit at the gate.
  FAILURE: An allowlisted, credits-proven series has one open permanently-OUT member. L3 marks its fresh siblings explore. Floor gate computes expected credit at rung-3 size (50ct), passes $1.20; the explore clamp then rests 5ct, whose real period credit is ~1/10 of the modeled figure and lands under the venue's $1 floor -> sub-$1 drive-by accrual plus fill risk on every such sibling. (SELECT_BUDGET's walk at :5063 has no allowlist condition either, so budget est and sizing agree; only the floor gate diverges.)

**m-9 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1504 — SERIES_ALLOW entries are not whitespace-stripped (SERIES_DENY is) — space-formatted lists silently bench proven payers** (rev: quoter:1-2600 select+gates)
  SERIES_ALLOW = [s for s in env.split(',') if s.strip()] keeps the RAW element (verified: 'KXA, KXB' -> ['KXA', ' KXB']; ' KXB' never equals ticker.split('-')[0]). SERIES_DENY (:1517) strips. Every consumer — the allowlist drop/probe demotion (:2010), the A6 deny-override (:2021-2022), the vol24 exemption (:2145), rank()'s explore_exempt (:2201), and the floor-gate probe clamp (:1324) — does exact membership tests, so a space-containing entry is silently absent from the allowlist. The knob is also read via raw os.environ.get, so it never appears in env_absent telemetry.
  FAILURE: Operator edits live.env to 'KALSHI_SERIES_ALLOW=KXAAAGASM, KXAAAGASW'. KXAAAGASW silently demotes to a probe row (5ct instead of full size on a receipts-proven payer, ALLOW_PROBE_EXCEPTION=1), loses its A6 protection so a KXAAAGAS deny prefix would drop it entirely, and loses the high-activity exemption — with zero warning anywhere. Cannot verify the current VPS live.env formatting from this sandbox, so live impact is unproven; the code asymmetry is verified.

**m-10 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3107 — ACTIVATE-path $1 floor lacks the JOIN path's resting-continuation exemption** (rev: quoter:1-2600 select+gates)
  The JOIN-path floor gate (:2933-2952) deliberately exempts markets we are already resting in (_resting_here) because accrued credit is additive and pulling quotes strands it. The activate-path clone (:3098-3110) has no such exemption: `if _expA < MIN_CREDIT_USD: return []` unconditionally. An activate book we have been seeding (and accruing on) gets its whole ladder cancelled the first cycle the expected-credit model dips under $1.20 — and since our departure thins the book further, the refusal is one-way.
  FAILURE: We activate a void market early-window and accrue ~$0.80 of estimated credit. Mid-window a rival adds depth / frac_left decays, _expA drops to $1.10 -> activate quotes return [] -> diff cancels our resting depth -> the side no longer meets Target -> nobody earns, our $0.80 finishes under the $1 program floor and pays $0.00. Exposure is bounded (activate fired in 73 of 7,080 cycles per the in-code measurement) but the forfeiture is exactly the spread-thin-earn-zero class F1 was fixed for.

**m-11 [INFERRED] kalshi_live/maker_kalshi_quoter.py:2933 — Floor-gate continuation status is amnestied by a one-cycle absence (rotation/fetch-fail) — stranding sub-$1 accruals** (rev: quoter:1-2600 select+gates)
  _resting_here is derived from CURRENT standing orders (own from this cycle's standing read). If a ticker falls out of the footprint for one cycle (rotation, fetch failure, budget refusal) the diff cancels its book; next cycle own=0, so the floor gate treats it as a fresh flat entry and refuses re-admission when remaining-window expected credit < $1.20 — even though the program has banked sub-$1 accrual that only continued presence can push over the $1 cliff. DROP_GRACE=0 (default, not in live flags) so nothing bridges the absence; F14 solved this exact sawtooth class for the D3 ramp clock but the floor gate has no analogous memory. EST_FEED (dark) would partially heal it via the venue-accrual floor, but EST_FEED_MIN_FRAC=0.25 disables the est floor precisely in the late window where accrual dominates.
  FAILURE: A $140/day-pool market we rested in for half its 1-day window (accrued ~$0.9 modeled) rotates out for one cycle. On return, frac_left=0.4, expected remaining ~$0.7 < $1.20, not resting -> refused. Accrual finishes at $0.9 < $1.00 -> venue pays $0; we carried the fill risk for the first half-window for nothing.

**m-12 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:157 — Malformed env values silently revert to code defaults and are invisible to env_absent telemetry** (rev: quoter:1-2600 select+gates)
  _envi/_envf (:154-167) catch TypeError/ValueError and return the default with no counter and no log. env_absent() (:180-182) reports only keys NOT in os.environ, so a set-but-malformed knob reads as 'configured' in telemetry while the code runs the default. The hot-reload path has the same asymmetry: a malformed WATCH (safety) knob value silently `continue`s (:3921) while a malformed restart-only value warns loudly (:3907-3908) — the loud path guards the less-critical tier.
  FAILURE: Operator sets KALSHI_DAILY_LOSS_HALT_USD='10.5 usd' (or a stray quote from a copy-paste). Import parse fails -> halt arm silently becomes the $20 code default, doubling the daily loss envelope; the plan row's env_absent list does NOT name the knob (it is present in the environment), and the hot-reload refresh silently keeps whatever is running. Every subsequent number the operator reasons from assumes $10.

**m-13 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:177 — _envb accepts ONLY the literal '1' — 'true'/'yes'/'on' silently disable default-on safety flags** (rev: quoter:1-2600 select+gates)
  _envb evaluates `os.environ.get(name, '1' if default_on else None) == '1'` (verified: value 'true' with default_on=True -> False). KALSHI_TAKER_FLATTEN and KALSHI_PAIR_BOTH_SIDES are default-ON protections created through this helper; setting either to any truthy-looking non-'1' string turns the protection OFF. The hot-reload watch list casts TAKER_FLATTEN with int(), which raises on 'true' and silently keeps the (now wrong) running value, so the mistake also survives the emergency-lever path.
  FAILURE: Operator writes KALSHI_TAKER_FLATTEN=true intending to confirm the backstop is on (the WARNING at :4052 explicitly tells operators to 'set each explicitly'). The last-resort taker de-risk silently disarms; the next hard inventory breach near settlement rides to resolution because the guaranteed-flat backstop never fires.

**m-14 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3848 — PRESENCE_GATE (live) and other armed selection flags are in neither the hot-watch nor the restart-only warning list** (rev: quoter:1-2600 select+gates)
  _refresh_safety_knobs hot-applies CAPTURE_GATE, STANDDOWN and NETEV_GATE (the same entry-gate family) and loudly warns for restart-only knobs including the dark EST_FEED/MACRO_* pair. KALSHI_PRESENCE_GATE — armed live and controlling the $1.20 entry floor — appears in neither dict, as do D3_RAMP, PIVOT_SELECT, SCORE_RANK, D2_FEEDBACK, FARCLOSE_PAYING_EXCEPTION and DROP_GRACE. An operator editing any of these in live.env mid-flight gets a silent no-op with no needs-restart warning — the exact Gov-D9 defect class the function documents itself as closing.
  FAILURE: The 08-07 APRPOTUS checkpoint shows the floor gate refusing payers; operator sets KALSHI_PRESENCE_GATE=0 in live.env mid-incident expecting the same live-apply the NETEV gate gets. Nothing changes, nothing prints, and the gate keeps refusing entries until someone notices a restart is required.

**m-15 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:764 — D-A est feed: a program-map row with MISSING end_date is treated as live evidence while an unparseable one is excluded** (rev: quoter:1-2600 select+gates)
  In _est_feed_cached, `if _pend and parse_iso(_pend).timestamp() <= now_ts: continue` skips ended programs, and the except branch skips unparseable ends ('not evidence'). But _pend None/absent falls through both -> the estimate row is summed into the floor table. An ended program whose map row simply lacks end_date contributes banked (already-earned) dollars to an ENTRY-admitting floor — the exact class review #4 excluded, re-admitted through the missing-field path. Dark today (EST_FEED=0).
  FAILURE: kalshi_program_map.json carries a row without end_date for a program that concluded yesterday with a $3 estimate. With EST_FEED armed, max(model, est)=3 floors the gate open on the successor market ticker -> we rest quotes justified by dollars that are already banked and cannot grow, buying pure fill risk (the same 'accrued-not-earnable' mechanism reviews #3/#4 were written against).

**m-16 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3732 — cap_desired keeps the WHOLE quote list of any unwind-bearing market — accumulating siblings skip the walk** (rev: quoter:2500-5000 sizing+budget)
  The unconditional keep (:3731-3736) triggers on any(q.reason=='unwind') and keeps kept[t]=qs — the full list, accumulating quotes included — before the capital/family walk runs. Today mixed lists arise only from drop-grace retention of held books (existing standing, no new create, harmless). With PAIR_UNWIND armed (dark, :3601), the appended unwind legs land on tickers that may carry fresh JOIN quotes, exempting those joins from the priority walk; the create-loop committed/funding gate remains the real over-commit backstop, so the damage is allocation-priority distortion, not a breach.
  FAILURE: PAIR_UNWIND=1: a pair-leg ticker with JOIN quotes gets an unwind leg appended -> whole ticker kept first in cap_desired -> its join notional consumes total-cap headroom ahead of higher-usd_day markets, and the total after unconditional keeps can already exceed _total_cap before the walk starts (walk then admits nothing else). Wrong allocation, no uncapped commit.

**m-17 [INFERRED] kalshi_live/maker_kalshi_quoter.py:5841 — Per-side create failure leaves a one-sided accumulating book resting up to the ratchet cool-off** (rev: quoter:2500-5000 sizing+budget)
  Creates are per-order (:5830-5891); if the YES join create is rejected (post-only cross on a stale mirror, tick complaint) while the NO create lands, the book rests one accumulating side. On repeat failure the J6 ratchet (:79-93) blocks the ticker's accumulating creates up to CREATE_FAIL_RATCHET_MAX_S=3600s while the already-resting NO side stays live — a standing one-sided lean the JOIN branch's 'both sides always rest' doctrine (:3148-3150) never re-checks. The event-delta hard-stop one-sides deliberately; this path does it by accident.
  FAILURE: Mirror serves a stale YES book for a few cycles -> YES create rejected 3x ('would cross') -> ratchet arms for 60s..1h. The resting NO side fills 10ct on a one-way move with no paired YES quote; inventory leans exactly when our pricing source is degraded. Bounded by D3 rungs (5-50ct) and the holding=>exit-only flip after the fill.

**m-18 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2517 — Throttle step-back can cross the entry band and drop the throttled side instead of shrinking it** (rev: quoter:2500-5000 sizing+budget)
  _throttled_quote returns best − TICK·THROTTLE_STEP_TICKS (:2517); the final emit re-checks _ok_entry_price (:3210-3215), whose lower bound is EXCLUSIVE (:2381). At best = MIN_PRICE_DOLLARS + 1 tick (0.02 with default 0.01), the stepped price equals MIN_PRICE and fails the band -> the side emits NOTHING rather than staying at ref with shrunken size (the outcome the THROTTLE_SMART free-win branch shows is available).
  FAILURE: Event delta > INV_SOFT on a longshot book with best_y=0.02: YES throttled to 0.01 -> band-dropped -> one-sided NO presence; the throttle intended 'smaller and slower to fill', not 'absent'. Narrow price window; loses that side's reward and un-pairs the join while the event is already directional.

**m-19 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:4872 — day_baseline_reset marker surviving the day roll fires on cycle 2 of a later day — silent budget amnesty** (rev: quoter:2500-5000 sizing+budget)
  The seed branch is `st.get('equity_day') != _day or _consume_day_baseline_marker()` (:4872). On the first cycle of a new day the first arm is True and short-circuits — the marker is NOT consumed while the day re-seeds anyway. On the SECOND cycle equity_day == _day, the marker is consumed, and the branch re-baselines day_start/day_peak to current equity mid-day, erasing the day's peak and refreshing the $10 drawdown budget with only a plan-row day_baseline_reset=1 as evidence.
  FAILURE: Operator runs restart_bundle.sh late in the day; every remaining cycle is torn (marker consumption requires an untorn cycle) or the service stays down past 00:00Z. Next day at cycle 2 the marker silently re-baselines: any losses already taken since 00:00Z are amnestied and the halt measures from a fresh peak — a day-budget amnesty on a day the operator never named.

**m-20 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3021 — NETEV model fallback and capture gate score full-join capture while the D3 ramp rests 5-10ct (no F3)** (rev: quoter:2500-5000 sizing+budget)
  F3 scales _expected_credit_usd by eff_ct/join_ct (:1321-1331), but the NETEV unproven-family fallback (:3021-3024) and the capture gate (:3046) call _prospective_capture with the full _capped_join size. An unproven family is admitted on a ~100ct-modeled capture, then _d3_apply_ramp rests rung-1 sizes (10ct, W7 clamp for unproven series) — the admitted market's ACTUAL share is ~10x smaller than the signal that admitted it.
  FAILURE: Unproven series, pc/HAIRCUT − fingerprint marginally positive at modeled 100ct: gate opens; 10ct rests; realized share cannot clear the venue $1 floor over the window -> the sub-$1 drive-by presence class W10 documented, with NETEV_GATE=1 live and D3_RAMP=1 live. No direct money loss beyond fill risk; corrupts the gate's own admit/skip calibration data.

**m-21 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:6269 — _rest_maker_offset client_order_id lacks a ticker component — same-second exit re-rests collide on venue dedup** (rev: quoter:4900-7317 exec+exits)
  client_order_id = f"mk-{tag}-{int(time.time())}-{side}" (:6269). Two tickers whose taker crosses leave residuals in the same wall-clock second (strand-cross loops all due tickers; with HTTP_POOL a cancel-confirm+cross+re-rest sequence is sub-second-capable) produce identical ids when tag and side match. The module's own C5 comment (:6504-6508) establishes Kalshi rejects a reused client_order_id — so the second EXIT re-rest is rejected and only the printed WARNING (:6282, :6729) records it.
  FAILURE: Two long-yes strand residuals cross partially in the same second; both re-rests are `mk-rerest-<sameepoch>-no`; the second is rejected — that position holds no working exit until the next cycle's unwind pass.

**m-22 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3996 — Taker crosses on the STOP-sentinel branch never persisted: governor counts and re-entry cooldowns lost** (rev: quoter:4900-7317 exec+exits)
  The STOP branch (:3961-3998) calls _flatten_all before load_state() and returns without save_state. _taker_cross_capped increments _TAKER_XN (:6721) during the escalation, but on this path the module dict was never seeded from state and is never written back; likewise no reentry_cool stamp is written for crossed tickers (both feeds ARE wired on the in-cycle paths :5154-5157, :5949-5954). The finally-block re-sync (:6146-6150) only exists inside run_once's main try.
  FAILURE: Bot halts, STOP flatten pays taker to leave ticker X; operator clears STOP; first live cycle sees mkt_taker_xn without X's crosses and no cooldown → the taker-governor (TAKER_GOV_CROSSES) undercounts a paying-to-leave market, and the bot can immediately re-enter the book it just paid to exit.

**m-23 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:6906 — _strand_cross has no per-invocation market budget (settle and preclose takers cap at TAKER_MAX_MKTS=8)** (rev: quoter:4900-7317 exec+exits)
  The settle-taker stops at TAKER_MAX_MKTS (:5131) and preclose enforces _pc_taker_mkts (:6817); _strand_cross iterates every `due` ticker (:6906-6998) and fires one IOC burst each. Per-ticker pacing (STRAND_CROSS_S re-arm) bounds repetition in time but not breadth: a cycle where many tickers' strand clocks expire together (post-breaker, post-outage backlog since the clocks keep running while TAKER_FLATTEN=0/dry) pays half-spread+fee across all of them in one cycle.
  FAILURE: 12 naked residuals all >= STOP_TAKER_MIN_CT age past STRAND_CROSS_S during a TAKER_FLATTEN=0 interval; the operator re-enables the flag; the next cycle crosses all 12 books at once — a single-cycle taker-cost spike the other two backstops were explicitly budgeted to prevent.

**m-24 [INFERRED] kalshi_live/kalshi_ws_feed.py:278 — seq-key disappearance silently disables ALL gap detection while deltas keep applying** (rev: daemon+client+wsfeed)
  Gap accounting only runs when d.get('seq') is not None (:277-285). If a venue dialect migration moves/renames the top-level seq field, _sub_seq stays None forever, the gap check never fires, deltas continue to apply, and mirrors stay clean with zero drop protection. PARSE_FAILS counts unparseable side/price/delta keys but is blind to a missing seq — this is the one dialect-drift axis the 07-30 gauge does not cover, and it is precisely the 'stale books quoted as fresh' class Stage C's safety predicate assumes cannot happen.
  FAILURE: Venue ships a WS v2.1 that relocates seq (payload keys unchanged, so books still parse). A dropped delta under load leaves a phantom or missing level in a mirror; mirror stays clean; Stage C quotes off the misassembled book indefinitely with no counter ticking anywhere.

**m-25 [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:625 — WS reconnect never invalidates HotContext — fills missed during the gap leave hot trusting pre-fill inventory** (rev: daemon+client+wsfeed)
  The only ctx invalidations are on_fill, cold cycles, and hot-write errors. Feed.run reconnects internally (dirtying mirrors) but has no on_reconnect callback and the daemon cannot observe that a disconnect happened. A fill executed during the socket gap arrives on no channel; after reconnect the fill-ack gate passes (new connection acks fill), mirrors re-seed clean, and ctx (up to WS_STALE_S=90s old) still shows event_inv=0 and pre-fill counts for the affected event.
  FAILURE: WS_HOT=1: fill lands during a 5s reconnect blip; post-reconnect book move triggers hot; event-inventory gate reads stale 0; hot reprices the accumulating side of a ticker that now carries inventory, bypassing the reduce-side protection until the next cold cycle (<=60s). Refill is still structurally blocked by the cancel-response clamp, so exposure is mispriced accumulation for one interval, not runaway size. Latent while Stage B is off.

**m-26 [ESTABLISHED] kalshi_live/kalshi_ws_feed.py:288 — feed_lat_ms grows unbounded in the long-lived daemon — slow memory leak** (rev: daemon+client+wsfeed)
  self.feed_lat_ms.append(...) runs on every frame carrying ts_ms (:286-288) and is only ever read by --smoke. A daemon Feed persists until a footprint resubscribe (which requires the sorted watch set to actually change) — on a stable footprint that is days. ~30B/frame; at 10 frames/s across 80 books that is ~25MB/day of pure dead weight in one list.
  FAILURE: Stable footprint week: daemon RSS climbs by hundreds of MB for no purpose; on a small box or alongside a busy day this is the OOM-kill contributor nobody can attribute, and the daemon restart it causes drops all quotes for the reconnect duration.

**m-27 [INFERRED] kalshi_live/kalshi_ws_feed.py:144 — apply_delta lacks _norm_rows' 0<p<1 range guard — out-of-range price becomes a phantom level** (rev: daemon+client+wsfeed)
  Snapshot rows pass through _norm_rows which drops any price outside (0,1) (:74-77). apply_delta's only price guard is price <= 0 (:137): a price that normalizes above 1.0 (e.g. a future cents-as-string dialect: '36' -> _f -> 36.0, which _norm_price does NOT treat as cents because it is a string) is inserted as a real level at $36.00 instead of dirtying the mirror. best() = max(dict) then returns the phantom as best bid.
  FAILURE: Venue migrates delta prices to a shape that parses as a number >1. Mirror stays clean, best_yes reads 36.0, Stage C hands the poisoned book to the quoter; the crossed-book guard (best_y+best_n>=1.0) happens to catch the two-sided case, but one-sided books flow into pricing with a garbage reference. Fails loud only by luck of downstream gates rather than by the mirror's own contract.

**m-28 [HYPOTHESIS] kalshi_live/kalshi_ws_feed.py:273 — ANY error frame tears down the whole connection — one rejected ticker can pin the feed in reconnect churn** (rev: daemon+client+wsfeed)
  _dispatch returns False on every type=='error' frame with no distinction between a fatal connection error and a per-command rejection (e.g. one invalid/settled ticker in the 80-ticker subscribe). run() then reconnects with the SAME watch list, gets the same error, and escalates to the 60s backoff cap forever. The watch list only changes when the sorted footprint-union-standing set changes AND the 120s resub hysteresis allows it, so a rejected ticker held by a lingering standing order can poison the feed for hours.
  FAILURE: A watched market is deactivated venue-side while we still have a resting order on it; subscribe now errors every connection. All mirrors permanently dirty, Stage C 100% REST, Stage A degrades to the 60s heartbeat — the daemon silently becomes the old 1-2min timer while ws_daemon_log shows only reconnect lines. No money lost, but the entire latency edge is gone unobserved.

**m-29 [ESTABLISHED] kalshi_live/maker_kalshi_client.py:194 — No 429/Retry-After handling anywhere in the client — rate-limit episodes handled only by generic exception paths** (rev: daemon+client+wsfeed)
  _request raises plain HTTPError on 429 like any other status; _write has only the fixed 0.16s floor spacing and _get_paginated has no spacing at all (up to 50 sequential authed GETs). Nothing reads Retry-After, and the two live client instances (run_once's and the daemon's) do not share _last_write, so back-to-back writes across the cold_lock boundary can violate the floor. A 429 during _exec_hot's create (after a successful cancel) means the quote is simply gone until the next cold cycle — safe direction, but the daemon then immediately schedules a full cold cycle (~40 reads + writes) into the same rate-limit window at the 5s debounce.
  FAILURE: Venue tightens limits or a burst coincides with another consumer of the key: every hot op 429s, each 429 triggers cycle_req, cold cycles hammer the same limit every 5s, and the plan log shows fetch_failed/cold_cycle_error rows with no backoff ever engaging. Degradation, not loss — creates are never auto-retried so no double-order risk.

**m-30 [INFERRED] kalshi_live/maker_kalshi_client.py:311 — cancel_remaining_ct field shape never live-verified — the B3-1 refill guard rests on an assumed scale** (rev: daemon+client+wsfeed)
  The hot path's structural anti-refill guarantee is place = min(want_count, remaining) where remaining = float(resp['remaining_count_fp']). The fp-suffix convention on this repo's verified endpoints means decimal-string ('5' or '5.00'), under which this is correct — but the DELETE response shape has never been exercised live (Stage B never armed) and the canon warns exactly that plain-vs-fp field guesses invert half the time. If remaining_count_fp were centi-scaled (200 = 2.00ct), min(50, 200)=50 re-creates 50 when only 2 were unfilled — reintroducing the exact partial-fill refill B3-1 exists to prevent.
  FAILURE: WS_HOT armed without first probing one live cancel response: partial fill of 48/50, cancel says remaining '200' (centi), hot re-rests 50, both fill -> 98ct position where the model intended 50, on the accumulating side. Verify the response shape on a throwaway order before arming; a None result fails safe by design.

**m-31 [INFERRED] kalshi_live/kalshi_attribution_ledger.py:315 — Strict-> watermark drops late-visible fills/settlements forever, permanently inflating rewards_residual positive** (rev: recorder+ledger+estrec)
  collect() filters `settled_time > sw` (line 315) and `created_time > fw` (line 320), then advances watermarks to the max timestamp of rows SEEN this run (lines 391-392). If the venue's list endpoints ever surface a row late (batch settlement processing, replication lag) with a timestamp <= the already-advanced watermark, that row is excluded from every future window: its cash/revenue is never modeled, while its dollars DID land in `balance`. The residual (bal - prev_bal - fills_cash - settle_rev, line 362) is then permanently overstated by that amount — and the standing operator rule reads any unexplained POSITIVE step as a REWARD. Same class as the equality edge: a second fill sharing the exact created_time as the watermark but visible only after the run. The position-recon self-check (lines 328-338) would catch a dropped FILL (replayed position diverges from venue position_fp) but has no coverage at all for a dropped SETTLEMENT (settled tickers are excluded from `checkable` at line 331 by design).
  FAILURE: A settlement batch posts revenue to balance at 14:00:05Z with settled_time=13:59:58Z; the ledger runs at 14:00:00Z having just processed another settlement with settled_time=14:00:01Z from the same batch (sw advances to 14:00:01Z); the late row appears on the API at 14:00:20Z. Next run: balance is up by the payout, the settle window excludes the row forever, rewards_residual books the payout as a phantom reward with no warning.

**m-32 [ESTABLISHED] kalshi_live/kalshi_attribution_ledger.py:80 — Ledger's private paginator silently truncates at 10k rows; settlements truncation has zero guard coverage** (rev: recorder+ledger+estrec)
  The ledger uses its own get_paginated (lines 80-90) instead of the client's _get_paginated. The client version (maker_kalshi_client.py:336-368) pages at limit=1000, bounds at 50k, and prints a LOUD warning when it exhausts the bound with a live cursor. The ledger version pages at limit=200, bounds at 10k rows, and returns a silently partial list. For fills, the position-recon self-check at :328-338 explicitly compensates (its own comment names the 50x200 cap). For SETTLEMENTS there is no compensating check: settled tickers are removed from the recon-checkable set (line 331), so a truncated settlements pull undercounts settle_rev with no symptom — and since truncation drops the oldest rows, `settled_tickers` at line 328 also shrinks, which can spuriously ADD long-flat tickers back into the recon check. Reachability today is far off (~147 settlements, ~1.2k fills vs 10k cap) but the tape only grows and the failure is silent by construction.
  FAILURE: Months from now the settlements list crosses 10,000 rows; each ledger run silently drops the oldest settlements from all_settles. In any window where a dropped ticker matters, settle accounting and the settled_tickers exclusion set are wrong with no printed warning — unlike every other truncation path in the lane, which is loud.

**m-33 [ESTABLISHED] kalshi_live/kalshi_attribution_ledger.py:383 — Ledger still emits RULE-ZERO-banned rewards_residual/rewards_alloc_by_series labeled MEASURED in --report** (rev: recorder+ledger+estrec)
  rewards_residual and rewards_alloc_by_series are canon-BANNED as reward bases (credit_history is the reward truth), yet collect() writes both every run (lines 383-384) and report() prints per-day residual under the heading "per-day (MEASURED)" (lines 471-473) and per-series allocations (474-476), with only a trailing prose note. The module header itself documents that a deposit lands in the residual as a fake reward (lines 14-20). Nothing in the money path reads these fields, but the report is a one-command human-facing surface that presents banned numbers with a MEASURED label — the precise RULE-ZERO trap shape.
  FAILURE: A future session (or the operator) runs `python kalshi_attribution_ledger.py --report 14` while assembling the P2 window numbers, sees "rewards +X (MEASURED)" spanning a day with a deposit or a late-settlement drop, and quotes it — reintroducing exactly the +$57.82/day class of error the file's own history documents.

**m-34 [ESTABLISHED] C:/Users/samwa/AppData/Local/Temp/claude/C--lockes-picks-polymarket-ai-v2/5dfe0ebf-2821-475d-946c-72012db44c3b/scratchpad/kalshi-wt/kalshi_live/kalshi_attribution_ledger.py:387 — Row written before state saved: a crash between them double-counts the same fills in the JSONL series** (rev: recorder+ledger+estrec)
  collect() appends the ledger row (lines 386-388) and only then advances last_balance and both watermarks in save_state (lines 390-393). A crash or exception between those two steps (e.g. disk error on STATE_FILE) leaves a written row whose window is re-ingested wholesale on the next run: the same fills_cash, settle_rev, new_fills and trade_by_series appear in TWO rows. report()'s per-day and per-series sums (lines 417-425) then double-count that trading cash; the residual itself self-corrects (it is balance-anchored) but the per-day trade column does not.
  FAILURE: Run N writes a row with fills_cash=-$4.20, then save_state raises on a transient I/O error; run N+1 re-processes the same fills into a second row. `--report` for that day shows trade cash -$8.40, feeding an inflated drag figure into any window comparison built on the report.

**m-35 [ESTABLISHED] kalshi_live/kalshi_cash_recorder.py:263 — Cash recorder has no per-run tape-integrity check; its claimed truncation guard runs in a different process** (rev: recorder+ledger+estrec)
  cum_fills_cash's docstring (lines 141-143) points its truncation guard at kalshi_attribution_ledger.py:328-338 — but that check runs in the LEDGER's timer, over the ledger's own separately-fetched tape (its private 10k-cap paginator), not over the tape the recorder just pulled via the client's _get_paginated at line 261. The recorder's protections are the client's live-cursor print (loud but journal-only, 50k bound) and nothing else: no venue-position-vs-replay reconciliation is run before writing the row, so a truncated, reordered, or shape-degraded pull writes a wrong cum_fills_cash into cash-YYYYMM.jsonl with at most a stdout warning in a timer journal. Since this file is the primary input to the unexplained_todate_* series that the reward/drag decomposition (and the P2 window arithmetic) is differenced from, a bad row corrupts canon measurement with no in-row validity marker.
  FAILURE: A transient API issue returns a complete-looking but partial fills list within the 50-page bound (no live cursor, no warning); replay_fills nets against wrong starting positions; the row's cum_fills_cash is off by dollars; offline differencing books the error into that interval's unexplained delta — which under the standing rule reads as a reward if positive.

**m-36 [INFERRED] kalshi_live/kalshi_credit_feedback.py:103 — never_paid_due judges due-ness only over OUR filled tickers — open sibling strikes can't shield their event** (rev: scores+rank+feedback)
  filled_by_event collects only tickers WE filled (:92); the due loop (:104-111) requires all of THOSE settled >72h. If the venue settles some strikes of an event early (early-determined threshold markets) while sibling strikes we never filled remain open, the event/program has not concluded, the close+1 credit lump cannot exist yet, but the verdict arms never_paid_due. The A-2 fix repaired the event KEY (metadata event_of) but not the quantifier: due-ness is still judged on the filled subset, not the event's full ticker set or the program end.
  FAILURE: We fill 2 early-settled strikes of a 10-strike event on day 1; the event runs 10 more days. On day 5 the builder convicts the series (never_paid_due), D2_FEEDBACK=1 halves its rank multiplier and the W7 clamp holds it at probe rungs, for up to ~10 days until the event's real credit lands and 'paid' clears it. A paying series is penalized during exactly the window it is accruing. Self-healing (paid checked first) but a live false-penalty window.

**m-37 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5384 — D4 score-cache fold (the rank's only live measurement source) is nested inside `if MKT_TELEMETRY:`** (rev: scores+rank+feedback)
  The ks.update fold (:5408-5423) sits inside the MKT_TELEMETRY block (:5384) and after the quotes-jsonl file write (:5391-5393). D9 deliberately moved the ATTEMPT stamps outside this block (:5370-5380, 'frontier progress must not depend on telemetry config or telemetry write health') but the MEASUREMENT fold was left coupled: with MKT_TELEMETRY=0, or under a sustained telemetry write failure (full disk raises at :5391 before the fold), no live capture measurement ever enters SCORES.
  FAILURE: Operator disables telemetry to save disk during an incident: every market decays to stale then unknown over hours, SCORE_RANK degrades to pool-prior ordering, incumbency/swing/capture evidence all age out — a large silent behavior change from a flag documented as observation-only. The disk-full variant is at least counted (_SILENT mkt_telemetry_fail) but the counter does not say the RANK is starving.

**m-38 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5311 — Explore sizing clamp and floor-gate probe clamp use different guards — F3 class resurfaces if allowlist is emptied** (rev: scores+rank+feedback)
  _expected_credit_usd clamps eff_ct to EXPLORE_PROBE_CT only when `SERIES_ALLOW` is non-empty and the series is outside it (:1324-1329). The actual sizing clamp (:5311) fires on `EXPLORE_PROBE_CT > 0 and m.get("explore")` alone. Today rows are only explore-tagged when SERIES_ALLOW is active (probe_only at :2010-2019, rank explore_exempt), so the guards coincide; if the pilot allowlist is ever cleared (SERIES_ALLOW empty), rank() still tags explore rows, the sizing clamp still rests 5ct, but the floor gate models the full _capped_join size.
  FAILURE: Allowlist removed post-pilot: explore markets pass the $1.20 floor on full-size expected credit (say $3 at 50ct) while resting 5ct whose true expected credit is ~$0.30 — sub-$1 drive-bys admitted at probe scale, the exact spread-thin-earn-zero class F3 closed. Config-reachable, not live today.

**m-39 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2661 — F5 residual: both sweeper paths compute prospective capture WITHOUT own_orders** (rev: scores+rank+feedback)
  _sweep_measure (:2661) calls _prospective_capture with no own_orders, and the offline kalshi_capture_sweep.py:103 delegates to _market_telemetry_row likewise without own_orders. In live mode the public book includes our resting orders (the F5 premise), so a swept market we occupy measures share ~S/(B+2S) — the exact double-count F5 fixed at the quote-loop call sites.
  FAILURE: Damped by consumption: shadow_rank substitutes prospective only for kind=='unknown', and markets we rest in normally carry live folds (scored/stale), so the biased pcap is rarely read — it becomes load-bearing only when a held market's live measurement ages past 7d eviction or after a cache wipe, where our own presence then UNDER-ranks the market we already occupy (conservative direction). Mostly a data-quality wart in pcap collection (Phase-3 wiring would inherit it).

**m-40 [ESTABLISHED] kalshi_live/kalshi_capital_rank.py:189 — Live allocation key's commit denominator is D3-ramp-blind — young tickers charged full join (~10x actual)** (rev: scores+rank+feedback)
  est_commit_usd models _capped_join at INV_HARD_CT (:80-103; quoter passes INV_HARD_CT at :3667), but with D3_RAMP=1 a non-proven series can only rest D3_RUNGS[clamp] = 5-10ct. The select-budget walk was made ramp-aware (min(est, _d3_est_ct), :5067-5070, operator-ruled 08-06), but the cap_score ORDERING that decides walk order, cap_desired and bound_creates still divides by the full-size commit.
  FAILURE: A promising unproven market ($20/day modeled capture, will rest 5ct ≈ $5) is scored against commit ~$50-125: cap_score reads up to ~25x lower than reward-per-actually-committed-dollar, so it sorts behind proven full-size books even when its per-dollar capture is superior. Direction is conservative (favors receipt-proven payers, aligned with follow-the-profit) but the distortion is large, unmodeled, and asymmetric across the ramp — the ordering key and the charging key disagree by construction every cycle.

**m-41 [ESTABLISHED] kalshi_live/kalshi_netev_rebuild.py:200 — Notional price fallback uses raw YES price for NO fills (no complement) and drops zero prices via `or`** (rev: tools:netev+settlement)
  `_px = _f(row.get(f"{_o}_price_dollars")) or _f(row.get("yes_price_dollars"))` — for a NO fill whose no_price_dollars is absent (or parses to 0.0, which `or` treats as missing), the fallback is the RAW yes price, not 1-yes as kalshi_attribution_ledger.fill_price (:159-173) correctly does. Notional also reads only count_fp with no count fallback, unlike fill_count (:155-156). Unreachable on the current tape — verified AUD_fills.json: live fills carry both *_price_dollars and count_fp — but a venue field rename silently inflates NO-side notional by (1-p)/p (a 5ct NO booked at 95ct notional, 19x) and DILUTES net_pct in the optimistic direction, weakening the gate.
  FAILURE: Venue renames no_price_dollars (the exact *_fp/_dollars rename class the repo tripwires elsewhere): a family heavy in cheap NO fills gets an ~19x-inflated denominator, its -8% receipt verdict reads -0.4%, and the armed gate keeps quoting a losing family with no alarm — MISSING_FIELD_ROWS-style counters exist in kalshi_fill_costs but not here.

**m-42 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3026 — netev_min_signal telemetry min()s incompatible units (receipt net-% vs model $/day)** (rev: tools:netev+settlement)
  For receipt families `signal = net_pct` (a fraction, e.g. -0.05, :3017-3019); for unproven families `signal = pc/NETEV_MODEL_HAIRCUT - NETEV_FINGERPRINT_USD_DAY` (dollars/day, e.g. -4.8, :3021-3024). Both feed `stats["netev_min_signal"] = min(...)` (:3026). In any cycle mixing receipt and unproven families — the live steady state, since the deployed table has 6 receipt + ~30 unproven families — the min is dominated by the $/day scale and the key is uninterpretable.
  FAILURE: Post-B8 verdict analysis or a knob-tuning session reads netev_min_signal from plan rows to characterize how close families run to the margin: the number is -4.9 on a cycle where the worst receipt family is -0.046, and any threshold reasoning built on it (RULE SIX: verify every denominator) is unit-corrupted at the source.

**m-43 [INFERRED] kalshi_live/kalshi_fill_costs.py:122 — Fee convention mismatch between live rows and settled rows in the fill-cost feed** (rev: tools:netev+settlement)
  Settled rows book realized = revenue + replay cash, and fill_cashflow SUBTRACTS fees (ledger :212), so their realized_pnl_usd is definitively NET of fees while fees_usd is ALSO reported. Live rows book the venue's realized_pnl_dollars, whose fee convention the module header itself declares UNVERIFIED (:10-12) — the test_settlement_pnl venue pins cannot settle it because all three VENUE_POSITIONS fixture tapes carry fee 0. A consumer that computes realized - fees applies fees twice on settled rows or zero/once on live rows; cost_usd_day is not comparable across the two row types.
  FAILURE: The capital-aware ranking (kalshi_capital_rank) or a future net-EV consumer compares cost_usd_day between a settled taker-heavy market (fees inside realized) and a live one (convention unknown): the settled market ranks worse by exactly its fee total, biasing capital away from settled-and-proven families. Small dollars today (maker fills fee-exempt) but it is the feed the net-EV calibration cites as its base.

**m-44 [ESTABLISHED] kalshi_live/kalshi_fill_costs.py:155 — main() comment still declares defect 14 'unfixed' while the same file ships and invokes the fix** (rev: tools:netev+settlement)
  The main() comment block (:150-162) states 'NEW DEFECT 14, reported to the operator, unfixed' and 'the equivalent repair here is Phase-C work and is the operator's to name' — but build() (:100-140) contains the settlements repair, main() fetches settlements and passes them (:166-170), and test_fill_costs_settled.py pins the fixed behavior under the banner 'defect 14 root fix, 2026-08-03'.
  FAILURE: A future canon audit or defect-status review greps this file, reads 'unfixed', and either re-reports defect 14 as open (corrupting the defect ledger the restart gate depends on — restart gate = all defects root-fixed) or re-implements the repair on top of the existing one.

**m-45 [ESTABLISHED] kalshi_live/kalshi_presence_calibrate.py:142 — Unknown-life markets' cost silently dropped from the families table, against the module's own anti-bias rule** (rev: tools:netev+settlement)
  measure() promises markets with no known life are 'still counted for cost' (:84-86) and accrues their hours/fees/orders into a (family, 'unknown') bucket (:133-138) — but the emission loop skips any bucket with an empty presence list (`if not b["presence"]: continue`, :142-143), and unknown-life buckets never get presence entries. Their cost exists only in per-market rows; every family-level fees_per_resting_hour figure excludes exactly the markets whose life lookup failed — the bias the docstring says the design avoids. Additionally, currently-RESTING orders' intervals end at last_update_time (:92), so live coverage since the last amend is uncounted, biasing presence down for active markets.
  FAILURE: B8/W12 calibration consumes fees_per_resting_hour per family-bucket to judge whether longer presence pays: a family where half the markets 404'd their life lookup (delisted, renamed) shows only the surviving half's cost — cost per hour understated on precisely the family where market metadata is flaky.

**m-46 [INFERRED] kalshi_live/maker_kalshi_client.py:433 — get_credit_history: single un-cursored GET (limit=1000), no truncation tripwire, consumed as full lifetime tape** (rev: tools:netev+settlement)
  kalshi_netev_rebuild.main (:389) consumes get_credit_history() as the complete credit history for whole-market credit attribution and the credits_out_of_scope reconciliation. The client method (:419-435) issues one GET with limit=1000 and, unlike _get_paginated (:336-368, which counts and prints on truncation), never inspects a cursor or compares len(rows) to limit. Lifetime credits are 58+ and growing daily per-event across ~30 families.
  FAILURE: Months out (or if the venue caps the page below 1000), the oldest credits silently vanish from the read: families are under-credited, receipt verdicts bias pessimistic, and the document's own reconciliation (credits_out_of_scope vs lifetime figure) mis-reconciles with no warning — the exact 'feed that quietly stops mentioning things' class the client's own pagination comment names.

**m-47 [INFERRED] kalshi_live/w17_coverage_ledger.py:151 — Hardcoded 8-day far-close horizon can drift from the quoter's MAX_DAYS_TO_CLOSE knob** (rev: tools:ledgers+w16+w17+w11)
  w17 attributes 'far_close (unproven)' using dt.timedelta(days=8) (also days=8 at line 203 for probe liveness), but the quoter gates on KALSHI_MAX_DAYS_TO_CLOSE (maker_kalshi_quoter.py:1622, default 3.0, env-tunable) and the paying exception checks PROGRAM end (:3239), not market close_time. w17 already parses live.env (line 59) but never reads this knob.
  FAILURE: Operator tunes KALSHI_MAX_DAYS_TO_CLOSE (e.g., back to 3 per the 07-25 directive, or to any value != 8): markets closing between the two horizons get mislabeled — quoter-excluded far-close markets land in UNKNOWN (false headline alarms), or quoter-eligible markets get a false 'far_close' EXCLUDED attribution that hides the true drop reason.

**m-48 [ESTABLISHED] kalshi_live/w17_coverage_ledger.py:124 — EARNING bucket measured on INTENDED size, not resting presence — placement failures masked as earning** (rev: tools:ledgers+w16+w17+w11)
  ct = max(y_ct, n_ct) uses the telemetry row's intended size (quoter row comment: 'our INTENDED size (0 while gated/parked)'); the row also carries y_rest_ct/n_rest_ct ('what is ACTUALLY resting'), which w17 ignores. A market whose orders consistently fail to place (rejects, 1c-wall refusals, torn lines) still shows intended 25-50ct.
  FAILURE: A ticker with persistent order-placement failures is bucketed EARNING with a $200/day pool; the coverage ledger — whose whole purpose is finding non-earning pool dollars — reports the dollar as covered while zero contracts rest at the venue.

**m-49 [ESTABLISHED] kalshi_live/w11_replay.py:154 — never_paid demotion uses frozen full-window fills — lookahead in the D2 replay policy** (rev: tools:ledgers+w16+w17+w11)
  ctx['fills_by_series'] is computed once from the whole frozen snapshot (line 129) and fed to every cycle; make_policy_credit_feedback demotes series with fills>0 and no credit-to-date. Credits are timeline-guarded (credited_now = t <= ts, line 152) but fills are not: at an early cycle ts the policy 'knows' about fills that happened days later. test_w11_replay.py pins only the credit boundary.
  FAILURE: A series first filled on day 5 of the window is demoted by the replayed d2_feedback from day 1 — the live D2 (which only sees fills-to-date) would not have demoted it. Replay verdicts on never_paid_mult are biased pessimistic for late-filling series, corrupting the D2 knob-tuning measurement.

**m-50 [ESTABLISHED] kalshi_live/kalshi_ledger.py:272 — 'IDENTITY CHECK closes' is tautological — it can never fail** (rev: tools:ledgers+w16+w17+w11)
  rewards is DEFINED as equity - deposits - realized - unreal (line 255), so deposits + realized + rewards + unreal - equity ≡ 0 to float precision; the docstring's 'must close to the cent, every run' promises an integrity gate that structurally does not exist. The only real gate is the sign reconcile at line 228.
  FAILURE: Operator sees 'IDENTITY CHECK closes' and reads it as independent validation of the derived rewards figure; a corrupted input (wrong deposits, unmarked lots) still prints 'closes'.

**m-51 [ESTABLISHED] kalshi_live/kalshi_ledger.py:193 — Unpriceable open lots contribute $0 unrealized — the error lands in the rewards residual** (rev: tools:ledgers+w16+w17+w11)
  mark_open: if no bid/ask/last price is found, the lot is appended to detail with u=0.0 and skipped from unreal (lines 192-194). Because rewards is the residual, the lot's true mark value transfers dollar-for-dollar into 'REWARDS (derived)'. The n/a marker appears only in the top-10 detail print, sorted by |uPnL| — a $0 row sorts last and is the first truncated out of view.
  FAILURE: A thin market with an empty book at read time (common near close) holds a $30 position; unreal omits it; REWARDS (derived) is inflated/deflated by up to the full mark value with the only trace hidden below the print cap.

**m-52 [ESTABLISHED] kalshi_live/w16_successor_finder.py:103 — kalshi_lineage.json has no reader and no writer anywhere — ruled lineage mappings are inert** (rev: tools:ledgers+w16+w17+w11)
  The C-2 comment says 'lineage table kalshi_lineage.json holds ruled mappings, both trust and conviction', but a repo-wide grep finds the filename only in this comment. No tool reads it: w16 cannot suppress already-ruled successor pairs (they re-alarm every run), conviction transfer to a ruled successor never reaches the quoter, and trust transfer never seeds the allowlist path.
  FAILURE: Operator rules 'KXADJOURNRECESS is KXSENATEADJOURN's successor, transfer trust'; nothing consumes the ruling — the successor keeps entering as a 5ct probe, and w16 keeps flagging the same pair as an open decision indefinitely.

**m-53 [ESTABLISHED] kalshi_live/w16_successor_finder.py:112 — Conviction-evasion watch reads quoter_state mkt_out only, missing the mkt_out_backup union** (rev: tools:ledgers+w16+w17+w11)
  The quoter unions quoter_state['mkt_out'] with operator-editable mkt_out_backup.json (maker_kalshi_quoter.py:942-962, including grandfathered one-strike bans); w16 builds its convicted set from quoter_state alone (lines 112-114).
  FAILURE: After a state rebuild (or if a ban lives only in the backup file), a banned franchise is absent from the evasion watch — the exact re-entry-under-new-name scan it exists for runs with an incomplete conviction list.

**m-54 [ESTABLISHED] kalshi_live/w17_coverage_ledger.py:113 — family_budget attribution bounded by the plan row's 40-ticker truncation — overflow lands in UNKNOWN** (rev: tools:ledgers+w16+w17+w11)
  plan['family_dropped_tickers'] is written truncated to [:40] (maker_kalshi_quoter.py:5099). w17's fam_dropped set therefore can only attribute the first 40 family-budget drops; markets dropped beyond the cap fall through to the fallback branch and, if inside the horizon, print as UNKNOWN 'never selected — NO ATTRIBUTED REASON'.
  FAILURE: A heavy family-budget day (>40 dropped tickers across trusted series) inflates the UNKNOWN headline with drops that actually have a named stage — alarm noise in the bucket whose contract is 'we cannot say why'.

**m-55 [ESTABLISHED] kalshi_live/kalshi_drift_labels.py:80 — Label elif-chain contradicts documented definitions; control sets contaminable on re-run (0 hits on frozen data)** (rev: tools:drift-suite)
  kalshi_drift_labels.py:78-87: a GAS position with a worthless lot but net pnl>0 lands in neg_strict, and GAS with any_zero and pnl==0 lands in neg_broad — the docstring (:14-16) defines both negative sets as 'no worthless lot'. Worse, a TEMP position containing a worthless lot but net pnl>=0 falls through to temp_control, i.e. a run-over contract inside the within-family CONTROL set that the entire study's load-bearing comparison rests on. The unused all_zero variable (:75) suggests a stricter rule was intended and dropped.
  FAILURE: Verified non-firing on the frozen CSV (0 positions with any_zero_exit and label != positive; 0 GAS pnl>0 with a zero lot) — so the 07-23 numbers are unaffected. But if the harness is re-run on new receipts (e.g. for the queued D-E study) where a partially-hedged position has one worthless lot and net positive P&L, run-over contracts silently enter the control denominator and depress every measured lift.

**m-56 [INFERRED] kalshi_live/signal_book_drift.py:228 — f_at substitutes future candles (up to +5 min) for at-fill fair value; leakage into prior-drift blocker sweep** (rev: tools:drift-suite)
  f_at (signal_book_drift.py:225-231) searches t-k*60 then t+k*60 for k in 0..5, so when the past minute is unusable it silently returns a fair value up to 5 minutes AFTER the requested time. f0 (at-fill) and fm4 (fill-4min) in section 4 (:238) can therefore embed post-fill prices. Because fair() rejects stressed books (one-sided/wide), the past is most likely missing exactly around run-over fills — the substitution is correlated with the positive class.
  FAILURE: For a run-over fill where the book collapsed in the fill minute, f0 is taken from a candle after the adverse move; prior = sgn*(f0-fm4) then includes part of the run-over itself, inflating the apparent TPR of the 'block the fill if prior-4min drift <= -X' rule in the section-4 tables. Frequency not measured (impact INFERRED); the no-book life-frac alternative in the same section is immune. Note: the sgn = book_side=='bid' convention itself is CORRECT — verified against the feed contract documented in kalshi_attribution_ledger.py:110-111 (317/317 agreement), not the side-derived sign trap.

**m-57 [ESTABLISHED] kalshi_live/kalshi_drift_fetch.py:155 — Resume logic freezes a transient market-fetch failure as a permanent empty entry; silent survivorship hole** (rev: tools:drift-suite)
  On a failed fetch_market, the cache stores {market: None, candles: [], trades: []} (:161); the resume check (:155-157) tests candles/trades 'is not None', so [] passes and the ticker is never retried. make_state (kalshi_drift_detect.py:295-296) then returns None for it and the position vanishes from every group with no per-ticker report — only the aggregate 'usable X/Y' line moves.
  FAILURE: A 5xx or timeout during the fetch run silently deletes that position from the study forever; if failures correlate with anything (e.g. delisted/settled markets), the sample is biased. Verified non-firing on the frozen run: 67/67 positions usable, 0 dropped. Also latent nearby: fetch_trades max_pages=12 caps tape at 12k trades (:114) — no cached ticker is near the cap (max <11k).

**m-58 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1324 — F3 ramp-scaling mirror exempts allowlist series from probe clamp; sizing clamp does not - floor over-admits 10x** (rev: flow:end-to-end trace)
  _expected_credit_usd's probe-clamp mirror (:1324-1329) applies EXPLORE_PROBE_CT only when the series is NOT in SERIES_ALLOW (the A1 carve-out). But the actual explore sizing clamp (:5311-5317) clamps EVERY explore-tagged row - and the L3 series-probe taint (:4094-4104, re-clamp :4578-4583) sets explore=True on ALLOWLIST siblings of a live mkt_out member. For those rows the $1-floor gate models the D3 rung size (up to 50ct) while 5ct actually rests, so it admits markets whose real 5ct share cannot clear the venue's $1 program floor - re-opening the spread-thin-earn-zero class F3 closed, on exactly the rows where the probe clamp fires. Same mismatch if SERIES_ALLOW is ever emptied (mirror never clamps, sizing always does).
  FAILURE: A gas strike is permanently OUT with its market still open; every gas sibling is L3-tainted to probe size; the presence gate evaluates each at 25-50ct (expected credit $2-4, passes), the book rests 5ct whose period share is ~$0.40 - below the venue floor. Quotes rest all window, earn $0, carry fill risk; the ramp-capped counter shows it but the gate never refuses.

**m-59 [INFERRED] kalshi_live/maker_kalshi_quoter.py:3021 — NETEV unproven-model arm not ramp-scaled: full-join capture minus flat $5/day while D3 rests 5-10ct** (rev: flow:end-to-end trace)
  The presence gate got F3 (expected credit scaled by ramp_ct/join_ct, :1321-1331); the netev fallback for unproven families (:3021-3024) still computes `_prospective_capture` at full _capped_join (45ct at live MAX_MARKET_CAPITAL=45) and subtracts NETEV_FINGERPRINT_USD_DAY=5 flat. The reward term is evaluated at ~5-9x the size that will actually rest while the cost term is size-independent, so the two gates run on inconsistent bases for the same market in the same cycle.
  FAILURE: An unproven allowlist family with pc=$18/day at 45ct passes netev (18/3-5>0) and the ramp rests 5ct whose true pc is ~$2/day - below the $5 fingerprint the gate charged for opening; conversely a market whose 5ct economics are fine can be refused because the flat $5 fingerprint was calibrated against full-size fills. Basis noise in exactly the gate the B8 haircut re-fit will calibrate.

**m-60 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3183 — Settle-ramp anchors on raw WIND_DOWN_MIN (45) while wind-down is effective (30-36) - MIN_QUOTE_CT 9-15min early** (rev: flow:end-to-end trace)
  D-G made wind-down proportional via _effective_wind_down_min (floor max(3, SETTLE_UNWIND_MIN=30)), but the ramp scale (:3183) still computes `(mins_left - WIND_DOWN_MIN) / (ramp_min - WIND_DOWN_MIN)` with the absolute 45. For a 180-min program: effective wind-down = 36 min, ramp_min = 90; scale hits 0 at 45 min left, so the market rests MIN_QUOTE_CT=2 from T-45 to T-36 even though quoting continues. Confirms known-open D-B NOTE #10 as real, bounded reward drag.
  FAILURE: Every 1-3h program (the D-G target class) earns near-zero share for the last 9-15 quotable minutes of every window - a systematic capture haircut on short programs that the D-G fix was intended to recover.

**m-61 [INFERRED] kalshi_live/maker_kalshi_quoter.py:2654 — D-B #9 premise wrong: sweeper's F5-less pcap IS live-consumed (ALLOC_KEY=1 + SWEEP_ENABLED=1)** (rev: flow:end-to-end trace)
  _sweep_measure (:2651-2661) calls _prospective_capture without own_orders, so on a book containing our resting orders the share reads ~S/(B+2S) (~0.5 solo) instead of ~1.0 - the exact F5 double-count. The known-open note says 'consumer gated off', but the 08-04 EOD handoff records ALLOC_KEY=1 and SWEEP_ENABLED=1 live: _alloc_priority (:3660-3666) merges SCORES pcap rows into the prospective feed and shadow_rank substitutes them for 'unknown' markets. Impact is bounded (the live quote-loop fold, which IS F5-threaded, refreshes footprint markets; the sweeper mostly measures books we do not occupy), but the note's inertness claim should not be relied on.
  FAILURE: A market we alone occupy rotates out of the footprint, its live score ages out, the sweeper re-measures it with our ladder still resting: pcap ~0.5x truth feeds the alloc walk and the market is budget-ordered behind genuinely worse books - the anti-thesis direction (we under-rank exactly the empty books we captured).

====================================================================================================
## S4. NOTES (55)

**n-1 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1355 — F17 sub-hour benefit half-eaten by the SETTLE_UNWIND_MIN floor — 58-min windows still forfeit ~52%** (rev: commits:0b+recorder+DB-DG)
  _effective_wind_down_min floors at max(WIND_DOWN_MIN_FLOOR=3, SETTLE_UNWIND_MIN=30). For the motivating 58-min temp window: min(45, 0.2*58=11.6) → floored to 30. Enterable window improves 13→28 min, not to ~46. The follow-up docstring is honest about this (C8 safety, taker-window scaling deferred), but the commit message's 'proportional wind-down' oversells.

**n-2 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:1330 — F3 scales by the MAX of the two sides' join sizes — asymmetric books get double-discounted** (rev: commits:0b+recorder+DB-DG)
  join_ct = max(_capped_join(yes), _capped_join(no)) but pc already models each side at its own join size; dividing the blended pc by the larger side's ratio under-scales the smaller side. Conservative (refuses more), never admits more.

**n-3 [ESTABLISHED] kalshi_live/w0b_payout_timing.py:45 — 0b throttle retries only HTTP 429 — a transient URLError still kills the run mid-evidence** (rev: commits:0b+recorder+DB-DG)
  _get's retry loop re-raises any non-429 HTTPError immediately and never catches urllib.error.URLError/socket timeout; the programs-pagination loop in main() has no per-call try, so one transient network fault aborts the whole evidence run after the per-credit section (or mid-pagination), reproducing a partial-evidence artifact of the same class the throttle fix was for.

**n-4 [INFERRED] kalshi_live/maker_kalshi_quoter.py:2807 — Telemetry share reads 0.0 for a resting-but-not-quoting solo book (wind-down keep-reduce edge)** (rev: commits:0b+recorder+DB-DG)
  In _market_telemetry_row, when we rest orders but the current desired quotes list has no accumulating quote on that side (e.g. wind-down keeps only the reducing side), score=0 while rest_df equals the whole book → denom = max(df_total−rest_df,0)+0 = 0 → share 0.0, though the venue is paying us ~1.0 share for the still-resting order until it is actually cancelled.

**n-5 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:697 — Armed-with-defaults D-C rests nothing: side cap $18 (0.9x HELD_MAX_USD=20) < ~$20.85 empty-book ladder cost** (rev: commits:DA+DC+followups)
  side_cap = min(60/2, 0.9*20) = $18; the default 3-rung ladder for need≈990ct costs ≈0.03*397+0.02*298+0.01*298 = $20.85 > $18 → every default-knob designation increments macro_probe_unaffordable and rests zero. Deliberate per the operator record ('defaults deliberately unaffordable') and the docstring names the two-knob decision — but the test fixture (HELD_MAX_USD=40) is the only place the ladder actually rests, so arming requires naming MACRO_PROBE_USD and HELD_MAX_USD together or D-C is a counter-only no-op.

**n-6 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2004 — Designation observability gaps: no-program designations vanish uncounted; deny-dropped ones still count as admitted** (rev: commits:DA+DC+followups)
  select_footprint only sees tickers present in the active liquidity-program list — a designated ticker with no program (or null target/discount fields) never reaches the macro branch and increments nothing. A designated ticker in a SERIES_DENY family increments macro_probe_admitted (:2009) and is then dropped at :2021-2028 (deny retained by design; unlike SERIES_ALLOW, designation does NOT outrank deny), so counters can read admitted=N with zero macro markets quoted.

**n-7 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:707 — Spec drift: D-F ruling says the ladder 'spans 3 real levels'; code accepts a 2-level ladder (refuses only <2)** (rev: commits:DA+DC+followups)
  With MACRO_PROBE_TOP=0.02 the price list is [0.02, 0.01] — len 2 passes the wall-refusal check (:707-710) and rests. Satisfies the never-1c-only-wall clause but not the literal 3-level clause of the D-F ruling; the test also asserts only >=2.

**n-8 [INFERRED] kalshi_live/maker_kalshi_quoter.py:5342 — Ramp trigger counts macro quotes (harmless copy); score-cache fold ingests macro-ladder cycles as capture data** (rev: commits:DA+DC+followups)
  any(count > _rct) fires on macro counts (~400ct), making a _q_fullsize copy though the ramp then skips macro quotes — the D8 recompute runs on identical quotes (no behavior change). Separately, the score-cache fold (:5408) treats macro-ladder cycles as genuine capture attempts (reason != unwind), so an armed designated ticker's SCORES entry is measured from deep-discount-ladder books; if that measures low, SCORE_RANK deranks the designated ticker in the very slot competition it has no reserved seat in (compounds known-open #6-part2).

**n-9 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2001 — Two selection drops without always-emit reasons: missing market_ticker rows, and close_past_selected** (rev: quoter:1-2600 select+gates)
  A program row with a null/absent market_ticker is dropped with `continue` and no counter (:2000-2002) — the only reasonless drop left in the funnel (drop_null_fields covers only target/df). Separately, the B-1 drop reason 'close_past_selected' (:2164) is not in _ALWAYS_EMIT_DROPS (:1097-1101), so its absence from a plan row is ambiguous between 'never fired' and 'stage never ran' — the exact ambiguity the A3 doctrine (and the FP_DROPS block itself) exists to remove.

**n-10 [INFERRED] kalshi_live/maker_kalshi_quoter.py:2287 — PIVOT coverage floor ignores pool_cap, so the candidate pool can exceed the read-reserve guarantee** (rev: quoter:1-2600 select+gates)
  pool_cap (:2267) bounds candidates to READ_BUDGET - PIVOT_READ_RESERVE, but the coverage loop (:2286-2289) appends PIVOT_COVERAGE rows for EVERY active series before the cap is consulted; only the density remainder honors it. With ~150 active liquidity series unscoped, picked starts ~150 > pool_cap 120, so the quote loop can spend past the reserve meant for strand/ladder/settle reads.

**n-11 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2661 — _sweep_measure pcap still lacks own_orders threading (F5 residue; consumer gated off)** (rev: quoter:2500-5000 sizing+budget)
  The sweeper seam calls _prospective_capture without own_orders (:2661), so on any book where we rest, the stored pcap carries the pre-F5 double-count (~0.5 share on a solo book). Consumers: sweeper cache keys and the ALLOC_KEY prospective merge (:3660-3666) — both off live (ALLOC_KEY=0, Phase 3 operator-gated). This is known-open D-B NOTE #9, confirmed.

**n-12 [INFERRED] kalshi_live/maker_kalshi_quoter.py:3535 — order_id_for nonce is whole-second — two cycles in one epoch second reuse client_order_ids** (rev: quoter:2500-5000 sizing+budget)
  cyc = int(now.timestamp()) (:4001); order ids are mk-{cyc}-{i}-{side} (:3535). Two run_once invocations inside the same second (event-driven daemon burst) generate identical (cyc, i, side) tuples for their early creates; Kalshi's client_order_id dedup then rejects or aliases the second cycle's creates, surfacing as anonymous create_fail.

**n-13 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5782 — Amends are not counted against WRITE_BUDGET_PER_CYCLE** (rev: quoter:2500-5000 sizing+budget)
  bound_creates budgets len(cancels)+creates (:3766-3771) but the amend list executes before and outside that accounting (:5782-5789). A large amend batch plus a full cancel/create budget can exceed the per-cycle write ceiling the budget exists to respect.

**n-14 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3667 — _alloc_priority runs shadow_rank while holding SCORES_LOCK** (rev: quoter:2500-5000 sizing+budget)
  The full ranking call sits inside `with SCORES_LOCK:` (:3660-3678); the sweeper thread's _sweep_store/_sweep_ages block for the ranking's duration every cycle when ALLOC_KEY=1. Flag is off live; hygiene only (rank on a snapshot instead).

**n-15 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:3766 — bound_creates can starve unwind creates when cancels alone consume the write budget** (rev: quoter:4900-7317 exec+exits)
  budget = max(0, WRITE_BUDGET_PER_CYCLE - len(cancels)) (:3771); cancels themselves are never capped, and although unwind groups sort first (:3775-3777), a cancel wave >= 400 zeroes the budget and drops every create including exits for the cycle. At FOOTPRINT_TOP=60 (~120 standing orders) this is unreachable today; it becomes live if FOOTPRINT_TOP or ladder counts grow.

**n-16 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:6287 — flatten_to_zero has zero callers — dead taker path with a divergent crossed-counter convention** (rev: quoter:4900-7317 exec+exits)
  Both former callers (settle-taker :5149, STOP escalation :6634) now use _taker_cross_capped; grep confirms no call site in quoter, daemon, or flatten_kalshi.py. It also counts `crossed += 1` per PASS (:6360) where _taker_cross_capped counts contracts (:6712) — a trap if ever revived. Rule 5: removal needs operator signoff; flagged only.

**n-17 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5129 — oids_by_t computed every settle-taker cycle and never used** (rev: quoter:4900-7317 exec+exits)
  Built at :5129; no other reference in the file (was the hint-oids feed for the removed flatten_to_zero call). Pure dead work each live cycle with held inventory.

**n-18 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5967 — plan['gated_out'] can go negative once strand-unwind/grace tickers enter desired** (rev: quoter:4900-7317 exec+exits)
  gated_out = len(footprint) - len(desired) (:5967), but desired gains entries NOT in footprint: strand-unwind exits (:5486-5490) and grace-retained tickers (:5718-5725). A cycle with few footprint markets and several strand exits emits a negative gated_out.

**n-19 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:5671 — at_ref/off_ref telemetry measures distance to the best bid, not to the LIP Reference Price** (rev: quoter:4900-7317 exec+exits)
  The split (:5663-5681) classifies a quote as at-ref only when |price - best_bid| < TICK/2. The venue reference is the first level where CUMULATIVE resting size >= Target/5, which can sit BELOW the touch — money resting between the true reference and the touch earns full 1.0x credit but is counted off_ref here. The bias is one-directional (understates at_ref_usd), so calibrating expected credits against this key will read placement as worse than it is.

**n-20 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:4872 — day_baseline_reset marker survives a UTC day roll and fires a redundant same-day re-baseline a cycle later** (rev: quoter:4900-7317 exec+exits)
  `st.get('equity_day') != _day or _consume_day_baseline_marker()` short-circuits: on the first cycle of a new day the marker is not consumed (left arm True), the baseline reseeds anyway, and the next cycle consumes the stale marker and re-baselines again, emitting day_baseline_reset=1 for a reset the operator's restart no longer semantically covers.

**n-21 [HYPOTHESIS] kalshi_live/maker_kalshi_quoter.py:6700 — If the venue drops BOTH fill_count dialects, residual is overstated and the re-rested exit could exceed the position** (rev: quoter:4900-7317 exec+exits)
  fill = float(o.get('fill_count_fp') or o.get('fill_count') or 0) (:6700, :6348): both keys absent reads a real fill as 0, `remaining` stays at |naked| though contracts actually crossed, and _rest_maker_offset then rests an exit sized to the stale remaining — larger than the true residual, so a full fill crosses through flat. Self-corrects next cycle when fresh positions re-derive sizing; requires an API shape migration removing both dialects simultaneously.

**n-22 [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:379 — ctx.build runs every live cold cycle even with WS_HOT=0 — duplicate portfolio reads** (rev: daemon+client+wsfeed)
  cold_cycle unconditionally rebuilds the full HotContext in live mode (fresh _live_standing, _held_cost, load_state, select_footprint) immediately after run_once just fetched the same data. With Stage B off, the only consumed output is the ticker set for the WS watch list; the breaker/headroom/inv machinery is dead weight costing 2-4 authed REST round trips and ~1-2s per cycle, on every book-move-triggered cycle.

**n-23 [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:346 — mirror_book cross-side torn read: yes and no captured non-atomically vs the delta-applying loop thread** (rev: daemon+client+wsfeed)
  rows() reads self.yes then self.no as two separate sorted() passes while the event loop keeps applying deltas; a delta (or an unsolicited snapshot rebinding both dicts) landing between the two reads yields a book whose sides are from different instants. The dirty re-check after rows() catches gaps but not ordinary mid-read mutation. Bounded skew (~one frame) and comparable to REST's own temporal skew across polls, so acceptable — but the docstring's claim of safety only covers the RuntimeError/dict-resize case, not this.

**n-24 [ESTABLISHED] kalshi_live/maker_kalshi_ws_daemon.py:692 — Watch cap [:80] truncates ALPHABETICALLY when footprint+standing exceeds 80 — the F9 'moot' claim has a reachable edge** (rev: daemon+client+wsfeed)
  tickers = footprint (FOOTPRINT_TOP=60, and PIVOT over-selects the pool) union standing tickers; after churn the union can exceed 80, and sorted()[:80] then drops the lexically-last markets from the fast feed entirely (no mirror, no Stage-A trigger, no hot defence) — exactly the pre-F9 failure at a higher threshold. Cold cycle still covers them at heartbeat cadence.

**n-25 [ESTABLISHED] kalshi_live/kalshi_ws_feed.py:34 — Feed always connects to PROD WS hosts regardless of client mode; dead-candidate ordering taxes every reconnect** (rev: daemon+client+wsfeed)
  WS_URL_CANDIDATES is built from PROD_BASE unconditionally — a demo-mode daemon would offer demo credentials to prod WS and fail its connect loop forever (Stage A degrades to heartbeat, silently). Separately, the candidate list is re-walked in order on EVERY reconnect; if the first candidate host does not serve WS or hangs, each reconnect pays up to CONNECT_TIMEOUT_S=30s of dead time before reaching the working host, extending every mirrors-dirty window.

**n-26 [INFERRED] kalshi_live/maker_kalshi_ws_daemon.py:656 — Bootstrap fallback ticker is expired (26JUL31); invalid subscribe may interact with the error-frame teardown** (rev: daemon+client+wsfeed)
  The last-resort bootstrap ticker expired 2026-07-31; it is only reached when the empty-first-cycle REST probe also fails, but subscribing to an expired ticker plausibly draws the venue error frame, which (per the error-frame finding) tears down the connection and loops. Also: on_fill logs msg.get('count') — under the *_fp field canon that key is likely absent, so fill telemetry rows probably carry count:null (cosmetic).

**n-27 [ESTABLISHED] kalshi_live/maker_kalshi_client.py:40 — Account UUID hardcoded as CREDIT_USER_ID default; dry-run intents list unbounded in a long-lived process** (rev: daemon+client+wsfeed)
  The account identifier ships in source as the env fallback (documented as non-credential, but it pins the module to one account and lands in every checkout). Separately, self.intents.append on every dry-run write never truncates — a long-lived dry-run daemon accumulates intents forever (same leak class as feed_lat_ms; harmless in live mode where the list is never written).

**n-28 [ESTABLISHED] kalshi_live/kalshi_fill_costs.py:82 — MISSING_FIELD_ROWS counts EITHER missing pnl/fee field but comment and printed warning both say BOTH** (rev: recorder+ledger+estrec)
  Code at line 82 counts a row when realized_pnl_dollars is None OR fees_paid_dollars is None (and test_ops_hardening_0730.py:157-159 pins either-counts). The module-level comment (lines 34-35: "rows with BOTH pnl/fee fields absent") and the operator-facing warning at line 177 ("missing BOTH pnl/fee fields") describe the opposite semantics. The tripwire itself is correct; the message an operator acts on is wrong.

**n-29 [INFERRED] kalshi_live/kalshi_fill_costs.py:92 — Live rows and settled rows sit on potentially different fee-netting bases in the same ranking feed** (rev: recorder+ledger+estrec)
  Live-market rows use the venue's realized_pnl_dollars, whose net-of-fees status the module's own docstring (lines 10-13) declares UNVERIFIED. Settled rows (defect-14 branch) use revenue + position-aware replay cash, which is definitionally NET of fees (fill_cashflow subtracts fill_fee). If the venue field is gross of fees, the two row populations in kalshi_fill_costs.json differ systematically by the fee total, skewing cost_usd_day comparisons between settled and unsettled markets in the capital ranking.

**n-30 [ESTABLISHED] kalshi_live/kalshi_cash_recorder.py:333 — JSONL appends are single unflushed writes; torn final lines possible and only some consumers tolerate them** (rev: recorder+ledger+estrec)
  cash recorder (:333-334), ledger (:387-388) and estimates recorder (:47-49) each append one json line via a buffered write with no fsync; a crash mid-flush leaves a torn line. Readers vary: ledger report() skips bad lines (:407-409), the quoter's est-feed reader fail-opens with a cushion (maker_kalshi_quoter.py:774-779), but any offline differencing of cash-YYYYMM.jsonl must implement its own skip — there is no shared reader, so each future analysis script re-decides how to handle a torn row in the money series.

**n-31 [ESTABLISHED] kalshi_live/kalshi_estimates_recorder.py:79 — Programs that ended before the recorder's first run are permanently unmappable, counted but unactionable** (rev: recorder+ledger+estrec)
  The map only ever merges from /incentive_programs?status=active, so a program that left the active list before 2026-08-06 (recorder deployment) can never acquire a market_ticker/end_date entry; its estimate rows (which persist until payout) stay in the `unmapped` count at line 79 forever. The quoter's est table silently drops them (pmap miss -> mt None). No per-id backfill path exists in the recorder. (The `next_cursor` pagination key IS correct for this endpoint family — confirmed by kalshi_capture_sweep.py:86.)

**n-32 [ESTABLISHED] kalshi_live/kalshi_attribution_ledger.py:279 — Non-atomic snapshot: balance read tens of seconds before fills, so in-flight activity lands as +/- residual noise** (rev: recorder+ledger+estrec)
  collect() reads balance first (:279), then paginates orders, positions, fills and settlements at 0.6s spacing (:282-302) — the fills read can complete 10-30s after the balance read. A fill executing in that gap is counted in fills_cash but not yet in bal, pushing this row's residual negative and the next row's positive by the same amount. Sums are unbiased; individual rows are not — relevant only because per-row residuals are exactly what the clean-interval and per-day surfaces display.

**n-33 [HYPOTHESIS] kalshi_live/kalshi_credit_feedback.py:67 — Credit rows' `status` field ignored — a pending/reversed credit would count as 'paid' forever** (rev: scores+rank+feedback)
  build_feedback counts any row whose reason matches the prefix (:68-72); the client documents a `status` field on credit rows but no value vocabulary is established. If the venue can emit non-final credits, credits_n > 0 flips the series to 'paid' (checked first, clears never_paid_due) and _d3_ramp_ct 'proven' unclamps sizing.

**n-34 [ESTABLISHED] kalshi_live/kalshi_market_scores.py:128 — Swing penalty tracks the YES reference only — NO-side reference volatility invisible to ref_move** (rev: scores+rank+feedback)
  update() is fed y_ref only (quoter :5422-5423 passes _cache_row['y_ref']); ref_move is an EWMA of the change in y_ref. A book whose NO-side reference whips (spread collapse/expansion with a pinned yes bid) fills us adversely on the NO side yet carries zero swing discount.

**n-35 [ESTABLISHED] kalshi_live/kalshi_market_scores.py:216 — At intermediate decay the swing penalty still partially discounts the pool-prior component of the blend** (rev: scores+rank+feedback)
  penalty_eff = 1+(penalty-1)*decay multiplies the WHOLE blend (measured + prior components). At decay=0.5 the prior half of the value carries half the swing discount, though ref_move describes only the measurement. Endpoints are exact (decay=1 full penalty on pure measurement; decay->0 no penalty on pure prior); the mid-curve mixing is a deliberate approximation per the 08-04 fix comment. Numerically verified convergence 0 -> 10.2 -> 26.9 -> 97.0 -> 100.0 over 0-24h for capture 0 / ref_move 0.12 / pool 100.

**n-36 [ESTABLISHED] kalshi_live/kalshi_credit_feedback.py:108 — Naive settled_time would crash build_feedback; truncation-alarm limit (1000) duplicated by hand** (rev: scores+rank+feedback)
  _iso only rewrites a trailing 'Z'; a naive ISO string parses tz-naive and `now - st` at :108 raises TypeError outside any try (the try at :96-99 covers parsing only). Venue shape is Z-suffixed 127/127 (client probe), so unreached today. Separately main() hardcodes limit=1000 at :206 to mirror get_credit_history's default — a change to either constant desynchronizes the B-7 truncation alarm.

**n-37 [ESTABLISHED] kalshi_live/kalshi_capital_rank.py:80 — est_commit_usd hard-assumes JOIN_SIZE=0 and complement pricing (no side = 1 - yes ref)** (rev: scores+rank+feedback)
  The estimator mirrors only the JOIN_SIZE=0 branch of _capped_join (documented); if KALSHI_JOIN_SIZE were ever set >0 the live sizing takes min(JOIN_SIZE, dollar_ct) with no quantize while the estimate keeps the INV_HARD_CT+quantize shape — shape drift the docstring claims cannot happen. It also prices the NO side at 1-p_yes (ignores spread), overstating the cheap-side price on wide books. Otherwise verified arithmetically identical to _capped_join (dollar cap, int truncation, INV_HARD_CT clamp, quantize-at->=5, floor 1).

**n-38 [ESTABLISHED] kalshi_live/maker_kalshi_quoter.py:2759 — _market_telemetry_row scores one quote per side — D-C macro-probe ladders would measure top level only** (rev: scores+rank+feedback)
  next((x for x in quotes if x.get('side')==side), None) takes the first quote per side (:2759); the macro-probe rests a 3-level ladder per side, so its capture/telemetry would count only one level's size at one distance.

**n-39 [ESTABLISHED] kalshi_live/kalshi_netev_rebuild.py:261 — A family whose only in-window activity is excluded taker fills is dropped, losing its taker audit trail** (rev: tools:netev+settlement)
  The families dict keys come from set(fam_pnl)|set(fam_credits)|set(fam_settle) (:261). With exclude_taker=True, a family whose every in-window fill is a taker never populates fam_pnl (the `continue` at :186-189 fires before :195), so fam_taker_ct/fam_taker_excl for it are computed and then never emitted.

**n-40 [ESTABLISHED] kalshi_live/kalshi_presence_calibrate.py:193 — presence_for's '*|bucket' wildcard fallback key is never produced by measure() — dead lookup path** (rev: tools:netev+settlement)
  presence_for consults table.get(f"*|{bucket}") (:193) as a family-agnostic fallback, but measure() only emits f"{fam}|{bucket}" keys (:144). No producer in the repo writes a '*|' key, so the fallback silently never fires and every unmatched family gets default=1.0 (assume-perfect).

**n-41 [ESTABLISHED] kalshi_live/kalshi_netev_rebuild.py:377 — Date-only --until bounds at 00:00Z, excluding effectively the whole final day; ET windows need manual math** (rev: tools:netev+settlement)
  _iso parses '2026-08-09' as 2026-08-09T00:00:00Z and _in_window uses inclusive <= (:110-116), so --until 2026-08-09 admits only the midnight instant of Aug 9. The canon-window caveat (:331-335) documents that like-for-like ET windows need T04:00Z bounds, but the CLI help ('window start, ISO') offers no inclusive-end or ET helper.

**n-42 [ESTABLISHED] kalshi_live/kalshi_attribution_ledger.py:383 — Banned fields rewards_residual / rewards_alloc_by_series still produced and persisted by their origin module** (rev: tools:ledgers+w16+w17+w11)
  Banned-field hunt result: the only occurrences in the reviewed surface are in kalshi_attribution_ledger.py, which still computes and writes both fields into its ledger rows (:359-396) and aggregates them in reporting (:419-422). None of the six reviewed tools consume them; kalshi_ledger.py imports the module but uses only P/get/get_paginated (module is __main__-guarded, import is side-effect-safe). The producer being alive means any new consumer is one attribute-access away from a RULE ZERO violation.

**n-43 [ESTABLISHED] kalshi_live/w17_coverage_ledger.py:221 — A-1 parse-rate alarm counts non-LIP credit types (referral etc.) against the 90% threshold** (rev: tools:ledgers+w16+w17+w11)
  matched filters on the LIP reason prefix; len(creds) includes every credit type. Today 2 referral rows of ~58 keep the ratio at ~97%, but the denominator conflates 'prefix changed' (the A-1 target) with 'account received other credit types'.

**n-44 [ESTABLISHED] kalshi_live/w11_replay.py:146 — Replay candidate pool is the shadow's top-FOOTPRINT_TOP (60) — deeper candidates invisible to every policy** (rev: tools:ledgers+w16+w17+w11)
  caprank components store shadow[:FOOTPRINT_TOP] only (maker_kalshi_quoter.py:860-872); replay policies can only reorder within that top-60. A payer series the shadow ranked 61+ can never be selected by any replayed policy, so a D2 variant's true ability to rescue deep payers is untestable from this telemetry.

**n-45 [ESTABLISHED] kalshi_live/w16_successor_finder.py:113 — Single convicted TICKER escalates to whole-SERIES conviction in the evasion watch (grain escalation)** (rev: tools:ledgers+w16+w17+w11)
  convicted |= {t.split('-')[0] for t in mkt_out}: mkt_out entries are per-ticker operator bans; the watch scans at series grain, so one banned strike marks the entire franchise convicted for successor-matching purposes. Watch-only output (operator decision), but the label 'CONVICTED/BANNED' overstates the ruling's grain.

**n-46 [ESTABLISHED] kalshi_live/kalshi_ledger_reconstruct.py:10 — API key id hardcoded as an in-source default** (rev: tools:ledgers+w16+w17+w11)
  KID falls back to the literal production access-key id when the env var is unset. The private key is not exposed (path only), but the access-key id is committed to the repo; hygiene divergence from the client/attribution tools which require env/file config.

**n-47 [ESTABLISHED] kalshi_live/kalshi_ledger.py:310 — --rate spans first-ever to last snapshot, including deposit changes and both residual eras** (rev: tools:ledgers+w16+w17+w11)
  show_rate diffs rows[0] vs rows[-1]; if --deposits was updated between snapshots, rewards_derived jumps by the deposit delta and the 'rewards/day' rate absorbs it; there is no same-deposits filtering or windowing.

**n-48 [ESTABLISHED] kalshi_live/kalshi_drift_detect.py:353 — evaluate()/point_samples swallow all detector exceptions as 'did not fire'; bugs read as negative results** (rev: tools:drift-suite)
  detect.py:352-355 and eval.py:163-165 wrap detector calls in except Exception → False/None. A detector that crashes on every call (typo, schema drift on re-run) reports TPR 0.00 and is read as 'this signal carries no information' rather than as broken.

**n-49 [ESTABLISHED] kalshi_live/kalshi_drift_jump.py:56 — decompose worst-step ignores 60s spacing (sibling checks it); latent jump_share inflation on gappy data** (rev: tools:drift-suite)
  kalshi_drift_jump.py:54-58 computes steps over consecutive timeline entries without checking ts spacing, so a step across a multi-minute hole would be booked as a 'single-minute' move — inflating jump_share, the number the 'undetectable gap' verdict rests on. signal_book_drift.py's concentration section checks spacing (==120, :188-189); jump.py does not.

**n-50 [ESTABLISHED] kalshi_live/kalshi_drift_detect.py:1 — Frozen one-shot artifacts, not standing tools; live path never imports them; timezone handling verified clean** (rev: tools:drift-suite)
  All six files are the 2026-07-23 drift study family over frozen inputs: kalshi_transactions_2026-07-23.csv (timestamps carry explicit -04:00 offsets; labels.ts()/fetch.ts() via offset-aware fromisoformat().timestamp() are timezone-CORRECT against epoch candle ts — no tz/window defect found) and kalshi_drift_cache.json (built 07-23). Grep confirms the only importers are the study siblings (eval/jump/selftest/cadence_refute/AUD_diag) — the live quoter never touches them; the .txt/.json outputs are the frozen deliverables.

**n-51 [INFERRED] kalshi_live/maker_kalshi_ws_daemon.py:448 — Hot path (dark) calls desired_quotes without own_orders - F5-less gate basis if WS_HOT is armed** (rev: flow:end-to-end trace)
  hot_event passes own/inv/ev/cost but not own_orders, so presence/netev/capture would evaluate with the double-counted denominator the F5 fix removed from the cold path. Safe today by construction (reprice-pair diff only acts on same-side same-count matches; a gate divergence yields no_op), but the two paths would compute different desired books from the same state.

**n-52 [INFERRED] kalshi_live/maker_kalshi_ws_daemon.py:692 — Watch list truncates alphabetically at 80 over the PIVOT pool - resting tickers can fall off the fast feed** (rev: flow:end-to-end trace)
  ctx.build's tickers = select_footprint pool (up to PIVOT_POOL_MULT*FOOTPRINT_TOP = 120 rows) union standing; `sorted(tickers)[:80]` keeps the alphabetical head. A resting/held market late in the alphabet gets no mirror and no book-move trigger (heartbeat-only, 60s). Moot on today's ~33-row pilot; becomes real when the allowlist widens.

**n-53 [INFERRED] kalshi_live/maker_kalshi_quoter.py:3771 — bound_creates can drop unwind creates when cancels alone consume the 400-op write budget** (rev: flow:end-to-end trace)
  budget = max(0, WRITE_BUDGET - len(cancels)); with cancels >= 400 every create group is dropped, unwind-first ordering notwithstanding - a de-risk create starved by write budget in an extreme churn cycle (e.g. the finding-2 teardown cycle itself).

**n-54 [INFERRED] kalshi_live/maker_kalshi_quoter.py:7271 — _standing_from_rows falls through remaining_count_fp==0 to initial count, overstating a filled resting row** (rev: flow:end-to-end trace)
  `o.get("remaining_count_fp") or o.get("remaining_count") or o.get("initial_count_fp") or o.get("count")` - a legitimate 0 remaining (venue listing lag) is falsy and falls through to the initial size, so `standing`, committed capital, and the diff see a phantom full-size order.

**n-55 [INFERRED] kalshi_live/maker_kalshi_quoter.py:5216 — PIVOT early-stop counts unwind-only/retained tickers as earners; gated_out can go negative with strands** (rev: flow:end-to-end trace)
  `len(desired) >= FOOTPRINT_TOP` treats reduce-only and fetch-fail-retained tickers as filled earner slots, stopping the pool read early; later strand-unwind tickers enter desired from outside footprint, so plan gated_out = len(footprint)-len(desired) can under-count or go negative. Telemetry/slot-efficiency semantics only.

====================================================================================================
## S5. KNOWN-OPEN NOTE ASSESSMENTS (fed to reviewers per handoff §0; 79 assessments)

- (commits:0b+recorder+DB-DG) **F5 own-size double-count fix (4d76994 + f932797 guard)** -> Correct as shipped on the run_once path. Verified: own_orders threaded through all three run_once call sites (desired_quotes L5287, telemetry row L5388, full-size cache row L5416) via _own_order_levels(_f5_standing.get(t)); dry-run correctly passes {} (simulated orders never in the public book — blind-review BLOCKER #1 fix present at L4180); legacy byte-identical when own_orders absent (checked algebraically: total>0 always in the raw qualifying walk, so the max(total,1e-9)/`our>0` rewrite changes nothing without own_orders); qualification deliberately judged on the raw book, matching the venue counting our depth toward Target; subtraction window [lowest_q, ref] is consistent with the walk including the full final level; _f5_standing is defined on every surviving code path (failure paths return 0). Remaining gaps are the ws-daemon hot path (finding above), the fill-race staleness (finding above), and the already-recorded NOTE #9 sweeper/_sweep_measure + kalshi_capture_sweep paths — I verified those are genuinely inert for live ranking (KALSHI_SWEEP_ENABLED default 0 at kalshi_market_sweeper.py:63; capture_sweep writes kalshi_prospective_capture.json consumed by caprank telemetry only).
- (commits:0b+recorder+DB-DG) **F1 program-period floor** -> Period source verified correct: m['end']/m['life_min'] are built from the incentive PROGRAM row (end_date/start_date, quoter L2030-2035), not market close_time, so FIX-H far-close markets integrate to program end, not close. Timezone clean: parse_iso coerces naive→UTC, utcnow is aware. days_left = life_min*frac = min(remaining, window) — cannot exceed the program window. Caveat (not a defect): pc is the M7 model that over-predicts 2-6x with NO haircut at this gate, now multiplied by up to 31 days — the floor is nearly non-selective on long programs; the only cost of over-admission is presence, consistent with the operator ruling.
- (commits:0b+recorder+DB-DG) **F3 clamped-size floor / EXPLORE_PROBE_CT=0 mirror** -> The follow-up guard (EXPLORE_PROBE_CT > 0, L1325) is correct and matches the actual sizing clamp's guard — without it the code default 0 would have zeroed every explore market's expectation. _d3_est_ct verified side-effect-free (no setdefault, never starts a ramp clock). The restore-ordering dependency is the MAJOR finding above.
- (commits:0b+recorder+DB-DG) **F14 keep-grace persistence** -> Correct. Grace map persisted (st['d3_last_desired'] filtered to first_seen keys), restored in BOTH paths (_d3_first_seen_ensure L570-575 and the quote-loop belt L5326-5332 per review #7), absence clocks never backdate (setdefault to now on first observation), unbounded-growth leak for never-ramped tickers pruned (review #6). Prune keeps `t in desired` alive through unlimited unwind-only periods — pre-existing semantics, unchanged by F14. Python 3.13 scoping clean (global declarations present where rebinding occurs).
- (commits:0b+recorder+DB-DG) **F17 wind-down floored at SETTLE_UNWIND_MIN** -> C8 coherence verified: the import-time clamp keeps SETTLE_UNWIND_MIN <= WIND_DOWN_MIN, and _effective_wind_down_min's floor max(3, SETTLE_UNWIND_MIN) guarantees eff >= settle window always, so accumulating quotes can never rest inside the armed taker window; boundary equality (eff == settle) is safe. Applied consistently at both consumers (select_footprint cutoff and the desired_quotes pull), both against program end — correct clock for reward purposes. The halved sub-hour benefit is the NOTE above.
- (commits:0b+recorder+DB-DG) **test_db_reward_fixes.py (9 tests)** -> Ran: 9 passed, EXIT CODE 0 (python -m pytest kalshi_live/test_db_reward_fixes.py -q, Python 3.13.3). Failing-before verified by construction: F5 tests pass an own_orders kwarg that did not exist pre-fix (TypeError), F1 asserts exp >= 1.20 where the old min(1-day) unit yields <1.20, F3 asserts clamped <= full/5 where pre-fix they were equal, F14/F17 call functions introduced by the fix (AttributeError pre-fix). The rewritten test_presence_gate long-thin test retains a refusal pin (sub-$1 period still rejected, presence_skipped counted), so the floor's teeth stay tested. test_score_cache_integrity window widening (900→1200) keeps both original markers asserted — no weakening.
- (commits:0b+recorder+DB-DG) **0a recorder + 0b tool API shapes** -> Verified against canon and the client: credit_history fields amount_cents/created_at match every established consumer (kalshi_credit_feedback, w10, w11, netev_rebuild); _request(base=...) kwarg exists (maker_kalshi_client.py:155) and signing covers path-without-query as required for the v1 host; close dates read from /markets close_time (never ticker stems); pool printed as period_reward/10000 $/day per R1 canon; status=active matches the live quoter's own program fetch; banned rewards_residual/rewards_alloc_by_series not used anywhere in these commits.
- (commits:DA+DC+followups) **D-C #6-part2: operator-designated macro-probe tickers have NO reserved footprint seat (can be crowded out of the select ** -> MAJOR when armed, confirmed in code: the macro branch only force-admits into `rows`; slot allocation (round-robin :2229-2254 or PIVOT density pool :2255+) then ranks by usd_day/score with no macro carve-out, and the score-cache fold of macro-ladder cycles (:5408) can actively derank the ticker (see NOTE finding), making crowd-out self-reinforcing. A crowded-out designation is silent no-probe (macro_probe_admitted increments in selection, macro_probe_markets never does — that counter pair is the detection signal). Should be built (S effort: force-append designated rows into `picked` post-allocation) before relying on D-C data; acceptable to arm a 1-2 ticker pilot with the counter pair watched.
- (commits:DA+DC+followups) **D-C #8: the select_footprint macro-probe hunks are untested.** -> MINOR (folded into the broader test-gap finding — ALL thirteen 7e43554 follow-ups are unpinned, not just the selection hunks). Hunks at :2004-2009, :2041-2052, :2145-2148, :5360-5361. A regression silently kills designation with the same admitted-vs-markets counter divergence as #6-part2. Cheap to pin (S).
- (commits:DA+DC+followups) **D-C #12: a 3ct dust fill suspends a macro probe on an empty book until strand clears.** -> MINOR money risk, MAJOR probe-uptime risk when armed. Verified mechanism: INV_TOLERANCE=3.0 (:1570); abs(inv)>=3 routes to _reducing_quotes (:2868-2869), which returns [] on an empty opposite side; strand-unwind (:5455-5456) skips footprint tickers, so nothing rests at all — the probe (the market's entire purpose) is dead until the book forms or the settle-taker window. Dollar exposure is pennies for true dust (3ct at 1-3c); a large fill correctly goes reduce-only. Worth a macro-specific dust rule before broad arming; tolerable for the initial pilot.
- (commits:DA+DC+followups) **D-C #16: ladder count churn when the extension changes.** -> MINOR for the designed use (empty books — rival depth static, counts stable), worse if designated on active books: counts derive directly from need = target − ext_rivals with NO quantization (join sizing quantizes to multiples of 5 at :2348-2349 precisely to avoid this), so any 1ct rival change re-places up to 6 orders — queue-position forfeiture (the thing LIP pays for) plus write-budget burn. S-effort fix: quantize need to ~25ct buckets.
- (commits:DA+DC+followups) **D-B NOTE #9: sweeper pcap own-size threading NOT done (consumer gated off).** -> NOTE today, becomes a measurement-corruption MAJOR the day Phase-3 wiring consumes pcap. Verified: kalshi_market_sweeper.py:23 'Live ranking DOES NOT consume pcap yet', KALSHI_SWEEP_ENABLED default 0 (:423, :2709). But stored pcap rows are accumulating NOW with the F5 own-size double-count bias baked in — when pcap is wired into ranking, either thread own_orders first or discard the pre-fix rows, or the rank inherits the exact defect F5 fixed.
- (commits:DA+DC+followups) **D-B NOTE #10: settle-ramp uses an absolute anchor.** -> NOTE — deliberate and documented in-code (:1485-1491): _effective_wind_down_min floors at SETTLE_UNWIND_MIN=30 absolute, so sub-~30min programs stay unenterable; scaling the taker window for short programs is named future work. Conservative direction (protects C8 maker-first/taker-second ordering); pure opportunity cost, no money risk. Fine to leave until short-window programs matter to the earn plan.
- (commits:DA+DC+followups) **D-B NOTE #11: F3 asym-join is conservative.** -> NOTE — join_ct = max of the two sides' _capped_join (:1322) as the F3 scaling denominator underestimates expected credit on asymmetric books → extra false refusals right at the $1.20 floor margin. Conservative direction only (never over-admits); the D-A est feed, when armed, is precisely the compensating signal for this class of false refusal. No change needed pre-checkpoint.
- (quoter:1-2600 select+gates) **D-C #6-part2: operator-designated macro-probe tickers have no reserved footprint seat** -> CONFIRMED and sharpened, MAJOR-when-armed (dark now). Two distinct holes: (a) no seat — a designated ticker competes in the ordinary round-robin/rank and, in the SELECT_BUDGET walk (:5043-5087), has no `in MACRO_PROBE_TICKERS` keep, so `_saidno9` can drop it at drop_budget_full/drop_family_budget and the operator's designation silently never rests; (b) the walk's cost estimate is WRONG for macro rows — :5063-5070 clamps est to min(explore 5ct, D3 ramp ct) ≈ $5 while the ladder actually commits up to 2×side_cap ≈ $36 at defaults (D3 sizing exempts macro via reason but the EST path does not mirror that exemption), so when the ticker IS admitted the budget under-reserves ~7x. Both must be fixed before arming D-C.
- (quoter:1-2600 select+gates) **D-C #8: the select_footprint macro-probe hunks are untested** -> Stands. The individual hunks (:2004-2009, :2041-2050, :2145-2146) are simple, but #6-part2 shows the FAILURE is in composition (selection -> budget walk -> quote loop). The missing test is end-to-end: 'a designated ticker under a full budget and a crowded footprint still rests its ladder'. Worth writing before arming, since the feature can silently no-op.
- (quoter:1-2600 select+gates) **D-C #12: a 3ct dust fill suspends a macro probe on an empty book until strand clears** -> WORSE than stated — MAJOR-when-armed. For fills in [INV_TOLERANCE=3, STOP_TAKER_MIN_CT=5) ct on an empty book the suspension is PERMANENT, not until-strand: :2867-2869 goes reduce-only, _reducing_quotes returns [] when the opposite side is absent (:2438-2439, empty book), and the strand taker never fires below STOP_TAKER_MIN_CT — so no exit rests, inventory never clears, and the ladder stays pulled until a rival happens to populate the book. 1-2ct fills are fine (under tolerance); >=5ct clears via strand. The [3,5) band is a deadlock.
- (quoter:1-2600 select+gates) **D-C #16: ladder count churn when the extension changes** -> Confirmed, MINOR-when-armed. counts = int(need*w/wsum)+1 with need = Target - rival depth (:701-713): ANY rival depth change re-sizes all up-to-6 ladder orders and the exact-(side,price,count) diff cancels+recreates them. Queue position is irrelevant at deep discount, so the cost is write-token burn (~12 ops/cycle worst case against the 400 budget) and snapshot-capture gaps during the swap. Quantizing need (e.g. to 25ct) would remove most of it cheaply.
- (quoter:1-2600 select+gates) **D-B NOTE #9: sweeper pcap own-size threading NOT done (consumer gated off)** -> Confirmed still absent: _sweep_measure (:2651-2661) calls _prospective_capture without own_orders, so sweeper pcap for any book we occupy carries the F5 double-count (~0.5 share on a solo book). Agree it is inert while the prospective consumers are off — note the select-budget walk DOES consume the sweeper's 'ref' (:5026-5033), but ref is a price, not pcap, so it is unaffected. Must be threaded before ALLOC_KEY/prospective arming or the F5 fix is only half-landed.
- (quoter:1-2600 select+gates) **D-B NOTE #10: settle-ramp uses an absolute anchor** -> Not re-verified — the settle-ramp block sits outside my 1-2600 range; deferring to the range owner. In-range I confirmed the adjacent piece is coherent: _effective_wind_down_min floors at max(WIND_DOWN_MIN_FLOOR, SETTLE_UNWIND_MIN) (:1498) so proportional wind-down cannot open the taker window ahead of quote-pull.
- (quoter:1-2600 select+gates) **D-B NOTE #11: F3 asym-join is conservative** -> Confirmed and acceptable. :1322 takes max(_capped_join(y), _capped_join(n)) as the denominator, so on asymmetric books the ramp scaling shrinks expected credit MORE than reality — errs toward refusal, the right direction for a floor gate. Leave as-is; the honest fix (per-side scaling) is small but not worth touching before receipts calibrate the model anyway.
- (quoter:2500-5000 sizing+budget) **D-C #6-part2: operator-designated macro-probe tickers have NO reserved footprint seat (can be crowded out of the select ** -> CONFIRMED and slightly broader than stated. The budget walk (:5043-5087) zero-cost-keeps held/incumbent/exit-only tickers but a designated macro ticker takes the full est path and can land in _saidno9 (family cap :5071, budget :5076) — evicted from footprint for the cycle. Additionally the PIVOT early-stop (:5216, break at FOOTPRINT_TOP quoted) can end the loop before an un-ranked designated row is ever tried, and finding 3 shows the L3/explore path can drop it in _cap_probe_slots. Matters only when D-C arms (list empty today); when armed it silently punches holes in the two-sided-snapshot coverage the ladder needs to accrue receipts. Fix is S: add `_t9 in MACRO_PROBE_TICKERS` to the keepset exemption and exempt designation from the PIVOT break.
- (quoter:2500-5000 sizing+budget) **D-C #8: the select_footprint macro-probe hunks are untested.** -> Still true. I traced the three hunks (:2004-2009 force-admit, :2041-2046 late-life/far-close exemption, :2146 vol-gate exemption) and they behave as documented in isolation — but the untested surface is exactly where finding 3 lives (L3 explore taint + explore clamp + _cap_probe_slots interactions were never exercised). A test that runs a designated ticker through a banned-series footprint would have caught it. Raise priority: test before arming, not after.
- (quoter:2500-5000 sizing+budget) **D-C #12: a 3ct dust fill suspends a macro probe on an empty book until strand clears.** -> CONFIRMED (:2867-2869 vs INV_TOLERANCE=3). Worse than 'suspends': on the empty book the reducing path usually finds no bid on the side it must rest (best None -> _reducing_quotes returns []), so the ENTIRE ladder is cancelled and nothing rests — coverage gone AND the dust strands until strand-cross pays a taker exit on a book with no liquidity to cross into. Practical severity MINOR-when-armed: dust at 1-3c is cents of risk, but the coverage interruption defeats the probe's purpose for the duration. A dust-tolerance carve-out (hold the ladder, park the dust for settlement) is a reasonable S fix.
- (quoter:2500-5000 sizing+budget) **D-C #16: ladder count churn when the extension changes.** -> CONFIRMED. need = target − rival_ext is recomputed every cycle (:702) and counts feed the exact-match diff (:3482-3491), so ANY rival depth drift re-issues cancel+create for up to 6 orders. Cost is write tokens and a momentary one-sided gap between cancel and create (a 1s snapshot landing in the gap voids that side for everyone). Queue position is irrelevant at deep discount (score is size×DF, not time priority), so this is cheaper than it looks. MINOR; a need-hysteresis band (re-ladder only when need moves >X%) is S/M.
- (quoter:2500-5000 sizing+budget) **D-B NOTE #9: sweeper pcap own-size threading NOT done (consumer gated off).** -> Confirmed at :2661; carried as a NOTE finding. Correctly dormant (ALLOC_KEY=0). Must be a hard precondition on the Phase-3/ALLOC_KEY arming checklist — the un-threaded pcap reproduces exactly the F5 steering error inside the capital key.
- (quoter:2500-5000 sizing+budget) **D-B NOTE #10: settle-ramp uses an absolute anchor.** -> Confirmed: the ramp scale anchors on global WIND_DOWN_MIN (:3183) while the wind-down gate uses _effective_wind_down_min (:2839, :1482-1499), which can sit well below it for short-life programs (floor = max(WIND_DOWN_MIN_FLOOR, SETTLE_UNWIND_MIN)). Between effective wind-down and the global anchor, scale floors at 0 and both sides pin at MIN_QUOTE_CT=2. Direction is conservative (small at settlement) so it only under-earns short windows; acceptable to leave, worth folding into any per-market ramp rework (C13 already made ramp_min per-market — the anchor just didn't follow).
- (quoter:2500-5000 sizing+budget) **D-B NOTE #11: F3 asym-join is conservative.** -> Confirmed at :1322 — join_ct = max of the two sides' joins, so eff_ct/join_ct under-scales expected credit whenever the sides' joins differ. Fails toward refusing entry (higher bar), consistent with a floor gate. Fine as-is; note that finding 9 is the mirror-image gap (the NETEV fallback got NO F3 scaling at all), which is the one worth fixing.
- (quoter:4900-7317 exec+exits) **D-C #6-part2: operator-designated macro-probe tickers have NO reserved footprint seat (can be crowded out of the select ** -> Confirmed in-range and slightly wider than stated: in the select-budget walk (:5043-5082) a designated ticker gets no exemption — it can land in _saidno9 via the family cap or total budget — and the PIVOT FOOTPRINT_TOP early-stop (:5216) can end the loop before reaching it at all. Additionally, when it IS admitted, its est is D3-ramp-clamped via _d3_est_ct (:5067-5070) even though _d3_apply_ramp deliberately exempts reason='macro_probe' quotes (:646), so the walk under-charges the macro ladder's real dollars (cap_desired backstop still bounds capital). Dormant today (MACRO_PROBE_TICKERS empty). Matters: yes — fix a reserved seat + honest est BEFORE arming D-C, else designation is silently ineffective on crowded cycles. Severity when armed: MAJOR for D-C's purpose; today MINOR.
- (quoter:4900-7317 exec+exits) **D-C #8: the select_footprint macro-probe hunks are untested** -> Outside my line range (selection); risk is concentrated at arming time and compounded by #6-part2 above. Matters: medium — the D-C arming checklist should include a pinned test that a designated ticker survives selection, budget, and FOOTPRINT_TOP on a crowded footprint.
- (quoter:4900-7317 exec+exits) **D-C #12: a 3ct dust fill suspends a macro probe on an empty book until strand clears** -> Mechanism confirmed plausible from in-range code: INV_TOLERANCE=3.0 means a 3ct fill counts as held; holding=>exit-only strips the accumulating ladder, and on an empty/one-sided book the strand path can only count strand_no_exit_side (:5477-5478) — no exit can rest, no cross can price, so the probe stays suspended indefinitely. Matters when armed (probe tickers are chosen FOR thin books, so this is the probe's home regime, not an edge). Suggest a dust carve-out (naked < STOP_TAKER_MIN_CT does not strip macro_probe quotes) at arming. Today MINOR (flag off).
- (quoter:4900-7317 exec+exits) **D-C #16: ladder count churn when the extension changes** -> Real but cheap: diff_orders keys on exact (side,price,count), so an extension-driven count change cancels+recreates ladder levels (write tokens, queue position). At deep-discount levels queue position is worth little (they exist to meet Target Size; fills there are rare), so the cost is mostly token spend — WRITE_SPACING keeps it under the rate ceiling. MINOR when armed; acceptable to leave.
- (quoter:4900-7317 exec+exits) **D-B NOTE #9: sweeper pcap own-size threading NOT done (consumer gated off)** -> Consumer (SCORE_RANK/ALLOC_KEY) is off in code defaults and not in the live flag list; the un-threaded own-size only biases the sweeper's prospective capture, which today feeds nothing live. Fine to defer; must be closed before SCORE_RANK/ALLOC_KEY arming or the rank inherits the exact F5 self-count bias just fixed in scoring.
- (quoter:4900-7317 exec+exits) **D-B NOTE #10: settle-ramp uses an absolute anchor** -> Confirmed: the late-life size ramp scales on (mins_left - WIND_DOWN_MIN)/(ramp_min - WIND_DOWN_MIN) (:3183) with the ABSOLUTE WIND_DOWN_MIN=45, while F17's _effective_wind_down_min (:1482-1499) lowers the actual pull time for short windows (floored at max(3, SETTLE_UNWIND_MIN)). Net effect: on short programs sizes taper toward zero well before the F17-kept window ends, partially forfeiting the capture F17 recovered — real but bounded money. MINOR; worth aligning the ramp anchor to _effective_wind_down_min in the same knob family as F17.
- (quoter:4900-7317 exec+exits) **D-B NOTE #11: F3 asym-join is conservative** -> Conservative direction (smaller join than theoretically allowed) — costs some reward share, risks nothing. Fine to leave; revisit only with D-E fills-vs-distance data in hand.
- (daemon+client+wsfeed) **F5 own-size double-count (own_orders threading)** -> Correct and complete on the cold path — all three run_once desired_quotes call sites pass _own_order_levels(_f5_standing.get(t)) (quoter :5291, :5390, :5419) and the model helpers treat own_orders=None as legacy byte-identical. NOT threaded into the daemon's hot path — reported as the first MAJOR finding.
- (daemon+client+wsfeed) **B3-1 partial-fill refill guard (cancel-response clamp)** -> Structurally sound as designed: place=min(want, remaining), None remaining -> no re-create, zero remaining -> hand to cold. Residual risk is only the unverified remaining_count_fp scale (MINOR finding) — must be probed live before WS_HOT arms.
- (daemon+client+wsfeed) **B2-1 cold/hot mutual exclusion** -> Verified: cold_lock is held across the whole run_once AND across every _exec_hot write; _abort_ok re-checks built_mono/STOP/in_cold before each venue write; no lock-order inversion (std_lock is never held while acquiring cold_lock); ctx.build mutation cannot overlap loop-thread reads because in_cold is set on the loop thread before to_thread starts and hot_event never yields.
- (daemon+client+wsfeed) **H7-1b headroom reservation** -> The delta(signed sum) vs spent(sum of max(0,.)) asymmetry was checked across all execute/abort permutations: every mismatch under-counts headroom (conservative — hot refuses more than it should until the next cold rebuild). No unsafe direction exists.
- (daemon+client+wsfeed) **Stage C mirror shape contract** -> Verified end-to-end: mirror_book returns {yes_dollars, no_dollars} with [[price_str, size_str]] ascending rows; _get_book's consumers read exactly ob.get('yes_dollars')/ob.get('no_dollars') through _levels; empty-clean mirror maps to the existing empty_books counter; provider exceptions are counted (src_err) and degrade to REST; the _get_book use at quoter :5459 is the resting strand-exit path (a resting quote), consistent with the daemon's 'crossing reads stay REST' claim.
- (daemon+client+wsfeed) **Write-retry / double-create audit (task hunt item)** -> Clean: _write sends once with no retry in both transport paths; pooled path sets urllib3 retries=False and re-raises HTTPError so a 4xx can never read as success; ambiguous create failure in _exec_hot invalidates ctx and defers to the next cold cycle's REST standing read, which absorbs a ghost order via the diff. No double-create path found in these files.
- (daemon+client+wsfeed) **STOP-under-daemon behavior** -> Verified: cycle_req honored under STOP, WS_STOP_COLD_S=60 equals normal cadence, hot dead via stop_file precheck and _abort_ok, ctx not built under STOP. Matches the re-review REGRESSION note.
- (recorder+ledger+estrec) **Defect 13 fix (position-aware cash, delegation to kalshi_attribution_ledger.replay_fills)** -> Correctly implemented: kalshi_cash_recorder.cum_fills_cash (:167-168) delegates to the validated model rather than reimplementing; import is side-effect-free as claimed; the sign convention (acquired-outcome, position-aware open/offset) matches the 51/51-settlement-validated model. The ACTION-ONLY yes-signed canon trap is correctly treated as an ORDER descriptor, not a cash sign, in both fill_outcome and order_reservation (:96-109, which correctly keys reservation off outcome_side and documents why action is the wrong key).
- (recorder+ledger+estrec) **Defect 14 fix (settled-market reconstruction in kalshi_fill_costs.build)** -> Happy path is correct: revenue (cents/100, matching the 127/127 measurement) + position-aware replay cash = realized, never shadowing a live row, correctly avoiding the yes/no_total_cost_dollars phantom-loss trap. The defect found (MAJOR finding) is confined to the replay-EXCEPTION fallback path and its silence, not the fix itself. The quoter's in-process refresher (maker_kalshi_quoter.py:2667-2705) supplies settlements correctly and counts a settlements-fetch failure.
- (recorder+ledger+estrec) **W8 settlement-revenue basis (venue revenue/100 everywhere)** -> Verified consistent across all three consumers: kalshi_cash_recorder.settlement_payout (:236), kalshi_attribution_ledger.settlement_revenue (:258), kalshi_fill_costs settled branch (:129). The convention markers (settle_payout_basis / fills_cash_basis) and the missing-`revenue` tripwire are present and test-pinned. No fee double-count remains between fills and settlements in any of the three modules (fees live inside fill cash only; settlement fee_cost is correctly ignored as a roll-up).
- (recorder+ledger+estrec) **Estimates recorder centicents and pagination conventions** -> Conversion is correct at both ends (recorder print /10000 at :80, quoter table /10000 at :771, matching 10000cc=$1 canon). The `next_cursor` pagination key for /incentive_programs is CORRECT — confirmed against kalshi_capture_sweep.py:86 which explicitly documents that this endpoint family paginates with next_cursor, NOT cursor. The quoter-side ended-program exclusion (review #4, banked-dollars-never-floor-entry) and torn-line cushion (review #9) both behave as documented.
- (recorder+ledger+estrec) **F5/F1/F3/F14/F17 recent fixes** -> All five live in the quoter's scoring/floor/ramp/unwind paths, outside the four files in this review's scope; nothing in these measurement modules interacts with them except the fill-cost feed the ramp/rank reads, covered by the MAJOR fill-costs finding and the health-counter opportunity. No side-effects of those fixes were visible from this scope.
- (recorder+ledger+estrec) **Timezone / window boundaries** -> All four modules are consistently UTC (datetime.now(timezone.utc) throughout); JSONL month-file naming, the ledger report's lexical file sort and per-day ts[:10] bucketing, and ISO-string watermark comparisons are all UTC-coherent. No local-time or naive-datetime defect found. The only boundary defects found are the watermark late-arrival class (finding at :315) and the non-atomic balance-vs-fills snapshot skew (NOTE at :279).
- (scores+rank+feedback) **F5 own-size double-count fix (own_orders threading)** -> Verified correct at every LIVE call site: desired_quotes (:5287-5291), telemetry row + score-cache fold incl. the D8 full-size recompute (:5388-5419), _expected_credit_usd -> _prospective_capture -> _qualifying_score chain (:1316, :2596-2600, :2570-2577 with the p<=ref stale-book guard and rest_df<=df_total clamp), and dry-run correctly zeroes _f5_standing (:4180, simulated orders never in the public book). Qualification deliberately judged on the raw book (venue counts our depth toward Target) — matches spec. RESIDUAL: both sweeper paths omit own_orders (reported as MINOR finding).
- (scores+rank+feedback) **F1 floor over remaining PROGRAM period** -> Correctly implemented: payout_days = life_min/1440 * frac_left (:1318-1320), per-period unit with two-directional evidence documented. Remaining gap (by design, not a bug): already-ACCRUED credit is not added, so a late-window flat entry whose program total clears the floor can be refused on remaining-only expectation — conservative; the continuation split (:2922-2935) covers resting markets, and arming D-A (venue estimate floor, :1334-1340) closes the flat-entry case.
- (scores+rank+feedback) **F3 floor at ramp-clamped size** -> Correct and side-effect-free (_d3_est_ct never starts a ramp clock). Two soft edges: (a) scaling uses max(join_yes, join_no) so the smaller side is over-scaled down — conservative; (b) the probe branch's SERIES_ALLOW guard differs from the sizing clamp's guard — reported as MINOR finding (empty-allowlist config).
- (scores+rank+feedback) **F14 ramp keep-grace D3_KEEP_S persisted** -> Verified: _D3_LAST_DESIRED persisted to state (:612-613), restored in BOTH the lazy quote-loop path (:5330-5332) and _d3_first_seen_ensure before the budget walk (:573-577); absence clock never backdates (setdefault now, :599); orphan stamps pruned (:607-611). No defect found.
- (scores+rank+feedback) **F17 proportional wind-down floored at SETTLE_UNWIND_MIN** -> NOT reviewed at line depth — outside the three assigned files and not on the reward-math path reviewed here. Explicitly flagging the gap rather than claiming coverage.
- (scores+rank+feedback) **Pool math / estimates unit / fee-canon traps** -> period_reward/10000 correct at the live row build (:2065) and stand-down (:2531); est feed reward_centicents/10000 correct (:771); orders read uses fill_count_fp (canon *_fp), credit amount_cents/100 matches the client-probed shape; get_orders/get_settlements cursor-paginated, get_credit_history single-shot limit=1000 WITH a matching truncation alarm; df = discount_factor_bps/10000 matches 0.5^ticks via the program's own df. Tests: 86/86 pass across test_score_rank, test_capital_rank, test_d2_feedback, test_score_cache_integrity, test_explore_full_sweep, test_a1_explore_exempt, test_db_reward_fixes.
- (scores+rank+feedback) **A1 explore_exempt rank path** -> Verified: exempt series filtered from both unseen and stale explore queues (kalshi_market_scores.py:279, :291), tags only ever ADDED, allowlist rows never pre-tagged (probe_only=False for allowlist at quoter :2010), deterministic ats/ts+ticker tie-breaks prevent value-ordered sweep leakage. Numeric probe confirmed exempt pre-tagged rows keep their tag (documented containment behavior). Row dicts are rebuilt every cycle at :2064, so explore tags cannot leak across cycles.
- (scores+rank+feedback) **D1 staleness-label fix + penalty aging + shadow_rank stale handling** -> Numerically verified convergence (the 7.69-forever bug is gone; stale label flips at exactly 1800s while the value stays continuous). shadow_rank keeps the decayed measurement for stale rows, applies unknown_haircut to unknown+stale, and substitutes the prospective model ONLY for kind=='unknown' — all matching the documented 08-04 fixes. Verdict grain (series) matches between builder output and consumers (shadow_rank :183, _d3_ramp_ct :547); 'proven' correctly requires credits_n>0 (receipt), not due_filled_events.
- (tools:netev+settlement) **All six receipt-grade families on the deployed table (kalshi_netev_table.json, window 2026-07-24..08-03T17:06Z) read net** -> Verified against the deployed table file and the gate condition (quoter :3015-3018). Already flagged verbatim in the quoter comment (:3004-3014) with a measured margin ladder and the ruling that moving the threshold is the operator's call; the honest fix is the B8 rebuild on post-fix trading. Not re-reported as a bug — but findings 1 and 2 above say the B8 rebuild as currently coded will not produce the clean verdict this plan assumes.
- (tools:netev+settlement) **NETEV_MODEL_HAIRCUT=3.0 was fitted on the UNSHAPED capture model; arming W12_PRICE_SHAPE without re-fitting double-disco** -> Correctly self-documented at quoter :2616-2618 with 'Re-fit at B8'. Checked: the haircut is applied at exactly one point (:3023) and never to receipt rows. No new defect; carried into the catalog as the B8 re-fit item.
- (tools:netev+settlement) **MIN_RECEIPT_FILLS=40 vs the CSV canon's MIN_RECEIPT_TRADES=20 (~24 fills equivalent at the measured 1.1885 fills/row).** -> Deliberate 1.7x-stricter choice, honestly documented (:65-75) including that the original 'equivalence' reasoning was corrected. KXTRUMPENDORSEMENTS-type families near the bar flip verdict on the choice; correctly labeled the operator's to set. One nuance the docstring doesn't state: n_fills counts WHOLE-LIFE fills of in-window-traded markets (consistent with the whole-market rule), not in-window fills.
- (tools:netev+settlement) **Recent fixes F5/F1/F3/F14/F17 (own-size double-count, floor-over-remaining-program, ramp-clamped floor, keep-grace persi** -> These live in the quoter money path, outside this subtask's canon-tools file scope; within scope I verified F5's own_orders is threaded into both _prospective_capture call sites the netev/capture gates use (:3021-3022, :3046-3047) and that the D3 ramp does not interact with the rebuild tools. No side-effects on the four canon tools found.
- (tools:netev+settlement) **Fill-direction sign trap (action-only, yes-signed) and settled-market blindness (defect 14) — the two headline hunt item** -> Both handled correctly in the reviewed tools: kalshi_settlement_pnl.fill_signed_qty is action-only and pinned all-four-cases (test :580-603); kalshi_netev_rebuild uses replay_fills whose fill_outcome reads book_side/outcome_side (ledger :140-152) — verified against real fill shapes in AUD_fills.json. The netev rebuild is NOT built on /portfolio/positions, so defect 14 does not propagate into it; kalshi_fill_costs carries the settlements repair (with the stale-comment and fee-convention findings above). Taker fee formula is never recomputed in these tools — fees come from venue fee_cost fields only, so the 0.07·C·P·(1-P) canon cannot be violated here.
- (tools:netev+settlement) **Test suite state.** -> All 84 tests across the five named files pass (1.61s, read-only run). The suite has unusually strong mutation-derived pins (credits-guard mutant, notional-guard deletion, naive-formula meta-tests). Known gap, self-documented at quoter :2988-2992: the loosened thin-negative-family model path is only exercised at usd_day=50 where the model returns [] — the LOOSENED usd_day>=200 case has no pin.
- (tools:ledgers+w16+w17+w11) **Selection-stage drops are counter-only (drop_* ints in plan rows) so per-ticker attribution is impossible for w17's fall** -> Confirmed: w17 handles it honestly (UNKNOWN 'never selected — NO ATTRIBUTED REASON'), which is the designed alarm, not a mislabel. The two real mislabel vectors found on top of the known class are the 8-day horizon hardcode and the 40-ticker family_dropped truncation (reported as findings). Root burn-down belongs quoter-side (see opportunities).
- (tools:ledgers+w16+w17+w11) **Defect 14 — /portfolio/positions (and thus the kalshi_fill_costs.json feed) is blind to settled markets.** -> kalshi_lumps_ledger.py builds its per-family 'realized damage' entirely on that feed and labels it 'validated feed, live' — its totals silently exclude every loss that became real at settlement. Already operator-named (08-03); not re-reported as a new finding, but the lumps ledger header should carry the defect-14 caveat until the feed is re-based on get_settlements.
- (tools:ledgers+w16+w17+w11) **Recent fixes F5/F1/F3/F14/F17 checked for side-effects on these tools, per instruction.** -> F5 verified in _market_telemetry_row: rest_df subtracted from the denominator only within the qualifying set, clamped to df_total, own_orders=None legacy-safe; the new y/n_rest_df and y/n_rest_ct fields are additive and do not break w17's y_ct/n_ct/gates reads (w17 ignores them — see the intended-vs-resting finding for the missed opportunity). No adverse interaction between F1/F3/F14/F17 and the six reviewed tools found — they consume none of the affected state.
- (tools:ledgers+w16+w17+w11) **Canon traps sweep results.** -> Verified clean: fee_cost is receipt-verified dollars (attribution :176-184); portfolio_value is integer cents (canon M7f) so /100.0 at kalshi_ledger.py:233 is correct; settlement `revenue` is populated cents (attribution :237-246) so reconstruct :98 is unit-correct; incentive_programs pagination via next_cursor with lowercase status=active matches the quoter-proven pattern in both w16 and w17; credit rows' amount_cents/created_at match the client contract; fills carry a real integer `ts` field (verified on AUD_fills.json — kalshi_ledger.py's sort key is valid, a candidate finding was retracted after checking the live tape); no Python 3.13 scoping traps found in the six files.
- (tools:drift-suite) **F5/F1/F3/F14/F17 recent fixes** -> None of the five recent fixes touch these six files; no interaction found. The drift study family predates them (all files dated 07-23) and shares no code with the live quoter.
- (tools:drift-suite) **Canon trap: fill direction is ACTION-ONLY yes-signed; side-derived signs invert ~half the tape** -> signal_book_drift.py:237 uses fl['book_side']=='bid' — checked against the verified feed contract in kalshi_attribution_ledger.py:110-111 (book_side='bid' <-> yes, 317/317 agreement on live tape): the convention is CORRECT, not the side-derived trap. Public-tape taker_side ('yes'/'no') usage in build_tape_minutes is also correct for tape data; 15,333 cached trades all carry a valid taker_side.
- (tools:drift-suite) **Timezone/window defects (task hunt item)** -> No defect found: the receipt CSV carries explicit -04:00 offsets, fromisoformat().timestamp() converts correctly to epoch, and candle end_period_ts is epoch UTC. Residual hazard only if a future CSV export ships naive timestamps (silent machine-local parse).
- (tools:drift-suite) **Label leakage (task hunt item)** -> Detectors verified strictly trailing (all index/time lookups are backward). Future info enters only window-END construction (dead_ts) and lead-time deadlines, which is by design for measuring lead; matched-window/first-K frames are clean. The one real leakage vector is f_at's future-candle substitution (reported as MINOR finding).
- (tools:drift-suite) **Survivorship (task hunt item)** -> Verified no bias materialized: 67/67 labelled positions produced usable states (0 dropped by missing cache/market/short timeline). The fetch resume-freeze hazard is real but latent (reported as MINOR finding).
- (tools:drift-suite) **Eval section A hazard framing** -> Point-level labels inherit the position's final outcome, so pre-drift points of a positive count as positives — this depresses TPR (conservative direction) and is inherent to the stated hazard framing, not a defect.
- (flow:end-to-end trace) **D-C #6-part2: operator-designated macro-probe tickers have NO reserved footprint seat** -> REAL and it matters at arming. Crowd-out points traced: PIVOT pool_cap slot ordering (usd_day/near-money, no macro priority), the SELECT_BUDGET walk (admits but under-charges - see finding 3), cap_desired priority, bound_creates ordering, and the per-order create capital gate (the binding one at live equity ~$290). Force-admission (:2004-2009) only guarantees entry into `rows`, not a rested ladder, and the FOOTPRINT_TOP early-stop can end the quote loop before the macro row is even read. Fix before arming: reserved seat + full-cost walk charge + group-atomic capital admission.
- (flow:end-to-end trace) **D-C #8: the select_footprint macro-probe hunks are untested** -> CONFIRMED test gap, MINOR. test_dc_macro_probe.py (4 tests, passing) covers _macro_probe_quotes/desired_quotes; the selection-time exemptions (:2041-2051 late-life/far-close override, :2145-2146 activity exemption, deny-still-applies at :2021) have no pins. A regression there silently drops the designated ticker - which reads identically to 'the probe found nothing'. Close before arming; cheap.
- (flow:end-to-end trace) **D-C #12: a 3ct dust fill suspends a macro probe on an empty book until strand clears** -> CONFIRMED as a permanent deadlock, worse than stated. inv >= INV_TOLERANCE (3.0) makes the macro branch return _reducing_quotes (:2867-2870), pulling the whole two-sided ladder; on the empty rival book the exit side has no best price so _reducing_quotes returns [] - no exit rests at all; strand cross needs >= STOP_TAKER_MIN_CT (5.0) so it never fires. The probe stays dark until a rival appears or the market closes. Money-trivial (~$0.09 at 3c) but it defeats the receipts test on exactly the empty books D-C targets - the failure mode reads as 'never pays'. Needs a dust carve-out (keep the ladder alongside sub-threshold dust, or a macro-only taker floor) before arming.
- (flow:end-to-end trace) **D-C #16: ladder count churn when the extension changes** -> MINOR. need = Target - (ext - own) is stable while rivals are stable (the own-size subtraction at :700-702 removes self-churn); counts only move on rival-depth change, and a churn tears down/rebuilds <=6 orders (~72 write tokens) at 1-3c levels where queue position is worth little. Acceptable; watch cancel/create counters on designated tickers after arming.
- (flow:end-to-end trace) **D-B NOTE #9: sweeper pcap own-size threading NOT done (consumer gated off)** -> Premise WRONG: with ALLOC_KEY=1 and SWEEP_ENABLED=1 recorded live (08-04 EOD env listing), _alloc_priority consumes pcap for 'unknown' markets - see finding 7. Impact bounded but the note should be re-labeled live-consumed; thread own_orders through _sweep_measure before relying on alloc ordering.
- (flow:end-to-end trace) **D-B NOTE #10: settle-ramp uses an absolute anchor** -> REAL - promoted to finding 6. The ramp reaches MIN_QUOTE_CT at T-45min while effective wind-down lets the market quote to T-30/36; a 9-15 minute per-window capture haircut on every short/mid program. One-line fix candidate (anchor on _effective_wind_down_min(life_min)); operator-gated.
- (flow:end-to-end trace) **D-B NOTE #11: F3 asym-join is conservative** -> CONFIRMED conservative-only. join_ct = max of the two sides' _capped_join under-scales eff/join on skewed books, so the floor refuses marginal markets it could keep. Safe direction; revisit when the estimates feed can calibrate the whole expected-credit chain instead.

====================================================================================================
## S6. OPPORTUNITY CATALOG (ruling 4a: cataloged only, nothing built; reviewer order as returned — unreordered)

**O-1 [S] Make _d3_first_seen_ensure unconditional at cycle start when D3_RAMP** (rev: commits:0b+recorder+DB-DG)
  One call to _d3_first_seen_ensure(st) before the quote loop (not gated on SELECT_BUDGET) removes the rung-0 restore-ordering deadlock (MAJOR finding) and makes the F3 gate independent of flag combinations. The function is already idempotent.
  EV: Eliminates a latent book-wide under-admission mode after restarts; no behavior change under today's live flags.

**O-2 [M] Upgrade the share model's reference price to the venue's Target/5 cumulative walk** (rev: commits:0b+recorder+DB-DG)
  Change _qualifying_score/_qualifying_breakdown (and the kalshi_market_scorecard source replica) to walk cumulative size to Target/5 for the reference, 1.0x at/better, df^ticks below, and score all resting orders on a qualifying side. Re-fit NETEV_MODEL_HAIRCUT afterwards (the same coupling is already documented for W12).
  EV: Correct rival weighting on thin-top books, decoy resistance, and a genuinely formula-matched model under the F5-corrected shares — improves every gate and the SCORES ranking; also sharpens B8 calibration.

**O-3 [S] Thread own_orders through the WS hot path and the offline sweeps** (rev: commits:0b+recorder+DB-DG)
  hot_event already holds ctx.standing — pass own_orders=_own_order_levels(standing_t) into M.desired_quotes; same one-liner for _sweep_measure and kalshi_capture_sweep (needs a standing snapshot, or an explicit rivals-only caveat in their output).
  EV: Restores hot repricing on solo/thesis books under WS_HOT=1; de-biases the two offline capture feeds before any Phase-3 consumer is armed.

**O-4 [M] Scale the settle-taker window for short programs (F17 second half)** (rev: commits:0b+recorder+DB-DG)
  SETTLE_UNWIND_MIN=30 is sized for multi-day markets' settlement liquidity; sub-hour temp markets could use min(30, frac*window) with a floor, unlocking the remaining ~19 min of the 58-min window F17 targeted. Needs a taker-cost measurement on temp books first (the follow-up docstring already names this as future work, not built).
  EV: Roughly +65% enterable presence on hourly temp programs (28→~47 of 58 min), the highest-frequency window class in the allowlist.

**O-5 [S] Per-market payout-timing attribution via the estimates program map** (rev: commits:0b+recorder+DB-DG)
  0b currently attributes credits to event max-close. The 0a recorder's kalshi_program_map.json gives program_id→market_ticker; joining estimates history to credit_history at program granularity makes BEFORE_CLOSE/CLOSE_WINDOW tags exact per market and settles close+1 vs program-end cleanly at the 08-07T15:00Z APRPOTUS checkpoint.
  EV: Converts the 0b discriminator from suggestive to decisive; directly gates the D-A arming decision and the FIX-H cash-lock model.

**O-6 [S] Atomic write + raw-row retention for kalshi_program_map.json** (rev: commits:0b+recorder+DB-DG)
  Copy the _refresh_fill_costs tmp-then-os.replace pattern; optionally append program rows into the monthly jsonl as well so the map is reconstructible from the raw record.
  EV: Protects the est→credit join (the D-A truth chain) from a single crash erasing ended-program mappings.

**O-7 [M] Period-length-aware haircut on the F1 integral** (rev: commits:0b+recorder+DB-DG)
  pc assumes today's book persists for the whole remaining period (up to 31 days). A simple decay or cap (e.g. integrate at most N days at full pc, discounted beyond) between pc and the floor would restore selectivity on very long programs without re-refusing the audit's spread-thin payers. Calibratable from the estimates feed's observed accrual slopes once D-A data accumulates.
  EV: Reduces presence wasted on long programs whose competition arrives later; measurable from recorder data before touching the gate.

**O-8 [S] Per-level clamp for F5 standing staleness** (rev: commits:0b+recorder+DB-DG)
  Cap the subtraction per price level at the fetched book's actual size at that price (the book cannot contain more of us than it shows at our level), instead of only the aggregate min against df_total in the telemetry row and no clamp at all in _qualifying_score.
  EV: Removes the one-cycle anti-conservative over-admission after a fill; also hardens against remaining_count parse drift.

**O-9 [S] Reserved footprint seat for designated macro tickers** (rev: commits:DA+DC+followups)
  After slot allocation in select_footprint, force-append any designated row not already in `picked` (bounded by len(MACRO_PROBE_TICKERS), operator-bounded by construction). Closes #6-part2 and the score-derank compounding; makes macro_probe_admitted == macro_probe_markets the steady-state invariant.
  EV: High when arming D-C — without it the feature can silently not run on exactly the low-pool markets the thesis targets.

**O-10 [M] All-or-nothing macro placement: cancel the side's rungs if any rung fails to rest** (rev: commits:DA+DC+followups)
  Track macro-rung create results; on a post-only reject (or any placement failure) of one rung, cancel the side's sibling rungs that cycle. Extends the build-time rest-NOTHING rule to placement time, closing the partial-ladder MAJOR. Alternative cheap guard: refuse the ladder at build time when 1 − best_opposite_bid <= MACRO_PROBE_TOP (would-cross check).
  EV: Eliminates the one armed-path scenario where the probe rests unqualifiable depth at near-fair prices (bounded but pure-loss fill risk).

**O-11 [S] Quantize macro ladder counts** (rev: commits:DA+DC+followups)
  Round `need` up to a bucket (e.g. 25ct) before splitting 40/30/30, mirroring the join path's multiple-of-5 quantization rationale (:2342-2349). Ladder only re-places when rival depth moves a full bucket.
  EV: Preserves queue position and write budget on designated tickers with any rival activity (kills #16).

**O-12 [M] Macro-market dust handling** (rev: commits:DA+DC+followups)
  For designated tickers with 3ct <= |inv| < a macro dust bound, keep resting the ladder with the filled side reduced by |inv| instead of suspending entirely (or rest a deep reduce order alongside). Operator decision on the bound (never-demote rule: propose, don't silently raise INV_TOLERANCE).
  EV: Probe uptime: a single 3ct fill currently kills the designated market's entire reward capture until strand clears (#12).

**O-13 [M] Est-feed tail read + knob-map completeness + follow-up tests** (rev: commits:DA+DC+followups)
  (a) Read the estimates file tail via seek from EOF instead of full-file scan; (b) add KALSHI_EST_FEED_MAX_AGE_S to the restart_only map; (c) pin the 7e43554 follow-ups: ramp exemption, self-depth subtraction (own = a standing ladder), 0.9*HELD_MAX floor at live defaults, ended-program exclusion, MIN_FRAC guard, torn-line retention, wall refusal, late-life/far-close/incumbent overrides.
  EV: Hygiene bundle: hot-path IO bound, closes the one remaining silent-knob case, and locks the review fixes against regression before arming.

**O-14 [M] Post-checkpoint est semantics upgrade: banked vs marginal decomposition** (rev: commits:DA+DC+followups)
  Once the 2026-08-07T15:00Z APRPOTUS est→credit mapping is known, replace max(model, est) with an explicit decomposition: if banked_est >= $1 the floor is CLEARED (gate on marginal model credit vs marginal fill risk instead of vs $1); if banked < $1, gate on banked + model_marginal >= floor. Removes the activate-path over-admission and makes EST_FEED_MIN_FRAC unnecessary.
  EV: Turns the est feed from a blunt floor-override into the correct marginal-economics input — the reward-capture upside D-A was ruled for, without the banked-dollars fill-risk leak.

**O-15 [M] Fall-through for designated tickers whose Target is met by rivals** (rev: commits:DA+DC+followups)
  When both sides qualify on rival depth alone, _macro_probe_quotes returns [] and the market rests nothing — designation currently forfeits normal join earning on a book that has become competitive (i.e. proven the thesis). Option: fall through to the normal quality-gated join path in that state.
  EV: Captures the payoff phase of a successful probe — the moment the probed market develops is exactly when share is earnable; today the bot exits the game it just seeded.

**O-16 [S] Coverage-ledger bucket for macro designations (CLASS NOT INSTANCE)** (rev: commits:DA+DC+followups)
  Extend w17_coverage_ledger to bucket each designated ticker per cycle: LADDER_RESTING / UNAFFORDABLE / CROWDED_OUT (admitted but unselected) / DENIED / NO_PROGRAM / SUSPENDED_DUST. Directly instruments every silent-no-op mode found in this review.
  EV: Makes D-C's health a daily-read fact instead of a counter-diff exercise; UNKNOWN bucket is the alarm per the 08-06 feedback rule.

**O-17 [M] Implement the venue's Target/5 Reference Price walk in _qualifying_score/_qualifying_breakdown** (rev: quoter:1-2600 select+gates)
  Compute per-side ref = first level where cumulative size >= target/5 (walking from best bid); score 1.0x for at/better, DF^ticks beyond, keeping the existing cum>=target qualification. Pin against synthetic books where touch depth < Target/5. Re-fit NETEV_MODEL_HAIRCUT/W12 afterward — part of the current 2-6x M7 over-prediction is exactly this anchor error, so the haircut is compensating for a formula defect.
  EV: Corrects the live $1.20 floor gate in both directions (fewer sub-$1 drive-bys AND fewer false refusals), and puts SCORE_RANK/D2/telemetry capture on the venue's actual formula — the single highest-leverage correctness item in this range.

**O-18 [M] Give the $1 floor gate accrued-credit memory (accrued + remaining >= floor)** (rev: quoter:1-2600 select+gates)
  Persist a per-program accrued-credit estimate (venue est feed when armed; model integral of past expected-$/cycle otherwise) in quoter_state, and test `accrued + expected_remaining >= MIN_CREDIT_USD` instead of remaining-only. This subsumes the continuation exemption, fixes the one-cycle-absence amnesty (finding 5), the activate-path asymmetry (finding 4), and replaces D-A's max(model, est) with the economically correct sum once the 08-07 APRPOTUS checkpoint settles est semantics.
  EV: Directly rescues the spread-thin-earn-zero class the reward audit measured; every sub-$1 accrual pushed over the cliff is pure found money at zero added risk beyond presence we already chose.

**O-19 [M] Macro-probe arming pack: reserved seat + honest budget est + dust-fill escape** (rev: quoter:1-2600 select+gates)
  (a) force-keep MACRO_PROBE_TICKERS in the SELECT_BUDGET walk charging the real ladder cost (2x side_cap), not the $5 explore/ramp clamp; (b) quantize ladder `need` to ~25ct steps to kill the #16 rebuild churn; (c) for inventory in [INV_TOLERANCE, STOP_TAKER_MIN_CT) on an empty opposite book, let the ladder co-rest with a reducing quote (or exempt macro from the strand min) so a 3ct dust fill cannot permanently kill the probe.
  EV: Without this, arming D-C can silently no-op (crowd-out), overshoot the capital budget ~7x per market, or wedge permanently on a dust fill — all three block the receipts the whole D-C thesis exists to buy.

**O-20 [S] Env-knob hygiene sweep: strip SERIES_ALLOW, warn on malformed values, extend the restart-only list** (rev: quoter:1-2600 select+gates)
  Strip SERIES_ALLOW entries (one-line parity with SERIES_DENY) and register it in _ENV_DECLARED; make _envi/_envf print one WARNING and bump a _SILENT counter on parse failure; add PRESENCE_GATE, D3_RAMP, PIVOT_SELECT, SCORE_RANK, D2_FEEDBACK, FARCLOSE_PAYING_EXCEPTION, DROP_GRACE to _refresh_safety_knobs' restart_only warning dict; make the watch-tier malformed-value branch loud like the restart-only tier already is; optionally accept true/yes/on in _envb (or warn on non-'1' truthy values).
  EV: Closes four silent-misconfiguration traps around live money knobs (findings 3, 6, 7, 8) for a few dozen lines; the SERIES_ALLOW strip alone removes a one-space path to silently benching receipts-proven payers.

**O-21 [S] Record the venue-formula reference level per side in the telemetry row** (rev: quoter:1-2600 select+gates)
  Add y_ref5/n_ref5 (the Target/5 cumulative level) alongside the existing y_ref/n_ref in _market_telemetry_row — one extra walk over levels already in hand, zero reads. Emitting it BEFORE the model fix lands lets the est-feed/credit receipts calibrate best-bid-anchor vs Target/5-anchor predictions against real payouts (feeds the D-E fills-vs-distance study and B8).
  EV: Turns the reference-anchor question from argument into measurement using the estimates feed already being recorded every 5 minutes.

**O-22 [S] Always-emit completeness: close_past_selected + missing-ticker drop counter** (rev: quoter:1-2600 select+gates)
  Add 'close_past_selected' to _ALWAYS_EMIT_DROPS and count the null market_ticker drop (drop_no_ticker) in select_footprint.
  EV: Two-line change that keeps the A3 'absence means not-evaluated' contract intact for the newest selection stages.

**O-23 [S] Bound the PIVOT coverage floor by pool_cap** (rev: quoter:1-2600 select+gates)
  Cap the coverage loop's picks at pool_cap (or interleave coverage/density under one cap) so PIVOT_READ_RESERVE holds for any series count — matters before any full-universe (allowlist-off) run.
  EV: Preserves the strand/ladder/settle read reserve — the reads that protect held inventory — under exactly the config where the pool is biggest.

**O-24 [S] Reserve macro-probe seats: keepset-exempt MACRO_PROBE_TICKERS in the budget walk and the PIVOT early-stop** (rev: quoter:2500-5000 sizing+budget)
  Add `_t9 in MACRO_PROBE_TICKERS` to the zero-cost keep at :5057 (still charge est to _tot9 if preferred) and bypass the FOOTPRINT_TOP break for designated rows at :5216. Closes D-C #6-part2 before arming.
  EV: Guarantees the operator-designated receipts experiment actually runs every cycle; without it D-C results are confounded by silent coverage holes.

**O-25 [S] Exempt reason='macro_probe' from the explore clamp and designation from the L3 series taint** (rev: quoter:2500-5000 sizing+budget)
  Mirror the _d3_apply_ramp exemption (:646) in the explore clamp at :5313, and skip MACRO_PROBE_TICKERS rows in both L3 re-clamp sites (:4095, :4580) and in _cap_probe_slots eligibility (:3265). One review pass + a designated-ticker-through-banned-series test.
  EV: Removes the finding-3 failure mode entirely; mandatory before D-C arms on any ticker whose series could take a ban.

**O-26 [M] Venue-true reference: Target/5 cumulative-walk reference in the share model behind a flag** (rev: quoter:2500-5000 sizing+budget)
  Implement the published reference walk (first level where cum >= Target/5; 1.0x at/better, DF^ticks beyond) as a flag-gated variant shared by _qualifying_score, _qualifying_breakdown, the telemetry row and the scorecard; re-fit NETEV_MODEL_HAIRCUT and the W12 exponent on receipts at B8 on the corrected basis.
  EV: Fixes the systematic share over-estimate feeding two LIVE gates and every capture measurement; likely explains part of the M7 2-6x over-prediction the haircut currently papers over.

**O-27 [M] Revive the activate lane: model the activate credit on the book AS SEEDED, not the book as found** (rev: quoter:2500-5000 sizing+budget)
  In the activate-path presence check (:3105), compute expected credit with our intended add_y/add_n injected into the walk (we ARE the missing Target depth; our share on a book we complete is large by construction). Pair with the complete-to-Target-or-nothing rule.
  EV: Re-opens the only organic route into low-competition/empty books — the exact pools the reward audit says the thesis targets — instead of leaving it entirely to per-ticker operator designation.

**O-28 [S] Complete-to-Target-or-nothing on activate (mirror the macro-probe rule)** (rev: quoter:2500-5000 sizing+budget)
  At :3122-3131, if min(add, INV_HARD_CT clamp) cannot bring ext+add to target on BOTH sides, rest nothing and count it (macro_probe_unaffordable's sibling). Removes the finding-4 non-qualifying rest.
  EV: Eliminates a class of $0-earning fill risk; makes the gate and emitter agree on affordability.

**O-29 [S] Apply F3 ramp-size scaling to the NETEV model fallback and capture gate** (rev: quoter:2500-5000 sizing+budget)
  Scale pc by eff_ct/join_ct (as _expected_credit_usd does at :1321-1331) before the :3023 signal and the :3050 floor comparison.
  EV: Stops admitting unproven families on 10x the size that will actually rest; tightens the sub-$1 drive-by leak with NETEV_GATE live.

**O-30 [M] Tie join-side fates on create failure** (rev: quoter:2500-5000 sizing+budget)
  If one side of a two-sided join fails to create (or is ratchet-blocked) for N consecutive cycles, withhold/cancel the other accumulating side until both can rest. Keep unwind exemptions untouched.
  EV: Closes the one-sided-lean window (finding 6); small but it fires exactly when the pricing source is degraded — the worst time to lean.

**O-31 [S] Macro-ladder need hysteresis** (rev: quoter:2500-5000 sizing+budget)
  Re-issue the ladder only when need drifts beyond a tolerance (e.g. 5% of Target) or a level empties; otherwise keep counts byte-stable so the diff no-ops.
  EV: Kills the D-C #16 churn: saves write tokens and removes the cancel-create gaps that void snapshots on the probed market.

**O-32 [M] Split cap_desired's unwind-keep: keep only the reducing quotes unconditionally** (rev: quoter:2500-5000 sizing+budget)
  At :3731-3736 keep the unwind subset unconditionally and route accumulating siblings of the same ticker through the priority walk. Precondition for arming PAIR_UNWIND.
  EV: Restores allocation-priority integrity for any future mixed-list producer; prevents the finding-5 bypass class structurally.

**O-33 [S] Count amends in the write budget and add a monotonic component to order_id_for** (rev: quoter:2500-5000 sizing+budget)
  Charge len(amends) against WRITE_BUDGET_PER_CYCLE in bound_creates, and add a process-monotonic counter to the cyc nonce (:3535).
  EV: Two cheap hygiene closes: honest write-rate ceiling; zero same-second client_order_id collisions.

**O-34 [S] Thread own_orders through _sweep_measure** (rev: quoter:2500-5000 sizing+budget)
  Pass the sweeper's view of our standing orders (shared snapshot or a callable) into _prospective_capture at :2661. Gate: must land before ALLOC_KEY/pcap consumers arm.
  EV: Completes F5; without it the future capital key halves the rank of exactly the solo books the thesis targets.

**O-35 [S] Add fetch-fail retention to the strand-unwind loop (mirror of the quote loop's :5234-5258)** (rev: quoter:4900-7317 exec+exits)
  On Exception/RuntimeError in the strand loop's book read, copy the ticker's standing orders into desired verbatim with the unwind tag (exact idiom already exists twice: quote-loop retention and apply_drop_grace's A9-F4 held-tagging). Closes finding #1 at its root; diff becomes a no-op on transient errors and the exit keeps its queue position.
  EV: Eliminates the one live path that destroys a resting exit on a single transient read error; preserves exit queue position (time-priority is the maker edge on the reducing side).

**O-36 [S] Retain (not cancel) the in-flight market's book on read-budget RuntimeError in the quote loop** (rev: quoter:4900-7317 exec+exits)
  In the :5229 RuntimeError branch, apply the same standing-retention as the generic-Exception branch for the current market before breaking (or pop it from `consumed` so grace can cover it). Closes finding #2.
  EV: Stops budget-exhaustion cycles from tearing down an earning book plus its exit; removes a churn amplifier exactly when the cycle is already read-starved.

**O-37 [S] Use the close-time cache in the settle-taker arming read** (rev: quoter:4900-7317 exec+exits)
  :5135 calls public_get(/markets/{t}) fresh for EVERY naked ticker EVERY cycle although _close_cache_get/_close_cache_put exist and persist across restarts (close_cache in state, :6090). close_time is static per market.
  EV: Frees ~1 REST read per naked ticker per cycle from the 200/cycle budget — directly reduces the budget-exhaustion pressure that triggers findings #1/#2.

**O-38 [S] Ticker-qualify _rest_maker_offset's client_order_id** (rev: quoter:4900-7317 exec+exits)
  Include a per-invocation counter or a ticker hash in the id (mind Kalshi's id-length limit; the cycle path solved this with cyc+index, :3531-3535). Closes finding #4.
  EV: Removes a same-second dedup rejection on the exit re-rest path — the one failure class that must never be probabilistic.

**O-39 [M] Port the C7 failed-cancel deferral into _flatten_all pass 1** (rev: quoter:4900-7317 exec+exits)
  Track per-oid cancel success in _cancel_each (it already knows), and skip the fresh offset create for any ticker+side whose cancel failed — same rule as :5837-5843. The kept stale order IS the exit, so deferring the create strands nothing.
  EV: Removes the through-flat sign-flip window on the STOP path (finding #3) — the state where a wrong position hurts most because nothing else is trading.

**O-40 [S] Add a TAKER_MAX_MKTS-style budget to _strand_cross** (rev: quoter:4900-7317 exec+exits)
  Cap crossings per invocation (reuse TAKER_MAX_MKTS=8; excess due tickers keep their re-armed clocks and go next period). One `if` plus a counter, mirroring :6817.
  EV: Bounds worst-case single-cycle taker spend after backlog events; makes all three taker backstops share one budget doctrine.

**O-41 [M] Persist STOP-branch cross bookkeeping** (rev: quoter:4900-7317 exec+exits)
  On the STOP branch, load state before _flatten_all and write back mkt_taker_xn + reentry_cool stamps for tickers the escalation crossed (finding #5). Minimal state IO, no behavior change to the flatten itself.
  EV: Taker-governor and re-entry cooldown see paid exits that happened while halted — prevents post-resume re-entry into books the bot just paid to leave.

**O-42 [M] LIP-faithful at_ref telemetry** (rev: quoter:4900-7317 exec+exits)
  Compute the true per-side Reference Price (walk cumulative depth to Target/5 using the _levels lists already parsed per market) and classify at/better-than-reference as 1.0x. Keys additive (at_ref2_usd) so history stays comparable.
  EV: Turns the placement split into a calibration-grade predictor of credits (feeds the D-A est-vs-model checkpoint and any D-E distance study); today's key systematically understates full-credit money (NOTE at :5671).

**O-43 [M] Thread standing-order hint oids into _cancel_ticker_resting_confirmed from in-cycle callers** (rev: quoter:4900-7317 exec+exits)
  _taker_cross_capped calls it with no hints (:6668), forcing two full paginated get_orders reads per cross. The cycle already holds `standing`; passing its oids allows skipping the pre-read (keep the confirming re-read — that is the safety property). Delete the dead oids_by_t (:5129) in the same change.
  EV: Roughly halves authenticated reads per taker cross; faster cross-to-re-rest latency on thin books where the touch is moving.

**O-44 [M] Reserve a footprint seat + honest est for macro-probe tickers before arming D-C** (rev: quoter:4900-7317 exec+exits)
  Exempt MACRO_PROBE_TICKERS from the select-budget refusal paths and the FOOTPRINT_TOP early-stop (or prepend them to the walk), and charge their real ladder dollars (MACRO_PROBE_USD) instead of the ramp-clamped est (:5067-5070) since _d3_apply_ramp exempts macro quotes.
  EV: Makes operator designation deterministic — without it D-C can silently no-op on crowded cycles (known-open #6-part2) and under-charge admitted ladders.

**O-45 [S] Dead-code removal batch (operator signoff): flatten_to_zero, oids_by_t, _offset_size** (rev: quoter:4900-7317 exec+exits)
  All three are call-site-orphaned (grep-verified for flatten_to_zero and oids_by_t; _offset_size self-documents as orphaned at :2455-2458). Rule 5 requires operator naming; one commit, tests pinning absence of behavior change.
  EV: Removes two divergent-semantics traps (pass-counting crossed, full-position crossing) from the taker path a future session could revive wrongly.

**O-46 [S] Count amends in write_tokens/order_ops telemetry** (rev: quoter:4900-7317 exec+exits)
  plan['write_tokens'] = creates*10 + cancels*2 (:5964) ignores amends entirely; report()'s token pace inherits the gap. Add the venue's amend token price (verify from rate-limit docs) as its own term and key.
  EV: Keeps the write-pace gauge honest once AMEND_DECREASE arms; prevents a false sub-ceiling reading on amend-heavy cycles.

**O-47 [S] Serve the strand-cross exit-calc receipt book read through _get_book (WS mirror) instead of raw public_get** (rev: quoter:4900-7317 exec+exits)
  :6926 always REST-fetches the orderbook for the receipt even when the daemon mirror holds it; _get_book preserves identical raise semantics.
  EV: Saves post-quote-loop REST reads exactly when the budget is most depleted; fresher receipt on fast books.

**O-48 [M] Reference-price-walk trigger: requote on DEPTH changes, not just best-price moves** (rev: daemon+client+wsfeed)
  on_book gates Stage A on a >=1-tick move of the BEST bid (daemon :603-605), but the LIP reference price is the level where cumulative resting size reaches Target/5 — competitor size changes shift the reference without moving the best, leaving us at 0.5^ticks (half pay per tick) until the 60s heartbeat. The mirror already holds full level maps; compute the per-side reference walk in on_book and trigger when the REFERENCE moves. Pure trigger-side change, no quoter modification.
  EV: Direct reward capture: every tick off reference halves that side's score share; on active books this is the largest per-snapshot multiplier lever the daemon controls.

**O-49 [S] Cheap frozen-mirror detector: one mirror-vs-REST shadow compare per cold cycle** (rev: daemon+client+wsfeed)
  Stage C deliberately has no per-ticker age bound; the residual holes (single-ticker server-side sub drop, error-frame freeze window, seq-key migration) all share one signature — mirror diverges from REST while claiming clean. Sample ONE random vouched ticker per cold cycle, fetch its REST book (one ~320ms read), diff full level maps, and on mismatch dirty every mirror + count a gauge in the cold_cycle log row. Recreates the 78/78 validation continuously for ~1/40th of the read cost Stage C saved.
  EV: Converts the whole 'stale book quoted as fresh' class from silent to counted within ~40 cycles of onset; directly de-risks arming/keeping WS_BOOK_COLD.

**O-50 [S] Dirty mirrors + clear acks BEFORE the error-frame backoff sleep** (rev: daemon+client+wsfeed)
  Move the mirror-dirtying and confirmed_channels reset to the moment _dispatch returns False (or into the if-not-ok branch before the sleep), matching what the seq-gap and except arms already do. Three lines; closes the MAJOR frozen-mirror window entirely.
  EV: Eliminates the up-to-60s stale-quote window under Stage C; restores the daemon docstring's stated invariant.

**O-51 [S] Thread own_orders into HotContext + hot desired_quotes call** (rev: daemon+client+wsfeed)
  ctx.build already holds standing; store _own_order_levels(standing.get(t)) per ticker in by_ticker and pass it at daemon :448. Precondition for ever arming WS_HOT; also makes the hot/cold quote parity property testable offline.
  EV: Unblocks Stage B (the ~2-4s -> ~250ms reprice latency win) without the churn regression; nothing changes while the flag is off.

**O-52 [S] Gate ctx.build on WS_HOT (keep a cheap watch-set computation for Stage A)** (rev: daemon+client+wsfeed)
  With Stage B off, replace the full HotContext rebuild each cold cycle with select_footprint + the standing dict run_once just fetched (or persist the ticker set from run_once's plan state). Saves 2-4 authed REST round trips and ~1-2s per cycle at event-driven cadence.
  EV: Faster cycles = tighter tracking of moved books (reward), lower rate-limit exposure; zero behavior change to any guard.

**O-53 [S] 429-aware global backoff + Retry-After parsing in KalshiOrderClient** (rev: daemon+client+wsfeed)
  On HTTP 429 in _request: honor Retry-After, set a shared monotonic 'venue-cool-until' respected by both _write and _get_paginated, and count it. Also share the write-spacing clock across client instances (module-level like the quoter's _last_req) so the cold/hot instance boundary cannot violate the floor.
  EV: Turns a venue rate-limit episode from a 5s-cadence hammer-loop into a bounded pause; protects the account's standing with the venue.

**O-54 [S] Reconnect fill reconciliation: notify daemon on WS reconnect and invalidate ctx** (rev: daemon+client+wsfeed)
  Add an on_reconnect callback to Feed (fired from the except arm and the not-ok break) that the daemon wires to ctx.invalidate() + cycle_req.set(). Closes the missed-fill-during-gap hole with one guarded cold cycle instead of trusting a <=90s-old ctx.
  EV: Removes the last stale-inventory path into Stage B hot writes; cost is one extra cold cycle per reconnect (rare).

**O-55 [S] Arm KALSHI_HTTP_POOL after a read-only soak** (rev: daemon+client+wsfeed)
  Connection pooling is built, correctly retry-disabled, and measured at 2-3x latency cut on every REST call (253->127ms from the VPS) but ships dark. Soak it on reads (cold-cycle standing/positions/settlements) and compare cycle wall clock in ws_daemon_log before touching writes.
  EV: Shaves seconds off every cold cycle — compounding with Stage C it takes event-to-requote latency toward the 2s floor; faster requote = better snapshot-qualification odds.

**O-56 [M] Verify amend-decrease live and evaluate amend-for-reprice** (rev: daemon+client+wsfeed)
  amend_order_v2 exists, unit-tested, dark behind KALSHI_AMEND_DECREASE. Beyond the queue-preserving size-decrease case, an amend reprice (though it forfeits queue) is ONE venue op instead of cancel+create: no interval where our size is absent from the book, half the write tokens, and no ambiguous cancel/create seam. Needs one live probe on a throwaway order to verify the response shape (and remaining-count semantics) first.
  EV: Fewer zero-presence instants during reprices (each 1s reward snapshot we are absent from is lost share) plus halved write-token burn on the churniest path.

**O-57 [M] Per-command error handling in the WS feed + watch-list prioritization** (rev: daemon+client+wsfeed)
  Distinguish subscription/command error frames (log, drop the offending ticker from self.tickers, resubscribe) from connection-fatal errors (reconnect). Separately, replace sorted()[:80] with footprint-order-then-held prioritization so lexical position can never evict a quoted market from the fast feed.
  EV: Removes the one-bad-ticker feed-poisoning failure mode and the silent alphabetical eviction edge; both protect availability of the latency edge rather than P&L directly.

**O-58 [S] Count seq-missing frames in PARSE_FAILS** (rev: daemon+client+wsfeed)
  One line in _dispatch: if typ is orderbook_* and seq is None, tick PARSE_FAILS (or a sibling gauge) — the cold_cycle log row already surfaces it. Closes the only dialect-drift axis the 07-30 gauge misses.
  EV: A venue seq migration becomes visible within one cold cycle instead of never; guards the entire gap-detection foundation of Stage C for ~zero cost.

**O-59 [M] Estimate->credit join harness (the missing P2/D-A deliverable)** (rev: recorder+ledger+estrec)
  All the raw data now exists — estimates-YYYYMM.jsonl (per-program centicent series), kalshi_program_map.json (ticker/end_date/period_reward), and get_credit_history (per-event credit receipts) — but no script joins them. A small offline tool that takes each program's last pre-end estimate and matches it to the credit that lands at payout answers, with receipts, every open 0a question: cumulative-vs-projection, whether the $1 floor and final pool scaling are inside the estimate, and the estimate's predictive error distribution. This is the direct input to the 08-07T15:00Z APRPOTUS checkpoint verdict and to whether the D-A entry floor can be trusted at all.
  EV: High — converts the estimates feed from telemetry into a calibrated predictor; gates D-A arming on evidence instead of a single checkpoint anecdote.

**O-60 [S] Carry health counters inside the feed files instead of stdout** (rev: recorder+ledger+estrec)
  SETTLED_REPLAY_FAILS, MISSING_FIELD_ROWS, PAGINATION_TRUNCATED and the recorder warnings all die in timer journals. Writing them into the artifacts they qualify (a `health` object in kalshi_fill_costs.json's top level, a `recon_ok`/`warnings` key in each cash row, an `unmapped` count in the map file) lets every reader — kalshi_capital_rank, the quoter, offline P2 analysis — fail open on a degraded feed the way the SCHEMA field already allows, and gives each money row an in-band validity bit.
  EV: Closes the whole silent-degradation class the modules keep re-warning about; makes the MAJOR fill-costs failure mode self-announcing.

**O-61 [S] Atomic write + daily backup of kalshi_program_map.json** (rev: recorder+ledger+estrec)
  tmp+os.replace (three sibling files already do exactly this) plus keeping yesterday's map as kalshi_program_map.json.bak makes the merge-only memory actually durable. Optionally also probe whether /incentive_programs supports a per-id or status=ended read to backfill pre-deployment and lost entries.
  EV: Protects the est->credit join's only ticker-resolution source across the exact window (program end -> payout) where it is irreplaceable.

**O-62 [S] In-recorder position reconciliation before each cash row** (rev: recorder+ledger+estrec)
  kalshi_cash_recorder already fetches positions and the full fill tape; running the ledger's venue-position_fp vs replay_fills comparison (excluding settled tickers) and stamping `recon_ok`/mismatch detail into each cash row costs one dict comparison and no extra API calls. Each row of the P2-critical series then carries its own validity evidence instead of borrowing a guard from a different process.
  EV: Turns silent tape corruption into a per-row flag; materially hardens the series the reward/drag decomposition is differenced from.

**O-63 [M] Settlement watermark grace-window re-pull with dedupe** (rev: recorder+ledger+estrec)
  In the ledger, re-fetch settlements with min_ts = watermark - 24h (get_settlements already honours min_ts, measured) and dedupe by ticker against already-processed rows persisted in state. Removes the late-arrival/equality drop class for settlements — the one place the recon self-check has no coverage — and the analogous fills fix is a >= comparison plus a processed-fill_id set for the boundary timestamp.
  EV: Eliminates the only mechanism found here that can permanently fabricate a positive residual (phantom reward) with zero symptoms.

**O-64 [S] Operator-maintained external-money JSONL folded into the recorders** (rev: recorder+ledger+estrec)
  The residual's deposit-blindness is documented but unmitigated in code. A tiny deposits.jsonl ({ts, amount, note}, hand-appended per the existing operator rule) that both the ledger and the offline cash differencing subtract closes the $9.30/$5.77 open residual channel and makes 'unexplained positive step = reward' mechanically checkable instead of a convention.
  EV: Removes the largest standing confound in the cash identity; cheap because the operator already commits to noting money movements.

**O-65 [S] Verify realized_pnl_dollars fee-netting (single measurement)** (rev: recorder+ledger+estrec)
  The fill-cost feed's own declared open question. One settled ticker with known fills: compare venue realized_pnl_dollars (from a pre-settlement snapshot or a flat live row) against revenue + replay cash with and without fees. Answers whether live and settled rows in kalshi_fill_costs.json share a basis; if not, one line normalizes it.
  EV: Removes a systematic settled-vs-live skew from the capital ranking; converts a documented UNVERIFIED into canon.

**O-66 [S] Point the ledger at the client's loud paginator (or port the live-cursor warning)** (rev: recorder+ledger+estrec)
  kalshi_attribution_ledger's private get_paginated is the only silent-truncation paginator left in the lane (10k cap, no warning). Either import maker_kalshi_client's _get_paginated semantics or add the same leftover-cursor warning + counter. Shared-module protocol applies but the change is contained.
  EV: Brings the last money-adjacent read path up to the lane's own loudness standard before the tape grows into the cap.

**O-67 [S] Quarantine or retire the banned residual surfaces in --report** (rev: recorder+ledger+estrec)
  Now that credit_history is the reward truth, the report's per-day 'rewards (MEASURED)' column and per-series allocation table are attractive nuisances. Options (operator to name): print them as residual_UNVERIFIED with an explicit credit_history pointer, replace the rewards column with a credit_history pull, or drop report() entirely.
  EV: Removes a standing RULE-ZERO trap from the most convenient human-facing money summary in the lane.

**O-68 [M] Implement the true LIP scoring kernel (Target/5 ref walk, side-level qualification) behind a flag; re-fit haircuts at B8** (rev: scores+rank+feedback)
  One shared kernel replacing the ref=best-bid walk in _qualifying_score/_qualifying_breakdown/scorecard, validated against the 08-07 APRPOTUS est->credit checkpoint and the estimates feed (D-A gives per-program venue truth to regress against — the first time model-vs-venue error is directly measurable per market). Likely explains a chunk of the M7 2-6x over-prediction; shrinking it tightens the $1.20 floor, the NETEV unproven fallback, and cross-sectional ranking simultaneously. Must re-fit NETEV_MODEL_HAIRCUT and the W12 shape in the same change (both were fitted on the biased kernel).
  EV: High — the floor gate and every capture-based rank consume this number; removes a known anti-conservative bias class instead of scalar-haircutting it

**O-69 [S] Age-gate the ref inside shadow_rank (same ALLOC_PCAP_MAX_AGE_S bar as the budget walk)** (rev: scores+rank+feedback)
  Pass a max_ref_age or pre-filtered refs into shadow_rank/est_commit_usd so the live allocation ordering and the budget-walk charging agree on freshness; stale ref -> 0.50 max-commit (the walk's existing conservative convention). Closes the split-key inconsistency reported as MAJOR.
  EV: Medium-high — removes a live misordering lever driven by up-to-7-day-old extreme refs

**O-70 [S] Ramp-aware commit denominator in the live allocation key** (rev: scores+rank+feedback)
  Thread a per-ticker eff_ct (the _d3_est_ct value the walk already computes) into est_commit_usd's contract ceiling so cap_score = net reward per dollar ACTUALLY restable. Today unproven tickers carry up to ~25x inflated denominators; the ordering key then disagrees with the charging key every cycle.
  EV: Medium — better capital ordering during ramp phases; currently conservative-direction so value is efficiency, not risk

**O-71 [M] Due predicate over the event's full ticker set (or program end), not just our filled tickers** (rev: scores+rank+feedback)
  kalshi_credit_feedback already fetches per-ticker market metadata for event resolution; the same read carries close_time/status for sibling strikes — require the EVENT (or its program) concluded +72h before arming never_paid_due. Kills the early-settled-strike false-conviction window reported as MINOR.
  EV: Medium — prevents transient 0.5x rank penalty + probe-rung clamping on paying series during their accrual window

**O-72 [M] Decay or scope the 'paid' verdict** (rev: scores+rank+feedback)
  One credit EVER marks a series paid forever (kalshi_credit_feedback.py:121-122) and 'proven' for the W7 size clamp — a series that paid in July and zero-pays its current programs keeps D2_BONUS and full rungs indefinitely. last_credit_ts is already recorded (B-4); add a recency window or per-program-generation scoping when receipts accumulate.
  EV: Medium — trust plane currently only ratchets up; a venue-side program change would be invisible to sizing

**O-73 [S] Decouple the D4 score-cache fold from MKT_TELEMETRY and the jsonl write** (rev: scores+rank+feedback)
  Move the ks.update fold outside the telemetry conditional and ahead of the file write (mirror of the D9 attempt-stamp decoupling, same rationale: measurement flow must not depend on telemetry config/health). Also give the starving-rank case its own counter distinct from mkt_telemetry_fail.
  EV: Medium — removes a silent-degradation landmine on an innocuous-looking flag

**O-74 [S] Thread own_orders through both sweeper paths** (rev: scores+rank+feedback)
  _sweep_measure and kalshi_capture_sweep.prospective_capture take the same _own_order_levels input the quote loop already builds (in-process) or a fresh orders read (offline). Closes the last F5 call sites before Phase-3 pcap wiring makes them load-bearing.
  EV: Low-medium now, medium once pcap feeds live ranking

**O-75 [S] Fix kalshi_market_scorecard usd_day (/days) to the R1 canon form** (rev: scores+rank+feedback)
  One-line change plus a pin test against the quoter's row build so the diagnostic and the live pool math can never disagree again (the quoter fix comment explicitly documents the refutation; the tool was missed).
  EV: Medium — protects canon measurements; zero live-path risk

**O-76 [S] NO-side swing tracking** (rev: scores+rank+feedback)
  Fold max(|dy_ref|, |dn_ref|) (or the pair) into ref_move; n_ref is already on every telemetry row so the collection cost is zero, matching the module's own 'costs nothing to collect' design note.
  EV: Low-medium — closes a one-sided adverse-fill blind spot in the swing discount

**O-77 [S] Multi-level-aware telemetry scoring before D-C arms** (rev: scores+rank+feedback)
  _market_telemetry_row should sum df-weighted contributions over ALL quotes per side instead of next() — required for macro-probe ladders to self-measure; also makes any future multi-level quoting measurable for free.
  EV: Low today (dark), gating-relevant the day MACRO_PROBE_TICKERS is named

**O-78 [S] Cursor pagination for credit_history** (rev: scores+rank+feedback)
  At 58 lifetime credits the 1000 limit is years away, but the alarm-only design means the first response to truncation is a scramble; a cursor follow (the client already has _get_paginated) makes the alarm dead code instead of a deadline, and removes the hand-duplicated limit constant.
  EV: Low now, removes a future trust-plane cliff

**O-79 [S] Exclude-or-mark unsettled markets in build_table before B8** (rev: tools:netev+settlement)
  In kalshi_netev_rebuild.build_table, cross the traded-in-window set against the settlements list and either (a) drop still-open markets from attribution with a per-family 'open_markets_excluded' count + excluded-notional field, or (b) keep them but emit per-family 'open_collateral_usd' so the operator can see how much of a negative net is unresolved collateral. Directly neutralizes finding 1 for the Aug 9-10 rebuild; (a) is the conservative default since the module already refuses to judge half round trips.
  EV: High — prevents the armed gate benching receipt families on money that has not been lost; the single biggest poisoning vector for the B8 table given D-C/est-feed far-close holdings.

**O-80 [S] Per-family pre-window cash contamination metric** (rev: tools:netev+settlement)
  During the event loop, split fam_pnl into in-window and pre-window components (the _in_window test is already computed per event) and emit 'pre_window_cash' + its share of net per family, plus a caveat when the share exceeds a threshold. Turns finding 2 from an invisible laundering channel into a reported number without changing the verdict formula.
  EV: High — makes the 'clean window' claim of the B8 table verifiable instead of assumed; CLASS-NOT-INSTANCE compliant (standing detector, not a one-off check).

**O-81 [S] Split netev_min_signal into per-branch telemetry keys** (rev: tools:netev+settlement)
  Emit netev_min_receipt_pct and netev_min_model_usd separately in the plan row (quoter :3026). Two keys, both units meaningful; keep the old key for one release if any consumer greps it.
  EV: Medium — restores usability of the gate's own headroom telemetry for knob tuning; currently the mixed-unit min is unusable in any mixed cycle.

**O-82 [S] Truncation tripwires on the credit read and the notional field reads** (rev: tools:netev+settlement)
  (a) get_credit_history: warn loudly when len(credits) == limit or a cursor/next token appears (mirror _get_paginated's PAGINATION_TRUNCATED pattern). (b) kalshi_netev_rebuild notional loop: count fills where the acquired-side price field or count_fp is missing (mirror kalshi_fill_costs.MISSING_FIELD_ROWS) instead of silently falling back to the raw yes price.
  EV: Medium — closes two silent-degradation channels feeding the money gate; cheap insurance in the venue-rename class the repo already tripwires elsewhere.

**O-83 [S] Settle the realized_pnl_dollars fee-convention question with one designed receipt check** (rev: tools:netev+settlement)
  Pick a live market with fee-bearing (taker) fills, snapshot /portfolio/positions realized_pnl_dollars, and compare against the pre-fee vs post-fee replay of its own tape — the existing VENUE_POSITIONS pins can't discriminate because their fixture fees are all zero. Then align kalshi_fill_costs so live and settled rows share one convention and update the 'UNVERIFIED' header.
  EV: Medium — removes a known unknown from the feed the net-EV calibration cites; one API read plus one test fixture.

**O-84 [M] Mark-to-market option for open positions in the rebuild** (rev: tools:netev+settlement)
  Alternative/extension to the exclusion opportunity: value still-open positions at book mid (or last trade) at read time, reported as a separate 'unrealized_usd' column never mixed into the receipt net. Lets the operator see families where open exposure is the story without corrupting the realized verdict.
  EV: Medium — richer B8 verdicts for far-close/macro families whose whole thesis is held-to-close positions; strictly additive column.

**O-85 [S] Inclusive-date / ET-window CLI helpers for the rebuild** (rev: tools:netev+settlement)
  Add --until-inclusive (date-only -> 23:59:59.999Z) and/or --et-dates (apply the -04:00 canon-clock arithmetic the caveat documents) to kalshi_netev_rebuild.main. The parsing fix regression test already covers _iso; these are thin wrappers.
  EV: Low-medium — removes the off-by-one-day operator footgun on every future window, the same error class that produced the retracted 08-02 'FREE TEST' dates.

**O-86 [S] Emit unknown-life cost rows and count live resting coverage in presence calibration** (rev: tools:netev+settlement)
  In kalshi_presence_calibrate.measure, emit (family,'unknown') buckets with presence_median=None but real hours/fees/orders (consumers already null-check fees_per_resting_hour), and extend currently-resting orders' intervals to `now` when status=resting. Fixes both halves of finding 7.
  EV: Medium — presence and cost-per-hour feed the W12/B8 knob calibration; today both are biased down exactly where data is thinnest.

**O-87 [S] Wire the '*|bucket' cross-family fallback the lookup already expects** (rev: tools:netev+settlement)
  Have measure() additionally aggregate a family-agnostic ('*', bucket) row so presence_for's existing wildcard path (:193) serves new families a measured life-bucket median instead of default 1.0 — or delete the dead lookup if the operator prefers assume-perfect for new families (RULE NINE: ask, don't demote).
  EV: Medium — new-series estimates are the ones feeding the $1-floor gate where over-prediction admits sub-$1 drive-bys (W10 §5); a life-bucket prior is the cheapest correction available.

**O-88 [M] Re-fit NETEV_MODEL_HAIRCUT jointly with W12 arming at B8** (rev: tools:netev+settlement)
  Already self-flagged at quoter :2616-2618: the 3.0 haircut encodes the unshaped model's 2-6x over-prediction, part of which IS the extreme-price signature W12 now models. Fit both on P2/estimates-feed receipts in one pass; also add a pin for the thin-negative-family model path at usd_day>=200 (the loosened case the T-HARDEN pins cannot see, quoter :2988-2992).
  EV: High — the unproven-model fallback is currently the ONLY path opening quotes (all six receipt families benched), so its calibration error is the live bot's dominant selection error.

**O-89 [S] Preserve taker-only-family audit rows in the rebuild document** (rev: tools:netev+settlement)
  Include set(fam_taker_ct) in the families key union (or emit a top-level taker_excluded_families map) so an exclude_taker run never drops the audit trail of a family whose in-window activity was entirely takers.
  EV: Low — audit completeness for an experiment flag; one-line union change plus a test.

**O-90 [S] Rebase kalshi_ledger.py rewards on credit_history; demote the residual to 'unexplained'** (rev: tools:ledgers+w16+w17+w11)
  The client already has get_credit_history (maker_kalshi_client.py:419). Replace --rewards-actual manual entry with an API pull, print credit-sum as REWARDS, and relabel the residual as UNEXPLAINED (the actual integrity signal — nonzero residual after subtracting true rewards means a model gap). Turns the tautological identity check into a real one and retires the last residual-labeled-rewards surface.
  EV: Removes the standing RULE-ZERO violation surface; converts a canon hazard into a genuine reconciliation gate that can detect fill-model drift to the cent.

**O-91 [S] w11: add the true live baseline from cyc['actual'] and a --variant flag** (rev: tools:ledgers+w16+w17+w11)
  The actual footprint is already in every caprank row. Add a policy that returns cyc['actual'] verbatim, report shadow-vs-actual divergence per day, and expose replay()'s existing variant parameter on the CLI (currently hardwired to 'env').
  EV: D2/knob decisions measured against what the bot actually does, not the shadow; free PIVOT_SELECT impact measurement from data already on disk.

**O-92 [M] w11: time-slice fills for a true no-lookahead never_paid demotion** (rev: tools:ledgers+w16+w17+w11)
  Snapshot orders rows carry created_time; build fills_by_series_to_date(ts) the same way credits_timeline is guarded, so the replayed D2 sees only fills known at cycle time. Pin with a boundary test like the existing credit one.
  EV: Removes the pessimistic bias against late-filling series in D2 replay verdicts — directly improves knob-tuning fidelity for the ruled next-session queue.

**O-93 [S] w17: read the quoter's actual gating knobs from live.env instead of hardcoding** (rev: tools:ledgers+w16+w17+w11)
  w17 already parses live.env; read KALSHI_MAX_DAYS_TO_CLOSE (and FARCLOSE_PAYING_EXCEPTION) and use them at both attribution sites (lines 151, 203). Also add truncation awareness: if len(family_dropped_tickers)==40, print a 'truncated' caveat on the family_budget bucket.
  EV: Eliminates two silent attribution-drift vectors from the UNKNOWN headline; keeps the alarm trustworthy across operator retunes.

**O-94 [S] w17: require resting presence for EARNING (use y/n_rest_ct)** (rev: tools:ledgers+w16+w17+w11)
  Bucket EARNING only when max(y_rest_ct, n_rest_ct) > PROBE_SCALE_CT; when intended>probe but resting==0, emit a new PLACEMENT-FAIL bucket line. Fields already exist in every telemetry row (F5 additions).
  EV: The coverage ledger stops certifying pool dollars as earned when nothing rests at the venue — catches reject/wall/torn-line classes at the headline.

**O-95 [M] Quoter: capped per-ticker drop-reason sampling for trusted series** (rev: tools:ledgers+w16+w17+w11)
  Root burn-down of the known UNKNOWN class: when a trusted-series (allow|proven) market is dropped at a selection stage, append (ticker, stage) to a per-cycle capped list in the plan row (e.g. 60 entries, trusted-only, pool-ranked). w17 then attributes selection drops exactly. Shared live-quoter change — needs full ship discipline and operator gating.
  EV: Converts the largest permanent UNKNOWN source into named EXCLUDED stages; directly serves the class-not-instance invariant that w17 implements.

**O-96 [M] Implement the lineage table (reader first)** (rev: tools:ledgers+w16+w17+w11)
  Define kalshi_lineage.json schema {old_series: {successor, ruling, date, kind: trust|conviction}}; w16 suppresses ruled pairs and lists conviction-transfer candidates; optionally the quoter's credit-feedback loader treats a ruled trust-successor as proven (that half is a live-path change — operator signoff).
  EV: Operator rulings stop evaporating; successor programs ramp as payers instead of re-probing at 5ct; evasion watch stops re-alarming settled decisions.

**O-97 [S] lumps ledger: replace manual REWARDS_BY_FAMILY with credit_history aggregation** (rev: tools:ledgers+w16+w17+w11)
  Credits itemize per event ('Liquidity Incentive for event <EVENT>'); aggregate amount_cents by event-series to fill the rewards column automatically, and re-base realized on get_settlements to close the defect-14 blind spot in the same tool.
  EV: Removes a stale manual step (M2b) and the settled-loss undercount from the toxicity-review surface; per-family damage-vs-reward becomes exact.

**O-98 [S] Quarantine or rewrite kalshi_ledger_reconstruct.py on the position-aware model** (rev: tools:ledgers+w16+w17+w11)
  Its conclusions should be re-derived using kalshi_attribution_ledger's fill_outcome/fill_price/position-aware replay (already venue-verified 51/51). Until then a QUARANTINED header comment prevents citation. Also drop the hardcoded key id default.
  EV: Prevents a refuted sign model's outputs from re-entering canon; one less prod credential string in the repo.

**O-99 [M] w17 FEED: join the D-A estimates API for early coverage alarms** (rev: tools:ledgers+w16+w17+w11)
  Once KALSHI_EST_FEED arms, cross-check per-market estimates against the EARNING/LIMITED buckets: presence with zero estimate = qualification failure (Target/side-short) detectable same-day instead of waiting for program-end credits; estimate present on an EXCLUDED market = attribution-error detector.
  EV: Cuts coverage-alarm latency from program-conclusion (days) to same-day; validates the LIP qualification model (Target/5 reference walk) against the venue's own live projection.

**O-100 [S] w17: fix the stale sentinel and de-noise the parse-rate denominator** (rev: tools:ledgers+w16+w17+w11)
  Use a min-epoch sentinel (or None-means-stale) at line 233 so proven-but-invisible series alarm; exclude known non-LIP reason types (referral) from the A-1 denominator, or alarm on LIP-row count deltas instead.
  EV: Restores the B-4 detector's worst-case coverage and de-noises A-1 — both were explicit wave-2 hardening deliverables.

**O-101 [M] Port fair()'s book-sanity + time-based windows into the detect/eval harness before D-E reuses it** (rev: tools:drift-suite)
  Two surgical changes: (1) build_timeline adopts the audited sanity gate (b>0, a<1, spread<=0.20) with an explicit 'unusable' flag instead of shell-book mids / last-trade fallback; (2) every windowed detector and score keys on ts deltas, not index deltas (the pattern already exists in signal_book_drift.py:98). Then re-emit the detect/eval/jump tables. This is the precondition for trusting any drift number the queued D-E fills-vs-distance study produces.
  EV: High — removes both MAJOR distortions from the measurement canon; the jump-vs-drift program decision gets a clean numeric basis instead of a directionally-lucky one.

**O-102 [L] Re-run the drift study on the maker-era tape (post-07-23) with the attribution-ledger fill feed** (rev: tools:drift-suite)
  The entire 07-23 verdict rests on 6 RUNOVER positives and 27 TEMP controls from a 3-day taker-era CSV. The maker era has months of fills with a verified direction contract (kalshi_attribution_ledger.py book_side mapping, 317/317) and far larger n. Same harness (after the fixes above), new labels from settled maker positions. Directly calibrates D2_FEEDBACK adverse-selection response and the D-E study's fills-vs-distance axis.
  EV: High — turns an n=6 negative result into a properly powered measurement on the regime the bot actually trades in.

**O-103 [S] Capture a live shell-book/one-sided flag in the quoter's rest_df stream** (rev: tools:drift-suite)
  The study had to reconstruct book quality retroactively from candles and got burned (finding 1). The live quoter already sees the real book each cycle; emitting a per-cycle flag (one-sided / spread>0.20 / bid<=0.01) into the D-B/D-G rest_df fields lets every future adverse-selection measurement condition on book state at capture time, for free.
  EV: Medium — permanently removes the retroactive-book-quality problem for all future studies; near-zero risk (measurement-only field).

**O-104 [M] Formalize the life-frac fill blocker study on canon fills** (rev: tools:drift-suite)
  signal_book_drift section 4's no-book alternative (block fill if life-frac >= X) was the one signal with a clean TPR/FPR table and zero data requirements. Live code already has late-life/far-close designation overrides (7e43554), so first measure overlap: how many run-over fills would today's FARCLOSE/late-life gates already have blocked, and what does a life-frac threshold add on top. Pure measurement, no gating change.
  EV: Medium — either validates the existing far-close gates with real numbers or surfaces uncovered late-life run-over exposure; feeds the knob-tuning-data queue item.

**O-105 [S] Fetcher hygiene: retry frozen-empty entries, flag the 12k trade cap, guard naive timestamps** (rev: tools:drift-suite)
  kalshi_drift_fetch.py: treat market=None cache entries as retryable on resume (:155), warn when fetch_trades exits at max_pages (:114) so tape truncation is visible, and have labels.ts()/fetch.ts() raise on naive timestamps instead of silently using machine-local time. Only worth doing if the harness is reused (it will be, per the D-E queue).
  EV: Low-medium — cheap insurance against silent survivorship and tz drift on the next data pull.

**O-106 [S] Fix the label elif-chain to match its documented definitions** (rev: tools:drift-suite)
  kalshi_drift_labels.py:78-87 — make neg_strict/neg_broad/temp_control require not any_zero_exit as the docstring states (and delete or use the dead all_zero at :75). Zero effect on the frozen data (verified 0 hits), pure re-run safety for the control-set denominator.
  EV: Low now, high insurance — control-set contamination on a re-run would silently depress every measured lift.

**O-107 [S] Budget-exhaustion retention: treat RuntimeError like a fetch-fail for every unread market** (rev: flow:end-to-end trace)
  On read-budget exhaustion, retain standing verbatim (with unwind tagging + exit-only strip, exactly the :5232-5258 idiom) for all remaining footprint and strand tickers instead of breaking into a diff that cancels them. Removes the finding-2 teardown landmine before allowlist widening makes it reachable.
  EV: Eliminates a one-cycle no-exit window on held tickers and mass queue-position loss under read pressure; protects the LIP time-integral the whole system farms.

**O-108 [S] NETEV margin unit hardening** (rev: flow:end-to-end trace)
  Accept both scales at the gate (if abs(NETEV_MIN_MARGIN_PCT) > 1: divide by 100, warn loudly) or rename the knob to a fraction with a startup print; fix the in-code margin-ladder comment to state fraction values. Do it before B8's margin re-rule.
  EV: Restores the only receipt-based family bench to operator control; prevents a silent no-op at the B8 re-rule.

**O-109 [S] Program-list cache for run_once** (rev: flow:end-to-end trace)
  run_once refetches /incentive_programs and re-runs selection over the full program list every daemon cycle (~5-8s cadence); the daemon already holds a 10-min cache (_programs) used only by ctx.build. Share it (env-tunable TTL, ~60s) with a bypass on footprint-affecting events.
  EV: Frees 1-2 reads plus selection CPU per cycle and shrinks cycle latency - the metric that decides how fast fills/moves get requoted.

**O-110 [M] D-C arming hardening bundle** (rev: flow:end-to-end trace)
  Before designating tickers: (a) reserved footprint seat + macro rows exempt from the FOOTPRINT_TOP early-stop; (b) walk charge = actual ladder cost, not the D3 est; (c) group-atomic capital admission at the create loop (all ladder levels or none, mirroring _macro_probe_quotes' own unaffordable rule); (d) dust carve-out for #12; (e) pins for the select_footprint exemption hunks (#8).
  EV: Makes the low-competition receipts thesis actually testable; without it the probe can silently never rest, rest partially (pure fill risk), or deadlock on dust - all of which read as 'macro probes do not pay'.

**O-111 [M] Estimates-feed calibration loop (D-A follow-through)** (rev: flow:end-to-end trace)
  Daily offline job regressing venue per-program estimates (recorder snapshots) against our model expected-credit per market: fit one measured multiplier per family/life-bucket and feed it into _expected_credit_usd and the netev model arm, replacing the guessed NETEV_MODEL_HAIRCUT=3.0 and the M7 2-6x folklore. First per-user ground truth short of credits.
  EV: Converts three guessed haircuts into measured numbers; reduces both false refusals (lost reward) and false admissions (dead quoting). The audit's own claim is that the blocker was telemetry - this is the analysis the new feed unlocks.

**O-112 [L] Deep-ladder earning lane (generalize the macro-probe shape)** (rev: flow:end-to-end trace)
  The venue competition map (08-06 audit) shows winning farmers rest deep cheap ladders, not at-touch size. LIP's distance penalty is only 0.5^tick while pickoff risk collapses behind the touch: split the join size across ref/ref-1/ref-2 (credit-weighted), keeping the touch presence small. Macro-probe already implements the deep mechanics; this is the earning-lane variant, A/B'd on receipts.
  EV: Largest reward-shape item on the table: near-equal credit at a fraction of adverse-fill exposure - attacks the structural drip (gas ~ -$9.42 of the 08-06 halt window) at its mechanism instead of via gates.

**O-113 [M] Scale SETTLE_UNWIND_MIN for short programs** (rev: flow:end-to-end trace)
  F17 noted-not-built: the 30-min settle-taker window floors effective wind-down at 30, keeping sub-hour programs (the hourly temp flagship, ~$1,920/day pools historically) roughly half unenterable. Scale the taker window with program life (e.g. min(30, 0.25*life)) with the C8 maker-first ordering preserved.
  EV: Unlocks the flagship hourly lane the moment those programs return; per the code's own numbers the wind-down chain currently forfeits most of a sub-hour window.

**O-114 [S] Arm AMEND_DECREASE after its one-live-cycle watch** (rev: flow:end-to-end trace)
  Built, tested, ships OFF only because the amend endpoint is unverified live. Size decreases are the common D3/throttle path; every one currently forfeits queue position via cancel+create.
  EV: Free time-on-book on every trim - direct LIP credit, zero behavioural trade-off per the venue's own amend semantics.

**O-115 [S] Watch-list priority before the [:80] cut** (rev: flow:end-to-end trace)
  Order the daemon watch set as held union standing first, then pool order, then alphabetical - instead of sorted()[:80].
  EV: Guarantees resting/held markets keep event-driven requoting and mirror service as the pool grows past 80.

**O-116 [M] D2 evidence widening via the estimates feed** (rev: flow:end-to-end trace)
  credit_feedback's per-event due gate (all tickers settled >72h) shrinks conviction coverage on serial series. Add 'program ended + venue estimate > $1 + no credit after the payment batch' as an independent never-paid evidence source once est-to-credit semantics are confirmed at the APRPOTUS checkpoint.
  EV: Faster, broader follow-the-profit convictions with receipts-grade provenance; shrinks capital parked in never-payers.

====================================================================================================
## S7. OPERATOR-ORDERED ADDITIVE CATALOG ITEMS (appended per 2026-08-06 order; verified this session)

# OPERATOR-ORDERED ADDITIVE CATALOG ITEMS (2026-08-06, append-only — do not reorder existing items)
# Independently verified this session before writing. Sources cited inline.

## OP-ADD-1. Capital-deployment funnel waste — selection admits markets the quote gates refuse; reservations stay charged all cycle

MECHANISM — VERIFIED (ESTABLISHED, code read at HEAD e981bf1 = deployed md5 5c7aed6f):
- The SELECT-TO-BUDGET walk (quoter:5010-5101) runs BEFORE the quote loop ("before the
  quote loop, so it sees this cycle's truth" — its own comment at :5010). It charges
  est_commit_usd per admitted market (capped by EXPLORE_PROBE_CT and, since the wave-1
  ramp-aware fix, by _d3_est_ct at :5070) into _tot9. Nothing after the quote loop
  re-credits the charge when a market's gates refuse it: gated_out is computed at
  quoter:5967 as len(footprint) - len(desired), purely for telemetry. Confirmed: no
  release path exists.
- Measured (plans-20260806.jsonl, re-read at source 2026-08-06T18:58:24Z row):
  select_budget_used 198.00 / limit 201.42, committed_usd 59.77, footprint 39,
  gated_out 33, drop_budget_full 7. ESTABLISHED. (Operator-quoted figures confirmed
  exactly.)
- Composition of the refusals (quotes-20260806.jsonl, latest full cycle
  19:19:59.808832Z, 39 rows; single-cycle sample — ESTABLISHED for this cycle only):
  gate_one_sided_book 15 · gate_entry_band 7 · presence_skipped 7 (5 late_entry) ·
  d3_ramp_capped 3 · explore_probe_capped 3 · netev_skipped 1 · gate_wide_or_asym 1 ·
  holding_exit_only 1 · loss_exitonly_stripped 2 (counters overlap per row).
  So ~22/39 rows this cycle (one-sided + entry-band) were refused for PRICE-VISIBLE
  reasons.
- Aggravating detail found during verification (INFERRED; per-row est is NOT logged —
  itself a telemetry gap, see OP-ADD-1c): the walk's est source treats a missing/stale
  score ref as "unknown -> max est" by design (quoter:5028-5032, audit lens 2 #3).
  One-sided/empty books tend to have no usable ref, so the rows MOST likely to gate out
  are charged the LARGEST est (bounded only by the ramp rung — a rung-4 gas strike
  charges ~$50 while one-sided-gated). The conservative-by-design choice interacts
  worst with exactly the gated-out class.

### (a) Cheap pre-weed at selection — price fields on the selection path
- Operator premise CHECKED, two corrections:
  1. Selection iterates INCENTIVE-PROGRAM rows (quoter:1988-2077) which carry NO price
     fields. Market objects are fetched lazily at quoter:2117 in the close/vol
     pre-filter — and that fetch DOES return populated prices, but the code keeps only
     close_time + volume_24h_fp; bid/ask are discarded.
  2. Field-name trap CONFIRMED LIVE (venue read 2026-08-06T19:19:40Z,
     /trade-api/v2/markets/KXTOPMODEL-26AUG31-CLAU5): plain yes_bid/yes_ask are None;
     yes_bid_dollars="0.1200"/yes_ask_dollars="0.1500" are the populated fields. A
     naive plain-name read silently pre-weeds NOTHING (canon *_dollars trap).
- Zero-extra-API-cost holds only OPPORTUNISTICALLY: the close/vol cache makes
  steady-state market fetches ~zero (comment quoter:2085-2088), so fresh prices are
  only free during cache warmup / new-market fetches. A standing pre-weed needs a
  price TTL cache (stale-price weed) — decided 95c-class strikes drift rarely, but the
  NO-BANS RULE requires the weed to be a deprioritize/discount with periodic re-check
  (TTL expiry re-admits the row for a fresh look), never a terminal drop. gate-refused
  books DO drift back to quotable.
- What it can and cannot catch (this cycle's composition): can catch the
  one-sided-book + entry-band classes (~22/39 rows); CANNOT catch presence/ramp/
  netev/probe-cap refusals (~11 rows) — those are not price-visible.
- EFFORT: S-M (capture *_dollars fields into the existing lazy fetch + TTL cache +
  discount hook in the walk's est or ordering; the no-bans-compatible shape is a
  discount, not a filter).
- EXPECTED VALUE: frees a large share of the ~$138 gap (198.00 used vs 59.77
  committed, 18:58:24Z row — single-cycle figures) for the budget walk to fund
  quotable markets; hard dollar value UNQUANTIFIED until (c) telemetry exists.
  HYPOTHESIS on magnitude; mechanism ESTABLISHED.

### (b) Within-cycle est release / discount for gated-out markets
- VERIFIED absent: single-pass walk, no release (see mechanism above). drop_budget_full
  7 (18:58:24Z row) = rows the budget refused while 33 admitted rows produced zero
  resting orders — the budget turned away up to 7 candidates while reserving for
  refused ones. Whether those 7 would have quoted is UNVERIFIED (they may gate out
  too).
- Two implementation shapes exist (findings-only, priced not built):
  (b1) true within-cycle second pass: after the quote loop resolves desired quotes,
       re-walk the budget over _budget_said_no rows and quote survivors same-cycle.
       Adds a second book-read/quote sub-cycle — latency + read-budget cost. EFFORT: M-L.
  (b2) carry-forward discount: persist last cycle's gate outcome per ticker; the next
       walk charges a gated-out row a discounted est (never zero — keeps the row
       re-checkable, no-bans compatible; discount decays/expires so recovered books
       re-charge honestly). One-cycle lag, near-zero cost. EFFORT: S.
- EXPECTED VALUE: bounded per cycle by drop_budget_full (7 at 18:58:24Z; distribution
  across cycles unmeasured) x est per row; more quoted markets -> more snapshot share
  -> credits. HYPOTHESIS on magnitude until measured.

### (c) (Found during verification, additive) Per-row est telemetry gap
- The walk logs only the total (select_budget_used); per-ticker _est9 is not recorded
  anywhere, so the waste decomposition (which rows hold how much reservation) cannot
  be measured from live data. A plans-row field (capped like family_dropped_tickers
  at :5099) would make (a)/(b) EV quantifiable from one day of data. EFFORT: S.
  EXPECTED VALUE: converts both items above from HYPOTHESIS-magnitude to measured.

RELATED PRIOR WORK (do not double-count): wave-1 ramp-aware est fix (_d3_est_ct,
quoter:5070, operator-ruled 2026-08-06) already closed the full-join-est over-read;
these items are the gated-out RESIDUE on top of it.


====================================================================================================
## S8. REVIEWER SUMMARIES (12)

### commits:0b+recorder+DB-DG
Reviewed all five commits in the worktree at HEAD e981bf1 (deployed == HEAD). Per commit: ae89543/40bfd31 (0b payout-timing tool) — does what it claims, read-only, canonical field names verified; but its BEFORE_CLOSE discriminator compares credit time to the EVENT's LATEST market close, so multi-market events can mint a false \"payment not gated on close\" canon (MAJOR); the 429 throttle fix works but non-429 network faults still abort the run (NOTE). 2f5fb07 (estimates recorder) — correct, read-only, client base-override verified; program-map write is non-atomic and a crash silently resets the merge-only map (MINOR). 4d76994 + f932797 (D-B/D-G fixes + follow-ups) — F5/F1/F3/F14/F17 each verified to do what their messages claim: own_orders is threaded through every run_once call site with the dry-run guard present and legacy byte-identity confirmed; F1's period source is the PROGRAM end_date (not market close; timezone clean); F14's grace map persists and restores on both paths; F17 keeps C8 coherence (effective wind-down can never dip below the settle-taker window). The 9 tests in kalshi_live/test_db_reward_fixes.py pin the fixed behaviors, would fail pre-fix by construction, and pass: 9/9, EXIT CODE 0. Two further MAJORs found in the surrounding code: (1) F3 makes the presence-floor gate depend on _D3_FIRST_SEEN restore ordering — under SELECT_BUDGET=0 a restart can deadlock the whole book at rung-0 (5ct) expectations, self-sustaining because refusal prevents the belt restore (latent; live believed to run SELECT_BUDGET on); (2) the share model's reference price is best-bid, not the venue's Target/5 cumulative-walk reference from the 08-05 formula pull — a pre-existing mismatch the F5-corrected shares inherit, over-weighting us on thin-top books and decoy-manipulable. Lesser: the WS daemon hot path (WS_HOT=1 only, default 0) still calls desired_quotes without own_orders (gate flips degrade hot repricing to no-ops, no wrong orders); F5 subtracts cycle-start standing that a mid-cycle fill invalidates for one anti-conservative cycle; F17's sub-hour benefit is half-eaten by the SETTLE_UNWIND_MIN=30 floor (58-min window: 28 min enterable, not ~46). Eight catalog opportunities listed; findings only, nothing fixed, nothing written.

### commits:DA+DC+followups
Adversarial review of 5d9266f (D-A est feed), 1dd427e (D-C macro-probe), 7e43554 (follow-ups) at worktree HEAD e981bf1 (deployed quoter == 7e43554). (1) DARK NO-OP: ESTABLISHED — every hunk traced; with KALSHI_EST_FEED=0 and KALSHI_MACRO_PROBE_TICKERS empty, all new paths sit behind falsy short-circuits (`if EST_FEED and...` :1334, `if MACRO_PROBE_TICKERS and...` :2867, empty-frozenset membership at :2004/:2041/:2145/:5360, macro-reason ramp exemption :646 vacuous with no macro quotes); the only observable dark delta is potential restart-only knob WARNINGS from _refresh_safety_knobs (:3877-3890) — prints only, zero order-path effect. No BLOCKER. (2) ARMED: matches spec — side cap min(MACRO_PROBE_USD/2, 0.9*HELD_MAX_USD) :697; max(model, venue_est) never-substitutes :1339; 30-min staleness fail-open; disk-only (recorder file/map shapes verified against kalshi_estimates_recorder.py, ids str-compatible, centicents/10000 correct); own-depth subtraction shape-correct vs own_resting :3538; downstream caps (cap_desired/bound_creates) are whole-market granularity so no partial clamping; ramp/explore/W7 clamps verified exempt or unreachable for macro; _reducing_quotes safely handles empty book sides on the macro inventory path. One armed-only MAJOR: the ladder's all-or-nothing rule exists only at build time — a post-only rejection of a crossing rung (reachable because designation exempts late-life/far-close, so a designated market can ride into decided/longshot territory) rests a partial, LIP-unqualifiable ladder at near-fair prices = pure fill risk. Minor armed findings: est floors the $150 activate path with partly-banked dollars (semantics OPEN until the 08-07T15:00Z APRPOTUS checkpoint; EST_FEED_MIN_FRAC only partially bounds it); full-file jsonl rescan every 120s; EST_FEED_MAX_AGE_S missing from the knob-warning map; live defaults deliberately rest nothing (side cap $18 < ~$20.85 ladder cost — operator must name MACRO_PROBE_USD + HELD_MAX_USD together; the test fixture HELD_MAX_USD=40 masks this). (3) TESTS: python -m pytest kalshi_live/test_da_est_feed.py kalshi_live/test_dc_macro_probe.py -q → 8 passed, EXIT CODE 0; they pin base D-A/D-C semantics but NONE of the thirteen 7e43554 follow-up behaviors. (4) KNOWN-OPEN assessed: #6-part2 is the one that matters (MAJOR when armed — no reserved footprint seat, compounded by the score cache folding macro-ladder cycles as capture measurements → self-reinforcing derank; detection = macro_probe_admitted vs macro_probe_markets divergence); #12 dust-suspend is minor-money/major-uptime (empty book → _reducing_quotes returns [] and strand-unwind skips footprint tickers → market rests nothing at all); #16 churn is minor on empty books (no count quantization, unlike join's multiple-of-5); #8 folds into the broader untested-follow-ups gap; D-B #9 inert now (sweeper consumer confirmed off) but pcap rows accumulate F5-biased — thread own-size or discard before Phase-3 consumes them; D-B #10/#11 are deliberate conservative anchors, opportunity cost only. Catalog: 8 opportunities, headlined by reserved seat (S), all-or-nothing placement (M), count quantization (S), and post-checkpoint banked-vs-marginal est decomposition (M).

### quoter:1-2600 select+gates
Full line-depth review of maker_kalshi_quoter.py:1-2600 (plus cross-references into the quote loop, budget walk, probe machinery, hot-reload, and state I/O) at HEAD e981bf1. Every line in range was read. Headline finding (MAJOR, ESTABLISHED): the entire reward model anchors the LIP Reference Price at the BEST BID (:2559, replicated in _qualifying_breakdown and the scorecard) while the venue-published formula (08-05) defines it as the Target/5 cumulative-depth level — no module implements the Target/5 walk — so the live $1.20 floor gate, SCORE_RANK cache, D2 basis, net-EV model fallback, and all capture telemetry sit on a systematically wrong distance anchor that overstates our share on thin-touch books (the sub-$1 drive-by admission class) and overstates the throttle-step reward cost. Supporting findings: floor-gate/sizing probe-clamp condition divergence for allowlisted L3/explore rows (models ramp size, rests 5ct); SERIES_ALLOW entries not whitespace-stripped (verified — one space silently benches a proven payer, unlike SERIES_DENY); activate-path floor lacks the JOIN path's resting-continuation exemption; floor continuation is amnestied by a one-cycle footprint absence (DROP_GRACE=0), stranding sub-$1 accruals; malformed env values silently revert to defaults and are invisible to env_absent; _envb treats 'true' as OFF for default-on protections like TAKER_FLATTEN (verified); PRESENCE_GATE and five other armed flags are absent from both hot-reload lists (silent mid-incident no-op); D-A est feed admits ended programs whose map row merely lacks end_date. Known-open assessments: D-C #6-part2 is confirmed and worse than filed (no budget-walk seat AND the walk under-charges the ladder ~7x — $5 est vs ~$36 committed); D-C #12 is a PERMANENT wedge for fills in the [3,5)ct band on an empty book (reduce-only can't rest, strand taker won't fire), not an until-strand pause; #16 churn and D-B #9/#11 confirmed as filed; #10 deferred (outside range). Recent fixes F1/F3/F5/F14/F17 verified correctly implemented in range, with F3's asym-join conservatism and F14's grace-map persistence both sound. Seven catalog opportunities, led by the Target/5 walk (M), accrued-credit floor memory (M), and a macro-probe arming pack (M) that should gate D-C arming.

### quoter:2500-5000 sizing+budget
Full line-depth adversarial review of maker_kalshi_quoter.py:2500-5000 (plus cross-referenced helpers and the quote/budget/create loops at 5000-6100) at HEAD e981bf1, worktree C:/Users/samwa/AppData/Local/Temp/claude/C--lockes-picks-polymarket-ai-v2/5dfe0ebf-2821-475d-946c-72012db34c3b/scratchpad/kalshi-wt. No BLOCKER: sizing, unwind exemptions, budget walk, committed-capital and funding gates, governor rungs, and halt arithmetic are internally consistent, and the recent F5/F1/F3/F14/F17 fixes are correctly threaded at every live gate call site traced. Four MAJORs: (1) the PRESENCE_GATE activate credit check is a constant-refuse for every FLAT activate (the model returns $0 on any sub-Target book, which is what void means when flat) \u2014 the activate/seed lane has been silently dead since the 08-02 fix, not gated; (2) the share model anchors its reference at best bid while the venue-published LIP reference is the Target/5 cumulative walk, systematically under-counting the rival denominator on thin-topped books, over-admitting through the live $1.20 floor and NETEV fallback, and skewing all capture telemetry feeding B8; (3) when D-C arms, an L3 series taint can mark a designated macro-probe ticker explore and the explore clamp (which, unlike the D3 ramp, has no macro_probe exemption) collapses the Target ladder to 5ct dust \u2014 receipts can never arrive while telemetry reports success; (4) the activate emitter's INV_HARD_CT clamp contradicts the dollar-based qualification gate, resting depth that cannot complete a side to Target (currently masked by finding 1). Five MINORs (cap_desired whole-market unwind-keep bypass relevant to PAIR_UNWIND arming; one-sided book via per-side create failure + J6 ratchet; throttle step-back band-drop; stale day_baseline_reset marker granting a next-day budget amnesty at cycle 2; NETEV fallback un-scaled by the D3 ramp) and four NOTEs. All seven known-open review notes assessed: D-C #6-part2 confirmed and broader (the PIVOT early-stop is a second crowd-out path), #8 still untested and demonstrably where a real bug hides, #12 confirmed worse than stated (whole ladder pulled, dust strands), #16/#9/#10/#11 confirmed at stated severity. Eleven catalog opportunities, S-to-M effort, headlined by the venue-true reference walk and the D-C arming preconditions. Review was read-only; static analysis only \u2014 no tests were run and no live data was queried, so reachability claims marked INFERRED were not exercised against the venue.

### quoter:4900-7317 exec+exits
Full line-depth review of maker_kalshi_quoter.py:4900-7317 (worktree C:/Users/samwa/AppData/Local/Temp/claude/C--lockes-picks-polymarket-ai-v2/5dfe0ebf-2821-475d-946c-72012db34c3b/scratchpad/kalshi-wt, HEAD e981bf1), with cross-references followed into the diff/amend/cap/bound helpers (:3370-3928), the halt/equity/day-baseline block (:4676-4937), kalshi_exit_calc.py, and maker_kalshi_client.py. Verified CORRECT against canon: taker fee is exactly 0.07*C*P*(1-P) with no rounding (kalshi_exit_calc:39, receipt-verified); taker exits map to the venue's netting semantics correctly (long-yes -> 'ask' at yes-bid = sell YES, long-no -> 'bid' at yes-ask; never a flipped-side resting entry); halt math is sound (dd = mark-equity drawdown from the day peak, N-of-5 confirmation window, torn-cycle freeze, deposit/credit-immune, day_baseline_reset marker consumed once on untorn cycles); F17's proportional wind-down floors at max(3, SETTLE_UNWIND_MIN), preserving maker-first ordering; F5 own-order threading and F14 d3 keep-grace persistence are consistent at every in-range call site; state persistence coverage is broad (strand/preclose/step/touch clocks, halt history, day baselines, d3 maps, taker-xn re-synced in finally). Focused pytest subset over the range's machinery: 74 passed. No BLOCKER. Three MAJORs, all in the exit machinery's failure branches: (1) the strand-unwind loop cancels a live resting exit on any transient book-read error (no retention, unlike the quote loop's identical case); (2) read-budget exhaustion mid-quote-loop cancels the in-flight market's standing book including exits; (3) the STOP flatten's pass 1 places fresh offsets over failed cancels, allowing stacked reducing size to cross through flat while halted. Plus 3 MINORs (client_order_id collision on exit re-rests, STOP-branch crosses invisible to the taker-governor/cooldown, no per-cycle market budget on strand crosses) and hygiene NOTEs (dead flatten_to_zero/oids_by_t, negative gated_out, at_ref telemetry measuring touch-distance not the LIP reference, marker surviving the day roll). Exits are otherwise never gated: capital cap, write budget (unwind-first), create ratchet, and failed-cancel deferral all carry correct unwind exemptions; the only theoretical write-budget starvation of exits needs >=400 cancels in one cycle (unreachable at current sizing). All 7 known-open notes assessed \u2014 the D-C items (#6-part2, #8, #12) should gate D-C arming; the D-B notes are safe to defer, with #10's absolute settle-ramp anchor worth aligning to F17's effective wind-down.

### daemon+client+wsfeed
Full line-depth review of maker_kalshi_ws_daemon.py (766), maker_kalshi_client.py (480), kalshi_ws_feed.py (395) at worktree HEAD e981bf1, with quoter-side contract verification (BOOK_SOURCE/_get_book shape, desired_quotes signature and F5 call sites, _standing_from_rows/own_resting shapes). NO BLOCKER in the currently-armed live paths (Stage A + client): auth/signing matches the proven scheme, writes are strictly send-once with no retry in both transport paths (no double-create route found), pagination is cursor-complete with loud truncation, and the Stage B safety lattice (cold_lock join, abort re-checks, cancel-response clamp, headroom reservation) verifies sound — the reservation asymmetry is conservative in every permutation. Two MAJORs, both latent behind flags: (1) the hot path's desired_quotes call omits the F5 own_orders threading that every cold call site has — arming WS_HOT as-built would ship a hot/cold quote-model mismatch causing perpetual reprice churn and queue-position loss; (2) the WS error-frame break path is the one disconnect class that neither dirties mirrors nor clears channel acks before its up-to-60s backoff sleep, so Stage C's mirror_book vouches for frozen books for the whole window, contradicting the daemon's own documented invariant. Supporting MINORs: seq-key disappearance silently disables all gap detection (the one dialect-drift axis PARSE_FAILS misses); no reconnect-driven ctx invalidation (missed fills leave hot trusting pre-fill inventory <=60s); unbounded feed_lat_ms growth in the long-lived daemon; apply_delta missing the price-range guard snapshots have; whole-connection teardown on any error frame (one bad ticker can pin the feed at heartbeat-grade latency); no 429/Retry-After handling; and the cancel_remaining_ct field scale — the load-bearing member of the anti-refill guarantee — has never been live-verified. Top catalog items by value: reference-price-walk (depth) triggers for requoting (the LIP share game moves on cumulative depth, not best price), a one-ticker-per-cycle mirror-vs-REST shadow compare that continuously re-proves Stage C's staleness predicate, and the three S-effort pre-arm fixes for WS_HOT (own_orders threading, reconnect invalidation, remaining-count live probe).

### recorder+ledger+estrec
Full line-depth adversarial review of the four money-measurement modules in C:/Users/samwa/AppData/Local/Temp/claude/C--lockes-picks-polymarket-ai-v2/5dfe0ebf-2821-475d-946c-72012db34c3b/scratchpad/kalshi-wt/kalshi_live/ (kalshi_cash_recorder.py, kalshi_attribution_ledger.py, kalshi_estimates_recorder.py, kalshi_fill_costs.py), cross-checked against maker_kalshi_client.py (_get_paginated/get_settlements/get_credit_history) and the live consumers in maker_kalshi_quoter.py (_est_feed_cached :727-781, _refresh_fill_costs :2664-2705). No BLOCKER: the deployed happy paths of the defect-13, defect-14 and W8 fixes are correct, fee handling has no fill/settlement double-count, centicent and revenue-cents conversions match canon, and the ACTION-ONLY sign trap is handled correctly everywhere. Two MAJORs: (1) kalshi_fill_costs' settled-market branch, on a replay fault, silently prices EVERY settled market at revenue-only (cost $0) instead of the documented fall-back-to-ABSENT \u2014 and its SETTLED_REPLAY_FAILS counter is never surfaced anywhere, so the capital-rank/net-EV feed can degrade to 'all settled losses are free' with zero symptoms in the live quoter path; (2) the estimates recorder's program map \u2014 the only ticker-resolution source for the est->credit join feeding the 08-07 checkpoint \u2014 is written non-atomically and silently resets to {} on any load failure, permanently losing ended-program entries that cannot be re-merged. Five MINORs cover the ledger's phantom-reward channels (strict-> watermark late-arrival drop with no settlement-side guard; silent 10k pagination cap; row-before-state crash double-count), the RULE-ZERO-banned residual surfaces still printed as MEASURED, and the cash recorder's lack of any in-process tape-integrity check on the series the P2 verdict differences. Nine cataloged opportunities, highest-value being the missing estimate->credit join harness (all raw inputs exist, no join exists) and in-band health counters in the feed files.

### scores+rank+feedback
Full line-depth review of the reward-math brain (kalshi_market_scores.py 303L, kalshi_capital_rank.py 201L, kalshi_credit_feedback.py 221L) plus the quoter formulas they contract with (_qualifying_score/_qualifying_breakdown/_prospective_capture/_expected_credit_usd/_capped_join/_alloc_priority), checked against the venue-published LIP spec. 86/86 module tests pass; recent fixes F5/F1/F3/F14 and A1/D1 verified correct at their live call sites (F17 not reviewed — flagged). Headline findings: (1) MAJOR/ESTABLISHED — the model's reference price is best-bid everywhere; the venue's Target/5 cumulative-walk reference is implemented nowhere, under-counting rivals near the top of thin books and inflating modeled share (anti-conservative into the live $1.20 floor gate, score cache, and net-EV fallback); (2) MAJOR/INFERRED — the scored set is truncated at cumulative Target, excluding deep rivals the spec scores at 0.5^ticks (same anti-conservative direction; both biases are baked into the fitted 3.0 haircut as a scalar, so cross-sectional ranking stays shape-biased); (3) MAJOR/ESTABLISHED — kalshi_market_scorecard still divides the pool by window days, the exact R1 form the canon refuted; (4) MAJOR/INFERRED — shadow_rank (the live allocation key under ALLOC_KEY) uses cached refs with no age cutoff for its commit denominator, the same stale-extreme-ref defect already fixed in the select-budget walk, so the ordering key and charging key disagree. Secondary: credit_feedback's never_paid_due quantifies due-ness only over our filled tickers (early-settled strikes can transiently convict a paying series), the D4 score-cache fold is coupled to MKT_TELEMETRY, the explore probe clamp guards diverge under an empty allowlist, sweeper paths still lack F5 own_orders, and the live allocation denominator is D3-ramp-blind. Pool math (period_reward/10000), estimates units (centicents/10000), *_fp field usage, credit amount_cents, and pagination discipline all check out. Eleven catalog opportunities listed, headed by implementing the true LIP kernel and re-fitting haircuts against the incoming estimates-feed ground truth.

### tools:netev+settlement
Full line-depth adversarial review of the four canon-feeding tools (kalshi_netev_rebuild.py, kalshi_netev_calibrate.py, kalshi_settlement_pnl.py, kalshi_presence_calibrate.py), their five test files (84/84 pass, read-only run), the shared replay model (kalshi_attribution_ledger), the fill-cost feed, and the quoter's NETEV_GATE consumption path. No BLOCKER: the live money path consumes the table correctly, the fill-direction sign trap is handled action-only and pinned, fee math never recomputes the taker formula, and the rebuild is not built on the defect-14-blind positions endpoint. Two MAJORs both threaten the B8 rebuild (~Aug 9-10) rather than today's orders: (1) markets still OPEN at API-read time are scored as pure loss — open collateral counts as cash-out with no offsetting value, the exact half-round-trip defect the module fixed at the window edge but left open at the read edge; with D-C macro probes and est-feed designations designed to hold far-close positions, a B8 run with open inventory benches receipt families on money not yet lost. (2) Whole-market attribution pulls pre-window launch-defect-era cash into any 'clean' window via straddling markets (weekly gas, far-close macro), laundering agent defects into family verdicts with no contamination metric — the precise failure the module's own docstring forbids. Six MINORs: a NO-side notional fallback that omits the price complement (optimistic-direction dilution on a venue field rename, currently unreachable — verified against real fill shapes), mixed units in netev_min_signal telemetry, a live-vs-settled fee-convention mismatch in the fill-cost feed (the venue pins cannot discriminate it — fixture fees are all zero), a stale 'defect 14 unfixed' comment sitting beside the shipped fix, presence calibration dropping unknown-life cost from family aggregates contrary to its own docstring, and an un-cursored credit_history read with no truncation tripwire. Twelve catalog opportunities, the highest-value being open-market exclusion + pre-window contamination metrics before B8, and the joint W12/HAIRCUT re-fit (the unproven-model path is currently the only one opening quotes, since all six receipt-grade families on the deployed table read negative and are benched at margin 0.0 — a known, operator-flagged state).

### tools:ledgers+w16+w17+w11
Full line-depth review of the six canon-feeding tools at HEAD e981bf1 (worktree kalshi-wt/kalshi_live), read-only; test_w11_replay 6/6 passes. No BLOCKERs — none of these tools touch the live order path. Four MAJORs: (1) kalshi_ledger_reconstruct.py re-implements the side-derived sign model the 07-23 attribution root fix refuted — verified against the real tape (542 fills, exactly (buy,yes)/(sell,no) shapes): all 265 'sell no' NO-acquisitions are booked as cash-IN/+yes-equiv, so its committed 'gross round-trips away / NET-within-market' evidence is corrupt; (2) kalshi_ledger.py's headline REWARDS is a residual — the RULE-ZERO-banned measurement class — under a docstring ('no API exposes this') refuted by the 08-01 credit_history canon, with a tautological identity check and unmarked open lots silently flowing into the reward number; (3) w11_replay's 'recorded' baseline is the shadow env-variant ranking, not the live selection — cyc['actual'] is logged but never read, so D2-vs-baseline replay conclusions measure shadow-vs-shadow; (4) w17's B-4 staleness detector uses a 'never-in-window' sentinel that lexically compares NEWER than every ISO date, silently suppressing the alarm for proven series with zero visible credits — the strongest rename/decay signal it has. Banned-field hunt: rewards_residual/rewards_alloc_by_series survive only in their producer kalshi_attribution_ledger.py (still written to ledger rows; no reviewed tool consumes them; kalshi_ledger.py imports only its HTTP helpers). Grain sweep: series-from-ticker splits are correct throughout; w16 escalates ticker-grain mkt_out bans to series-grain convictions (watch-only) and misses the mkt_out_backup union; the C-2 lineage table kalshi_lineage.json has zero readers/writers — rulings are inert. w17's UNKNOWN headline has two false-alarm vectors beyond the known counter-only class: a hardcoded 8-day horizon vs the quoter's env-tunable MAX_DAYS_TO_CLOSE (default 3.0), and the 40-ticker family_dropped truncation; EARNING is measured on intended not resting size (y/n_rest_ct available, unused). w11 fidelity: credits are lookahead-guarded but the never_paid fill set is frozen full-window (pessimistic bias for late-filling series), and policies can only reorder the shadow's top-60 components. Canon traps verified clean: fee_cost dollars, portfolio_value cents, settlement revenue cents, next_cursor + lowercase status=active, amount_cents/created_at, fills' integer ts field real (candidate finding retracted after tape check), no 3.13 scoping traps. Eleven catalog opportunities listed; highest-value: rebase the ledger's rewards on credit_history (S), true-actual baseline + time-sliced fills in w11 (S/M), env-derived knobs + resting-presence EARNING in w17 (S), per-ticker drop sampling in the quoter (M), and the D-A estimates-feed join for same-day coverage alarms (M).

### tools:drift-suite
All six drift files are FROZEN 2026-07-23 one-shot study artifacts (inputs: the 07-23 receipt CSV with explicit -04:00 offsets — timezone handling verified correct — and a cache built 07-23); nothing in the live money path imports them, so no BLOCKER is possible. I still did a full line-depth pass with data verification because their conclusions ('jump not drift', 'prior drift weak') feed canon and the queued D-E study. Two MAJOR measurement defects, both ESTABLISHED with quantified evidence from the frozen data: (1) build_timeline (detect.py:89) computes the value mid with none of the book-sanity the sibling fair() was audited into — the largest runover's headline 0.59 one-minute step is the mid of a 0.20/0.99 shell book (the exact book signal_fair_value_audit condemned), and 4/22 positives' worst step involves a spread>0.20 book, inflating jump_share; the last-trade fallback is additionally present but fired 0/4893 window minutes (latent). (2) Every windowed detector/score treats candle index as minutes while 750/4893 (15.3%) of in-window steps are gaps up to 59 minutes (36/67 positions) — all published TPR/FPR/AUC numbers are horizon-inconsistent, with a bias direction that flatters detectors, so the negative verdict survives qualitatively but no number is clean. Lesser findings: latent label-rule/docstring mismatch that can contaminate the temp_control denominator on a re-run (verified 0 hits on frozen data); f_at future-candle substitution leaking post-fill prices into the section-4 'prior drift' sweep (INFERRED impact; the book_side sign convention there is verified CORRECT per the attribution-ledger feed contract); resume logic freezing transient fetch failures as permanent survivorship holes (verified 67/67 usable, latent); blanket exception-swallowing that turns detector crashes into 'never fires'; and a latent gap-spacing miss in jump.py's decompose (verified all 21 worst steps sit on exact 60s spacing — lucky, not designed). Survivorship and label-leakage hunts otherwise came back clean: detectors are strictly trailing, matched-window frames are sound, 0 positions dropped. Six catalog opportunities, headlined by porting book-sanity + time-based windows into the harness before the D-E study reuses it, and re-running the study on the maker-era tape where n is no longer 6.

### flow:end-to-end trace
END-TO-END TRACE (live flags: D3_RAMP=1, D2=1, PRESENCE_GATE=1, NETEV_GATE=1 margin -7.0, FARCLOSE_PAYING_EXCEPTION=1, PIVOT_SELECT=1, ALLOW_PROBE_EXCEPTION=1/PROBE_MAX_SLOTS=5/EXPLORE_PROBE_CT=5; per 08-04/08-05 handoffs also SCORE_RANK=1, ALLOC_KEY=1, SWEEP_ENABLED=1, SELECT_BUDGET live, FUNDING_GATE=1, JOIN_SIZE=0, MAX_MARKET_CAPITAL=45, INV_HARD_CT=50; EST_FEED/MACRO dark). One cycle as executed: daemon on_book/on_fill sets cycle_req (5s min gap) -> cold_cycle holds cold_lock -> run_once: safety-knob hot reload -> STOP check (paced flatten path with its own read-budget reset) -> reads reset -> state load (strand steps, taker counts, equity_prev -> _TOTAL_CAP_EFF) -> plan seeded (A3 always-emit) -> programs fetch (refetched EVERY cycle; the daemon's 10-min cache is unused here) -> select_footprint: liquidity/null drops -> macro force-admit -> allowlist (non-members pass probe-only, explore-tagged) -> deny (allowlist wins) -> date/late-life with effective wind-down = max(30, min(45, 0.2*life)) (the SETTLE floor dominates short windows) -> far-close program cap with the FIX-H receipts exception -> usd_day = period_reward/10000 -> market-clock prefilter (close+vol cached, budget <=100 reads, B-1 past-close kill) -> probe slot cap (5, refusal-streak rotated) -> SCORE_RANK rank (explore quota, allowlist exempt; D2 is NOT in this key) -> PIVOT pool (coverage floor + density remainder; ALLOC_KEY=1 keys series rotation on rank order) -> L3 series-probe taint (tags ALLOWLIST siblings explore) -> far-close belt -> alloc_prio = shadow_rank (capture > pcap > pool bases, D2 bonus/never-paid mult, fill-cost lambda, est_commit denominator) -> live standing read (fail -> blackout guard), positions read (fail-closed), loss-governor ladder ($3 exit-only / $5 OUT, settlement top-up, departed-ticker carry), torn-read-proof mark equity -> day-peak drawdown halt (3-of-5 confirm, marker-based baseline reset) -> breaker (naked velocity/level) -> settle-taker (naked-only capped cross, cancel-confirmed -> cross -> re-rest) -> SELECT_BUDGET walk (est = min(commit_est, ramp-ct-as-$, probe-ct); held/stripped charged 0) -> quote loop over the pool until FOOTPRINT_TOP quoted: _get_book (WS mirror declinable) -> desired_quotes gate order: bad clock -> wind-down (reduce if held) -> crossed book -> MACRO bypass -> one-sided/entry-band (reduce if held) -> unqualifiable (Target reach) -> PRESENCE $1.20 floor (F1 period unit, F3 ramp scaling, EST_FEED floor when armed, entry-not-continuation) -> NETEV (receipt fraction vs margin; unproven -> model pc/3 - $5) -> capture/standdown (off) -> wide/asym -> JOIN both sides at reference with stand-down/settle-ramp/event-throttle shaping -> explore probe clamp (5ct) -> D3 ramp clamp (rungs 5/10/25/50, W7 receipt clamp; macro/unwind exempt) -> governor/incumbent strips -> telemetry row + D4/D8 score fold (full-size recompute) -> PIVOT collapse footprint=consumed -> strand unwind for off-footprint held -> ladder escape hatch (latent) -> breaker reduce-only shape -> PAIR_UNWIND (off) -> at-ref split telemetry -> probe refusal streaks -> drop grace (off) -> cap_desired (unwind unconditional; family 25%-of-equity cap seeded with held $; total cap = min(350, mark equity)) -> split_amends (off) -> diff_orders (exact side/price/count) -> bound_creates (unwind-first, 400 ops) -> execute: amends -> cancels -> creates (unwind first, create-fail ratchet, failed-cancel deferral, per-order capital/funding gate) -> preclose flatten (off) -> strand cross (exit-calc ladder, sweep veto/trend arms, taker capped at FRESH naked) -> plan/state persisted in a step-guarded finally (taker-xn re-sync, close-cache snapshot, D3 first-seen F14-graced prune, score evict/save). Restart path: close-cache restore (jittered TTL), D3 first-seen + grace-map restore (walk-time ensure + quote-loop belt), probe streaks, incumbents, equity_prev, day-baseline marker consumed once on an untorn same-day cycle; ctx.build re-runs select_footprint after every cold cycle for the (dark) hot path. FINDINGS: two MAJOR live-relevant cross-module misalignments - (1) NETEV margin unit mismatch (fraction-valued table vs percent-scale knob -7.0: the receipt-benching arm cannot fire on any realistic family, and the in-code margin ladder is 100x off, priming a silent no-op at the B8 re-rule) and (2) read-budget exhaustion mid quote loop tearing down the unread tail's standing book including held-ticker exits (RuntimeError bypasses the fetch-fail retention; latent on the 33-row pilot, armed by allowlist widening); one MAJOR dark-flag mechanism (macro-probe budget under-charge + per-order capital gate -> a PARTIAL Target ladder that pays nobody while carrying fill risk); four MINOR (F3 ramp-mirror allowlist carve-out vs the unconditional sizing clamp on L3-tainted rows; the netev model arm not ramp-scaled; settle-ramp anchored on raw WIND_DOWN_MIN=45 vs effective 30-36; the sweeper's F5-less pcap both unthreaded AND live-consumed - the 'consumer gated off' premise of D-B #9 is wrong under ALLOC_KEY=1+SWEEP_ENABLED=1); five NOTEs (hot-path own_orders gap, watch-list alphabetical truncation at 80, bound_creates unwind starvation at 400 cancels, remaining_count 0-falsy fallback, PIVOT earner-count semantics). All seven known-open items assessed (D-C #12 confirmed as a PERMANENT dust deadlock: a 3ct fill >= INV_TOLERANCE pulls the ladder, an empty book yields no reducing quote, and the strand cross needs 5ct - money-trivial but thesis-killing; #6-part2 real with the partial-ladder sharpening; #8 a real selection-hunk test gap; #16 minor; D-B #10 promoted to a finding; #11 conservative-only). Recent fixes verified sound: F5 own-orders threaded through presence/netev/capture/telemetry with the dry-run guard; F1 period-unit and F3 clamped-size scaling correct in _expected_credit_usd (modulo the allowlist-carve-out mismatch); F14 keep-grace persisted on both restore paths; F17 wind-down floored at SETTLE_UNWIND_MIN (which itself still gates short windows). Targeted suites run read-only: test_da_est_feed + test_dc_macro_probe + test_db_reward_fixes + test_d3_ramp = 25 passed. NOT verified: actual live.env values (NETEV margin -7.0 and the ALLOC_KEY/SWEEP/SELECT_BUDGET states come from handoff docs, not a VPS read) - operator verify with: grep -E 'NETEV_MIN_MARGIN|ALLOC_KEY|SWEEP_ENABLED|SELECT_BUDGET|SCORE_RANK' /opt/pa2-maker-kalshi-live/live.env.
