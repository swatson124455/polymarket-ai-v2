# MB HYGIENE REVIEW - both bots, every pathway, money-logic standard

**Generated 2026-08-25 (~17:05Z).** Operator charter: both bots if used;
ratification is not a why - money logic only; unmeasured -> measured;
report-only; live traces; elevation test per item. Method: 9 pathway
auditors + adversarial verification (165 steps, 138 confirmed; the
legacy-exits verifier infra-failed - that pathway is SINGLE-SOURCE,
marked [u]) + a live measurement battery with adversarial re-verification
(see MB_STATE 2026-08-25 16:45Z block - incl. the WITHDRAWN spread-gate
headline and the dedup journal-horizon resolution). Full step tables:
`docs/mb_hygiene_findings.json` (165 steps, every what/why/works/evidence).
Nothing here changes anything - every action is operator-gated.

## VERDICT COUNTS

| pathway | steps | DEFECT | why-FLAG | verified |
|---|---|---|---|---|
| v3-chain | 25 | 4 | 2 | 24/25 |
| v3-rtds | 19 | 1 | 2 | 19/19 |
| v3-gates | 10 | 3 | 1 | 8/10 |
| v3-readout | 18 | 6 | 1 | 18/18 |
| legacy-signal | 15 | 4 | 5 | 12/15 |
| legacy-gates | 26 | 6 | 2 | 26/26 |
| legacy-exits | 21 | 7 | 2 | 0/21 |
| legacy-sizing | 21 | 6 | 2 | 21/21 |
| cross-system | 10 | 3 | 3 | 10/10 |

## ALL DEFECTS AND FLAGS (details + evidence in the JSON; [u]=unverified)

### v3-chain
- **[why-FLAG/elev-FLAG][V] Boot 2: DB init + restore_state gate the watcher** - As coded it does gate: any DB/restore failure returns 1/3 and the watcher never starts; systemd restart loops repeat the DB attempt while chain detection stays dark. Combined with the non-persistent cursor (below), each such cycle is a permanent sample gap.... (evidence: mirror_v3/run.py:45-99, :104-111; mirror_v3/state_restore.py:46-78; copy_watcher.py:48-50 ('no DB writes'))
- **[DEFECT][V] WatcherConfig.from_env — explicit-presence wiring** - One coupling escapes the rule: MIRROR3_BIDSIM=1 with MIRROR3_RTDS_AB unset passes from_env, yet bidsim FILL and EXPIRE processing lives only in rtds_watch (on_print called only at :874-878, sweep_expired only at :885) — the chain path only registers (:652-661). That config yields a bid registry that... (evidence: copy_watcher.py:353-386, :52-62)
- **[elev-FLAG][V] Conviction median seeding from cache** - Seeding aggregation (per-tx sum of size*price, BUYs only, sorted by timestamp) matches the live path's merge_same_tx estimator — consistent estimator, verified by reading both. Cosmetic: the cold-start log hardcodes '20' (copy_watcher.py:526) instead of MIN_MEDIAN_OBS.... (evidence: copy_watcher.py:518-526; sizing.py:39-40, :82-117)
- **[DEFECT][V] rpc_call timeout wrapper + ensure_client** - The contract ('EVERY web3 RPC await', :306) is violated once: the cursor init at copy_watcher.py:552 — `cursor = int(await bc.w3.eth.get_block_number()) + 1` — is the ONLY unwrapped RPC await on the path (verified by reading every await: :538, :546, :562, :570, :596, :628, :645 are wrapped). Failure... (evidence: copy_watcher.py:302-317, :537-538; base_engine/data/blockchain_client.py:108-176)
- **[DEFECT][V] Block timestamp fetch + fallback** - The fallback silently writes detect_lag_s≈0 with no marker distinguishing measured from fabricated. Failure scenario: a flaky RPC afternoon makes get_block fail on 5% of signals -> the lag distribution grows a spurious spike at ~0s -> p50/p90 lag understated -> the latency case for/against the RTDS ... (evidence: copy_watcher.py:644-649, :389-405)
- **[DEFECT][V] FirstBuyDedup** - The flag implements per-PROCESS first, not per-HISTORY first: _seen dies with the process, and canonical readouts consume the flag as-is (mb_canon.py:69, band_tracker.py:59). Failure scenario: service restarts at T; a rostered trader who bought token X last week adds to the position at T+1h; the rec... (evidence: copy_watcher.py:444-457, :650-651; consumers: scripts/mb_canon.py:69, scripts/band_tracker.py:59 (filter on first_buy AN)
- **[elev-FLAG][u] Conviction annotation (medians + multiplier)** - Mechanically correct and tested (tests/unit/test_mirror3_stress.py:313-340). But the branch's output is consumed by NOTHING: no script computes the pre-registered Option-D Spearman gate (grep cited in the seeding step). ~13 months of annotation with no readout is instrument-without-a-dial.... (evidence: copy_watcher.py:663-671; sizing.py:34-53, :19-23)
- **[why-FLAG/elev-FLAG][V] Dead code on the module: decode_fill (V1) + get_logs_compat** - Both functions are internally correct (tested), just unreachable from the pathway.... (evidence: Grep 'decode_fill\b|get_logs_compat' across *.py: only callers are tests/unit/test_mirror3_copy_watcher.py:24-49 and :31)
- *Artifact branches:* The pathway as a whole is NOT an artifact: the shadow sink is the most-consumed dataset in the repo (grep 'mirror3_shadow' -> 20 files; readers with default --log wired to it: scripts/analyze_shadow.py:348, scripts/shadow_readout.py:1070, scripts/band_tracker.py:166, scripts/mb_canon.py:69, scripts/cohort5_qualification.py:292, scripts/build_fee_map.py:36, scripts/canon_verify.py:42, scripts/rtds_

### v3-rtds
- **[DEFECT/why-FLAG/elev-FLAG][V] 8. Bidsim's fill/expiry engine lives ONLY in rtds_watch — unguarded coupling** - Latent, not active (both flags are on in the current deployment, MB_STATE.md:240,:271-276). Failure scenarios: (a) MIRROR3_BIDSIM=1 + RTDS_AB off -> chain path posts bids that can NEVER fill and never expire until a restart's rehydrate emits 'expired across restart' (:1084-1089) -> a fabricated ~0% ... (evidence: grep on_print/sweep_expired: copy_watcher.py:873,885 only; :652-662 (chain register-only); :361-366 vs :383; run.py:131-)
- **[why-FLAG/elev-FLAG][V] 14. Conviction annotation on the RTDS stream** - Mechanically correct (stats-before-observe order preserved, same A+D rule). The flag is on purpose, not correctness: as A/B parity evidence the fields are misleading (different semantics), and as conviction evidence they are consumed by nothing.... (evidence: copy_watcher.py:919-926; sizing.py:34-53, 56-79; grep: build_fee_map.py reads only token_id from this sink (shadow_token)
- *Artifact branches:* No branch of this pathway is consumed by nothing — but two sub-items are near-artifacts. Greps performed: (1) grep "mirror3_shadow_rtds|rtds_sink|RTDS_SHADOW_PATH" across the repo -> sole scripted consumer of the RTDS sink is scripts/build_fee_map.py:275 (and it extracts ONLY token ids, build_fee_map.py:143-147); shadow_readout.py:1070, band_tracker.py:166, cohort5_qualification.py:223 read the ch

### v3-gates
- **[why-FLAG][V] 1. Gate-knob config load (cents → dollars)** - Parsing/scaling is correct and test-pinned (tests/unit/test_mirror3_copy_watcher.py exercises from_env). FLAG on why: 2c and 5c carry ZERO stated measurement provenance anywhere in repo — the repo's own review file says exactly this (docs/mb_overhaul_review_findings.json finding 'Gate knobs max_chas... (evidence: mirror_v3/copy_watcher.py:337-339, :375-377; deploy/env.mirror3.example:37-39)
- **[DEFECT][V] 3. Quote the book: quote_book → (best_bid, best_ask) + crossed-book sanity alarm** - The quoting itself works (side mapping pinned by scripts/verify_clob_price_sides.py). DEFECT = failure-mode conflation: every exception (timeout, transport, 500) is indistinguishable from a genuinely empty book — both become ask=None → NO_BOOK (:257-258) with no counter, streak detector, or alarm, v... (evidence: mirror_v3/copy_watcher.py:461-485, :408-423, :672-676)
- **[DEFECT][V] 6. Gate: PRICE_NO_UPSIDE (best_ask > max_fill=0.98; added 2026-08-19)** - The gate fires correctly in the live watcher (test-pinned :512-517; ~92 blocks in first ~3h per finding 85). THE DEFECT IS DOWNSTREAM AND UNDOES IT: analyze_shadow.repair_record re-derives verdicts from ladders with only NO_BOOK/SPREAD/CHASE branches — NO PRICE_NO_UPSIDE, no max_fill parameter anywh... (evidence: mirror_v3/copy_watcher.py:243-260, :339, :678-680, :933-935; docs/MB_STATE.md:477-492, :560-564, :595-601, :630-649; scr)
- **[DEFECT][u] 7. Gate: SPREAD_TOO_WIDE (ask − bid > max_spread=0.05; self-disables when bid is None)** - Two-sided logic works and is test-pinned (boundary: exactly 5c passes). DEFECT = the self-disable contradicts the gate's own why: the trades it most wants to block (no bids = effectively infinite spread, zero exit liquidity, no mid anchor) are exactly the ones it waves through, and they are indistin... (evidence: mirror_v3/copy_watcher.py:261-262; tests/unit/test_mirror3_copy_watcher.py:63-65; docs/MEASUREMENT_CANON.md:91-92 (bias )
- *Artifact branches:* The pathway as a whole is NOT an artifact — it is alive at both ends. Producer: run.py starts watch()/rtds_watch() when MIRROR3_COPY_WATCHER is set (mirror_v3/run.py:106-118). Consumers of the sink (grep 'mirror3_shadow' → 20 files): scripts/analyze_shadow.py:348 (default --log), shadow_readout.py:261, band_tracker.py:82-84, stack_vs_firstbuy_forward.py:59, cohort5_qualification.py, build_fee_map.

### v3-readout
- **[DEFECT][V] 4. Measurement repair (repair_records) — the drop/keep filters** - Two divergences from the live gate set. (1) REALIZED, known: the watcher's 2026-08-19 max_fill=0.98 gate (PRICE_NO_UPSIDE, copy_watcher.py:259-260, default :339) is ABSENT from repair (analyze_shadow.py:95-102 has no branch), so every current-era ladder-armed record the live watcher gated at ask>0.9... (evidence: analyze_shadow.py:77-122, :380-384; watcher gate mirror claim :83-85; gates copy_watcher.py:241-265)
- **[DEFECT][V] 7. Label join B — CLOB supplement + zero-row refusal** - shadow_readout: works as designed. band_tracker and cohort5: the zero-row guard is bypassed — if the supplement file exists and parses but happens to label 0 of the current shadow tokens, both scripts silently grade on flattered DB-only labels and can mint IMMUTABLE locks (band PASS, cohort5 QUALIFI... (evidence: shadow_readout.py:190-227, :485-504; band_tracker.py:87 (no zero-check); cohort5_qualification.py:139 (no zero-check); f)
- **[DEFECT/elev-FLAG][V] 8. Label merge precedence** - The canonical conflict-detecting merge exists, is tested, and is consumed by nothing; every live label join — including the NEWEST instrument (C1 group, registered the same day as the canon) — still silently resolves conflicts DB-wins. The LABEL CHECK in canon_verify partially compensates (samples 8... (evidence: shadow_readout.py:230-236 (used at :505); band_tracker.py:88; cohort5_qualification.py:140; mb_canon.py:97-113; grep: me)
- **[DEFECT][V] 9. Label provenance disclosure** - Works in shadow_readout; absent in the two lock-minting scripts whose labels are most at risk (they also bypass the zero-row guard, step 7) — the disclosure is missing exactly where it matters most.... (evidence: shadow_readout.py:239-247, :548; band_tracker.py:100-111 (no provenance in output); cohort5_qualification.py:131-137, :1)
- **[DEFECT][V] 10. Fee assignment (three-tier precedence)** - Precedence chain itself traced correct and identical across implementations. Two defects: (a) band_tracker/cohort5 fee-map loads have no corrupt-file guard — a corrupt JSON raises an undisclosed traceback which the band cron grep then swallows entirely (silent equation-change class the shadow_readou... (evidence: analyze_shadow.py:163-187 (docstring), :225-234; mb_canon.py:37-47; band_tracker.py:68-74, :89-92; cohort5_qualification)
- **[why-FLAG/elev-FLAG][V] 11. Edge computation — the estimand (per-fill vs per-market, current state)** - Each implementation does what its own docstring says; the DEFECT is systemic: (a) the daily readout and the original-20 qualification still grade/lock on the fill-weighted number printed under the bare name 'edge', violating the (draft) naming law with no in-output tag; (b) charter-lock justifies ke... (evidence: analyze_shadow.py:214-239 (per-fill: vals flattened across tokens :236, edge :239); cluster bootstrap token-resampled bu)
- **[DEFECT][V] 17. Eligibility gate (cohort5 eligible_admits)** - The union cannot represent a DEMOTION: if the re-review regraded a base-dir ADMIT to REJECT (its whole purpose — regrading on complete labels, which previously flipped flattered numbers negative), the stale base-dir ADMIT file still admits the trader — the loop adds any ADMIT from either dir and nev... (evidence: cohort5_qualification.py:80-97)
- *Artifact branches:* The pathway as a whole is CONSUMED end-to-end — no removal candidate at the script level: shadow_readout.py runs daily via deploy/shadow_readout_cron.sh:26 and its log/ALERT files are the documented steward trigger; band_tracker.py, cohort5_qualification.py, canon_verify.py run via deploy/label_and_fee_refresh_cron.sh:25,:30,:39; mb_scoreboard.py:32,:48 consumes the [band] line and cohort5_qual_lo

### legacy-signal
- **[why-FLAG/elev-FLAG][V] 2. Scan-loop wiring + WS market-channel handler registration** - Cannot tell from code whether Polymarket CLOB market-channel payloads ever include user.address (docstring at elite_watchlist.py:723-735 asserts it; public channel is generally anonymous). If they don't, the registration is inert weight that only inflates _events_received; if they do, dedup by trans... (evidence: mirror_bot.py:863-901; websocket_manager.py:298-313 (dispatch), :208-221 (subscribe_market))
- **[why-FLAG/elev-FLAG][V] 4. 'Elite' definition — live watchlist build (refresh_watchlist)** - Does what it codes: builds a 300-address set from the monthly PnL board with graceful 3-tier fallback and keeps stale list when all sources fail (:478-480). But it does NOT do what the module's own design docstring says it does (per-category, ROI-ranked, quota'd, daily) — needs_refresh is 6h (:691) ... (evidence: elite_watchlist.py:457-508 (build), :121-184 (fetch), :495 (efficiency), :512-530 (num_trades supplement); getting on = )
- **[DEFECT/elev-FLAG][V] 5. S155 per-category pipeline (stage1-4, sunset list)** - Never executes. refresh_watchlist() (:457) calls _fetch_monthly_leaderboard, not the stages; nothing else in the repo calls them. The module docstring (:8-12) advertising 'per-category diversified selection with ROI ranking and hard quotas' describes code that cannot run. Also stage3's rate-limit sl... (evidence: Definitions elite_watchlist.py:186-453; grep for callers of _stage1_category_pulls|_stage2_dedup|_stage3_enrichment|_sta)
- **[DEFECT/elev-FLAG][V] 6. M2 inactivity decay** - _last_trade_time is an in-memory dict of time.monotonic() values — never persisted. Every process restart erases it, and traders with no entry are explicitly kept as-is (:540-541). The 14d/21d thresholds can therefore only ever fire after ≥14 days of continuous uptime; if MB restarts more often (dep... (evidence: elite_watchlist.py:532-552, :990-991, :69)
- **[DEFECT][V] 9. on_trade_event — parse, bounds, side resolution** - The _get_token_side fallback 'return YES' (mirror_bot.py:286) means a whale's NO-token BUY in any market absent from the local markets table (or with unmatched token ids) is copied as a YES entry — the exact opposite position, silently. Failure scenario: RTDS event with empty/unrecognized outcome st... (evidence: elite_watchlist.py:737-797; mirror_bot.py:247-286)
- **[DEFECT/why-FLAG/elev-FLAG][V] 12. Signal confidence construction (efficiency bonus)** - Contradiction between stated purpose and consumption: grep of _raw_upstream (3 hits: :3604, :3848, :4277) proves logging-only. Failure scenario is epistemic, not monetary: a tuner adjusting the 0.55/0.15/0.70 constants changes nothing but a log field. Efficiency DOES still reach sizing, but via a di... (evidence: elite_watchlist.py:847-855; downstream: mirror_bot.py:3604 captures it as _raw_upstream, consumed ONLY at :3848 (log fie)
- **[why-FLAG][V] 14. Post-execute bookkeeping in on_trade_event** - Persist task pattern is correct (S156: ref held, errors logged, not awaited in hot path). The stub block (:899-908) is unreachable in practice — sequentially after a successful execute, _pos_key is always present (inserted at mirror_bot.py:4437). It is not harmful, but it is dead weight whose commen... (evidence: elite_watchlist.py:877-932; mirror_bot.py:4433-4445 (success path already inserted the position with real filled size BE)
- **[why-FLAG/elev-FLAG][u] 15. Parallel legacy remnant: elite_traders list** - The code runs and populates the list; it simply serves nothing. Note the adjacent reliability-tracker refresh (:852-861) inside the same block IS load-bearing (feeds _base/F1 at :3482-3543) and must survive any cleanup of elite_traders.... (evidence: grep 'elite_traders' in mirror_bot.py: 7 hits — init :79, refresh condition :844, log line :965 ('MirrorBot scan: elites)
- *Artifact branches:* YES — four branches of this pathway are consumed by nothing (all greps run repo-wide on C:/lockes-picks/mb-steward). (1) The entire S155 per-category pipeline: grep '_stage1_category_pulls|_stage2_dedup|_stage3_enrichment|_stage4_rank_and_select|_update_sunset_list|_sunset_traders|_category_watchlist|_shadow_last_comparison' returns only definitions and __init__ fields (elite_watchlist.py:96-99, 2

### legacy-gates
- **[elev-FLAG][V] 4. Trader wallet blacklist** - Prefix matching verified: settings entries are 10-char prefixes, lowercased both sides, compared against trader_address.lower()[:10]. Correct.... (evidence: mirror_bot.py:2952-2967; config/settings.py:455-458)
- **[DEFECT][V] 7. Per-market cooldown (entry check)** - Comment/config contradiction: the S172 D8 comment (:3011-3012) says cooldown was 'increased to 24h (86400s)' and the check-site getattr fallback is 86400, but Settings DECLARES the field with default 1800 (settings.py:445), so both sites read 1800 (30 min) unless the VPS env overrides. The 86400 fal... (evidence: mirror_bot.py:3013-3026 (check), :4422-4426 (set), :2485-2525 (persist/restore); config/settings.py:445 (default 1800))
- **[DEFECT][V] 16. Market-quality composite gate (S168 Phase 7)** - Line 2440 `_factors = [f for f in [...] if f > 0]` drops any component that reaches its WORST value (0.0) from the geometric mean instead of letting it zero the score: (a) spread ≥ 0.15 → _f_spread=0.0 → dropped → catastrophic spread contributes NO penalty here (the separate ≥0.25 hard block at :366... (evidence: mirror_bot.py:3419-3434 (gate), :2389-2443 (score), :2440 (the f>0 filter), :2565-2609 (IE refresh))
- **[DEFECT][V] 17. Stale-input hard blocks: spread and NO-heavy-favorite (S153/S154, run in BOTH scoring modes)** - The trader-WR block works. The spread and NO-fav blocks key off DB index prices that the project's OWN S244 finding measured as ~92% >7d stale — S244 fixed only the ENTRY price (_fresh_side_price), not the spread/no_price inputs to these gates. Failure scenario: a market whose index row is 57 days o... (evidence: mirror_bot.py:3390-3395 (_spread from yes_price+no_price), :3681 (_nfp from no_price); staleness documented at config/se)
- **[elev-FLAG][V] 19. Reliability multiplier + multi-factor confidence + split scoring + mode toggle** - Arithmetic traced and internally consistent. But WHICH regime decides is env-dependent: with the settings default (false), gate_score, kelly_prob, and ALL NINE weighted factors are decision-inert — trades are decided by _old_confidence (clamped 0.35-0.75) vs min_confidence 0.55, plus the 3 embedded ... (evidence: mirror_bot.py:3479-3607 (factors), :3612-3825 (split block), :3828-3837 (toggle); config/settings.py:494)
- **[why-FLAG/elev-FLAG][V] 20. Stale '4h gate' comment — no hard TTR gate exists** - Whether soft-only TTR handling is adequate is unmeasured. A signal at TTR=0.5h with good volume/spread passes: quality ≈ geomean(1,1,0.30,0.5)≈0.62 > 0.30, and in legacy mode _ttr_adj -0.05 is easily offset by conv+copy adjustments.... (evidence: mirror_bot.py:3568 (stale comment); grep 'hours_until_resolution|4h' across mirror_bot.py+elite_watchlist.py shows only )
- **[DEFECT][V] 21. Signal enhancements skip + calibration stack + pre-gate prediction log** - Mechanics work, but the prediction_log row records predicted_prob=kelly_prob and confidence=gate_score (:3928-3932) even when legacy mode is live and the DECIDING number is _old_confidence — so calibration/drift analyses built on prediction_log (PredictionDriftDetector :2540-2563 consumes it) measur... (evidence: mirror_bot.py:3879-3886, :3891-3916, :3923-3935; config/settings.py:420)
- **[DEFECT][V] 23. Flat sizing + Kelly-shadow side effect** - The 'comparison-only' Kelly call has a hidden side effect: calculate_bot_position_size stashes _last_intended_size_usd (base_bot.py:598), which place_order reads and writes into event_data['intended_size_usd'] (base_bot.py:342-348) → order_gateway persists it (order_gateway.py:966) → scripts/counter... (evidence: mirror_bot.py:4009-4031; base_engine/risk/bankroll_manager.py:39; base_bot.py:588-598)
- **[DEFECT/why-FLAG/elev-FLAG][V] 25. Per-market cap, daily-remaining cap, S218 invariant, S227 dust clamp-UP** - Three findings. (1) NULLIFICATION: at default cap $300, floor = $25 and flat = $30, so the entire sizing chain of step 24 — NO dampener 0.15-0.75x, risk budget 0.15-1.0x, adaptive safety — produces $4.50-$30, then everything below $25 is clamped back UP to $25: effective sizes live in [$25,$30], a 1... (evidence: mirror_bot.py:4169-4259; config/settings.py:395-397; bankroll_manager.py:39)
- *Artifact branches:* The pathway as a whole is consumed: entries flow to place_order → base_engine/order_gateway, and the rejection sink is real — `grep -rln "mirror_rejected_signals" --include="*.py"` hits live consumers bots/mirror_backtest/replay.py, bots/mirror_backtest/data_access.py, bots/mirror_scoring/validation.py, scripts/backtest_tail_leaderboard.py, scripts/crypto_kill_test.py (plus writer database.py:3396

### legacy-exits
- **[DEFECT][u] 3. Mark refresh: DB price sync + RTDS overlay** - Works for covered tokens. DEFECT (gap, feeds the Step-13 deadlock): a token outside position_manager's ~500-market WS subscription, absent from market_prices_latest, and with no RTDS trade in the last 300s keeps its stale mark FOREVER — exactly the profile of the stuck 678h position whose mark sat a... (evidence: bots/mirror_bot.py:1102-1128, :1200-1270 (guard condition :1233, max_age :1225))
- **[DEFECT][u] 7. Exit rule: absolute force-exit at 96h** - The TRIGGER works (fires every scan once >= 96h). The GUARANTEE is broken: 'force' exit is not forced — it routes into the Step-13 slippage guard which can reject it forever (the live 678h position IS a force-exit loop, MB_OVERHAUL_REVIEW_AGENDA.md:27-32). A force-exit that can be vetoed by a price-... (evidence: bots/mirror_bot.py:1288, :1366-1377; grep MIRROR_FORCE_EXIT_HOURS config/settings.py = 0 matches)
- **[DEFECT/why-FLAG/elev-FLAG][u] 11. Execution phase entry: dedup, backoff skip, exit-price selection (the stale anchor)** - This line is the root of the deadlock: for the stuck position the anchor was 0.68 while the tradeable bid was 0.31. Everything downstream (gateway walk, paper guard) is then measured against a fiction. validate_price only checks 0<p<=1 — it cannot catch staleness.... (evidence: bots/mirror_bot.py:1460-1489; bots/base_bot.py:381-391 (validate_price is range-check only))
- **[DEFECT][u] 12. Pre-SELL sanity chain: zero-size force-close, WI-6 terminal pre-check, A.5 live-bid drift re-validation, live balance guard** - (a),(b),(d) traced and work. (c) is HALF-BUILT and this is the second half of the deadlock: the code FETCHES the true bid (0.31), logs 54% drift — and then for force_exit/take_profit/max_hold/trader_sell reasons proceeds to submit at the STALE price anyway (:1694-1699 uses exit_price, never _live_bi... (evidence: bots/mirror_bot.py:1491-1692, :2078-2124, :2126-2212)
- **[DEFECT/why-FLAG/elev-FLAG][u] 13. THE DEADLOCK: paper-engine directional adverse-slippage guard vs its own force-exit SELL** - DEADLOCK MECHANISM (complete): stale mark (Step 3 gap) -> hides loss from hard-stop/SL -> position ages to 96h -> force_exit claims it (outranking SL, so A.5's mark-refresh branch is unreachable) -> SELL priced at stale anchor -> gateway walks real bids, VWAP == truth -> paper guard measures truth a... (evidence: base_engine/execution/paper_trading.py:21, :456-463, :513-521, :560-574; base_engine/execution/order_gateway.py:1013-103)
- **[DEFECT][u] 17. Terminal close: _close_position_terminal (Bug 21 / WI-6 / phantom)** - Structure traced and sound (idempotency via one-RESOLUTION-per-bot+market, historical mode tagging, fail-closed phantom logic). One accounting DEFECT: realized_pnl deducts a taker fee on the RESOLUTION payout — (payout-entry)*size - payout*size*TAKER_FEE_BPS(150bps) (mirror_bot.py:2000-2003; setting... (evidence: bots/mirror_bot.py:1869-2076 (payout logic :1972-1981, exec-mode :1983-1992, fee :2000-2003, size-zeroing rationale :202)
- **[DEFECT/elev-FLAG][u] 20. GHOST MECHANISM: 'clear traders -> triggers exit' (two call sites)** - The mechanism does not exist. Setting traders=set() triggers nothing; it actually makes things WORSE by erasing the trader set, which permanently disables the Step-6 trader-sell exit for that position. Hedged pairs and leader-orphans just sit until the 96h force-exit (which can itself deadlock, Step... (evidence: bots/mirror_bot.py:609-623, :759-765, :1340-1354; exhaustive grep of pos['traders'] consumers: :430, :614, :720, :765, :)
- *Artifact branches:* The pathway as a whole is consumed (it mutates _open_positions, exposure, positions table, trade_events ledger) — NOT a removal candidate. Three branches ARE consumed by nothing or near-nothing: (1) The 'mark for exit by clearing traders' convention at mirror_bot.py:614 and :765 — exhaustive grep of every pos['traders'] consumer (bots/mirror_bot.py lines 430, 614, 720, 765, 1342-1344, 3125, 4441) 

### legacy-sizing
- **[DEFECT][V] 1. Tier-0 pre-gates (price floor, trader blacklist, whale-size floor, market blocklist, cooldown)** - Gates work as coded, but the cooldown carries a contradiction: the S172 D8 comment at 3011-3013 claims re-entry was raised to 24h (86400s) and uses getattr fallback 86400, while settings.py:445 defines the key at 1800 — so the actual cooldown is 30min, not the documented 24h, unless the VPS env over... (evidence: mirror_bot.py:2929-3026; config/settings.py:444-461)
- **[elev-FLAG][V] 7. Reliability multiplier (rel_mult cap 1.0, sample ramp, cold-start floor)** - Traced through S154/S164 floor fix. But its downstream effect is largely neutralized by the dust clamp-up (step 17): rel_mult enters sizing only via a 0.30-weight deduction on a $30 base, and any result under $25 is raised back to $25.... (evidence: mirror_bot.py:3479-3509; config/settings.py:502)
- **[DEFECT][V] 12. Flat sizing $30 clamped to bankroll.max_bet_usd; Kelly computed for logging only** - Flat path works, but the shared phase cap is NOT enforced on it: PHASE_MAX_BET_USD paper=$200 binds only inside get_bet_size (bankroll_manager.py:429), which MB uses only for logging. MB's real chain is min(flat, max_bet_usd) + S218 invariant (4215) — both use max_bet_usd only. If flat/max_bet are e... (evidence: mirror_bot.py:3997-4031; base_engine/risk/bankroll_manager.py:34-55,312-458; bots/base_bot.py:575-618)
- **[elev-FLAG][V] 13. Unified risk budget (additive deductions, floor 0.15)** - Arithmetic traced; floor 0.15 → min USD before dampeners = $4.50. But see step 17: any product below $25 is clamped back UP, so on the $30 base the entire risk budget can only express itself in the $25-$30 band — at most a 17% sizing effect instead of its designed 15-100% range.... (evidence: mirror_bot.py:4033-4083; config/settings.py:503; bots/mirror_adaptive_safety.py:168-185)
- **[DEFECT][V] 14. NO-side gates + tiered dampener** - The two hard gates work. The dampener is then largely UNDONE by the dust clamp-up: $30 base × risk_mult(0.15-1.0) × NO dampener(0.15-0.75) yields $0.68-$22.50 — always below the $25 floor — so step 17 raises every surviving NO trade back to $25, i.e. 83% of the max size the same signal could ever ge... (evidence: mirror_bot.py:4089-4144; config/settings.py:463-467)
- **[why-FLAG/elev-FLAG][V] 15. Extreme-price + favorite dampeners — both no-op since S119** - They correctly do nothing (×1.0). The promised Mar-29 re-evaluation never happened; the data-collection why expired.... (evidence: mirror_bot.py:4146-4164; config/settings.py:507-508)
- **[DEFECT/why-FLAG/elev-FLAG][V] 17. S218 invariant clamp + S217/S227 dust floor CLAMP-UP** - Both clamps execute as written — the defect is the interaction: with defaults, effectively all surviving entries land in [$25,$30] regardless of reliability, tier, Baker-McHale, adaptive-safety, or NO dampeners (min possible pre-clamp NO size $0.68 → $25 = 37x the intended risk). The sizing engine u... (evidence: mirror_bot.py:4207-4259; config/settings.py:1603-1606)
- **[DEFECT/elev-FLAG][V] 19. OrderGateway pass — the shared LIMITS layer is skipped for MB entries** - The architecture contract 'BotBankrollManager handles SIZING; risk_manager handles LIMITS. Both must pass' (bankroll_manager.py:12-13, echoed in CLAUDE.md) is FALSE for MirrorBot entries: check_risk_limits (incl. PipelineGate data-freshness, MAX_CONSECUTIVE_LOSSES, RISK_MAX_POSITION_SIZE_USD, total-... (evidence: order_gateway.py:463-714, 102-112; config/settings.py:422-424; bots/elite_watchlist.py:865-874; bankroll_manager.py:12-1)
- **[DEFECT][V] 21. Persistence + restore ordering (what sizing state survives a restart)** - Ordering WORKS (restore strictly precedes RTDS start on first scan). Three restore defects: (1) counter/restore asymmetry — the running counter decrements exits at ENTRY price (3248: _filled_sell × entry_price) but the restore query subtracts EXIT rows at EXIT price (339-340), so a mid-day restart a... (evidence: mirror_bot.py:311-699, 771-890, 925-926, 2663-2677)
- *Artifact branches:* Yes — multiple limbs of this pathway are consumed by nothing (all greps run in C:/lockes-picks/mb-steward, weather-engine vendored copy excluded). (1) mirrored_trades dedup store + its entire Redis persistence limb (_save_dedup_to_redis/_restore_dedup_from_redis, mirror_bot.py:2701-2727, flush at 836-840, prune at 958): grep -rn "mirrored_trades" bots base_engine → only the write (elite_watchlist.

### cross-system
- **[why-FLAG/elev-FLAG][V] 1. Legacy MirrorBot lane runs in paper (the writer side of every seam)** - The mechanism does what Decision 5 asked — stream flows, rejections logged, labels backfilled. It is the justification, not the machinery, that has expired. Side cost: it keeps accreting zero-decision-weight MirrorBot rows in positions/paper_trades/trade_events (ledger item 27) and known latent bugs... (evidence: deploy/polymarket-mirror.service:21-23; main.py:79-97 (:81 MirrorBot); bots/mirror_bot.py:2763-2821 (_log_rejection -> i)
- **[why-FLAG/elev-FLAG][V] 2. Shared ingestion label-maintains the legacy stream (3 call sites)** - Traced: UPDATE-only on existing rows with a temporal-ordering guard; failures are swallowed at warning level (ingestion_scheduler.py:393) so it cannot hurt trading paths. Cost is a recurring UPDATE pass over a ~17.5M-row table (M2 figure) whose freshest labels nothing living reads.... (evidence: base_engine/data/database.py:3950-4010; base_engine/base_engine.py:1791-1800; base_engine/data/ingestion_scheduler.py:38)
- **[why-FLAG/elev-FLAG][V] 3. mirror_rejected_signals consumer branch — the Decision-5 beneficiary check** - The label stream is intact and would function IF the gate were run — the branch is not broken, it is unconsumed. 'Kept alive for labels' currently protects only an idle charter plus superseded/quarantined scripts. The one paper-thin living beneficiary is MB_REBUILD_PLAN §5 step 3's wind-down precond... (evidence: Greps run this session: mirror_rejected_signals files_with_matches (49 files — none under mirror_v3/); grep -c over scri)
- **[DEFECT][V] 6. chain_audit.json — triple-role hub (live roster + cohort ledger + audit output)** - The hub works but carries four verified defects: (1) boot-time-only roster reads mean a ledger edit changes grading immediately but detection only at next watcher restart — a deliberate split-brain that sessions must remember (08-24: 'watcher restart not needed... detection set identical'); (2) hand... (evidence: mirror3_shadow_deploy.sh:22,70-75; mirror_v3/copy_watcher.py:111-121,513-515,798; scripts/shadow_readout.py:74-160 (:153)
- **[DEFECT][V] 7. Three-checkout deploy seam (/opt/polymarket-ai-v2, /opt/mirror3, /opt/pa2-shared/mb_readout)** - Four seam behaviors: (1) SAME-DAY VERSION SKEW — the 11:40Z chain never refreshes the checkout, so grading/lock-consumption runs code from the PREVIOUS day's 12:30Z reset while the readout 50 minutes later runs branch HEAD; sessions paper over it with manual syncs; (2) branch HEAD auto-deploys into ... (evidence: mirror3_shadow_deploy.sh:17-36 (BRANCH pin, reset --hard); shadow_readout_cron.sh:9-21 (BR pin, fetch+reset, frozen-code)
- **[DEFECT][V] 8. 11:40Z cron chain: labels -> fees -> cohort5 -> band -> scoreboard -> canon** - Stage-death walk: labels dies -> coherent-but-stale gamma survives (atomic write), graders run SILENTLY on stale labels (SupplementError doesn't catch staleness) — mostly conservative delay of crossings; fees dies -> graders fall back toward flat-2%, the exact divergence class that already consumed ... (evidence: deploy/label_and_fee_refresh_cron.sh:1-40; shadow_label_supplement.py:26-33,231-240 (atomic+backup+guards); build_fee_ma)
- *Artifact branches:* YES — one branch of this pathway is consumed by nothing living: the mirror_rejected_signals label stream (and with it the sole remaining money-justification for running the legacy MirrorBot lane at all). Greps run this session: (1) files_with_matches for 'mirror_rejected_signals' = 49 files, of which the only WRITER is bots/mirror_bot.py (_log_rejection :2763-2821), the only maintenance is the sha
