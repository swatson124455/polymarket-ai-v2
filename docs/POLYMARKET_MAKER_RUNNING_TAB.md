# POLYMARKET MAKER — RUNNING TAB (living ledger; append, never overwrite history)

Started 2026-07-21, adopted from the Kalshi lane's ledger practice after the
operator-directed review of their arc (their tab made a losing week fully
auditable; ours starts BEFORE first live money so the whole pilot is on the
record). This file is the chronological ledger of record for the Polymarket
Maker lane. Definitions/quote-rules live in `docs/MAKER_NUMBERS_LEDGER.md`
(the definition of record) — this file records EVENTS.

Rules for every session (binding):
- APPEND dated entries; never silently revise an old row — post a CORRECTION
  row that names what it corrects.
- Every number carries source + method + date. Tiers per
  `MAKER_NUMBERS_LEDGER.md`: CONFIG / MEASURED / MODEL / NOISE / BANNED.
- REWARDS BASIS ONLY (`MAKER_MASTER_PLAN.md` §0a); no derived EV/return until
  a real on-chain reward receipt anchors it (§0b). Trading marks: band only.
- Concentration-check pooled numbers before quoting (Protocol 14).
- md5 comparisons across machines: state the normalization
  (`tr -d '\r' | md5sum`) — CRLF working trees hash differently.

---

## ⚠ DO NOT CONFLATE — TWO SEPARATE CAP PROBLEMS (read before touching caps)

The gross cap has had **two independent defects**. They sound alike, they live
on the same line of code, and merging them in your head will either re-open a
closed bug or silently drop an open one.

| | **MERGE-BLINDNESS** | **CAP SIZING** |
|---|---|---|
| Status | ✅ **FIXED 2026-07-22** (`a660aa1`) | 🔴 **STILL OPEN** (GAP-4, parked) |
| The defect | The cap charged a hedge its GROSS cost, ignoring that buying the complement of held inventory forms $1 pairs on fill. `merge_pairs` nets spend down AFTER the fill; the cap is checked BEFORE the order — so it denied the very order whose fill would relieve it | The cap is a flat **$150 per market** regardless of that market's min-quote size. 30/140 live markets have `msz > $150`, so a single minimum two-sided quote already exceeds it |
| Symptom | Bot holds an unwanted directional position it is structurally forbidden from hedging; both legs stop ⇒ all quoting stops | Those 30 markets are **structurally unquotable** — the bot can never quote them at all, at any inventory level |
| Fix shape | Price the guaranteed merge: `eff_cost = cost − min(sz, held_opposite)` | Size the cap off `msz` (or per-sector), NOT a flat dollar figure. **Needs an operator capital decision** |
| Depends on inventory? | YES — only bites once you hold a directional position | NO — bites on an empty book, from the first quote |

**The tell:** if the bot cannot quote a market it has **never traded**, that is
SIZING. If it cannot quote a market it is **already holding a position in**,
that was merge-blindness (and is fixed — if it recurs, it is a regression, not
GAP-4).

**Related but also distinct:** `MAKER_ONESIDED_DERISK` is neither of the above —
it handles the case where the accumulating leg is legitimately capped and only
the hedge leg should still go out. If `derisk1` fires often, the root cause is
usually SIZING, not hedging.

---

## A. LEDGER (chronological)

| date | event | verdict / state | source |
|---|---|---|---|
| 07-20 | Session 5B close: operator GO (staged, min-size, DEDICATED wallet, $75 floor); hardened engine DEPLOYED paper+halted; venv fixed; NO-TAKER hard rule; numbers discipline rebuilt (rewards basis only) | blocked ONLY on operator wallet | `AGENT_HANDOFF_2026-07-20_MAKER_SESSION5B_CLOSE.md` |
| 07-21 | Session 6 health check: engine md5 `1961f4b9…` VPS == branch HEAD `3531d83` (LF-normalized); unit active since 07-20 16:11:24Z, zero restarts; heartbeats clean (zombies/lmiss/feedfail/anom all 0); HALT from 07-20 07:00Z day-floor kill persisting by design; env has no wallet keys | paper arm HEALTHY; wallet still the only blocker | MEASURED — ssh journalctl/systemctl/md5sum 07-21 ~20:10Z |
| 07-21 | Kalshi-lane review (operator-directed, read-only): their live arc lost ~$45/$100 (their ledger) to execution microstructure the model never priced — taker fire-sales on flatten, unmanaged inventory accumulation, wrong-markets selection. Three lessons adopted here (rows below) | lessons imported BEFORE our first live dollar | their `KALSHI_RUNNING_TAB.md` + 07-21 handoff, read from `origin/claude/maker-kalshi-live` |
| 07-21 | ADOPTED #1 — `MAKER_SECTOR_ALLOWLIST` (CONFIG, `maker_live_engine.py` load_config/discover): non-empty ⇒ only listed sectors survive discovery, applied BEFORE ranking so a shrunk `MAKER_MAX_MARKETS` cannot admit an off-list market; overlap with excluded sectors fails loud; empty ⇒ behavior unchanged. Closes the Kalshi wrong-markets shape for our tiny footprint; fail-closes "unknown"-sector markets out of the pilot | built on branch; NOT deployed | this session's commit |
| 07-21 | ADOPTED #2 — cancel-SHAPE probe in `maker_preflight.py --stage scoring`: raw `cancel_orders` response captured, printed, and run through the ENGINE'S own `_cancel_shortfall` (imported, never copied). An unanticipated shape now surfaces at scoring, not at the first live kill | built on branch; NOT deployed | this session's commit |
| 07-21 | ADOPTED #3 — this running tab | active | this file |
| 07-21 | CLASSIFIER BUG caught by the allowlist's first-output cross-check on live gamma: weather KW `heat-` matched "miami-heat" NBA slugs — "Will LeBron James play for the Miami Heat in 2026-27?" classified WEATHER (would have entered the weather pilot slice; the exact Kalshi mis-labelled-market shape). Engine-only fix: `heat-` → heat-wave/heatwave/heat-index/heat-advisory/extreme-heat (+lowest-temp); LeBron → "unknown" → fail-closed out of any allowlist. ⚠ family recorder arms v1–v6 + census + research scripts keep the OLD pattern mid-era (measurement attribution only — census/canon "weather" sector stats may include team-name matches); family sync = PROPOSE-ONLY, own era stamp | fixed in engine; post-fix live cross-check = 8/8 genuine temp markets | MEASURED — discover() run vs live gamma 07-21, pre/post diff |

| 07-21 | INDEPENDENT ADVERSARIAL REVIEW of the session's changes → SHIP-WITH-FIXES, all fixed same session: (1) MAJOR — pre-existing partial-discovery guard's 40-market floor would have FROZEN any allowlisted (by-design-small) universe after first adoption; every refresh logged "PARTIAL" + discarded; daily-churn weather slice stale within a day. Fixed: `discovery_suspect()` extracted+testable, floor waived when allowlisted, relative half-shrink guard retained. (2) MINOR — heat-regex fix orphaned real heat-event slugs (heat-dome/heat-warning/heat-emergency/record-heat/excessive-heat) → restored. (3) MINOR — typo'd allowlist drops everything silently (universe.json write is gated on non-empty picked) → loud "matched ZERO" journal line added. (4) banner now prints allowlist. (7) preflight cancel-shape probe validates the SIBLING engine file → runbook rule: run preflight from /opt/pa2-maker-live. 126/126 tests | discipline held: reviewer found what the author's own tests missed | reviewer report, this session |

| 07-22 | WB RELAY IN (WB session 07-21/22). **(1) c13 CLOSED-EMPTY — DO NOT PURGE the WB feed.** The standing premise (pre-c5 mislabeled ~0.44 nowcast lines in `/opt/pa2-maker-feeds/wb_forecasts.jsonl`) does NOT hold: WB's audit of 17,220 lines (07-17T18:57:36Z→07-22T02:06:23Z) found ZERO lines with prob∈[0.42,0.46] across the entire pre-c5 window (07-17/18/19); exactly one line matches the c13 signature (Jeddah, 07-20T05:00:48Z, prob 0.4438), and the nowcast signal's FIRST `prediction_log` row is 07-20 19:20:31 — ~14h AFTER that line, so it cannot be a nowcast row. Purging would delete genuine data and bias our own tilt study. **(2) Shared RedisCache `raise_on_error` is on master (`0e26f70`) + deployed in master release `20260721_232241`**; backward-compatible, capability-only until a caller opts in — **NO ACTION for Maker: no Maker script imports the shared `RedisCache` at all** (verified: only `seed_unpriced_blacklist.py` uses raw `redis.asyncio`; the v6 grep hit was the word "rediscovery") | c13 closed; tilt readout UNBLOCKED; Redis = no-op for us | WB relay + MEASURED grep of `scripts/**` 07-22 |
| 07-22 | ⚠ **FEED LANDMINE (WB, FYI):** master's committed `deploy/polymarket-weather.service` was MISSING `/opt/pa2-maker-feeds` from `ReadWritePaths`, and `deploy.sh` overwrites the live unit every master deploy → `ProtectSystem=strict` would have made our drop read-only, and WB's exporter swallows errors by design, so **the feed would have stopped SILENTLY**. Fixed on master as `4b50ce7` before the deploy; verified after (feed grew 17,734→17,754 lines across it). **If the WB feed ever goes quiet, check that `ReadWritePaths` line FIRST** | near-miss, closed | WB relay 07-22 |
| 07-22 | **TILT-vs-CONTROL READOUT DELIVERED (owed to WB).** Method `scripts/maker_research/mm_tilt_readout.py` (new, in git). Paired P6_tilted vs P0_base on identical books; **era guard** applied (P0's ledger carries from 07-17 02:36Z vs P6's 21:55Z — raw `acc` would fake a P0 win on boundary-spanning markets; primary cut = markets dated ≥Jul 19, robustness ≥Jul 20; 50 date-unparseable EXCLUDED+disclosed). n=227 covered markets primary / 172 robust. **REWARDS (MODEL, unverified): the tilt COSTS rewards — −14.0% primary, −13.5% robust** (stable across cuts). Concentration CLEAN (top1 2.4–3.2%, top5 11–14% — broad-based, not one market). **NULL CONTROL PASSES EXACTLY: the w=0.00 tier (HK-excluded, zero weight) shows delta $0.00 on 8/6 markets** — where no tilt is applied P6≡P0 to the cent, so the harness is measuring the tilt, not drift. Cost is roughly FLAT in % across w=1.00/0.50/0.25 (−14.2/−14.2/−16.3%), consistent with the 1c cap binding regardless of weight. Trading leg (NOISE, band only, never a headline) points the SAME direction — no offsetting gain visible. **Mechanical cross-check (independent of the measurement):** the reward formula `((v−s)/v)²` puts a 56% score penalty on a 1c tilt against a 3c band; with 28% of markets tilted at snapshot the aggregate −14% implies ~50% on tilted markets — the measured cost matches the formula's prediction | VERDICT: tilt is reward-NEGATIVE at current parameters; no fill-quality offset measured | MODEL accrual, `mm_tilt_readout.py` run vs v5 state 07-22 |

| 07-22 | **KALSHI DOCTRINE PORT (operator: "mirror the kalshi plan in full … verify plan works on the platform")** → `docs/MAKER_KALSHI_DOCTRINE_PORT.md`. Every Kalshi element (6 trading principles, 7 cycle behaviors, 8 fixes) mapped with a verdict + evidence. **Headline platform finding: their taker machinery is N/A BY PLATFORM, not by preference** — on Polymarket both legs are BUYs, so reducing delta IS a post-only maker buy of the complement, which merges to a $1 pair (`merge_pairs:1158`). Their most expensive problem (unwinding costs spread; last resort = fire-sale) is structurally absent here. Their §2.5/§2.7/fix-G would violate our no-taker rule AND solve a problem we don't have. We were already ahead on: no-taker, blind-feed response (kill+halt at 10 fails vs their cancel-ids at 2), scoped kill primitive, never-faked held value | port complete, doc in git | this session |
| 07-22 | **CAPITAL DEADLOCK — real defect FOUND; attempted fix REVERTED on independent DO-NOT-SHIP review.** Defect (stands): `merge_pairs` nets `spent` down AFTER a fill but the gross cap is checked BEFORE the order → the cap denies the very order whose fill would relieve it; both legs cost money so `market_gross_cap` binding stops ALL quoting. Attempted fix (exempt risk-reducing orders from the 2 gross caps) was **ineffective AND unsafe**: (a) the sole caller checks legs in FIXED order yes→no and is TWO-SIDED-OR-NOTHING, so in long-YES the accumulating leg is denied first and the exempt leg is discarded — deadlock not relieved at all; long-NO relieved only on a random jitter draw; strict no-op at `MAKER_SIZE_JITTER=0`; (b) `reducing` computed once per CALL exempted the sibling ACCUMULATING leg too, and legs fill independently → traced to **1.65× market_gross_cap** + a sector-cap breach; (c) **4 of my 6 tests passed against a mutated `reducing=True`** — they were pinned by `market_net_cap`, not the new code. REVERTED in full; suite back to committed 126/126. ⚠ **PLATFORM FACT that constrains any retry: a single-legged quote scores ZERO rewards (two-sided MIN)** — so a lone hedge leg buys risk reduction ONLY and can never restore the lost income; "rewards stop" is a cap-SIZING problem (GAP-4), "cannot hedge" is a separate one-sided-exception decision. Retry bar recorded in port §3d (per-leg classification, caller in scope, mutation-tested tests, no RNG in guards) | defect OPEN + propose-only; no code shipped | adversarial review, this session |

| 07-22 | **CAPITAL DEADLOCK FIXED** across `a660aa1` → `87b7c30` → `44998b1` (3 commits, 3 adversarial review rounds, 13 mutants). (1) **merge-aware capital accounting** — the cap was MERGE-blind, not delta-blind: a complement buy against ALREADY-HELD inventory forms $1 pairs on fill (guaranteed, not contingent), so `eff_cost = cost − min(sz, held_opposite)`. Needs no exemption/boolean/sign-rule; pending-sibling merges deliberately NOT counted (legs fill independently) so ordinary two-sided quoting is capped exactly as before. (2) **one-sided de-risk placement** — necessary because the caller is two-sided-or-nothing, so the accumulating leg denies first and the correctly-priced hedge was discarded. ⚠ **it scores ZERO rewards (two-sided MIN) — it buys RISK REDUCTION ONLY and never restores the income the deadlock costs.** Behind `MAKER_ONESIDED_DERISK` (default ON for the paper window; reviewer withdrew its default-OFF objection, condition: operator re-decides before the first funded run). (3) **`match_fills_paper` blindness** — the row-skip test discarded ANY row with a None bid, so an ask-only quote (= the long-YES hedge shape) rested but could never fill in paper. Silent measurement blindness on the feature's only success path | deadlock CLOSED; not deployed | 3 reviews + mutation testing, this session |
| 07-22 | **REVIEW ARC — 2 DO-NOT-SHIPs before it was right; the failures are the value.** Attempt 1 (exempt "reducing" orders from gross caps) REVERTED: evaluated per-CALL so it exempted the accumulating sibling leg (traced **1.65× the cap**), was a strict no-op at `MAKER_SIZE_JITTER=0`, and **4 of its 6 tests passed a fully-broken mutant** (they were pinned by `market_net_cap`, not the new code). Attempt 2 blockers: **F1** my one-sided hysteresis poisoned the feature through its own SUCCESS path (nothing clears ob/oa on a FILL → stale leg reads as standing + net≠0 → market skipped for hours = phantom hedge, nothing resting, nothing earned; the relieving fill is the NORMAL outcome so the hold was anti-correlated with recovery); **F2** paper re-credits a standing row every print → ask-only row ran long-YES 300 into **long-NO 900**, falsifying the placement invariant in the mode meant to measure it; **F3** commit path had ZERO coverage — `want_n = 1` made the engine place-then-cancel every quote forever earning $0, with 140/140 passing. Attempt 3: fixing F1 by DELETING the hold opened **G1** — a lone hedge requoted every scan sits permanently back-of-queue and may never fill live, while paper (no queue model) shows it working = paper/live divergence on the success path. Final: hold restored with a **q_inv inventory snapshot** (release on any fill), whole caller extracted to `commit_placements`, `len(legs)` fail-closed guard (G3) | **LESSONS (binding): a guard cannot be reviewed apart from its CALLER; extracting only the arithmetic RELOCATES the untested surface; every safety test must kill a deliberately-broken implementation or it pins nothing** | review rounds 1–3 |

| 07-23 | **FIRST LIVE SMOKE TEST of the session's engine** — isolated scratch instance on the VPS (`/tmp` base + separate state, `--base` isolated), pilot config `MAKER_SECTOR_ALLOWLIST=weather MAKER_MAX_MARKETS=8`, paper mode. Production arm NEVER touched (verified after: md5 still `1961f4b9…`, unit active, HALT still present). RESULTS: `ENGINE START … max_mkts=8 excluded=['esports','finance'] allowlist=['weather'] floor=$75` (banner fix live); discovery **8/8 genuine weather markets, 402 dropped by allowlist, ZERO NBA/"unknown" contamination** (classifier fix confirmed against live gamma, not just tests); two-sided quote path ran end-to-end (167 order rows, `ob`+`oa` both set); **the new `q_inv` snapshot key is written correctly** (`[0.0,0.0]` matching y=n=0); `derisk1` absent = one-sided path correctly did NOT fire with no directional inventory; **0 error/traceback/exception lines**; clean py_compile. Scratch fully removed after | engine boots + quotes + persists correctly under pilot config | MEASURED — isolated VPS run 07-23 16:07–16:12Z |

| 07-23 | **OPERATOR DECISION — `MAKER_ONESIDED_DERISK` stays ON for the pilot** (took the recommendation), logged as a REVISIT item rather than a silent default. Plain terms: the bot normally rests both sides of a market because the reward only pays on a two-sided quote; if one side keeps filling it ends up holding an unwanted directional bet, and the cure is buying the opposite side (which makes the pair worth exactly $1 and removes the gamble). The switch lets it place that evening-out order alone when the spending cap has blocked everything else. ⚠ **The lone order earns NOTHING** (no pair ⇒ no reward) and, if price has drifted against the held side, evening out LOCKS IN that loss instead of leaving it to chance. Accepted trade: small pilot sizes, and being cornered holding an unwanted position into settlement is the worse first-live surprise. **REVISIT WHEN:** the first receipts land (real cost visible), or sizes scale past the pilot, or `derisk1` on the heartbeat shows it firing often — frequent firing means the real problem is cap SIZING (parked GAP-4), not hedging | decided; revisit item, not urgent | operator, 07-23 |

| 07-23 | **DEPLOYED to the paper arm + STRESS-TESTED (A/B against the pre-fix engine).** Deploy: backup `…bak-20260723_161838` (= `1961f4b9…`, the rollback), installed `3a525d7f…` = branch HEAD LF-normalized, compile-checked under the target venv first; unit active, clean boot `ENGINE START … allowlist=off floor=$75 halted=True`, 140 markets / 280 assets / 4 ws, 0 tracebacks. `allowlist=off` is CORRECT — the allowlist is pilot-only config, so the paper arm keeps its full universe for measurement continuity. **HALT preserved** (engine boots halted; resuming is the operator's call). **STRESS TEST — the deadlock fix proven end-to-end on a real engine for the first time:** isolated instance seeded with the exact deadlock state (long YES 300, spent $149 vs the $150 gross cap) → NEW engine placed the hedge leg ALONE (`ob`=False accumulating leg correctly denied, `oa`=True), wrote `q_inv=[300.0,0.0]`, `onesided_derisk_n=3`, 0 errors. **CONTROL: the pre-fix engine (`1961f4b9…`) on a byte-identical seeded state quoted NOTHING (`ob`=False, `oa`=False)** — the deadlock reproduced exactly as diagnosed, so the fix is what changed it, not luck. Churn (review G1) BOUNDED: 5 place / 2 cancel on the seeded market over the run, base dir 1 MB vs the 500 MB CLEAN-STOP — the `q_inv` hold is doing its job. Production arm healthy after deploy, scratch removed | deadlock fix PROVEN on real infra; arm running new engine, still halted | MEASURED — VPS deploy + A/B stress 07-23 16:18–16:30Z |

| 07-23 | **PAPER ARM RESUMED** (operator go). HALT removed (copy kept `HALT.cleared-20260723`). Diagnosis behind it: the arm carried **7 positions, ALL one-sided** ($238 spent, no pairs) — the fossil record of the capital deadlock; the halt note's own question answered: `settle_realized` was only **$2.26** of the $82.24 that tripped it, i.e. mark drift on unhedgeable inventory, NOT trading losses. 3 of 7 are departed w/ frozen marks; the resolution sweep runs even while halted so those self-clear on resolve. Post-resume: `q=75/140 halted=False accday=$0.04 derisk1=0` and **`deny={'market_gross_cap': 65}` — 65/140 denied on the gross cap, live confirmation of GAP-4 (SIZING) and WORSE than the documented 30/140** | arm quoting again; gate-policy clock restarted 16:38Z | MEASURED — heartbeat 07-23 16:38:59Z |
| 07-23 | **PILOT RE-SCOPED — operator: total budget well under $150.** That INVERTS GAP-4: a $150 per-market cap can never bind when the whole wallet is smaller, so cap sizing is a full-universe problem, NOT a pre-pilot blocker. Measured msz across the live universe: **weather is UNIFORMLY $100 (all 25)** ⇒ a sub-$100 wallet quotes ZERO weather, $100 quotes exactly ONE — **the census-backed weather-led plan is incompatible with this budget**. Cheap tiers live elsewhere: sports $20×3/$40×10/$50×12, entertainment $20×1/$50×11, politics $20×1/$40×4/$50×7. Also a hard discontinuity: **$100 and $150 wallets reach IDENTICAL coverage (100/140)** — the next tier is $200, so funding $150 over $100 buys nothing. **KEY CORRECTION: `msz` is the REWARD-QUALIFYING threshold, NOT the exchange's min order size** — $2–3 orders are accepted, they simply score ZERO. Since our thesis is trading-alone ≈ breakeven and the income IS the subsidy, a sub-msz pilot runs the strategy with its only profit source off. Gas is NOT a per-order cost (orders are signed off-chain; POL covers approvals/deposit/redeem) | pilot = 3 markets @ the $20 tier ≈ $60, NOT weather | MEASURED — universe.json 07-23 |
| 07-23 | **CLASSIFIER BUG CLASS #2 — found on LIVE data while vetting the $20 tier.** A real market, *"Will 'Arirang - BTS' be the top album"*, was classified **geopolitical** because `ar-IRAN-g` contains `iran`. Same substring class as the `heat-`/"miami-heat" bug — the adjacent-shape miss (fix one → enumerate its variants). Three more confirmed: `se-NATO-r`→geopolitical, `premier-E` (a movie)→sports, `d-EPL-oyment`→sports. **Pilot-critical: a mislabelled market walks INTO or DODGES the sector allowlist.** Fixed engine-only with word boundaries (`iran`, `nato`, `premier`, `epl`) — real ones still match (iran/iranian/nato/premier-league/epl). Family arms keep the old pattern mid-era, as with `heat-` | 157 tests | MEASURED — live universe + classifier probe 07-23 |
| 07-23 | **PILOT CONFIG STAGED (not applied)** → `deploy/maker-pilot-env.staged`. `MAKER_SECTOR_ALLOWLIST=sports,entertainment,politics` (excludes `unknown` = the fail-open gate hole, which costs us the 4 cheapest $20 markets — deliberate), `MAX_MARKETS=3`, `MAX_PER_SECTOR=2`. ⚠ **Caps RE-SIZED because the defaults are dangerous at this wallet: the stock day-loss floor $75 EXCEEDS a ~$60 wallet, so it would never fire — the wallet would be gone before the guard tripped.** Now market/event $30, sector $60, **day floor $15**. Open items recorded IN the file: settle sub-msz scoring EMPIRICALLY (place $3 and $20, call `is_order_scoring` on both), `MAKER_SIG_TYPE` at sanity, cancel-shape probe, and **"soft" IS UNMEASURED — nothing here measures competition; `pool/msz` is NOT used to rank because the numbers ledger warns it is NEGATIVELY correlated with realized yield** | staged, wallet-gated | this session |

| 07-23 | **SOFTNESS MEASURED — the gap I flagged, now closed with a tool** (`scripts/maker_research/mm_softness_probe.py`, read-only public CLOB books). Reward SHARE = `our_score/(our_score+Σ competitor_score)`, each resting order scored by the engine's own quadratic `S()`; soft = thin competitor score in the band. Run vs the 48 pilot candidates (sports/entertainment/politics, msz≤$50). **FINDING — share-rank and pool-rank DISAGREE COMPLETELY, which is the whole point:** the biggest pools (Rams/Thunder/Arsenal/PSG futures, $35–106 pool) return **0.0% share** — whales camp the band, so pool-based selection would have picked the worst markets. Most books are crowded (0.0–0.6% share). Genuinely soft markets are RARE. **Standout: FIFA-viewership `2954097` — 4.8% share AND $300 pool ⇒ ~$14/day (model), an order of magnitude above everything else** (soft AND big-pool). Small soft ones cluster ~$1–4/day (Bills season-series 7.3%/$4; Trump-Netanyahu ~9%/$1.75; an Odyssey box-office market ~6–15%). ⚠ **SNAPSHOT NOISE is real** — share moved run-to-run (Odyssey 15.5%→12.5%), so a single snapshot is noisy; averaging over several samples is needed before this drives final selection. All figures MEASURED competition metric + method tag, **NOT EV** (competitors' reaction to us unpriced) | pilot selection now has its missing input; needs repeated sampling to de-noise | MEASURED — mm_softness_probe vs live books 07-23 |

| 07-24 | **SESSION 7 OPEN — health check + a DEPLOY-DRIFT finding.** Unit `polymarket-maker-live` ACTIVE, 0 restarts since 07-23 16:18:43Z; ws 4 conns, books 280, `zombies/lmiss/feedfail/anom=0`, `http_hr≈8446/36000` (well under limit) — infra HEALTHY. **BUT (1) the arm RE-HALTED 07-23 19:09:15Z on the day-loss floor** (`pnl=-75.13 floor=-75.00`, of which `settle_realized=-0.09`) — i.e. mark drift on the legacy one-sided (deadlock-fossil) inventory, NOT live bleed; same shape as the 07-20 kill. `HALT.cleared-20260723` retained; new `HALT` present. gross fell $238→$216 since 07-23 close ⇒ the resolution sweep IS clearing fossils even while halted. **(2) ENGINE ONE COMMIT BEHIND BRANCH HEAD:** VPS `maker_live_engine.py` md5(LF)=`3a525d7f7b3b…` = **`44998b1`-era** (129799 B); branch HEAD `56b6dba` md5(LF)=`81712e09776b…` (130271 B). The 472-B gap is exactly commit **`d9dfd87`'s engine hunk = the classifier bug-class #2 word-boundary fix** (`\bpremier\b`,`\bepl\b`,`\biran`,`\bnato\b`); it landed 07-23 16:51Z, ~33 min AFTER the 16:18Z deploy, so it was never shipped. Tab's "deployed==HEAD" (§B) was true at deploy, went stale silently. **Impact: on the HALTED paper arm (allowlist=OFF) it is measurement-attribution only (sector labels), no behavioral/safety effect. On the LIVE PILOT it is CRITICAL — the staged allowlist gates on sector, and a mislabelled `ar-IRAN-g`/`se-NATO-r`/`premier-E`/`d-EPL-oyment` market would dodge/walk the allowlist.** ⇒ **REDEPLOY to HEAD is a REQUIRED pre-live step, to be bundled with the staged-env cutover.** **(3) `derisk1=63` — STABLE, not climbing** (frozen by the halt; q=0). deny (cumulative since boot) `market_gross_cap=1226 / market_net_cap=63 / pool_vanished=32` = documented GAP-4 sizing pressure. dayPnL oscillating −$4.5…−$10.4 = NOISE (mark drift, fossil inventory). Wallet still the ONLY live blocker | infra healthy; arm halted (mark-drift, not bleed); engine 1 commit behind HEAD — redeploy required before live | MEASURED — ssh systemctl/journalctl/md5sum + git 07-24 ~14:05Z |

| 07-25 | **ARM RESUMED FOR REVIEW (operator: "resume but only if to review, not touching kalshi").** Kalshi isolation RE-VERIFIED read-only first: our unit `polymarket-maker-live` is `ReadWritePaths=/opt/pa2-maker-live` ONLY (fs-sandboxed), no kalshi reference, kalshi lives in separate trees (`/opt/pa2-maker-kalshi*`) + separate units (untouched). Removed `/opt/pa2-maker-live/HALT` (sudo; archived `HALT.cleared-20260725-review`); `halt_cancel_ok=True` so it resumed, not re-armed. Post-resume hb 02:06:46Z: **`q=77/140 halted=False accday=$0.20`** (accrual restarted, MODEL), 0 errors — de-risk now able to hedge the 3 naked fossils ($100.84) instead of letting them ride to $0. **Correction to the 07-23 "mark drift not bleed" read:** the −$75 floor trip was REALIZED fossil settlement loss (2967324 resolved $0 / realized −$75.38 PAPER; siblings −$61/−$60/−$19/−$14), i.e. one-sided fossils resolving against the held side — the exact loss the merge-fix+de-risk close forward, not benign mark drift | arm quoting again (paper, review); Kalshi untouched | MEASURED — ssh systemctl/rm/journalctl 07-25 02:05–02:07Z |
| 07-25 | **WS HOT-PATH BUILD STARTED (operator: build if feasible + not touching kalshi).** Feasible + Kalshi-free (Poly-native refactor of Poly's own `ws_worker`/`run()`; `614eb5a` is design-reference only, its venue feed NOT ported; zero Kalshi files). Contract → `docs/MAKER_WS_HOTPATH_PLAN.md`: Stage 0 identity `run_once()` extraction (behavior byte-identical, differential+mutation tested) → Stage A event wakeup behind `MAKER_WS_HOT` (default OFF = 1s timer unchanged) → Stage B reprice-only hot path (capital monotonically non-increasing between colds, separate review round). Lower-risk than it looks: `run()` already follows the "extracted from run() so it's testable" pattern (commit_placements/plan_quote_commit/onesided_derisk_leg already out+tested). Ship gates per stage: tests → INDEPENDENT adversarial review (guard-with-caller, kills a mutant) → paper smoke → BUNDLE with cutover (decision 3), never standalone. Also: softness multi-sampling (elevation #2) launched (5 spaced runs, read-only public CLOB) to de-noise the $20-tier pick | build planned + underway; nothing shippable yet | this session 07-25 |

| 07-25 | **SOFTNESS DE-NOISED (5 spaced runs, elevation #2) — context beat the headline number.** The top rew/day markets are a TRAP: the `$500/day` "Will Trump say X during White House Correspondents" cluster (2954682/86/87/88/91) is (a) ONE correlated event, (b) `end=2026-07-24` = EXPIRING (VPS on 07-25) so the daily rate has ~0 window left, (c) quoting = holding into a speech-outcome settlement (the naked-into-settlement loss that cost −$75). Several are empty-band (100% share, comp=0) — no competitor because the market is done, not because it's soft. **EXCLUDED.** Also a hard pilot constraint surfaced: the re-sized **$30 market cap ⇒ only $20-msz markets are quotable** ($50 min > $30 cap), so every $50 softness winner is irrelevant to THIS wallet. **$20-tier reality (stability-adjusted share, mean−sd, over 5/5 runs):** exactly ONE genuinely soft market — **3046011 "72 Hours #2 US Netflix"** (27.1% share sd10.5, comp 32.8 real, model ~$30/day, end 07-28 real window); the other three $20 markets are CROWDED (2.0–5.1% share, whales in band). ⇒ **a 3-market soft $20 pilot is not currently available — the $20 soft universe is ~1 deep.** Options for the operator: (A) 1 soft (3046011) + 2 crowded fillers for diversification; (B) lift the market cap so $50 soft entertainment (3046241 sd1.4/27%, 3045963 50%) qualifies — a capital decision; (C) re-sample at go-live (these IDs expire 07-28; picks are PERISHABLE). Prior standout FIFA 2954097 has departed the universe. Numbers: pool/comp/end = MEASURED (gamma/CLOB); share/rew = MODEL competition metric, NOT EV | method works; pilot selection is capital-constrained + universe-thin — decision owed | MEASURED — mm_softness_probe ×5 + universe.json 07-25 02:08–02:28Z |

| 07-25 | **WS HOT PATH — STAGE A BUILT (`920a633`, branch only, flag default OFF, NOT deployed).** Design DEVIATION from the plan's run_once() reindent, for the better and documented (`MAKER_WS_HOTPATH_PLAN.md` top): Poly's loop body ALREADY re-runs a full guarded cycle each iteration, so only the WAIT needed to change — no 500-line reindent (that would relocate an untested surface into a live-capital engine for zero behavioral gain; run() has no test harness — the helpers do). Ships: `_WS_TICK` event set by the 3 book appliers; PURE `_next_wait_plan(ws_hot,ws_min_s,elapsed)` (OFF=`(1.0,None)`=unconditional sleep(1) byte-identical rollback; ON=floor to ws_min_s then wake on a tick within a 1s window, preserving the ≤1s minute/hb cadence); `_wait_next` replaces `time.sleep(1)`; `MAKER_WS_HOT`(OFF)/`MAKER_WS_MIN_MS`(250, bounds (0,1000)); banner prints ws_hot. **163 tests pass** (8 new); **3 broken mutants of the pacing fn all KILLED** (off-elapsed-aware, no-floor, remaining-uses-e). This = Kalshi "Stage A ws-triggered full guarded cycle" re-derived Poly-native, ZERO Kalshi files. Remaining gates before bundle: INDEPENDENT adversarial review (running) + paper smoke. Stage B (reprice-only) not started | Stage A on branch, gated; not live | this session 07-25 |

| 07-25 | **STAGE A — INDEPENDENT ADVERSARIAL REVIEW: SHIP-WITH-FIXES; fixes applied (`a38a0d3`).** All core safety claims CONFIRMED by the reviewer with reasoning: OFF-path byte-identity, no missed-tick stall (event coalesces; data lives in BOOKS; worst-case latency = the old 1s), spin-bounded (earliest next cycle = max(elapsed,ws_min_s) ≥ ws_min_s), cadence preserved (minute/hb gated inside the 60s block; accrual dt uses wall time → no double-accrual), thread-safe, fail-loud config. Fixes: **#1 (real footgun)** `MAKER_WS_HOT=off` silently evaluated to **ON** ('off' not in the disable set) — a natural disable word ENABLING the hot path in live; added 'off', test asserts off/OFF/Off disable. **#3** batched applier woke spuriously on untracked-asset batches → gated on a real book change. **#4** property test strengthened to assert the earliest-next-cycle spin bound. 163 tests. **#2 (OPERATOR ACK before ON in live, NOT a code fix):** ON ups the CLOB order rate ~4× (cycle 1s+→250ms) and the HTTP budget governs only `get()` fetches NOT the order path → invisible to the rate limiter; before ON rides live, confirm the CLOB limit won't trip / raise ws_min_s / add an order counter, and watch it in paper smoke. Also flagged a PRE-EXISTING twin footgun: `MAKER_ONESIDED_DERISK=off` also fails to disable (separate commit, signoff). Remaining Stage A gate: paper smoke with the flag ON (isolated instance) | Stage A review-clean + fixed; paper smoke + operator ack pending; still not deployed | INDEPENDENT review 07-25 |

| 07-25 | **STAGE A — PAPER SMOKE PASS (A/B, isolated VPS instance; prod arm + Kalshi untouched).** Instrumented THROWAWAY scratch copy only (per-cycle counter + faster hb; committed artifact stays `a38a0d3`), run ON vs OFF 4 min each, 20 mkts/40 assets, paper, `--base /tmp`, prod venv read-only. **Functional:** ON boots `ws_hot=True(min=250ms)`, discovers 20/40, quotes `q=6/20`, accrues, **ZERO real errors** (grep "5" was `halted` in `halted=False`), clean STOP shutdown. **#2 MEASURED:** cycle rate ON≈3.7/s vs OFF≈1.0/s (~3.7×, near the 250ms floor) **BUT order-path churn only ~1.3×** (ON 18 place/3 cancel vs OFF 14/2) — requote hysteresis absorbs the extra cycles; `http_hr` 121/36000 both (huge headroom). ⇒ the ~4× is CYCLES; ORDERS scale with price MOVEMENT not cycle freq — #2 substantially de-risked; residual ack = confirm the CLOB ORDER limit on the busier full pilot set before ON in live. **Isolation proven:** prod md5 still `3a525d7f`, unit active same uptime (no restart), HALT absent (resumed as left), prod quoting healthy `q=81/140 accday=$22.73`; no kalshi ref in scratch; scratch removed. **Stage A gate chain COMPLETE: tests(163)→independent review(SHIP-WITH-FIXES)→fixes→paper smoke PASS.** Ready to ride the cutover bundle (flag default OFF). Stage B (reprice-only) not started (optional/separate) | Stage A DONE + bundle-ready; not yet deployed (bundles with cutover) | MEASURED — isolated A/B smoke 07-25 |

| 07-25 | **WS HOT PATH — STAGE B BUILT (`cbf954d`, branch only, flag default OFF, NOT deployed).** Operator chose "build safe Stage B now". Variant **(A)** dirty-set targeted reprice — the concurrent-thread variant (B) was REJECTED in design as a live-capital concurrency hazard. On a hot sub-pass the fast loop reprices ONLY markets whose book ticked (`_DIRTY` asset set, under BOOKS_LOCK, filled by the 3 appliers), skipping the clean majority → O(dirty) not O(universe); a FULL pass still runs every `ws_full_s` (≤1s) so freshness/gate/stale-cancel cover ALL markets (clean skips would've no-op'd on hysteresis). Pure `_scan_targets(...)` → (markets, is_full); OFF ⇒ always (universe, True) = full pass every pass = byte-identical rollback. `MAKER_WS_TARGETED`(OFF)/`MAKER_WS_FULL_MS`(1000, bounds (0,10000]). **168 tests; 4 `_scan_targets` mutants all KILLED — incl. one (`last_full<=0` first-pass guard) that INITIALLY survived because a test passed for the wrong reason (now=100 tripped the interval check too); fixed the test to isolate the guard (the "test that doesn't kill a mutant pins nothing" lesson, live).** Capital-safe: dirty markets go through the full guard stack unchanged; no new market opened on a sub-pass; single loop thread (no concurrency). ⚠ HONEST SCOPE: Stage B has ZERO pilot benefit (3 markets) — it is SCALE infra; Stage A is the pilot's latency win. Gates before bundle: INDEPENDENT review (running) + paper smoke with `ws_targeted=1` | Stage B on branch, gated; not live | this session 07-25 |

| 07-25 | **STAGE B — INDEPENDENT REVIEW (SHIP-WITH-FIXES) then PAPER SMOKE CAUGHT A BOOT CRASH the review missed; fixed; re-smoke PASS.** Review (`cbf954d`): no blocker/capital hole, all 5 claims read as holding. Fixes `76e4438`: corrected an over-claim (clean-market stale/gate cancel is deferred ≤ws_full_s, NOT "behavior-neutral"), added the exact-boundary test (a `>=`→`>` mutant had survived), fixed stale comments. **THEN the smoke (isolated, ws_hot=1+ws_targeted=1) CRASHED ON BOOT: `UnboundLocalError: _DIRTY` — `_DIRTY -= snap` is an augmented assignment that REBINDS the name → Python made `_DIRTY` a run()-LOCAL for the whole function → `set(_DIRTY)` at loop top failed on iter 1. Crashed REGARDLESS of flag ⇒ the earlier "OFF = byte-identical rollback" claim was FALSE for Stage B; tests (no run() harness) + review (code-read) + py_compile ALL missed it; only live smoke caught it.** Fix `1b45939`: `_DIRTY.difference_update(snap)` (in-place, no rebind) + regression guard `test_run_never_rebinds_module_singletons` (asserts _DIRTY/_WS_TICK/BOOKS/GEN ∉ run().co_varnames; proven to catch the class). 169 tests. **RE-SMOKE PASS (fixed engine, isolated):** banner both flags, `q=7/20`, accrues, ZERO errors, clean STOP; **targeted repricing MEASURED engaged — `full=159 sub=417 avgscan=2.4`** i.e. hot sub-passes scanned ~2.4 of 20 markets (the ticked ones) while 159 full passes kept `stale=0/nobook=0` — the O(dirty) win real on live book flow. Prod UNTOUCHED (`3a525d7f`, same uptime, no HALT, healthy `q=77/140 accday=$30.93`); Kalshi untouched; scratch removed. **Stage B gate chain COMPLETE: tests→review→FIX→smoke(caught crash)→FIX→re-smoke PASS.** ⚠ BINDING LESSON REINFORCED: live smoke is NOT optional — it caught an OFF-breaking bug on a change 2 reviewers/168 tests had cleared as OFF-identical. Stage B bundle-ready, flag default OFF; scale-only (no pilot benefit) | Stage A + Stage B both DONE + bundle-ready; not deployed | MEASURED — isolated smoke + git 07-25 |

## B. CANONICAL STATE (latest-good; supersede by appending to §A with date)

| thing | state | as-of / source |
|---|---|---|
| Live pilot | APPROVED (staged, min-size first, dedicated wallet, $75 day-floor) — NOT started | operator decision, 5B handoff §1 |
| Blocker | operator-provisioned dedicated wallet (`MAKER_PK` + `MAKER_FUNDER` → `/opt/pa2-maker-live/env`) | 5B handoff §1.2 |
| Engine on VPS | **`3a525d7f…` = `44998b1`-era, ONE COMMIT BEHIND branch HEAD `81712e09…`** (deployed 07-23 16:18Z, before `d9dfd87` classifier-#2 fix landed 16:51Z — see §A 07-24). Paper; **RESUMED 07-25 02:05Z for review** (`q=77/140 halted=False`); the 07-23 halt was REALIZED fossil settlement loss, not mark drift (§A 07-25). **REDEPLOY to HEAD required before any live cutover.** Rollback: `maker_live_engine.py.bak-20260723_161838` + restart | MEASURED 07-24 |
| Real money traded | NONE, ever | — |
| Verified income numbers | NONE — first one arrives at `--stage receipts` after the first live 00:00Z window | `MAKER_NUMBERS_LEDGER.md` |
| Live path | sanity → scoring (incl. cancel-shape probe) → tiny footprint (allowlist-pinned, weather-led) → receipts → on-chain recon | 5B handoff §6 + this session |
| Parked (propose-only) | cap SIZING redesign (flat $150 gross leaves 30/140 unquotable — distinct from the merge-blindness fixed 07-22); "unknown"-sector in-play gate hole (de-fanged for the pilot by the allowlist, unfixed for full-universe); gate-policy lock (≥3 clean post-cliff days; v5-report ranks WORST first — rank on canon only); reconciler→engine wiring; active delta shaping (GAP-1); wind-down keeps reducing side (GAP-2); qh row consumption so paper stops over-crediting a partly-filled one-sided row (G2) | 5B handoff §6.6 + doctrine port §4 |
| **REVISIT LIST (decided, re-open on a trigger)** | **`MAKER_ONESIDED_DERISK` = ON** (operator 07-23) — re-open on first receipts, on scaling past pilot size, or if heartbeat `derisk1` fires often (⇒ the real issue is cap sizing, not hedging) | §A 07-23 |

## C. STANDING CAVEATS

1. Every return-shaped figure anywhere in this lane is MODEL until the first
   receipt; the receipt-vs-model divergence check is the promotion gate.
2. The paper arm's dayPnL/marks are NOISE-tier — never quote as points.
3. Kalshi-lane numbers cited in §A are THEIR ledger's figures (their
   citations), recorded here only as review context — never blend them into
   Polymarket expectations.

| 07-29 | **SESSION 8 OPEN — health check: the arm re-HALTED 07-25 12:05:58Z, ~4 days dark.** Trip `pnl=-76.20 floor=-75.00, settle_realized=-0.57` — unlike the (corrected) 07-23 read, this one IS mark-dominated: only −$0.57 realized; dayPnL swung −$26→−$76→−$25 across adjacent hbs post-kill (NOISE marks on held one-sided inventory). Since then `q=0/140 accday=$0.00`, unit active 0 restarts since 07-23 16:18:43Z, 0 tracebacks. **REVISIT TRIGGER FIRED: `derisk1` 63 (07-24, halt-frozen) → 650 at the kill — ~587 one-sided placements in ~10 quoting hours after the 07-25 02:05Z resume** (§B revisit list: frequent firing ⇒ cap SIZING, GAP-4). Also flagged unexplained: hb `gross=$-268` (was $1249 post-kill) — resolution sweep kept clearing fossils while halted, negative gross gauge NOT diagnosed. Kalshi isolation intact (`ReadWritePaths=/opt/pa2-maker-live` only); observed report-only: their recorder unit in activating/start churn. Resume = operator call (asked, pending) | arm halted, measurement dark since 07-25; findings reported | MEASURED — ssh systemctl/journalctl/HALT file 07-29 ~18:31Z |
| 07-29 | **KALSHI-AUDIT ELEVATIONS E-A…E-F BUILT (operator: "do all"), full gate chain: 181 tests, 9 mutants killed, independent review SHIP-WITH-FIXES (F1/F2/F3 all applied), 3-run isolated VPS paper smoke PASS.** Audit method: Poly engine @ HEAD vs Kalshi 07-29 LIVE_SCALED handoff + post-07-22 log, read-only, no blind copy; their exit machinery (strand-cross/taker-flatten/slippage) confirmed N/A BY PLATFORM. Shipped on branch (`67be127`→`8d6f273`), ALL default-off/behavior-preserving: **E-A two-arm halt** — `MAKER_DAY_REALIZED_FLOOR_USD` (0=off) REALIZED arm on `settle_realized_day` only, kill names its arm (MARK/REALIZED); the exact defect that took both paper halts. Smoke-proven: seeded −$20 vs $10 rfloor → `KILL … (REALIZED arm)` live; F3: same-day resume re-fires by design (realized loss can't un-realize) — stated in the kill message. **E-B** `MAKER_ONESIDED_DERISK=off` now disables (the flagged Stage-A twin footgun, closed) + banner echoes caps/derisk/rfloor/clock. **E-C** `MAKER_MIN_HOURS_TO_END`/`MAKER_MAX_DAYS_TO_END` clock veto in discover(), unparseable-end fails closed when enabled, `dropped_clock` in universe.json, zero-picked diagnostic names BOTH filters (F2); smoke: 24h veto dropped 134, picked 122, banner `clock=[24h,0d]`. **E-D** `mm_funnel_audit.py` per-market selection waterfall (engine fns imported, never copied). **E-E** softness probe + capital-efficiency rank (share-model rew/day ÷ measured two-sided cost ≈ msz·(1−v); NOT pool/msz). **E-F** `mm_live_monitor.py` signal-only journal filter, cross-checked against the real 07-25 kill tape (7/7 signals, quiet lines silent). Staged env gains rfloor=$10 + clock 24h/30d rows (operator reviews at cutover). Reviewer's live-gamma read during review: allowlist survivors 64, clock-veto would drop 38 → 26 remain (does NOT zero the pilot universe) | E-A…E-F on branch, gated; deploy still bundles with cutover; prod+Kalshi untouched (md5 `3a525d7f` unchanged, HALT as found) | this session + independent review + isolated smoke 07-29 |

| 07-29 | **Operator decisions:** #2 paper arm RESUMED (HALT archived `HALT.cleared-20260729-s8`; post-resume hb 19:21:40Z `q=73/140 halted=False accday=$0.16` — MODEL accrual restarted; `gross=$-268` oddity persists, still undiagnosed). #3 staged rfloor+clock rows APPROVED for cutover review. #4 GAP-4: proceed per rec — **propose-only msz-scaled cap design** appended to the elevations register (cap = K·msz·(1−v) clamped, telemetry-first, blocked on receipts + operator capital number); no code | arm quoting again; GAP-4 design on record | operator + MEASURED hb 07-29 19:21Z |

| 09-01 | **STATUS READ (session 8 continuation; date verified `date -u` both ends 13:06Z).** (1) Arm re-HALTED **2026-07-30 02:11:21Z**, ~7h after the 07-29 resume — THIRD single-floor trip, same shape: `pnl=-76.22, settle_realized=-0.17` (HALT file) — 3-for-3 mark-noise-dominated kills; the branch E-A REALIZED arm targets exactly this, still undeployed. Measurement dark since 07-30 (~33 days). (2) **NEW DEFECT: OOM-KILL 2026-08-26 06:48:43Z** — systemd `oom-kill`, 384.9M peak vs the 384M unit cap, auto-restarted 06:49:13Z; memory grew to the cap over ~4 weeks halted-but-running (books/ws stay hot while halted). Undiagnosed; engine md5 unchanged `3a525d7f` across the restart. (3) hb 13:01:48Z: `q=0/136 gross=$-488 accday=$0.00 derisk1=666`; universe drifted 140→136. (4) **No receipts ledger exists; real money never traded** — verified income numbers still NONE | arm halted + OOM finding logged | MEASURED — ssh date/journalctl/HALT/md5sum 2026-09-01 ~13:07Z |
| 09-01 | **PROCESS INCIDENT (own goal, caught + reverted same minute):** this worktree sat a month in the session temp dir; the OS cleaner gutted its working directory (the EXACT S7 phantom-deletion hazard the kickoff warned of). A `git add docs/RUNNING_TAB` of the missing-but-tracked file staged a DELETION; commit `a567a1e` deleted the tab and was pushed. Reverted (`git revert a567a1e`) + row re-applied. **Standing rule tightened: NEVER `git add` from an aged temp worktree without `git status` first; verify the file EXISTS on disk before staging it** | tab restored; no history lost (append-only survived via revert) | this session 09-01 |

| 09-01 | **PAPER ARM DEPLOYED TO BRANCH HEAD + RESUMED (operator: "deploy branch head to the paper arm so measurement runs").** Backup `maker_live_engine.py.bak-20260901_131442` (= `3a525d7f`, the rollback); installed md5(LF) **`7304dbc1`** = branch `72aa783` engine blob (unchanged since `8d6f273`), venv-compiled first; unit restarted 13:14:48Z, clean banner incl. new echo `caps=$150/$200/$600 derisk=True`, 136 mkts/272 assets/4 ws. Boot re-armed HALT from persisted meta (by design); removed at runtime 13:15:31Z → RESUMED. First hb 13:20:01Z: `q=86/136 halted=False accday=$1.36`, 0 tracebacks. **ZERO env changes — all S8 knobs at defaults (realized arm OFF, clock veto OFF): engine is behavior-identical to `3a525d7f` except classifier-#2 labels + banner echo. The mark floor $75 WILL trip again on mark noise (3-for-3 history) until the two-arm env values are operator-approved** (exact values proposed in-session: mark floor 300 / realized floor 75). The 07-24 "redeploy to HEAD required before live" item is now CLOSED for the paper arm; the staged-env cutover step remains wallet-gated | paper arm on branch HEAD, quoting; env split pending operator numbers | MEASURED — ssh md5/journalctl 2026-09-01 13:14-13:20Z |

| 09-01 | **TWO-ARM HALT ARMED ON THE PAPER ARM (operator approved the exact values).** Tier-2 config: env gains `MAKER_DAY_LOSS_FLOOR_USD=300` (mark arm was 75 default — now catastrophic-drift-only; the in-minute `day_floor` deny loosens identically) + `MAKER_DAY_REALIZED_FLOOR_USD=75` (realized arm ON at the original protection level). Backup `env.bak-20260901-s8arms`; unit restarted 13:23:55Z, banner `floor=$300 rfloor=$75`, booted un-halted; hb 13:29:09Z `q=85/136 accday=$3.95 halted=False`, 0 tracebacks. **Rollback:** remove the two lines + `sudo systemctl restart polymarket-maker-live` (restores floor=75, arm off). Measurement now survives mark noise by construction; a $75+ realized settlement day still kills, self-diagnosed | both arms live on paper; gate-policy measurement clock restarts 13:23Z | MEASURED — ssh env/journalctl 2026-09-01 13:23-13:29Z |

| 09-01 | **NEGATIVE-GROSS DIAGNOSED — accounting artifact, reconciles to the cent; capital path unaffected.** Mechanism (code): `merge_pairs` credits exactly $1/pair against `spent` and `try_settle` subtracts the realized payout then zeroes inventory — so a settled market keeps `spent = −(its realized net)` forever, and the hb `gross` sums `spent` over ALL entries incl. settled. MEASURED (state.json 13:32Z): settled 64 entries Σspent **−671.35** + live Σspent **+183.76** = **−487.59 ≈ hb gross −488**. ⚠ the −671 settled residue is NOISE-tier paper accounting (known paper over-credit bias, G2) — NOT a quotable realized P&L. Caps are unaffected: market gross cap reads the LIVE market's own spent; sector gross quarantines departed. Gauge-blindness only (RULE TWELVE class): "gross" stops meaning gross once settlements accrue. PROPOSED (needs sign-off): split the hb gauge into live-committed vs settled-residue — display-only change | diagnosed, reconciled; fix propose-only | MEASURED — state.json + code read 2026-09-01 13:32Z |
| 09-01 | **OOM DIAGNOSED (partially) — unbounded state accretion is the measured structural grower; full 140MB attribution needs a slope measurement.** MEASURED: state holds **8,921 market entries** (64 ever settled, 8,857 unresolved-departed/live, only 4 with inventory), state.json **5.3MB** re-serialized every minute; resolution-sweep drain budget = 30 lookups/hr (720/day, code const) vs daily universe churn inflow — backlog grows by construction, entries are NEVER pruned. Memory trend: 244.5M (07-29 status read, 6d uptime) → **384.9M peak at the 08-26 oom-kill** (journal; MemoryMax=384MiB, the ONLY oom in unit history) → young process now 131M. In-RAM cost of the 8.9k entries + per-minute 5MB JSON dump churn is the leading HYPOTHESIS for the ~140M/4wk growth; not fully attributed — follow-up = RSS slope on the fresh process over days. PROPOSED (all need sign-off): (a) prune fully-flat settled/departed entries after N days, (b) raise sweep budget, (c) raise MemoryMax as a stopgap | diagnosed to mechanism level; fixes propose-only; slope measurement owed | MEASURED — journalctl/systemd/state.json 2026-09-01 13:32Z |

| 09-01 | **OOM + GAUGE FIXES APPLIED AND VERIFIED LIVE (operator: "apply the fixes").** Engine `2ff84da`→`246a862` (187 tests, 6 prune/gauge mutants killed incl. two that survived round 1 — tautology test replaced with extracted `split_spent()` + corrupt-state guard), deployed md5(LF) `d8e5a02b`, backup kept. (1) **Hourly `prune_state`**: removes ONLY inert departed entries (settled-flat → residue archived to settlements ledger + `day_anchor` shifted for exact day_pnl invariance; zero-value husks → free delete); NEVER touches in-universe / departed-with-value / zombies / standing quotes / settled-with-inventory. First firing 13:43:39Z: **8,784 removed, state.json 5.3MB→101KB, entries 8,921→138**. (2) **hb gauge split**: `gross=` → `committed=$184 resid=$0` (committed matches the diagnosed live sum +183.76; resid zero because residues are now archived). (3) sweep budget 30→120/hr (measured respend=2 today — capacity for future backlogs). (4) unit `MemoryMax` 384M→768M stopgap, daemon-reloaded. **Invariance proof: dayPnL $0.88 IDENTICAL across the prune** (hb 13:29 pre vs 13:48 post); q=87/136, accday $9.65, 0 tracebacks, mem 99M/768M | all four fixes live on the paper arm; OOM driver removed at the root | MEASURED — ssh journalctl/state.json/systemd 2026-09-01 13:43-13:48Z |

| 09-01 | **INDEPENDENT REVIEW of the prune/gauge fixes (operator-ordered, post-deploy): SHIP-WITH-FIXES — keep the paper arm as-is; engine money-math CONFIRMED (invariance exact in every branch, rollover/sweep/caps/arms verified, 187 tests, 6/6 mutants re-killed by the reviewer independently).** Findings, ranked: **F1 (live-cutover blocker): `maker_onchain_recon.py` hard-DRIFTs vs a pruned state** — union check flags every pruned settled market as `ledger_without_state` (exit 2), and the chain check builds its token list from state so pruned markets' real on-chain tokens drop out of live coverage; recon must learn `act:"prune"` rows before cutover. **F2:** committed unit file still `MemoryMax=384M` — a unit redeploy silently reverts the box's 768M. **F3:** prune branch (a) lacks the zombie/standing-quote guard branch (b) has (boot-departure shape can delete order-id records); `collect_owned_assets` docstring ("never a delete") now false; backup script doesn't cover pa2-maker-live. **F4:** empty-universe boot (discovery outage) would prune every active husk — wants `if not universe_ids: return 0`. **F5:** husk prune destroys per-market `acc` (CAPTURE measurement source in maker_canon) unarchived. **F6 (note):** crash-window duplicate prune rows possible; dedupable. Also flagged: sweep@120 worst-case stall 20min if backlog ≥120 vs hanging gamma (pre-existing class, scaled); `report()` settled bucket drains post-prune (cosmetic). Verified non-issues: monitor regex matches new hb incl. negative dollars; no other `gross=` parsers; no memory spike | verdict SHIP-WITH-FIXES; fixes = operator decision (options presented in-session) | independent reviewer 2026-09-01 |
| 09-01 | **OPERATOR RULINGS:** wallet keys will be HANDED OVER but the bot stays NOT LIVE (sanity/scoring only until an explicit go); live-pilot loss budget = **max $10 per 24h** → staged floors re-cut mark $10 / realized $10 (`9ef179d`); prune/gauge review ordered (row above) with data+options back to operator | recorded | operator 2026-09-01 |
