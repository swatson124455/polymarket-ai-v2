# POLYMARKET MAKER — RUNNING TAB (living ledger; append, never overwrite history)

Started 2026-07-21, adopted from the Kalshi lane's ledger practice after the
operator-directed review of their arc (their tab made a losing week fully
auditable; ours starts BEFORE first live money so the whole pilot is on the
record). This file is the chronological ledger of record for the Polymarket
Maker lane. Definitions/quote-rules live in `docs/MAKER_NUMBERS_LEDGER.md`
(the definition of record) — this file records EVENTS.

Rules for every session (binding):
- APPEND dated entries; never silently revise an old row — post a CORRECTION
  row that names what it corrects.
- Every number carries source + method + date. Tiers per
  `MAKER_NUMBERS_LEDGER.md`: CONFIG / MEASURED / MODEL / NOISE / BANNED.
- REWARDS BASIS ONLY (`MAKER_MASTER_PLAN.md` §0a); no derived EV/return until
  a real on-chain reward receipt anchors it (§0b). Trading marks: band only.
- Concentration-check pooled numbers before quoting (Protocol 14).
- md5 comparisons across machines: state the normalization
  (`tr -d '\r' | md5sum`) — CRLF working trees hash differently.

---

## A. LEDGER (chronological)

| date | event | verdict / state | source |
|---|---|---|---|
| 07-20 | Session 5B close: operator GO (staged, min-size, DEDICATED wallet, $75 floor); hardened engine DEPLOYED paper+halted; venv fixed; NO-TAKER hard rule; numbers discipline rebuilt (rewards basis only) | blocked ONLY on operator wallet | `AGENT_HANDOFF_2026-07-20_MAKER_SESSION5B_CLOSE.md` |
| 07-21 | Session 6 health check: engine md5 `1961f4b9…` VPS == branch HEAD `3531d83` (LF-normalized); unit active since 07-20 16:11:24Z, zero restarts; heartbeats clean (zombies/lmiss/feedfail/anom all 0); HALT from 07-20 07:00Z day-floor kill persisting by design; env has no wallet keys | paper arm HEALTHY; wallet still the only blocker | MEASURED — ssh journalctl/systemctl/md5sum 07-21 ~20:10Z |
| 07-21 | Kalshi-lane review (operator-directed, read-only): their live arc lost ~$45/$100 (their ledger) to execution microstructure the model never priced — taker fire-sales on flatten, unmanaged inventory accumulation, wrong-markets selection. Three lessons adopted here (rows below) | lessons imported BEFORE our first live dollar | their `KALSHI_RUNNING_TAB.md` + 07-21 handoff, read from `origin/claude/maker-kalshi-live` |
| 07-21 | ADOPTED #1 — `MAKER_SECTOR_ALLOWLIST` (CONFIG, `maker_live_engine.py` load_config/discover): non-empty ⇒ only listed sectors survive discovery, applied BEFORE ranking so a shrunk `MAKER_MAX_MARKETS` cannot admit an off-list market; overlap with excluded sectors fails loud; empty ⇒ behavior unchanged. Closes the Kalshi wrong-markets shape for our tiny footprint; fail-closes "unknown"-sector markets out of the pilot | built on branch; NOT deployed | this session's commit |
| 07-21 | ADOPTED #2 — cancel-SHAPE probe in `maker_preflight.py --stage scoring`: raw `cancel_orders` response captured, printed, and run through the ENGINE'S own `_cancel_shortfall` (imported, never copied). An unanticipated shape now surfaces at scoring, not at the first live kill | built on branch; NOT deployed | this session's commit |
| 07-21 | ADOPTED #3 — this running tab | active | this file |
| 07-21 | CLASSIFIER BUG caught by the allowlist's first-output cross-check on live gamma: weather KW `heat-` matched "miami-heat" NBA slugs — "Will LeBron James play for the Miami Heat in 2026-27?" classified WEATHER (would have entered the weather pilot slice; the exact Kalshi mis-labelled-market shape). Engine-only fix: `heat-` → heat-wave/heatwave/heat-index/heat-advisory/extreme-heat (+lowest-temp); LeBron → "unknown" → fail-closed out of any allowlist. ⚠ family recorder arms v1–v6 + census + research scripts keep the OLD pattern mid-era (measurement attribution only — census/canon "weather" sector stats may include team-name matches); family sync = PROPOSE-ONLY, own era stamp | fixed in engine; post-fix live cross-check = 8/8 genuine temp markets | MEASURED — discover() run vs live gamma 07-21, pre/post diff |

| 07-21 | INDEPENDENT ADVERSARIAL REVIEW of the session's changes → SHIP-WITH-FIXES, all fixed same session: (1) MAJOR — pre-existing partial-discovery guard's 40-market floor would have FROZEN any allowlisted (by-design-small) universe after first adoption; every refresh logged "PARTIAL" + discarded; daily-churn weather slice stale within a day. Fixed: `discovery_suspect()` extracted+testable, floor waived when allowlisted, relative half-shrink guard retained. (2) MINOR — heat-regex fix orphaned real heat-event slugs (heat-dome/heat-warning/heat-emergency/record-heat/excessive-heat) → restored. (3) MINOR — typo'd allowlist drops everything silently (universe.json write is gated on non-empty picked) → loud "matched ZERO" journal line added. (4) banner now prints allowlist. (7) preflight cancel-shape probe validates the SIBLING engine file → runbook rule: run preflight from /opt/pa2-maker-live. 126/126 tests | discipline held: reviewer found what the author's own tests missed | reviewer report, this session |

| 07-22 | WB RELAY IN (WB session 07-21/22). **(1) c13 CLOSED-EMPTY — DO NOT PURGE the WB feed.** The standing premise (pre-c5 mislabeled ~0.44 nowcast lines in `/opt/pa2-maker-feeds/wb_forecasts.jsonl`) does NOT hold: WB's audit of 17,220 lines (07-17T18:57:36Z→07-22T02:06:23Z) found ZERO lines with prob∈[0.42,0.46] across the entire pre-c5 window (07-17/18/19); exactly one line matches the c13 signature (Jeddah, 07-20T05:00:48Z, prob 0.4438), and the nowcast signal's FIRST `prediction_log` row is 07-20 19:20:31 — ~14h AFTER that line, so it cannot be a nowcast row. Purging would delete genuine data and bias our own tilt study. **(2) Shared RedisCache `raise_on_error` is on master (`0e26f70`) + deployed in master release `20260721_232241`**; backward-compatible, capability-only until a caller opts in — **NO ACTION for Maker: no Maker script imports the shared `RedisCache` at all** (verified: only `seed_unpriced_blacklist.py` uses raw `redis.asyncio`; the v6 grep hit was the word "rediscovery") | c13 closed; tilt readout UNBLOCKED; Redis = no-op for us | WB relay + MEASURED grep of `scripts/**` 07-22 |
| 07-22 | ⚠ **FEED LANDMINE (WB, FYI):** master's committed `deploy/polymarket-weather.service` was MISSING `/opt/pa2-maker-feeds` from `ReadWritePaths`, and `deploy.sh` overwrites the live unit every master deploy → `ProtectSystem=strict` would have made our drop read-only, and WB's exporter swallows errors by design, so **the feed would have stopped SILENTLY**. Fixed on master as `4b50ce7` before the deploy; verified after (feed grew 17,734→17,754 lines across it). **If the WB feed ever goes quiet, check that `ReadWritePaths` line FIRST** | near-miss, closed | WB relay 07-22 |
| 07-22 | **TILT-vs-CONTROL READOUT DELIVERED (owed to WB).** Method `scripts/maker_research/mm_tilt_readout.py` (new, in git). Paired P6_tilted vs P0_base on identical books; **era guard** applied (P0's ledger carries from 07-17 02:36Z vs P6's 21:55Z — raw `acc` would fake a P0 win on boundary-spanning markets; primary cut = markets dated ≥Jul 19, robustness ≥Jul 20; 50 date-unparseable EXCLUDED+disclosed). n=227 covered markets primary / 172 robust. **REWARDS (MODEL, unverified): the tilt COSTS rewards — −14.0% primary, −13.5% robust** (stable across cuts). Concentration CLEAN (top1 2.4–3.2%, top5 11–14% — broad-based, not one market). **NULL CONTROL PASSES EXACTLY: the w=0.00 tier (HK-excluded, zero weight) shows delta $0.00 on 8/6 markets** — where no tilt is applied P6≡P0 to the cent, so the harness is measuring the tilt, not drift. Cost is roughly FLAT in % across w=1.00/0.50/0.25 (−14.2/−14.2/−16.3%), consistent with the 1c cap binding regardless of weight. Trading leg (NOISE, band only, never a headline) points the SAME direction — no offsetting gain visible. **Mechanical cross-check (independent of the measurement):** the reward formula `((v−s)/v)²` puts a 56% score penalty on a 1c tilt against a 3c band; with 28% of markets tilted at snapshot the aggregate −14% implies ~50% on tilted markets — the measured cost matches the formula's prediction | VERDICT: tilt is reward-NEGATIVE at current parameters; no fill-quality offset measured | MODEL accrual, `mm_tilt_readout.py` run vs v5 state 07-22 |

| 07-22 | **KALSHI DOCTRINE PORT (operator: "mirror the kalshi plan in full … verify plan works on the platform")** → `docs/MAKER_KALSHI_DOCTRINE_PORT.md`. Every Kalshi element (6 trading principles, 7 cycle behaviors, 8 fixes) mapped with a verdict + evidence. **Headline platform finding: their taker machinery is N/A BY PLATFORM, not by preference** — on Polymarket both legs are BUYs, so reducing delta IS a post-only maker buy of the complement, which merges to a $1 pair (`merge_pairs:1158`). Their most expensive problem (unwinding costs spread; last resort = fire-sale) is structurally absent here. Their §2.5/§2.7/fix-G would violate our no-taker rule AND solve a problem we don't have. We were already ahead on: no-taker, blind-feed response (kill+halt at 10 fails vs their cancel-ids at 2), scoped kill primitive, never-faked held value | port complete, doc in git | this session |
| 07-22 | **CAPITAL DEADLOCK — real defect FOUND; attempted fix REVERTED on independent DO-NOT-SHIP review.** Defect (stands): `merge_pairs` nets `spent` down AFTER a fill but the gross cap is checked BEFORE the order → the cap denies the very order whose fill would relieve it; both legs cost money so `market_gross_cap` binding stops ALL quoting. Attempted fix (exempt risk-reducing orders from the 2 gross caps) was **ineffective AND unsafe**: (a) the sole caller checks legs in FIXED order yes→no and is TWO-SIDED-OR-NOTHING, so in long-YES the accumulating leg is denied first and the exempt leg is discarded — deadlock not relieved at all; long-NO relieved only on a random jitter draw; strict no-op at `MAKER_SIZE_JITTER=0`; (b) `reducing` computed once per CALL exempted the sibling ACCUMULATING leg too, and legs fill independently → traced to **1.65× market_gross_cap** + a sector-cap breach; (c) **4 of my 6 tests passed against a mutated `reducing=True`** — they were pinned by `market_net_cap`, not the new code. REVERTED in full; suite back to committed 126/126. ⚠ **PLATFORM FACT that constrains any retry: a single-legged quote scores ZERO rewards (two-sided MIN)** — so a lone hedge leg buys risk reduction ONLY and can never restore the lost income; "rewards stop" is a cap-SIZING problem (GAP-4), "cannot hedge" is a separate one-sided-exception decision. Retry bar recorded in port §3d (per-leg classification, caller in scope, mutation-tested tests, no RNG in guards) | defect OPEN + propose-only; no code shipped | adversarial review, this session |

| 07-22 | **CAPITAL DEADLOCK FIXED** across `a660aa1` → `87b7c30` → `44998b1` (3 commits, 3 adversarial review rounds, 13 mutants). (1) **merge-aware capital accounting** — the cap was MERGE-blind, not delta-blind: a complement buy against ALREADY-HELD inventory forms $1 pairs on fill (guaranteed, not contingent), so `eff_cost = cost − min(sz, held_opposite)`. Needs no exemption/boolean/sign-rule; pending-sibling merges deliberately NOT counted (legs fill independently) so ordinary two-sided quoting is capped exactly as before. (2) **one-sided de-risk placement** — necessary because the caller is two-sided-or-nothing, so the accumulating leg denies first and the correctly-priced hedge was discarded. ⚠ **it scores ZERO rewards (two-sided MIN) — it buys RISK REDUCTION ONLY and never restores the income the deadlock costs.** Behind `MAKER_ONESIDED_DERISK` (default ON for the paper window; reviewer withdrew its default-OFF objection, condition: operator re-decides before the first funded run). (3) **`match_fills_paper` blindness** — the row-skip test discarded ANY row with a None bid, so an ask-only quote (= the long-YES hedge shape) rested but could never fill in paper. Silent measurement blindness on the feature's only success path | deadlock CLOSED; not deployed | 3 reviews + mutation testing, this session |
| 07-22 | **REVIEW ARC — 2 DO-NOT-SHIPs before it was right; the failures are the value.** Attempt 1 (exempt "reducing" orders from gross caps) REVERTED: evaluated per-CALL so it exempted the accumulating sibling leg (traced **1.65× the cap**), was a strict no-op at `MAKER_SIZE_JITTER=0`, and **4 of its 6 tests passed a fully-broken mutant** (they were pinned by `market_net_cap`, not the new code). Attempt 2 blockers: **F1** my one-sided hysteresis poisoned the feature through its own SUCCESS path (nothing clears ob/oa on a FILL → stale leg reads as standing + net≠0 → market skipped for hours = phantom hedge, nothing resting, nothing earned; the relieving fill is the NORMAL outcome so the hold was anti-correlated with recovery); **F2** paper re-credits a standing row every print → ask-only row ran long-YES 300 into **long-NO 900**, falsifying the placement invariant in the mode meant to measure it; **F3** commit path had ZERO coverage — `want_n = 1` made the engine place-then-cancel every quote forever earning $0, with 140/140 passing. Attempt 3: fixing F1 by DELETING the hold opened **G1** — a lone hedge requoted every scan sits permanently back-of-queue and may never fill live, while paper (no queue model) shows it working = paper/live divergence on the success path. Final: hold restored with a **q_inv inventory snapshot** (release on any fill), whole caller extracted to `commit_placements`, `len(legs)` fail-closed guard (G3) | **LESSONS (binding): a guard cannot be reviewed apart from its CALLER; extracting only the arithmetic RELOCATES the untested surface; every safety test must kill a deliberately-broken implementation or it pins nothing** | review rounds 1–3 |

| 07-23 | **FIRST LIVE SMOKE TEST of the session's engine** — isolated scratch instance on the VPS (`/tmp` base + separate state, `--base` isolated), pilot config `MAKER_SECTOR_ALLOWLIST=weather MAKER_MAX_MARKETS=8`, paper mode. Production arm NEVER touched (verified after: md5 still `1961f4b9…`, unit active, HALT still present). RESULTS: `ENGINE START … max_mkts=8 excluded=['esports','finance'] allowlist=['weather'] floor=$75` (banner fix live); discovery **8/8 genuine weather markets, 402 dropped by allowlist, ZERO NBA/"unknown" contamination** (classifier fix confirmed against live gamma, not just tests); two-sided quote path ran end-to-end (167 order rows, `ob`+`oa` both set); **the new `q_inv` snapshot key is written correctly** (`[0.0,0.0]` matching y=n=0); `derisk1` absent = one-sided path correctly did NOT fire with no directional inventory; **0 error/traceback/exception lines**; clean py_compile. Scratch fully removed after | engine boots + quotes + persists correctly under pilot config | MEASURED — isolated VPS run 07-23 16:07–16:12Z |

| 07-23 | **OPERATOR DECISION — `MAKER_ONESIDED_DERISK` stays ON for the pilot** (took the recommendation), logged as a REVISIT item rather than a silent default. Plain terms: the bot normally rests both sides of a market because the reward only pays on a two-sided quote; if one side keeps filling it ends up holding an unwanted directional bet, and the cure is buying the opposite side (which makes the pair worth exactly $1 and removes the gamble). The switch lets it place that evening-out order alone when the spending cap has blocked everything else. ⚠ **The lone order earns NOTHING** (no pair ⇒ no reward) and, if price has drifted against the held side, evening out LOCKS IN that loss instead of leaving it to chance. Accepted trade: small pilot sizes, and being cornered holding an unwanted position into settlement is the worse first-live surprise. **REVISIT WHEN:** the first receipts land (real cost visible), or sizes scale past the pilot, or `derisk1` on the heartbeat shows it firing often — frequent firing means the real problem is cap SIZING (parked GAP-4), not hedging | decided; revisit item, not urgent | operator, 07-23 |

## B. CANONICAL STATE (latest-good; supersede by appending to §A with date)

| thing | state | as-of / source |
|---|---|---|
| Live pilot | APPROVED (staged, min-size first, dedicated wallet, $75 day-floor) — NOT started | operator decision, 5B handoff §1 |
| Blocker | operator-provisioned dedicated wallet (`MAKER_PK` + `MAKER_FUNDER` → `/opt/pa2-maker-live/env`) | 5B handoff §1.2 |
| Engine on VPS | `1961f4b9…` = branch `claude/maker-bot` HEAD `3531d83`; paper; HALTED (07-20 day-floor) | MEASURED 07-21 |
| Real money traded | NONE, ever | — |
| Verified income numbers | NONE — first one arrives at `--stage receipts` after the first live 00:00Z window | `MAKER_NUMBERS_LEDGER.md` |
| Live path | sanity → scoring (incl. cancel-shape probe) → tiny footprint (allowlist-pinned, weather-led) → receipts → on-chain recon | 5B handoff §6 + this session |
| Parked (propose-only) | cap SIZING redesign (flat $150 gross leaves 30/140 unquotable — distinct from the merge-blindness fixed 07-22); "unknown"-sector in-play gate hole (de-fanged for the pilot by the allowlist, unfixed for full-universe); gate-policy lock (≥3 clean post-cliff days; v5-report ranks WORST first — rank on canon only); reconciler→engine wiring; active delta shaping (GAP-1); wind-down keeps reducing side (GAP-2); qh row consumption so paper stops over-crediting a partly-filled one-sided row (G2) | 5B handoff §6.6 + doctrine port §4 |
| **REVISIT LIST (decided, re-open on a trigger)** | **`MAKER_ONESIDED_DERISK` = ON** (operator 07-23) — re-open on first receipts, on scaling past pilot size, or if heartbeat `derisk1` fires often (⇒ the real issue is cap sizing, not hedging) | §A 07-23 |

## C. STANDING CAVEATS

1. Every return-shaped figure anywhere in this lane is MODEL until the first
   receipt; the receipt-vs-model divergence check is the promotion gate.
2. The paper arm's dayPnL/marks are NOISE-tier — never quote as points.
3. Kalshi-lane numbers cited in §A are THEIR ledger's figures (their
   citations), recorded here only as review context — never blend them into
   Polymarket expectations.
