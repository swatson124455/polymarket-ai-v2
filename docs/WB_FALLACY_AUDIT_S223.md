# WeatherBot Fallacy Audit — S223 (2026-07-06) · verify pass 2 completed S224 (2026-07-08)

**Status:** VERIFY PHASE COMPLETE. First pass (S223, operator machine): 18 of 62 verified before a model credit limit killed the remaining 43 verifiers + synthesis. Second pass (S224, cloud session): the raw finding TEXTS were lost (only truncated titles survived in the workflow dump — the journal lived on the operator's machine), so each of the 43 was **reconstructed from its title against the code and adversarially re-verified from scratch**. Results in the "SECOND VERIFY PASS" section below: ~28 newly confirmed, 7 duplicates of pass-1 findings, 2 unclear, 2 refuted-as-live (dead paths), plus 2 NEW adjacent findings discovered during verification.

**Finding #1 (sum-to-1 renorm) is FIXED — commit `caffc68` (S224):** deflate-only normalization in all 4 engine sites + METAR override renorm guard (no-evidence skip / singleton skip / 0.98 conditioning cap). Not yet deployed.

**Pivotal reframe (verified this session):** `WEATHER_FLAT_SIZE_USD=100` is default-on (unset on VPS). weather_bot.py:3421 sets `_raw_size=_flat_size` and SKIPS `combined_boost`. So the entire boost/dampener/Kelly/Smoczynski-Tomkins sizing chain is INERT for trade size (flat $100, then liquidity-clamped). Findings in the edge/boost/Kelly family are therefore DEAD-CODE + LYING-TELEMETRY, not live money loss. The live-corrupting findings are the CERTAINTY (model_prob->1.0) and CALIBRATION ones — they drive trade ADMISSION and DIRECTION and corrupt the S222 verification readout.

## Confirmed findings (18), ranked by severity

### 1. [CRITICAL · high-confidence] Sum-to-1 renormalization over non-exhaustive bucket sets manufactures literal model_prob=1.0 (today's live signal)
- **File:** `bots/weather/engine/base_engine/weather/probability_engine.py:218`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / probability-mass conservation applied to a partial partition
- **What:** bucket_probabilities clamps each bucket to [0.001,0.999] (line 214) then divides by the total so probabilities sum to 1.0 (lines 216-220; same pattern in empirical_bucket_probabilities lines 315-319 and the scipy-less fallback lines 374-381). This renormalization encodes the belief that the buckets passed in exhaust the outcome space. They frequently do not: the S221 thin-market filter (weather_bot.py:1730, _filter_thin_markets at 4536-4582) drops individual sibling buckets on liquidity/volume/spread BEFORE grouping (weather_bot.py:1777), and group_markets (market_mapper.py:584-594) also silently drops buckets on parse failure and builds a group from whatever survives, with no completeness check and no minimum bucket count. The only defense, the M7 coherence check (weather_bot.py:2714), fires only when len(group.buckets) >= 4 â€” a group stripped to 1-3 buckets bypasses it entirely. Degenerate case: a single surviving bucket with any model p in (0.01, 0.99) renormalizes to p/p = exactly 1.0, silently breaking the 0.999 clamp whose stated purpose is 'avoid 0/1'. This is the only code path that can emit a literal 1.0, and it exactly reproduces today's live signal: model_prob=1.0, price 0.43, edge 0.57=1.0-0.43, at 0.8h lead (with >=2 buckets the 0.001 sibling floors cap the renormalized max below 0.9995). With 2-3 survivors the same math inflates every probability by 1/total, fabricating edge on both YES and NO sides.
- **Harm:** A market where the model may actually assign ~40-60% is entered as a certainty: the fabricated 0.57 edge passes the 0.08 min-edge gate, the exec-edge gate (1.0 minus any ask is positive), and the negative-EV gate (capped confidence 0.95 > price 0.43); the Smoczynski-Tomkins allocator consumes the raw 1.0 and computes full Kelly edge f_i=1.0, awarding this bucket the maximum share of the group budget. The bot systematically buys whatever bucket survives the liquidity filter, at whatever price, on structurally thin events â€” the trade admission itself, its direction, and its allocation are all artifacts of which siblings got filtered, not of any forecast.
- **Verifier correction:** Two corrections, both widening the finding rather than weakening it. (a) probability_engine.py:216-220 is NOT the only code path that can emit a literal 1.0 as claimed: _apply_metar_resolution_day_override renormalizes at weather_bot.py:3021-3025 with no near-1 skip condition and no clamp, so a single-bucket group emits exactly 1.0 through that path UNCONDITIONALLY (whatever the override set â€” 0.98, 0.97, 0.92 â€” divides to 1.0). Today's 0.8h-lead signal is inside its <12h window (weather_bot.py:2669), so the METAR renorm is plausibly the final writer of today's 1.0 when METAR data is available; both sites are instances of the identical fallacy (sum-to-1 over a non-exhaustive bucket set) and both need the fix. (b) Scope is broader than the claim's 1-3-bucket framing: because M7 (weather_bot.py:2714) checks only the priced fraction of buckets that survived into the group, groups with >=4 buckets whose thin siblings were dropped pre-grouping (weather_bot.py:1730) also have every probability silently inflated by 1/total with no detection possible at any layer â€” the manufactured-edge harm applies to all incomplete groups, not just the <=3-bucket M7 bypass. Minor citation nit: the clamp is line 214 and the divide is lines 219-220; the cited :218 is the guard condition line.

### 2. [CRITICAL · high-confidence] METAR 'aggressive window' asserts near-certainty hours before the daily max exists, and renormalization inflates the deliberate 0.97/0.98 humility caps to a literal model_prob=1.0
- **File:** `bots/weather_bot.py:2974`  (lens: edge-and-boosts)
- **Fallacy class:** certainty-fallacy / stale-anchor premise
- **What:** lead_time_hours is anchored to LOCAL NOON (forecast_client.py:1229-1234, S102), so the `aggressive = lead_time_hours < 2.0` window (weather_bot.py:2964) opens at ~10:00-12:00 local time. The branch's premise â€” 'daily high is nearly established' (2975, 3005) â€” is climatologically false at that hour: daily maxima typically occur 15:00-17:00 local, 3-5 hours AFTER the anchor. Non-monotone assertions fire in that window: at_or_belowâ†’0.98 when running_max is merely below the ceiling pre-heating (2974-2976), at_or_higherâ†’0.001 rule-out (2989-2991), rangeâ†’0.92 (3003-3006). The in-code S222 rationale (2932-2934) reasons about an '18:00 UTC anchor' that the code no longer uses â€” the guard that removed the ungated pushes was calibrated against the wrong anchor and explicitly trusted the <2h window. Then renormalization (3021-3025) divides by a total<1, inflating the asserted 0.97/0.98 toward â€” and for a single-bucket group exactly to â€” 1.0, erasing the 2-3% tail-risk margin the caps were designed to keep. Today's live signal (model_prob=1.0, lead 0.8hâ‰ˆ11:12am local, edge 0.57 vs price 0.43) is this exact mechanism. There is no divergence guard anywhere: a 57-point gap to the market at sub-hour lead is treated as pure alpha rather than evidence of model/resolution mismatch â€” despite the resolution source being WU (per market description), not the METAR T-group the override treats as ground truth.
- **Harm:** The bot buys 'certain' outcomes at mid-morning against counterparties pricing the afternoon heating still to come; when the max rises past the asserted ceiling the position loses ~100% of stake, and the 1.0 probability makes every downstream gate (min_edge, negative-EV, Kelly) size it at maximum. Each firing is a maximum-conviction bet on a factually false premise.
- **Verifier correction:** Three corrections, none exculpatory. (1) ANCHOR: the claim says lead_time is anchored to LOCAL NOON (forecast_client.py:1229-1234) and that the in-code S222 rationale cites an 18:00 UTC anchor 'the code no longer uses'. Backwards: _analyze_group computes its OWN lead_time anchored to 18:00 UTC (weather_bot.py:2516-2520) and passes THAT to the override (2670-2672); forecast.lead_time_hours (local-noon) is a separate variable used for logging (2898) and engine calls. The S222 comment is accurate about this code path. Consequence: the aggressive window opens at 16:00 UTC â€” 12:00 EDT / 11:00 CDT / 10:00 MDT / 09:00 PDT â€” and ALL trading stops at 18Z (2521-2522), so every US aggressive-window trade is 2-8h before the typical 15:00-17:00 local daily max. Worse than claimed for western US stations; approximately benign for European (18-20h local) and Asian (day complete) stations. The fallacy is US-station-scoped, which is most of the market universe. (2) RENORM: inflation to exactly 1.0 requires the degenerate case (single-bucket group â€” permitted, market_mapper.py:584-594 has no min-bucket check and M7 (2714) only fires at â‰¥4 buckets â€” or siblings all ruled out at 0.001, giving ~0.995-0.999); when siblings retain material mass, renorm DEFLATES the 0.98. The degenerate case is what today's 1.0 signal implies. (3) HARM WORDING: Kelly confidence is capped at 0.95 (2814) and the calibrator clamps to â‰¤0.99 (685), so sizing does not literally consume prob=1.0; 'every downstream gate at maximum' overstates slightly. However the unclamped 0.57 edge flows into min_edge, the negative-EV gate, and edge-proportional sizing/boosts, and the missing divergence guard is aggravated: the edge cap was deliberately removed (S118, 2741-2743) on win-rate evidence from the contaminated-ground-truth era, and entry price caps (2765/2777) filter FOR maximum market disagreement. Severity stays critical.

### 3. [CRITICAL · high-confidence] Default-on flat sizing (WEATHER_FLAT_SIZE_USD=100) silently bypasses the ENTIRE combined_boost/dampener chain while telemetry still records it as applied
- **File:** `bots/weather_bot.py:3421`  (lens: edge-and-boosts)
- **Fallacy class:** double-bookkeeping / phantom-control
- **What:** settings.py:858 declares WEATHER_FLAT_SIZE_USD default '100.0' (S173). When flat mode is active, `_raw_size = _flat_size` (3421-3424) and combined_boost â€” the product of the S153 regime/severe/jump/NBM dampeners (3311-3315), station reliability factor (3327-3329), and city Brier dampener (3334-3335) â€” is never multiplied in (its only applications are the non-flat branches at 3428 and 3443). Yet the pending-signal meta still writes `signal_multiplier = combined_boost` (3699) and shadow entries log combined_boost as if applied. So either (a) the VPS env overrides flat to 0 and Kelly+dampeners are live, or (b) flat is on and every 'dampener' epistemics in the file â€” the S153 shadow-data conclusion, station reliability, city-Brier throttling â€” is dead weight that everyone believes is active. The observed 'boost stacking' on today's trade signal is consistent with (b): boosts logged, nothing applied. Note also flat $100 is payoff-blind (same dollar stake at price 0.05 and 0.85), the exact failure S222 already diagnosed.
- **Harm:** The executed sizing policy differs wholesale from the believed one: cities with Brier>0.35 that should be near-blocked (0.10x) get full $100 stakes; any future analysis of 'did dampeners work' run against signal_multiplier/trade logs evaluates a control that never touched a dollar, producing confidently wrong retunes.
- **Verifier correction:** Severity: critical -> high, two factual corrections. (a) Flat mode is not a silent divergence from believed policy â€” it is a DELIBERATE documented S173 mitigation (code comment weather_bot.py:3416-3419; docs/WEATHER_S222_STATUS.md:103 explicitly orders 'flat->Kelly re-enable LAST'). The real phantom controls are narrower: the S168 city-Brier throttle and station-reliability factor (risk controls conceptually independent of Kelly) are collaterally dead, and signal_multiplier/shadow combined_boost telemetry (3699, 3597, 3662) records them as applied. (b) 'Payoff-blind, same $ stake at price 0.05 and 0.85' is wrong: post-flat multipliers still scale _raw_size â€” YES 0.5x (3493-3494), YES price dampener to 0.10x floor above soft cap 0.50 (3499-3505), NO price dampener above 0.70 (3516-3522), lead-time 0.60-1.15x (3530-3545), optional confidence dampener (3554-3561). A YES at 0.85 stakes ~$5 (100 x 0.5 x 0.10), not $100. Payoff-blindness holds only within the un-dampened band (e.g., YES 0.05 vs 0.45 both stake $50 despite ~9x payoff difference, since WEATHER_YES_MIN_PRICE defaults 0.0/disabled at settings:864). (c) The 'Brier>0.35 city gets full $100' harm example additionally requires >=50 resolved trades in the window (1026-1027) and NO<=0.70/YES<=0.50 pricing; otherwise stake is smaller â€” the control is still genuinely dead, but the dollar impact is bounded. Core surviving finding: with flat mode on (committed default), the combined_boost chain is dead weight while telemetry asserts it fired â€” any retune of 'did dampeners work' against signal_multiplier or shadow combined_boost evaluates a control that never touched a dollar.

### 4. [HIGH · high-confidence] analyze_opportunity is a structural certainty machine: single-bucket call means output is always 0.0, ~0.99+, or exactly 1.0
- **File:** `bots/weather_bot.py:2032`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / normalization-over-singleton
- **What:** analyze_opportunity passes a one-element list [bucket] to bucket_probabilities. Given the sum-to-1 renormalization, the possible outputs are: {} -> model_prob 0.0 (when p<=0.01, via the .get default at line 2033), unchanged p only when p>=0.99 (renorm skipped inside the |total-1|<=0.01 window), or exactly 1.0 for all p in (0.01,0.99). So this method can NEVER return a calibrated intermediate probability â€” every answer is a near-certainty on one side or the other, and edge=model_prob-yes_price (line 2034) is a fabricated |edge| of either yes_price or 1-yes_price. The confidence field caps at 0.95 (line 2048) which cosmetically masks the 1.0 without fixing edge. The dashboard manual-analysis path (ui/dashboard.py:1760) consumes this method directly, and it is the documented BaseBot fallback for single-market analysis.
- **Harm:** Any consumer of the fallback path â€” operator dashboard evaluation, future base-engine integration, or a debugging session sanity-checking the model â€” sees a fabricated 40-60 point edge on essentially every weather market it inspects, in a random direction determined by whether raw p fell above or below 0.01. Decisions or code tuned against these outputs (e.g. 'the model sees huge edges, loosen the gate') inherit pure noise dressed as certainty.
- **Verifier correction:** Severity: high â†’ medium (latent, not active). The fallacy is real and the output-set math is exactly right, but the headline harm is overstated: the dashboard consumer (ui/dashboard.py:1760) feeds market_data lacking question/title, so WeatherBot.analyze_opportunity returns None at weather_bot.py:2007 before reaching the singleton bucket_probabilities call â€” the operator never sees the fabricated edges through any currently-wired path, and today's live model_prob=1.0 signal did NOT come from this method (live path is group-level at :2611/:2661). Two corrections to scope: (1) the real residual exposure is any direct caller passing a well-formed market dict (debug session, future base-engine fallback integration) â€” that path does produce guaranteed 0.0/1.0 outputs; (2) the live-relevant variant of this same normalization-over-singleton fallacy is elsewhere: both live probability paths share identical singleton-degenerate normalization (probability_engine.py:216-231 parametric, :315-328 empirical), and group_markets (market_mapper.py:512-616) has no minimum-group-size guard â€” it drops individual buckets on parse failure while keeping the group, so a group shrunk to 1 bucket flows into the live scan and yields model_probâ‰ˆ1.0 with a fabricated ~1âˆ’price edge in production. That singleton-group live path (not analyze_opportunity) is where this fallacy class can actually cost money and deserves the 'high' label; recommend a len(group.buckets)>=2 (or skip-normalization-for-singleton) guard covering both engine paths plus analyze_opportunity.

### 5. [HIGH · high-confidence] '<2h aggressive' certainty window keyed to a fixed 18:00 UTC anchor, not to when the daily max is physically determined
- **File:** `bots/weather_bot.py:2964`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / proxy-anchor treated as outcome determination
- **What:** Lead time everywhere anchors to 18:00 UTC ('target_noon', weather_bot.py:2516-2520, 2257-2259, 2469-2473). The METAR override's aggressive branch (aggressive = lead_time_hours < 2.0, line 2964) therefore opens at 16:00 UTC â€” noon EDT, 11am CDT, 9am PDT â€” and asserts 'daily high is nearly established': 0.98 for at_or_below well below ceiling (2974-2976), 0.92 for in-range (3003-3006), and 0.001 against reaching a floor 1 degree above current max (2989-2991). Climatological time-of-max for US stations is 14:00-17:00 LOCAL â€” i.e. 19Z-23Z, entirely AFTER the anchor â€” so these near-certainties are asserted while 3-8 hours of daytime heating remain; for West-Coast stations the premise is off by most of the heating cycle. Today's live signal at '0.8h lead' fired ~17:12Z = early afternoon local, structurally before Tmax. The code comment 'At <2h lead time, daily high is nearly established' (2962) encodes a belief that is true relative to end-of-day but is evaluated against an arbitrary schedule constant. Additionally the bot stops scanning entirely at lead<0 (2521-2522), so it never re-evaluates during the actual Tmax window and cannot learn this assertion class is premature.
- **Harm:** The bot sells off (0.001) buckets 1F above the 11am running max and buys 92-98% claims on 'established' highs hours before the physical high occurs; every warm afternoon converts these near-certainty entries into losses, and the sizing consequences are maximal precisely because the asserted probabilities are extreme.
- **Verifier correction:** Two minor corrections, substance intact. (a) "Climatological time-of-max 19Z-23Z, entirely AFTER the anchor" slightly overstates for EDT stations: 14:00-17:00 EDT = 18Z-21Z, so the window begins AT the 18Z anchor, not entirely after; for CDT/MDT/PDT/MST stations "entirely after" is accurate. The harm claim is unaffected since the aggressive window (16Z-18Z) precedes Tmax at every US station. (b) One of the four aggressive pushes is monotone-safe and should be excluded from the harm: 0.98 for at_or_higher when running_max is already at/near the floor (weather_bot.py:2983-2985) â€” the max only rises, so that assertion is sound. The genuinely premature assertions are the three non-monotone ones: 0.98 at_or_below (:2974), 0.001 at_or_higher-below-floor (:2989), 0.92 range (:3003). Additional supporting evidence not in the original claim: the codebase already contains the correct fix â€” forecast_client.py:1223-1233 ("S102 fix: use station timezone for local noon instead of hardcoded 18:00 UTC") â€” which was never applied to weather_bot.py's three 18Z copies, making this a known-wrong belief retained by omission, not merely an unvalidated one.

### 6. [HIGH · high-confidence] Resolution-source/settlement uncertainty is exactly zero everywhere: the only hook (boundary_risk) is logging-only, removed on contaminated P&L evidence
- **File:** `bots/weather_bot.py:2808`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / missing settlement-uncertainty term, tuned on stale ground truth
- **What:** The pipeline contains no term anywhere for P(resolution source disagrees with my measurement source). _near_boundary (weather_bot.py:5415-5443) documents the exact failure â€” 'A 0.5F/C gap between WU and NWS can flip the result â€” the Dec 2025 NYC incident (WU=29F vs NWS=30F)' â€” but S121 reduced it to a log line: 'boundary_risk tracked for logging but NO LONGER discounts confidence. Let Kelly self-regulate â€” the 50% discount was crushing profitable trades' (2807-2810). That removal rationale was a P&L-based tuning decision from the era when the WU scraper was dead and the bot was calibrated against ERA5 fallback (the ground truth itself was wrong), so the 'profitable trades' evidence was contaminated. Meanwhile the METAR override can stand probabilities at 0.97-0.98 and renormalization can reach 1.0: the system claims 30:1 to 1000:1 on outcomes that historically flip on source disagreement at far higher base rates than 0.1-3%. The engine-side clamps (0.001/0.999) are numerical guards against log(0), not calibrated floors reflecting settlement risk â€” nothing distinguishes 'model is unsure' from 'model is sure but the referee may read a different thermometer'.
- **Harm:** Every boundary-adjacent trade is systematically oversized and over-admitted relative to its true win probability; the specific dollar shape is repeated small edges collected until a knife-edge day (the documented 29F-vs-30F class) hits a max-confidence position, and the Brier/calibration telemetry looks fine right up until it doesn't because the miscalibration lives in the settlement layer the model refuses to price.
- **Verifier correction:** Three scope corrections. (1) The '30:1 to 1000:1' framing understates one channel and overstates another: the confidence channel is clamped at 0.95 pre-calibration (weather_bot.py:2814) and 0.99 post (685), but the channel that actually sizes trades â€” the S-T Kelly allocator at 3061 â€” uses RAW model_prob, which after the unclamped METAR renormalization (3021-3027) can reach literal 1.0 (infinite implied odds) for single-bucket groups; today's live 1.0 signal is exactly this path, and at p=1.0 Kelly allocates its full fraction. (2) One direction of the METAR override DOES carry a crude settlement-source guard: the 0.001 rule-outs require running_max to exceed the bound by unit_margin (0.5F/0.3C, matching the documented WU-vs-NWS gap). The missing-term fallacy is concentrated in the YES-asserts (0.97 at running_max >= low_bound - 0.5, i.e. before the floor is crossed on METAR) and in the base CDF path. (3) Concrete harm is bounded, not open-ended: kelly_mult=0.25 x group_budget plus group/city exposure caps (3125-3136) cap per-incident loss â€” the shape is systematic bounded oversizing plus over-admission (edge=1.0-price passes every gate and feeds the boost stack), not account-level blowup on one knife-edge day. Additionally, post-2026-07-01 the EMOS sigma re-learning vs real WU will implicitly fold some settlement scatter into the base-path distribution â€” but that mitigant is bypassed precisely where the certainty claims originate (the METAR override at lead<12h).

### 7. [HIGH · high-confidence] Trade-signal telemetry logs raw boost values (nbm_boost=1.3, jump_boost, regime_boost) while the code applies their reciprocals â€” wrong-sign measurement of the sizing chain
- **File:** `bots/weather_bot.py:3690`  (lens: edge-and-boosts)
- **Fallacy class:** measurement-corruption / sign-inversion
- **What:** S153 inverted all boosts into dampeners (3305-3315: applied multiplier = 1/boost), but the `weatherbot_trade_signal` log (3685-3690) still emits expiry_boost, regime_boost, jump_boost, nbm_boost as the RAW >1.0 values, and the jump log line (3290-3295) reports jump_boost=1.5-style numbers. Comments still carry the boost-era rationale ('markets lag', 'calibrated benchmark signal â†’ 1.3Ã— sizing multiplier', 3282, 3299-3300) directly above code that does the opposite. The operator's observation this week â€” 'nbm_boost=1.3 ... multiply Kelly size' â€” is a false belief manufactured by the bot's own telemetry: the applied value is 0.769 (or nothing at all in flat mode, see separate finding).
- **Harm:** Anyone auditing sizing from logs concludes the bot sizes UP on NBM/jump/regime signals when it sizes DOWN; a 'boosts are too aggressive' retune based on these fields would move the multipliers in the wrong direction, and P&L attribution by boost-flag is inverted.
- **Verifier correction:** Severity high is appropriate for an epistemics audit â€” this is confirmed measurement corruption that already manufactured a false operator belief this week â€” but note the sizing BEHAVIOR itself is correct per S153 intent; the defect is purely in telemetry/comments. Two refinements: (a) under the default flat-sizing config (WEATHER_FLAT_SIZE_USD=100.0, settings.py:858; flat branch weather_bot.py:3420-3424), combined_boost is not applied to size at all, so the logged boosts currently correspond to NOTHING, not just to their reciprocals â€” the claim's parenthetical is confirmed, not separate; (b) the true applied multiplier is recoverable from trade_events.event_data.signal_multiplier (weather_bot.py:3699) and shadow-entry emissions (3582/3646), so DB-based attribution is salvageable in aggregate â€” only per-signal decomposition and the log channel are corrupted. Also, severe_boost (3279, inverted at 3312) is not logged in trade_signal at all, and expiry_boost is a dead always-1.0 field (3262) â€” the log schema is stale on top of wrong-signed. Fix shape: log the applied dampeners (_regime_damp/_jump_damp/_nbm_damp/_severe_damp) plus combined_boost in weatherbot_trade_signal, and rewrite the 3281-3282/3299-3300 comments to dampener-era rationale.

### 8. [HIGH · high-confidence] Trade selection, ranking, and S-T capital allocation all use the RAW uncalibrated model_prob; the calibrator only touches Kelly and the negative-EV gate
- **File:** `bots/weather_bot.py:2738`  (lens: edge-and-boosts)
- **Fallacy class:** certainty-fallacy / inconsistent-probability
- **What:** Enumeration of every 'edge' in the pipeline: (1) midpoint edge = raw model_prob âˆ’ Gamma outcomePrices midpoint (probability_engine.py:416, side chosen by sign â€” symmetric, so NO abs_edge equals the YES one); (2) abs_edge gated vs min_edge 0.08/0.12 (weather_bot.py:2738) and used for ranking (2428 wind, 2229) and regime voting (4229); (3) single-market edge = model_prob âˆ’ yes_price (2034); (4) nbm_edge = nbm_prob âˆ’ midpoint (probability_engine.py:611); (5) exec edge = P(side) âˆ’ executable price (4494, side-adjusted only since today's S223 fix); (6) 'effective edge' = abs_edge âˆ’ relative slippage (3371, unit-broken, see separate finding); (7) S-T Kelly edge f_i=(pÂ·bâˆ’q)/b using RAW opp['model_prob'] (3061); (8) exit 'current_ev' = fresh_prob âˆ’ ENTRY price (4049). The confidence calibrator â€” built precisely because raw model_prob is overconfident (PIT U-shaped, S204) â€” is applied only to opp['confidence'] (2816-2822), which feeds Kelly (3439-3441) and the neg-EV gate (3574). The min_edge gate, bucket ranking, MAX_BUCKETS_PER_GROUP top-N selection (2877), and the entire S-T multi-bucket allocation (3061-3092) run on the uncalibrated number. The same trade thus embodies two different beliefs about P(win), and the calibrator cannot shrink an inflated edge below the entry gate.
- **Harm:** Overconfident tail buckets (exactly the [0.9,1.0) bin PIT-KS rejects) dominate selection and receive the largest S-T budget shares even when the calibrated belief implies the edge is below min_edge â€” the bot systematically concentrates capital where its probability model is known to be most wrong.
- **Verifier correction:** Real at claimed severity (high), with three scope corrections. (a) Partial neutralizer the claim omits: the neg-EV gate at weather_bot.py:3574 blocks any trade whose CALIBRATED confidence < price, so calibration CAN veto a trade whose calibrated edge goes negative â€” what it cannot do is enforce the min_edge floor; the unguarded window is calibrated edge in (0, 0.08/0.12), plus all mis-ranking/mis-allocation among survivors. (b) "Calibrator only touches Kelly and the neg-EV gate" is slightly incomplete: calibrated confidence also feeds one true SELECTION gate â€” the YES-only confidence floor at 2854 (WEATHER_YES_MIN_CONFIDENCE=0.35) â€” and the YES identity dampener at 2842-2844; but both are YES-only patches, and the NO side (the dominant funnel) has zero calibrated selection input. (c) The S-T budget-share harm is config-contingent: WEATHER_FLAT_SIZE_USD defaults to 100.0 (settings.py:858/882) and when >0 the flat branch at weather_bot.py:3420-3424 discards the S-T override entirely, making the 3061 raw-prob allocation dead code under defaults. However, boosts are multiplied only on the S-T/Kelly branches (3428, 3443), never the flat branch (3422) â€” so this week's live observation of nbm/regime/jump/expiry boosts multiplying Kelly size implies the live env runs flat sizing OFF and the S-T raw-prob allocation path IS live. The selection/ranking/top-N portion of the harm is unconditional regardless of sizing mode.

### 9. [HIGH · high-confidence] Slippage-adjusted edge subtracts a RELATIVE price percentage from an ABSOLUTE probability edge â€” unit mismatch that over-penalizes cheap YES tails and waves through expensive NO favorites
- **File:** `bots/weather_bot.py:3371`  (lens: edge-and-boosts)
- **Fallacy class:** unit-confusion / favorite-funnel reinforcement
- **What:** `_effective_edge = opp['abs_edge'] - _slippage_pct` (3371, repeated 3398, 3403). abs_edge is in absolute probability points; liquidity_guardian returns slippage = (avg_fill âˆ’ best)/best, RELATIVE to price (liquidity_guardian.py:146). The absolute cost is slippage Ã— price, so the correct deduction is _slippage_pct Ã— opp['price']. At price 0.10 a 20% relative slippage costs 0.02 absolute but the code deducts 0.20 â€” a 10x over-penalty that kills nearly any low-priced YES entry; at price 0.90 the deduction is roughly correct, so expensive NO-favorite trades pass. This asymmetry mechanically reproduces the S222-diagnosed favorite-buying funnel from inside the 'slippage protection'. Additionally the book-walk is sized at `self._default_size` ($25, 3365), not the actual intended stake, so the slippage estimate does not describe the trade being protected.
- **Harm:** Cheap-token entries with genuine post-cost edge are rejected while high-priced favorites with thin absolute margins pass â€” the gate skews the realized trade mix toward high-WR/low-payoff favorites, inflating win-rate while destroying EV, and corrupting any WR-based evaluation of the strategy.
- **Verifier correction:** Two corrections: (1) The bias is by PRICE, not by YES/NO side â€” NO opportunities carry the NO-token's own price (weather_bot.py:2461) and the book-walk runs on the NO token, so cheap NO tails are over-penalized identically and expensive YES favorites waved through identically; the funnel is toward high-priced favorites of either side. (2) The secondary claim (book walked at $25 _default_size â€” confirmed, settings.py:835 â€” not the actual stake) is partially neutralized on the execution side: get_max_safe_size(max_slippage_pct=0.02) sets _slippage_size_cap applied at weather_bot.py:3614, bounding executed size to 2%-slippage book depth; the residual defect there is only that the skip decision does not reflect the true stake. Additional corroborating detail not in the claim: depth_exceeded/insufficient_liquidity early returns lack a "slippage" key â†’ 0.0 penalty on the very thinnest books. Also note the exception fallback at 3403 (flat 0.005 absolute) is unit-consistent â€” the only dimensionally correct branch of the three.

### 10. [HIGH · high-confidence] Every magnitude in the sizing chain was tuned on pre-WU-fix / bug-contaminated data and none has been re-derived since the 2026-07-01 ground-truth fix
- **File:** `bots/weather_bot.py:3305`  (lens: edge-and-boosts)
- **Fallacy class:** stale-ground-truth / winrate-as-alpha
- **What:** Provenance audit of the chain's magic numbers, all from S118-S168 sessions (Feb-Apr era) â€” months when the WU scraper was dead, 96% of calibration ground truth was ERA5 fallback, the exec-edge gate was silently YES-biased (fixed today, 4479-4493), and P&L tooling had known bugs: S118 edge-cap REMOVAL justified by '0.70+ edge bucket had HIGHEST win rate 87.3%' (2741-2743) â€” win rate unconditioned on price, the textbook favorite/longshot confound; S141/S126 expiry_boost removal via P&L lead-time buckets (3258-3259); S153 boostâ†’dampener inversion via 'shadow data 30K+: boosted 64% vs unboosted 75% WR' (3305-3308) â€” again WR-as-alpha, unconditioned on price/side mix; S154 lead-time multipliers 1.15/0.70 from '91.2% WR, +$664' (settings.py:906-911); S162 YES_SIZE_MULTIPLIER 0.50 from '-$24.4K' (settings.py:865-867); S154/S155B price-dampener soft caps 0.50/0.70 and slopes 3.0/6.0 (3496-3528); S149/S135 YES gates (since flip-flopped: enabled S149, disabled S153 â€” settings.py:862-864 â€” each reversal re-tuned on the same contaminated population). The 07-01 WU fix changed the model's calibration substrate (memory: forecast corr 0.99 vs real WU), so the trade population these numbers describe was generated by a materially different, broken model â€” yet the entire policy surface survives unchanged.
- **Harm:** Sizing multipliers optimized against a phantom loss distribution now steer capital for a different model: e.g. the 72-120h 1.15x 'sweet spot' rests on a P&L figure that has already been restated three times (+$664 â†’ +$1,005 â†’ +$208, each 'canonical' at the time), and the YES 0.50x haircut may be throttling the fixed model's best side. Every retune iterates on noise, compounding rather than converging.
- **Verifier correction:** Two scope corrections, severity stays high. (A) 'Every magnitude in the sizing chain' slightly overreaches: the enumerated items all verify, but a few chain constants have non-P&L provenance (WEATHER_VARIANCE_INFLATION_FACTOR 1.4 cites Gneiting 2005 literature; jump threshold 3.0F is a prior; the S205 confidence dampener is config-gated default-OFF). (B) Live-surface correction: under the DEFAULT flat-sizing mode (WEATHER_FLAT_SIZE_USD=100.0, settings.py:858; branch weather_bot.py:3420-3424), combined_boost is NOT multiplied into size â€” it only applies on the Kelly path (3443) and S-T override path (3428). So the S153 dampener magnitudes, station-reliability factor, and city-Brier dampener are computed and logged but sizing-inert unless the VPS disables flat mode (unverifiable from repo). The contaminated magnitudes demonstrably steering capital in default config are: the edge-cap ABSENCE (all modes â€” the most dangerous item, and the one implicated in today's certainty-claim trade), the YES 0.50x multiplier, both YES/NO price dampeners, and the lead-time multipliers â€” all applied downstream of the flat size. The YES min-confidence/min-price gates are currently zeroed (inert knobs), though their disabled state is itself an S153 decision tuned on the same contaminated population. Additional supporting artifacts found: the S162 retune of the 0-24h lead multiplier to 0.60 never landed (getattr fallback at weather_bot.py:3541 is shadowed by the settings-declared 0.85 at settings.py:910), and today's 12f68cc fix means the YES-side numbers behind S149/S153/S162 were doubly contaminated (ERA5 ground truth + YES-biased executable-edge gate shaping the trade population).

### 11. [MEDIUM · medium-confidence] Ungated 0.97 YES push fires when the threshold has NOT been crossed (running_max still 0.5 below floor), with a unit-inconsistent margin
- **File:** `bots/weather_bot.py:2986`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / rounding-boundary treated as crossed
- **What:** In _apply_metar_resolution_day_override, the at_or_higher branch sets P=0.97 at ANY lead <12h when 'running_max >= bucket.low_bound - 0.5' (2986-2988) â€” i.e. when the observed max is up to half a degree BELOW the market threshold. The 0.97 encodes two stacked assumptions as near-certainty: (a) the resolution source will round half-up from the exact METAR/ASOS value (a 54.5 reading resolves a '55 or higher' market YES), and (b) the resolution source's reading equals the METAR T-group value to within the margin. Both are boundary/rounding coin-flips, not 32:1 events â€” and the WU-vs-METAR chain involves independent C-to-F conversion and per-source rounding. The margin is also hardcoded 0.5 in this branch while the unit-aware margin computed two lines up (unit_margin = 0.3 for Celsius, line 2965) is unused here, so for Celsius stations the push fires up to 0.5C (0.9F) below the threshold â€” even more aggressive than the F case, the opposite of the intended tightening.
- **Harm:** The bot buys YES at 97% asserted probability on markets whose outcome is literally a rounding coin-flip between two data sources; each such entry pays certainty prices for ~50-70% events, and on Celsius stations the mis-margined branch admits pushes on temperatures nearly a degree Fahrenheit short of the threshold.
- **Verifier correction:** Real, severity MEDIUM confirmed but scope narrowed in three ways. (1) "Coin-flip" applies only to exact half-boundary readings (e.g. T-group 12.5C = exactly 54.5F vs a 55F floor, nonzero measure due to 0.1C quantization), not the whole [floor-0.5, floor) band: half-up rounding is the modal convention, the running max is monotone-rising (more so pre-dawn in the 12h window -- the ungated direction is monotone-SAFE per the code's own S222 rule at 2928-2937, unlike the removed at_or_below push), and WU/ASOS daily maxima capture between-ob peaks so resolution max >= METAR ob-max -- all three errors favor YES. True P(YES) over most of the band is plausibly 0.85-0.97, i.e. "unvalidated but plausibly fine," not "demonstrably wrong." (2) The unit-inconsistency sub-claim is mostly a false positive: if C markets resolve in whole degrees C, the half-up rounding half-step in native units IS 0.5, and the docstring (line 2926) documents "-0.5" by design; unit_margin=0.3C is a measurement-uncertainty margin for a different concept (boundary_risk). The only genuine inconsistency is the <2h aggressive branch (2983) using unit_margin for the same conceptual test, whose entire effect is C stations in [floor-0.5, floor-0.3) getting 0.97 instead of 0.98 -- negligible and in the conservative direction. The "0.9F below threshold, more aggressive than F" framing is wrong measured in native rounding steps (both units get exactly one half-step). (3) What survives as the real fallacy: a hardcoded 0.97 that (a) asserts "crossed" for readings below the threshold based on a rounding convention never validated against the actual resolution source (assumption predates the WU fix; the ERA5-era contamination applies), (b) has no boundary-case calibration evidence anywhere in the code, and (c) is adversely selected by the edge gate toward exactly the convention-risk cases the market prices near 0.5. Fix shape: exclude or haircut the exact-boundary slice (running_max within one quantum of floor-0.5), or empirically validate the resolution source's rounding at boundaries post-WU-fix before asserting 32:1. Also fix the stale <6h docstring at 2923.

### 12. [MEDIUM · high-confidence] Precipitation empirical fallback claims 1000:1 from as few as 5 ensemble members (no Laplace smoothing), with uncapped 0.999 confidence
- **File:** `bots/weather/engine/base_engine/weather/precipitation_engine.py:181`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / small-sample frequency treated as probability
- **What:** _empirical_probabilities computes prob = count/n and clamps to [0.001, 0.999] (line 181). It is reachable with n as small as 5 (compute_bucket_probabilities requires only n>=5, line 72, and falls back here whenever fewer than 3 wet members exist, line 93). 5-of-5 members dry -> P(0 inches)=1.0 -> clamped to 0.999: a 1000:1 claim from five samples (a Jeffreys posterior on 5/5 gives ~0.92). The temperature empirical path learned this exact lesson and uses Laplace smoothing (count+0.5)/(n+1) (probability_engine.py:313); precip never inherited it. Compounding: compute_edges then sets 'confidence': model_prob directly (lines 218, 231) with no 0.95 cap â€” unlike temperature (weather_bot.py:2814) â€” so a 0.999 flows into Kelly sizing and the negative-EV gate as if it were a calibrated near-certainty. p_rain = n_wet/n (line 77) is similarly unsmoothed before its clamp.
- **Harm:** Dry-forecast days (the majority) systematically generate 0.999-confidence NO-rain signals whose true probability is 85-95%; the bot pays up to 0.99 for these buckets where break-even needs >99% win rate, and the uncapped confidence bypasses the negative-EV gate that would block the same trade on the temperature path.
- **Verifier correction:** Three corrections to the claim as stated. (a) 'n as small as 5' is not reachable from the bot: both callers require len(ensemble) >= 10 (weather_bot.py:2210 daily+monthly precip; :2322 snowfall), and a healthy fetch yields ~100+ members â€” the engine's n>=5 floor (precipitation_engine.py:72) is only hit under severe Open-Meteo 429 degradation (forecast_client.py:565-567 skips cooled-down models). Worst realistic case is 10-of-10 â†’ 0.999 vs Jeffreys ~0.955. (b) 'bypasses the negative-EV gate' â†’ the gate (weather_bot.py:3574) DOES execute for precip trades; it is neutered, not bypassed: precip confidence is the raw uncapped model_prob so 0.999 passes at any price, whereas the temperature path's 0.95 cap (weather_bot.py:2814) makes the same gate block price > 0.95. (c) 'pays up to 0.99' overstates harm: min_edge=0.08 (compute_edges default, weather_bot.py:2252) caps YES entry at ~0.919 for a 0.999 signal, and the S155B YES price dampener (weather_bot.py:3499-3511) plus 0.75x YES multiplier reduce size to near the 0.10x floor at high prices; NO-side entries from an inflated 1-model_prob confidence get the softer 0.70 soft-cap dampener (3516-3528). One aggravator the claim missed: the empirical fallback silently discards the NDFD PoP blend (computed precipitation_engine.py:80-87, unused by _empirical_probabilities), so the independent probability source that would moderate the 0.999 is dropped precisely on the days the fallback fires. Severity stays medium; the fallacy (unsmoothed frequency-as-calibrated-probability, uncapped, uncalibrated, asymmetric vs the temperature path) is genuinely encoded on the majority dry-day path.

### 13. [MEDIUM · high-confidence] Wind path applies zero dispersion correction: raw 10-member spread treated as full predictive uncertainty, tails asserted to 0.999
- **File:** `bots/weather_bot.py:2412`  (lens: certainty-and-tails)
- **Fallacy class:** certainty-fallacy / underdispersed-ensemble tails taken literally
- **What:** _analyze_wind_group (2394-2466) computes a plain normal CDF from the raw ensemble mean/std with only a 0.5-unit std floor (2412) â€” no variance inflation, no EMOS, no calibrator, no climate prior â€” and clamps at [0.001, 0.999] (2434). The temperature path required the S154 VIF=1.4 and the S222 A1 empirical-path inflation precisely because raw NWP ensembles are underdispersed 1.3-2.0x (documented at probability_engine.py:125-128); wind gust ensembles are MORE underdispersed than temperature (gusts are parameterized, not resolved), yet this path encodes the belief that 10 members' spread is the whole uncertainty. Tail buckets 3 sigma from the mean get 0.001/0.999, and 'confidence' is the raw model_prob up to 0.999 (2453, 2465) with no 0.95 cap. The min-members bar is 10 (2405), so the 0.999 tail claims can rest on ten correlated samples.
- **Harm:** Wind markets are entered on fabricated tail edges: the model calls a gust threshold impossible (0.001) or certain (0.999) when honestly-dispersed odds are more like 5-20%, so NO bets near 0.95+ prices and YES tail bets both pay certainty prices for materially uncertain outcomes â€” the exact PIT-U-shape overconfidence S222 diagnosed on temperature, rebuilt unmitigated in an adjacent product.
- **Verifier correction:** Claim is real but severity should be LOW-MEDIUM (latent), not active medium, for three reasons the claim missed. (1) The path is currently INERT: Gamma `tag_slug=wind` returns 0 active events (verified live 2026-07-06), so no wind markets are discoverable and the path executes zero trades today â€” same dead-tag-slug class as the S246 temperature tag bug; harm activates only if Polymarket relists wind markets (or the tag is fixed). (2) The Kelly-amplification channel is blunted by default config: WEATHER_FLAT_SIZE_USD defaults to 100.0 (config/settings.py:882 and bots/weather/engine/config/settings.py:858), so weather_bot.py:3420-3424 uses flat sizing and the 0.999 confidence does not scale position size; the real harm channels are entry selection (fabricated tail edge >= the 0.08 wind min-edge) and pacification of the negative-EV gate at weather_bot.py:3574 (raw confidence=0.999 never trips confidence<price). (3) 'Ten correlated samples' is the worst-case floor, not the norm: forecast_client.py:890 pools members from three models (gfs025 + ecmwf_ifs025 + ecmwf_aifs025, typically ~80+ members) and cross-model disagreement adds some dispersion â€” an uncalibrated partial mitigant, not a correction. Everything else in the claim verifies exactly: no VIF/EMOS/calibrator on the wind path (vs probability_engine.py:105-108 VIF=1.4 with the underdispersion citation, and the temp path's min(0.95, conf) cap at weather_bot.py:2814 plus calibrate() at 2817), clamp to [0.001,0.999] at 2434, confidence = raw model_prob at 2453/2465, std floor 0.5 at 2412, min 10 members at 2405, opps flow uncalibrated into _execute_weather_trade via _scan_psw_markets:2164-2166. Recommended framing: real embedded fallacy, dormant today; fix before any wind-market reactivation (apply VIF + 0.95 cap + calibrator parity, or gate the scan off until calibrated).

### 14. [MEDIUM · high-confidence] Empirical and parametric paths disagree on tail uncertainty: empirical discards learned EMOS sigma and applies blanket VIF; path selection is by member count, not calibration
- **File:** `bots/weather/engine/base_engine/weather/probability_engine.py:281`  (lens: certainty-and-tails)
- **Fallacy class:** inconsistent-uncertainty-model across paths
- **What:** empirical_bucket_probabilities fetches (emos_a, emos_b, _emos_sigma) but discards _emos_sigma (line 281) and always inflates the raw member spread by the fixed VIF=1.4 (294-297). The parametric path does the opposite: when EMOS sigma exists it USES the learned spread and skips VIF (122-128), on the rationale that 'EMOS sigma already captures the spread-skill gap'. Which model of uncertainty a station gets is decided by _n_clean >= 50 (weather_bot.py:2604-2605) â€” a data-availability accident â€” so the same station on the same day yields different tail probabilities depending on how many members Open-Meteo returned. For a station whose learned residual sigma exceeds 1.4x the raw spread (typical for hard stations), the dominant empirical route UNDERSTATES tail uncertainty relative to its own calibration data; the whole point of fitting sigma from resolved outcomes is bypassed on the production path. The comment at weather_bot.py:2610 rationalizes this ('raw ensemble spread is more informative than EMOS-corrected spread at n>=133') without any cited validation.
- **Harm:** Tail-bucket probabilities â€” where all YES entries and most edges live â€” are path-dependent artifacts: stations with painful learned residuals get their honest sigma silently replaced by a global 1.4 multiplier on the dominant route, regenerating overconfident tails and edges on exactly the stations where history says the model is worst.
- **Verifier correction:** Severity medium is correct, with three scope corrections. (1) The harm is bidirectional, not a guaranteed understatement: when a station's learned sigma exceeds VIF*raw_spread the empirical path understates tail uncertainty (claim's framing), but when sigma < VIF*raw it OVERSTATES it â€” either direction produces path-dependent artifact edges; the 'typical for hard stations' direction is plausible but unvalidated by anything in the code. (2) The fallacy is the unvalidated inconsistency plus the uncited n>=133 rationalization â€” NOT that sigma should win. Naively substituting the fixed homoscedastic sigma into the empirical path would destroy the day-specific shape information (bimodality/skew) that motivated the empirical path, and the TODO at probability_engine.py:120-121 already admits fixed sigma fails to track day-to-day spread variation. A defensible fix must reconcile the two (e.g., validated per-station spread ratio, or rescale members so empirical std matches a validated target), not just swap models. (3) Both beliefs were fitted/tuned against the months-long contaminated ERA5-fallback ground truth (WU scraper dead until 2026-07-01 fix), so neither the learned sigma nor the 1.4 VIF is currently validated against correct outcomes â€” the re-learning calibration pass should decide the winner empirically per station/lead, and until then the same-station path flip by member count remains an unmodeled source of edge noise.

### 15. [MEDIUM · high-confidence] Confidence calibrator trained only on lead>=48h trades but applied at all leads; lead_time_hours accepted at inference and ignored
- **File:** `bots/weather_bot.py:659`  (lens: certainty-and-tails)
- **Fallacy class:** out-of-distribution extrapolation of a correction layer
- **What:** fit_from_trade_events filters training data to (event_data->>'lead_time_hours')::float >= 48.0 (line 217), so the calibrator has never seen a short-lead trade. calibrate() (659-687) accepts lead_time_hours, bucket_type, and ensemble_spread 'for API compat' and uses none of them â€” it is a 1-D confidence-to-winrate map fitted on the >=48h regime, silently applied to 0.8h-lead signals where the probability-generating process is completely different (METAR overrides pumping 0.97-0.98, renormalization artifacts, the aggressive window). The output clamp is [0.01, 0.99] (685), so even post-calibration the system can state 99:1. The one mechanism that could have flagged today's model_prob=1.0 as uncalibrated is structurally blind to the regime that produced it, while its presence (_cal_fitted) is what disables the 0.85x YES identity dampener (2836-2844) â€” fitting on long-lead data REMOVES the only short-lead haircut.
- **Harm:** Short-lead near-certainties pass through the correction layer untouched (or slightly boosted), and the act of the calibrator graduating on long-lead data switches OFF the fallback dampener for short-lead YES entries â€” measured calibration improves on the trained regime while the untrained regime quietly gains sizing, corrupting any 'calibrator is healthy' conclusion drawn from the fit telemetry.
- **Verifier correction:** Severity medium confirmed, with three scope corrections. (a) Input cap: base_confidence = min(0.95, _raw_conf) at line 2814 means model_prob=1.0 enters the calibrator as 0.95, not 1.0 â€” the '99:1' arm stands (clamp 0.99 at 685, isotonic y_max=0.99 at 295) but raw certainty gets one haircut pre-calibration. (b) Partial mitigation, not neutralizer: WEATHER_LEAD_TIME_MULT_0_24=0.60 (line 3541) shrinks short-lead sizing but does not touch effective_confidence, so the negative-EV gate (conf<price) and Kelly's p-input still consume the OOD-corrected confidence. (c) The dampener-removal arm is conditional/latent: it fires only while _cal_fitted is true (refit at line 6191 requires >=48h-lead rows in the trailing 30-day window meeting min_samples); with the current market universe listing mostly short-lead weather markets, the calibrator can oscillate fitted/unfitted â€” the harmful state is precisely the fitted one, where stale long-lead calibration is applied to fresh short-lead trades with the haircut off. So: continuously-real OOD application of the correction layer, plus an armed-but-intermittent haircut-removal coupling.

### 16. [MEDIUM · high-confidence] NBM is double-counted: it anchors the location of the distribution that generates model_prob AND separately triggers a sizing multiplier keyed on NBM-vs-market disagreement
- **File:** `bots/weather_bot.py:3301`  (lens: edge-and-boosts)
- **Fallacy class:** double-counting / internal contradiction
- **What:** For US stations NBM IS deterministic_high (forecast_client.py:1153-1160), which since S222-A3 anchors the EMOS location correction of both probability paths (probability_engine.py:110-115 parametric, 281-289 empirical) â€” so model_prob, and therefore the edge, is already NBM-centered. compute_nbm_benchmark (probability_engine.py:562-619) then flags 'high conviction' when |NBM_CDF âˆ’ market| â‰¥ 0.15 and applies a second multiplier (3301, WEATHER_NBM_BOOST 1.3 â†’ 0.769 dampener via 3314). Because model_prob is NBM-anchored, the flag is nearly a deterministic function of edge magnitude â€” not independent confirmation. Under the original 1.3x boost this was double-counting the same information; under the S153 inversion it is a hidden edge-magnitude penalty that directly contradicts the S118 belief ('larger edges are MORE reliable â€” cap removed', 2741-2743) still encoded 500 lines upstream. Two opposite beliefs about large model-market divergence are simultaneously in force in the same trade path. The 1.3 magnitude and 0.15 threshold have no cited evidence anywhere (S121 merely 'externalized hardcoded values', settings.py:954, 2679).
- **Harm:** The bot's purportedly best-informed trades (biggest NBM-backed divergence) are systematically shrunk 23% while ensemble-only trades run full size â€” or, if the boost era returns via config, systematically over-bet on one signal counted twice; either way the multiplier moves size on information Kelly already priced.
- **Verifier correction:** Two corrections, neither fatal. (a) 'Nearly a deterministic function of edge magnitude' overstates the coupling: the benchmark CDF uses hardcoded lead-banded sigma (1.5/2.5/3.5/5.0Â°F, probability_engine.py:584-591) while model_prob uses EMOS sigma or VIF-inflated ensemble spread, so the flag and model edge can diverge when spreads differ materially. Accurate phrasing: strongly correlated via the shared NBM location anchor â€” not independent confirmation â€” rather than a near-deterministic function of edge. (b) Add two aggravating scope facts the claim missed: the fallacy WORSENED at S222-A3 â€” before it, EMOS anchored on the ensemble mean, so the NBM benchmark was genuinely semi-independent; S222-A3 collapsed that independence and the boost was never revisited (the exact inherited-assumption pattern this audit targets); and the 0.769x shrink applies only to US stations ('nbm' in models_used gate, weather_bot.py:2677) â€” international stations' local-model overrides never trigger compute_nbm_benchmark, creating an undesigned US-vs-international sizing asymmetry on the bot's highest-divergence trades. Also note the flag is keyed by market_id only (weather_bot.py:2905) with no check that the NBM edge direction agrees with the trade side, though the shared anchor makes directional agreement near-automatic in practice.

### 17. [MEDIUM · high-confidence] Mid-life exit 'current_ev' is anchored to ENTRY price, not the executable exit price â€” sunk-cost logic in the quantity named EV
- **File:** `bots/weather_bot.py:4049`  (lens: edge-and-boosts)
- **Fallacy class:** sunk-cost anchoring / mislabeled-EV
- **What:** The exit decision computes current_ev = fresh_prob âˆ’ entry_price (4049 YES, 4053 NO) and holds unless current_ev < âˆ’WEATHER_EXIT_MIN_EDGE (4110). The EV of the available actions depends on the CURRENT market price, not what was paid: with fresh_prob=0.30, entry=0.25, current=0.60 the code computes +0.05 â†’ HOLD, forgoing a sell at 0.60 of a token the model values at 0.30 (+0.30 exit edge); with fresh_prob=0.55, entry=0.70, current=0.35 it computes âˆ’0.15 â†’ SELLS a token at 0.35 that the model values at 0.55. Entry price is relevant only to realized P&L, which is sunk. current_price is available in-scope (4051/4055) and is used for the hard-stop but not the EV test. Now consequential: WEATHER_MID_LIFE_EXIT_ENABLED was declared today (settings.py:850) after the operator's =true was silently discarded for months, so this logic goes live for the first time.
- **Harm:** The freshly-enabled exit path will systematically hold overpriced losers-in-waiting and liquidate underpriced positions at a model-assessed discount â€” value transfer to counterparties on every exit decision where price has moved away from entry.
- **Verifier correction:** Severity medium is correct, but two scope corrections: (1) The claim's second example (entry 0.70, current 0.35 = âˆ’50% pnl) would be intercepted by the shared inviolable hard stop BEFORE the mid-life EV test â€” check_hard_stop_loss (bots/weather/engine/base_engine/risk/risk_manager.py:681-699, WB default âˆ’25% price move) runs first at weather_bot.py:4061-4066 and fires regardless of the mid-life flag. So the deepest sell-at-discount cases are absorbed by deliberate stop-loss policy; the mid-life fallacy's active damage is confined to the band above the âˆ’25% stop, bounding per-share value destruction to roughly (0.25Â·entry âˆ’ 0.05). (2) The HOLD-overpriced-winner branch is foregone value capture relative to the prior status quo (no mid-life exit existed at all), not a new active loss â€” the newly-enabled path adds the discount-sell errors and fails to add premium-sell wins, but does not make existing behavior worse on the hold side. Minor framing note: the fallacy is symmetric entry-price anchoring (both hold and sell errors), and the hard stop itself is also entry-anchored â€” but that is a deliberate risk-policy stop, not a mislabeled EV. Fix direction: gate the reversal exit on (current_price âˆ’ fresh_token_value) for the sell-premium case and (fresh_token_value âˆ’ current_price) for hold justification, keeping entry price only for realized-P&L reporting.

### 18. [LOW · high-confidence] S-T allocator and engine kelly_fraction consume raw uncapped model_prob, making the 0.95 confidence cap cosmetic
- **File:** `bots/weather_bot.py:3061`  (lens: certainty-and-tails)
- **Fallacy class:** double-standard probability consumption / cap bypass
- **What:** The temperature opp path caps confidence at 0.95 (2814) and routes it through the calibrator and dampeners before the risk-manager Kelly (3439-3443). But _smoczynski_tomkins_allocate computes per-bucket Kelly edges from opp['model_prob'] raw (3061-3069): at model_prob=1.0, q=0 and f_i=(p*b-q)/b=1.0, the maximum possible, so the renormalization-fabricated certainty dominates the group-budget split regardless of the confidence machinery. probability_engine.kelly_fraction (431-471) has the same property for any caller. Under current defaults WEATHER_FLAT_SIZE_USD=100 (settings.py:858) flattens size and hides this, but the allocation logic is live the moment flat sizing is turned off â€” and the flat default itself means the certainty's harm today is trade ADMISSION at $100, not scaled sizing.
- **Harm:** When flat sizing is disabled (the intended end state of the rebuild), fabricated 1.0/0.999 probabilities take the largest share of every group budget by construction â€” the buckets with the most artifact-driven certainty get the most capital, inverting the intent of every calibration and dampening layer built on the confidence field.
- **Verifier correction:** Two overstatements: (1) The 0.95 cap is NOT "cosmetic" â€” it fully governs the current live sizing path: flat sizing (settings.py:858 default 100.0) discards the S-T override at weather_bot.py:3420-3424, and the independent-Kelly fallback (3439-3443) sizes from the capped+calibrated opp["confidence"] via calculate_bot_position_size. The raw-model_prob consumption is a LATENT bypass, live only when WEATHER_FLAT_SIZE_USD=0. (2) "Harm today is trade ADMISSION at $100" is misattributed â€” the S-T allocator gates nothing: _execute_group_trades (weather_bot.py:3153-3159) calls _execute_weather_trade for every opp whether or not it received an allocation; today's model_prob=1.0 admission comes from upstream edge gates/renormalization, not this allocator. (3) The probability_engine.kelly_fraction half (bots/weather/engine/base_engine/weather/probability_engine.py:432-471) does consume raw model_prob but has zero production callers (only tests/unit/test_weather_bot.py:455-468) â€” dead code, not a live fallacy surface. Corrected framing: low severity today, but a must-fix precondition before flat sizing is ever turned off â€” change line 3061 to consume opp["confidence"] (or apply the same cap/calibration) so the S-T branch sees the same probability the rest of the confidence machinery produces.

---
---

# SECOND VERIFY PASS — S224 (2026-07-08)

Method: 7 thematic adversarial verifiers, each given the truncated titles of the lost
findings + instruction to reconstruct the claim from code and try to REFUTE it. All
verified against the VENDORED tree (`bots/weather/engine/base_engine/weather/**`) +
`bots/weather_bot.py`. Severity applies the pivotal reframe (flat sizing default-on →
pure-sizing findings are DEAD-CODE/LYING-TELEMETRY; admission/direction/calibration
findings are LIVE-CORRUPTING).

## Verdict table (43 reconstructed + 2 new adjacent)

| # | Finding (reconstructed) | Verdict | Severity / liveness |
|---|---|---|---|
| V1 | Calibrator trains on its own outputs (ENTRY confidence column = post-calibration effective_confidence; serve applies calibrate() to raw) — recursive train/serve loop | CONFIRMED | **HIGH · LIVE** (gates admission via YES floor :2853 + neg-EV :3574) |
| V2 | Calibrator trains only on lead≥48h rows, serves all leads; calibrate() ignores its lead_time_hours arg | CONFIRMED | MEDIUM · LIVE |
| V3 | Doubly-censored training sample: only entered AND held-to-resolution trades get labels (no RESOLUTION row for fully-exited positions; database.py:3775) | CONFIRMED | MEDIUM · LIVE |
| V4 | EMOS trains on weather_calibration.actual_temp = ~96% ERA5 pre-07-01; contaminated rows permanent (backfill only touches NULL, no source column) | CONFIRMED | **HIGH · LIVE** (shifts model_prob/direction) |
| V5 | Holdout fallback fits on FULL window incl. would-be holdout and skips the OOS rejection gate when train rows < min_samples (weather_bot.py:257-266, :533) | CONFIRMED | MEDIUM |
| V6 | No contamination cutoff at 2026-07-01 anywhere (conf-cal 30/90d windows, EMOS 90d) — ~77-92% of training data predates the WU fix as of 07-08 | CONFIRMED (partial dup of pass-1 #contaminated-calibrator) | **HIGH · LIVE** (EMOS facet distinct; self-heals ~07-31 / ~09-29 EXCEPT never-re-backfilled EMOS rows) |
| V7 | Survivorship: stop-lossed/exited positions never produce calibrator labels — P(win\|conf) estimated on "survived to resolution" sample | CONFIRMED | HIGH · LIVE |
| V8 | Training join is side-agnostic: single RESOLUTION row paired with FIRST entry's confidence; side-flip re-entries mislabeled; cross-side exit sizes can suppress the RESOLUTION row entirely (database.py:3643-3651) | CONFIRMED | MEDIUM · LIVE |
| V9 | Outcome label is sign(realized_pnl) not market resolution | CONFIRMED literally, LOW (pnl sign is a faithful proxy given 0.85 entry caps; real defects are V7/V8) |
| V10 | ERA5/Open-Meteo still silently substitutes as ground truth on ANY WU scrape failure (debug-level logs, no source column); sole truth for bootstrap + dynamic stations | CONFIRMED | **HIGH · LIVE** (ongoing mechanism, not just historical) |
| V11 | WU-vs-OM sanity check epistemically inverted: on >10°F disagreement it DISCARDS WU (the resolution source) and writes OM as truth; passes plausible garbage inside the band | CONFIRMED (as weak/wrong; "missing" reading refuted) | MEDIUM · LIVE |
| V12 | Resolution-station mapping asserted not validated: market `description` never read anywhere; dynamic stations trade on city-centroid grid forecasts with no resolution station | CONFIRMED | MEDIUM (static ICAOs likely right; validation absent) |
| V13 | Kelly graduation promotes _kelly_mult on forecast-MSE (proxy orthogonal to market edge); promoted value feeds only the flat-discarded S-T path | CONFIRMED | MEDIUM · DEAD+lying-telemetry |
| V14 | Graduation doubles stakes (S-T linear in kelly_mult) as a step function, no hysteresis | CONFIRMED (facet of V13) | LOW · DEAD |
| V15 | Kelly odds computed at Gamma midpoint (b=(1-price)/price on opp["price"]) — ~5x odds overstatement documented in-code (mid 0.525 vs exec 0.85) | CONFIRMED | MEDIUM · Kelly legs dead; midpoint ALSO feeds LIVE neg-EV gate + price dampeners |
| V16 | "Smoczynski-Tomkins" allocator is NOT S-T: hedge factor derived in comment then never applied; plain edge-normalized pro-rata deploying a FIXED fraction regardless of edge magnitude; weatherbot_st_allocation logs fabricated total_usd | CONFIRMED | MEDIUM · DEAD+lying-telemetry+lying-docstring |
| V17 | Sizing surface is a lattice: hard cliffs at 24/48/72/120h, <24h (0.85) sizes ABOVE 24-48h (0.70) inverting the S162 evidence, $5 min-trade floor partially cancels dampeners | CONFIRMED | MEDIUM · **LIVE** (post-flat multipliers execute) |
| V18 | S162 lead-time 0.60 never landed (settings 0.85 shadows getattr fallback) | DUPLICATE of pass-1 #10 | — |
| V19 | S153 boost→dampener inversion is an unsupported functional form: reciprocal of discredited boost magnitudes (evidence supported neutralize-to-1.0 at most; WR-not-EV comparison) | CONFIRMED | DEAD (latent) |
| V20 | Dampeners implemented as reciprocals contradict the tuned constants documented in the SAME comment block (severe actual 0.50 vs stated 0.7-0.85; jump 0.67 vs 0.75; nbm 0.77 vs 0.8) | CONFIRMED | DEAD + lying-documentation |
| V21 | jump_boost measures fetch-over-fetch (15-min cache cycle) not run-over-run; lead-bucket subsampling crossings fire it with no new run; °C stations need 3°C=5.4°F (intended 1.7°C); <60s refetch erases baseline | CONFIRMED | DEAD+lying-telemetry (forecast_delta semantics false) |
| V22 | Station reliability factor is a residual 1.2x BOOST violating the S153 "≤1.0 dampener-only" invariant (uncapped since S132 cap removal) | CONFIRMED | DEAD (activates the moment flat sizing is disabled) |
| V23 | City-Brier dampener pairs chosen-side confidence with a YES-frame was_correct label (database.py:3934-3937) — INVERTED for all PSW NO rows with P(NO)≥0.5 (winning NO calls scored as misses) | CONFIRMED | **LYING-TELEMETRY · LIVE** (feeds prediction_log accuracy + consecutive-loss alerts :952-954; dampener leg dead) |
| V24 | OOS-Brier Kelly cap + city-Brier double-count the same evidence stream; the fallback Kelly path escapes the OOS cap entirely (feeds TRAIN-set _cal_brier to BotBankrollManager) | CONFIRMED | DEAD · both legs flat-bypassed; shared Brier-0.25 "random" baseline too lenient for a NO-dominant book |
| V25 | Asymmetric min-edge 0.08/0.12 (US/intl) — unvalidated handicap; gate runs on uncalibrated model_prob anyway | UNCLEAR | LOW (fold into pass-1 #8) |
| V26 | ALL admission edges priced at Gamma/CLOB midpoint, never the executable book; WEATHER_MIN_EXECUTABLE_EDGE defaults 0.0 so effective min edge at the price actually paid is ZERO; order submitted at midpoint | CONFIRMED | **HIGH · LIVE** (admission) |
| V27 | Entry fill-probability gate is an invented price parabola 0.3+2.8·p·(1-p) consulting no book data, and mathematically CANNOT fire (min 0.3 > threshold 0.15/0.25) | CONFIRMED | LOW · phantom control (do NOT "fix" by lowering threshold — curve is unvalidated) |
| V28 | Favorite-buying funnel persists structurally post-S222/S223: side/ranking geometry emits sibling-NO signals at 0.70-0.85, every NO-side countermeasure is sizing-only, no calibrated input to NO admission; S223 exec-edge fix RE-OPENED the funnel the bug had strangled | CONFIRMED | **HIGH · LIVE** (direction/mix; corrupts WR evaluation) |
| V29 | Trade gating by invented fill-prob | DUPLICATE of V27 | — |
| V30 | "Renormalization assumes traded buckets partition the space" (title recovered verbatim) | DUPLICATE of pass-1 #1 — two lenses found it independently | FIXED `caffc68` |
| V31 | "Sum-to-1 renormalization over…" (3rd lens copy) + residual NBM renorm site probability_engine.py:599-603 | DUPLICATE of pass-1 #1; NBM site FIXED in `caffc68` too | — |
| V32 | Cross-city regime detector reads model-vs-market disagreement direction as a meteorological "front"; no US filter (US_CITY_NAMES has ZERO consumers); no date alignment; ≥3-same-direction is near-always true | CONFIRMED | DEAD (regime dampener flat-bypassed) |
| V33 | Front detection premise YES=warm / NO=cold is false for bucketed markets (YES on at_or_below = COLD call) | CONFIRMED | DEAD (same function as V32; fix together) |
| V34 | Synthetic 31-member Gaussian pseudo-ensemble fabricated around deterministic high with fixed 2°F spread (day-1 MAE misused as all-lead sigma; MAE≠σ; skew-MLE fitted to sampling noise; NO downstream marker — ensemble_count=31 looks real; hash-salted RNG varies across restarts) | CONFIRMED | **HIGH · LIVE** when ensemble fetch fails/429s while deterministic succeeds (forecast_client.py:1213-1218) |
| V35 | Dominant n≥50 empirical path silently skips climate prior + AFD widening on the false premise they are "parametric adjustments" (weather_bot.py:2609) — 120h trades bet pure ensemble while the small-ensemble fallback is climatology-protected; IFS+AIFS double-count (AIFS emulates IFS) overstates effective n | CONFIRMED (VIF facet = duplicate of pass-1, FIXED S222) | **MEDIUM-HIGH · LIVE** >72h |
| V36 | Precip wet-member threshold 0.01 is unit-blind (0.01 in vs 0.01 mm = 25x more sensitive intl); engine never reads bucket.precip_unit; "defaults to 0.5" reading REFUTED (unreachable) | CONFIRMED (code fact; title reconstruction low-confidence) | LOW exposure (US path internally consistent) |
| V37 | NDFD PoP wrong-day fallback: no period matches target → blends TODAY's PoP at 40%; NWS nulls ~0% periods and get_ndfd_pop skips nulls, so DRY target days preferentially trigger the wrong-day substitution (p_rain inflated up to +0.32) | CONFIRMED | **MEDIUM-HIGH · LIVE** (US daily precip admission/direction) |
| V38 | METAR <2h window keyed to 18Z anchor | DUPLICATE of pass-1 #2/#5 (footnote: docstring says <6h, caller gate is <12h) | — |
| V39 | 18Z anchor exists in FOUR copies; the 3 PSW copies retain the max(0,·) clamp S142 removed from temperature — expired PSW markets analyzed at lead=0; calibrator/sizing (18Z) vs engine (local-noon) see different lead times for the same market | CONFIRMED (distinct consumers) | MEDIUM · LIVE (PSW admission) |
| V40 | City auto-discovery would register centroid pseudo-stations w/o resolution source — but the path is DEAD: confidence gate reads a `score` field Open-Meteo geocoding does not return (verified live), so try_auto_register always returns False | CONFIRMED-latent / REFUTED-as-active | fail-safe today; latent HIGH if "fixed" without wiring resolution source |
| V41 | StationHealthMonitor equates "grid forecast exists for coordinates" with "resolution station reporting" for ALL °C stations (vacuous gate); fails open on every error path; never consulted by PSW paths | CONFIRMED | MEDIUM · LIVE (gate can silently never block) |
| V42 | Neutered protections believed active — headline: `_daily_pnl` is never incremented intra-day (assigned only at init/boundary-reset/restart-restore), so the daily loss limit (:3122,:3230) and 20% drawdown halt (:6325) are BLIND to all losses accrued during a continuous session | CONFIRMED → **FIXED `5baff62`** (headline; the other dead protections listed under V42 — tail calib, station-health, auto-discovery — remain) | **HIGH · LIVE** (the bot's only intra-day P&L circuit breakers structurally cannot fire mid-session) |
| V43 | Diverged top-level tree preserves the S222-fixed A1/A3 bugs (per-member EMOS, no VIF); only scripts import it; METAR-pushes hypothesis REFUTED (single weather_bot.py) | CONFIRMED (process risk) | DEAD for trading; audit-contamination + resurrection-on-import risk |
| **N1** | **NEW (found verifying V10):** bias sign-flip — bootstrap writes bias = forecast−actual (weather_bot.py:1279) while the actuals updater writes actual−forecast (:5229); simple-bias fallback applies corrected = X + bias, so bootstrap rows DOUBLE forecast error for exactly the cold stations that depend on the fallback | CONFIRMED → **FIXED `04185e8`** | **HIGH · LIVE** (cold-station model_prob) |
| **N2** | **NEW (edges batch):** NBM benchmark renormalizes over priced buckets only (probability_engine.py:599-603) — same fallacy class as #1 | CONFIRMED | was DEAD-path; **FIXED in `caffc68`** alongside #1 |

## Live-corrupting queue distilled from pass 2 (for OPEN DECISIONS triage)

1. **Calibrator feedback cluster** — V1 (self-training loop) + V7 (survivorship) + V8 (side-agnostic join) + V2 (lead mismatch) + V5 (holdout bypass). One remediation unit: fix the training-data assembly.
2. **Ground-truth cluster** — V4/V6/V10/V11 + N1 (sign-flip). One remediation unit: source column + contamination cutoff + WU-primacy + sign fix.
3. **Executable-price admission** — V26 (raise WEATHER_MIN_EXECUTABLE_EDGE from 0.0; Tier-1/2 env change candidate).
4. **Favorite-funnel NO-side admission** — V28 (needs a calibrated NO-side gate; design decision).
5. **Synthetic ensemble** — V34 (mark + gate or lead-scaled sigma).
6. **NDFD wrong-day PoP** — V37 (None when no period matches; treat null PoP as 0).
7. **Circuit-breaker blindness** — V42 (increment _daily_pnl on realized exits, or re-restore each scan).
8. **Telemetry truth** — V23 (was_correct frame), V16/V20/V21 (logs asserting things the code doesn't do).

Full verifier reports (evidence + refutation attempts + corrections per item) are preserved
in the session transcript; this table is the durable digest. Reconstruction caveat: the
original S223 finding TEXTS remain lost — verdicts above attach to the S224
reconstructions, which one batch confirmed can be recovered verbatim only where the
workflow dump preserved resultPreviews (V30 was; most were not).
