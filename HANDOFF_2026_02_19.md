# Session Handoff — 2026-02-19

## What Was Done This Session

### Training Feedback Loop (COMPLETE, 321 tests pass)
Closed the training feedback loop so ML models learn from paper trade outcomes and prediction accuracy, not just external trades from 1,004 resolved markets.

**Files changed (5):**

1. **`base_engine/prediction/prediction_engine.py`**
   - Added `_get_paper_trade_training_rows(session)` — queries resolved `paper_trades` joined to `markets`
   - Added `_get_prediction_log_training_rows(session)` — queries resolved `prediction_log` with `was_correct`, `confidence`, `edge`
   - Modified `_prepare_training_data()` — appends paper + prediction rows to training DataFrame with `_source` column
   - Added sample weight adjustments after line ~1434:
     - Paper trade rows: multiplied by `PAPER_TRADE_TRAINING_WEIGHT` (0.5)
     - Prediction log rows: multiplied by `PREDICTION_LOG_TRAINING_WEIGHT` (0.3)
     - **"Why wrong" boost**: wrong predictions with high confidence + high edge get 1.0-2.0x correction boost

2. **`base_engine/learning/scheduler.py`**
   - Modified `_retrain_cycle()` — after `get_trades_since()` returns empty, checks `paper_trades` and `prediction_log` for newly resolved rows
   - If feedback exists, triggers retrain instead of skipping

3. **`config/settings.py`**
   - Added 7 settings after `TRAINING_EMBARGO_DAYS`:
     - `TRAIN_ON_PAPER_TRADES` (true), `PAPER_TRADE_TRAINING_WEIGHT` (0.5), `PAPER_TRADE_TRAINING_MAX_ROWS` (5000)
     - `TRAIN_ON_PREDICTION_LOG` (true), `PREDICTION_LOG_TRAINING_WEIGHT` (0.3), `PREDICTION_LOG_TRAINING_MAX_ROWS` (10000)
     - `RETRAIN_ON_NEW_FEEDBACK` (true)

4. **`base_engine/monitoring/pipeline_gate.py`**
   - Modified `check_before_training()` — if external trades count fails gate, falls back to checking `paper_trades` and `prediction_log` resolved row counts

5. **`base_engine/base_engine.py`**
   - Modified `_on_resolution()` callback (~line 1214) — immediately runs `backfill_prediction_log_resolution()` and `backfill_paper_trades_resolution()` on market resolution event

### Migration 017: BLOCKED by Supabase Pooler
**Status: NOT applied. Columns `neg_risk` and `outcome_count` do NOT exist on `markets` table yet.**

**Root cause:** Supabase session pooler enforces server-side `statement_timeout` (~10s) that cannot be overridden. 7 internal Supabase processes (Realtime, schema cache) permanently hold `AccessShareLock` on `markets`. `ALTER TABLE` needs `AccessExclusiveLock`, waits for those locks, and gets killed by timeout.

**What was tried and failed:**
- SQL Editor (single/batch statements, with/without DEFAULT)
- asyncpg with SET statement_timeout (pooler overrides)
- psycopg2 with connection-string options (pooler overrides)
- Transaction pooler port 6543 (drops connection mid-query)
- Direct connection db.tfwevnuxmjqskmawvnfx.supabase.co (DNS failed)
- pg_terminate_backend() (not available through pooler)
- Rapid retry with lock_timeout=500ms for 60 attempts (locks never release)

**What to try next:**
1. Get the **direct (non-pooler) connection string** from Supabase Dashboard → Settings → Database → Connection string → URI (direct). Run migration through that.
2. Or: Pause/unpause the Supabase project to kill all connections, then immediately run migration.

**SQL to run once connected directly:**
```sql
ALTER TABLE markets ADD COLUMN IF NOT EXISTS neg_risk BOOLEAN;
ALTER TABLE markets ADD COLUMN IF NOT EXISTS outcome_count INTEGER;
ALTER TABLE markets ALTER COLUMN neg_risk SET DEFAULT false;
ALTER TABLE markets ALTER COLUMN outcome_count SET DEFAULT 2;
CREATE INDEX IF NOT EXISTS idx_markets_neg_risk ON markets (neg_risk) WHERE neg_risk = true;
```

**After migration succeeds:** Uncomment ORM columns in `database.py` (lines 78-80 area — search for `neg_risk`).

## Previous Session Fixes Still Active

### Position Management Fixes
- `base_engine/execution/position_manager.py`: 10-min grace period, min_edge_to_hold=-0.05, all exits use SELL, ghost position auto-close
- `base_engine/execution/order_gateway.py`: SELL bypass for drawdown controller + RL agent
- `base_engine/execution/paper_trading.py`: 0.01% float tolerance on SELL size check
- `base_engine/coordination/trade_coordinator.py`: ON CONFLICT DO UPDATE WHERE status='closed'
- `.env`: `RL_TRADE_TIMING_ENABLED=false`

### Core Problem Still Exists
All 11 models predict ~0.92 for everything. Only 1 of 30 high-confidence predictions had positive edge. The training feedback loop implemented this session is designed to fix this over time as markets resolve and models learn from their mistakes.

## To Resume Work

1. **Resolve migration 017** (see above)
2. **Delete model cache**: `del C:\lockes-picks\polymarket-ai-v2\data\model_cache.pkl`
3. **Enable VPN** (Surfshark — US IPs get 403 from Polymarket API)
4. **Start**: `python C:\lockes-picks\polymarket-ai-v2\main.py`
5. **Verify feedback loop**: Look for log lines:
   - `Training data: appended N paper trade rows (weight=0.50)`
   - `Training data: appended N prediction_log rows (weight=0.30)`
   - `New feedback: N paper trades + N predictions resolved since last cycle`
   - `'Why wrong' correction: N wrong predictions, avg boost=X.XX`
6. **Monitor trades**: `python scripts/diag_why_no_trades3.py`

## Test Command
```
cd C:\lockes-picks\polymarket-ai-v2
python -m pytest tests/unit/ -v --no-cov --tb=short
```
321 tests pass as of this session.

## Key File Locations
```
MEMORY.md reference .............. C:\Users\samwa\.claude\projects\C--lockes-picks-polymarket-ai-v2\memory\MEMORY.md
Plan file ....................... C:\Users\samwa\.claude\plans\zesty-launching-emerson.md
Migration SQL ................... schema/migrations/017_neg_risk_columns.sql
Training feedback code .......... base_engine/prediction/prediction_engine.py (methods at ~line 959-1020)
Scheduler feedback trigger ...... base_engine/learning/scheduler.py (_retrain_cycle ~line 334)
Pipeline gate fallback .......... base_engine/monitoring/pipeline_gate.py (check_before_training ~line 267)
Resolution backfill wire ........ base_engine/base_engine.py (_on_resolution ~line 1214)
New settings .................... config/settings.py (after TRAINING_EMBARGO_DAYS ~line 214)
```
